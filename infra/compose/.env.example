# Copy to .env in this directory if you need to override defaults.
# These credentials are for local shop Postgres only — never Nextcloud MariaDB.

SHOP_POSTGRES_USER=projectcar
SHOP_POSTGRES_PASSWORD=projectcar
SHOP_POSTGRES_DB=projectcar
SHOP_POSTGRES_PORT=5432
