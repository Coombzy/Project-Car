# Status — 2026-09-15

Living one-pager: what’s live, what’s next, locks. Product detail stays in the start-here specs. Stay-up / CORS / brochure-deploy runbooks live elsewhere — this file is not a runbook. **Monday #70 paper map** (when-to-open): [deployment-guide.md](deployment-guide.md) Soft-530 living paper index. Living ops: [doc-lid-restore.md](doc-lid-restore.md) (process wake only · **post-CLEAR stay-up evidence** for the next CF 1033) · [doc-unfreeze.md](doc-unfreeze.md) (Ben GO pull) · [unfreeze-readiness.md](unfreeze-readiness.md) (Lead **draft** Ben-GO gates **before** any pull — **draft ≠ GO**; vault OPEN does **not** block drafting; Soft-530 CLEAR duration ≠ auto-unfreeze ≠ auto **#82**) · [shared-fleet-cards.md](shared-fleet-cards.md) (Fleet Shared `shop-os-mc-plan.md` **rewritten / non-SSOT** — Docs held **#70** is SSOT; Shared = Chief standing notes only; discard Shared if it contradicts Docs tip / live; rewrite **DONE** ~15:42 America/Edmonton on Doc `~/Desktop/Fleet-Nextcloud/Memory/Shared/shop-os-mc-plan.md` — **not** still in progress; dropped false brochure **502 lock** + Garage-blocked-on-502; aligned Soft-530 **CLEAR** half-state (Shared ~15:42 — living now **OPEN** both 1033; half-state **OVER**) / camp vault-on-Doc / freeze **`4cf8924`/`5swmVz`** / **#82** not auto; brochure **LIVE** Option A since **2026-09-06**; **not** a Ben ask; no Garage/Zone fan-out; no unfreeze / **#82** / camp GO) · [shop-web-stay-up.md](shop-web-stay-up.md) · [api-stay-up.md](api-stay-up.md) · [vault-stay-up.md](vault-stay-up.md) (**PARKED for camp weeks** / **desk/lab optional** — McKing VW + cloudflared; Soft-530 ≠ vault **at home/lab only**; **not camp SSOT** · **post-CLEAR stay-up evidence** for the next vault flip) · [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) (**camp SSOT** — Doc hosts Soft-530 shop + vault + NC; McKing may sleep; Doc VW + Zone retarget; Soft-530 independence **home/lab only**; **Ben GO**, never auto-cutover; **not** McKing docker; Zone/Doc follow **this** card at camp GO; **CDM lid-close soak** hop **3a** required paper **before** Zone **Z5** — home Soft-530 **CLEAR** lid-open **never scores**) · [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md) (**home-off:** Docs **ABSENT** + Soft-530 **OPEN** is **EXPECTED**; **home-awake living:** Soft-530 **CLEAR** + vault **OPEN** is a valid **pre-camp half-state** — ≠ dual CLEAR ≠ camp GO; quiet-ops **HOLD**; stamp SAME-CLASS; complement to `fb86f90`, **not** a re-cutover; Soft-530 **CLEAR ≠ #82**; Soft-530 **OPEN ≠ #82 blocked**) · [dual-host-outage.md](dual-host-outage.md) (weekend dual-OPEN **wake order** — **SUPERSEDED for camp**; Doc-then-McKing is **home/lab/pre-cutover only**; after camp Ben GO, Doc is sole Soft-530+vault(+NC) origin — **`lightning` ABSENT must not trigger McKing wake**; `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning`; Doc forensics = lid-restore; McKing forensics = vault-stay-up) · [soft-530-extended-open.md](soft-530-extended-open.md) (quiet-ops lock once Soft-530 OPEN **~24h** + vault OPEN + **Mac.lan only** — no Ben re-nag; Lead/Chief stay armed; companions HOLD until Ben reopens, **not** weekend-only; Option A public SSOT; duration ≠ **#82** auto-GO / unfreeze / Zone / Bitwarden / companion re-ask; Soft-530 **OPEN ≠ #82 blocked**; **chronic-HOLD exception:** both api + vault calendar-day chronic HOLD → “Lookout continues” is **false for flip detection**; interim Chief `*/20` + Lead probes (accept **≤~20m**); Chronic Lookout ≠ quiet-ops failure ≠ Ben ping; CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md)) · [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) (**Mon–Thu nights** after last weekday plan-improve `*/20` ~17:40 MT until next weekday first fire ~06:00 — Lookout `api.` `/health` + vault `/alive` only; Option A Dynamic redirect **UNCOVERED** while watch **GONE** — accept that risk; **next** Option A smoke = first weekday `*/20` after ~06:00 or Ben-approved Lookout recreate; companions HOLD; no overnight Zone/Garage / invent-overnight-redirect-smoke; no Ben re-nag / second recreate ask; Friday last `*/20` → weekend coverage; mid-overnight reappear still first hops — **never** `Mac.lan`; **never auto-fire**) · [overnight-baseline-stamp.md](overnight-baseline-stamp.md) (last weekday `*/20` ~17:40 MT **one-liner** before Lookout-only — **refresh only on field DIFF**, else prior stamp stands; first weekday `*/20` after ~06:00 **diffs** — **same-class = stay quiet**; surface **Lead/Ben only on stamp class DIFF**; freeze/`BUILD_ID`/`changeme` are **`last_known` from last Soft-530 CLEAR only** — Soft-530 **OPEN** 530/1033 → `app.`+`ops.` `/login` unverifiable **≠ freeze DIFF** (live-verify waits on [soft-530-clear-smoke.md](soft-530-clear-smoke.md)); true-last `*/20` **~17:42** MT Mon 2026-09-14 **SAME-CLASS** vs ~17:25 — **do NOT rewrite last-weekday**; **first-hop absorbed DIFF** → no Ben re-surface + **refresh** living stamp; no rediscover / no Ben re-nag from stamp alone; CLEAR overnight still smoke + first-hop; living morning-refresh **`Mac.lan` + `lightning`** (`Mac.lan` flap **reappeared** vs ~12:45 after ~12:24 drop) / Docs **ABSENT** / vault **530 / CF 1033** connector-down (was 502) / HTTP subclass DIFF — Lead/Lookout field, **not a Ben re-nag**) · [doc-reappear-first-hop.md](doc-reappear-first-hop.md) (**first hop** when `Docs-MacBook-Pro` reappears after quiet-ops — assert machineId `95a229f5-9296-4a18-aa98-70fd300dabdf`; **never** `Mac.lan` / Porsche as Doc; **cold-boot** if Docs **ABSENT ≥1 calendar day** or Soft-530 **OPEN** 1033 **≥24h** with Docs **ABSENT** — verify cloudflared + shop KeepAlive + note CDM **then** CLEAR smoke; overnight **warm wake** `<24h` after a prior CLEAR stays shorter process-wake; **never** treat multi-day lid as overnight sleep; living Docs **ABSENT** since Fri 2026-09-11 ~16:48 **~4d**; only if CLEAR → forensics; then sequencer — **never auto-fire**; Chief-to-doc hop only after CLEAR smoke; vault stays Chief `lightning`) · [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (**first hop** when `lightning` / McKing reappears after extended vault OPEN / quiet-ops — assert machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` label **lightning**; **never** `Mac.lan`; then vault-clear-smoke `/alive` **200** + `/api/config` VW **2026.6.0** + **502→200**; only if CLEAR → vault post-CLEAR forensics; Soft-530 independence — Bitwarden gate is **vault CLEAR** (Ben GO after `/alive` + VW **2026.6.0** smoke), **not** Soft-530 CLEAR; Soft-530 **OPEN** can coexist with vault CLEAR + Bitwarden work; **never auto Bitwarden**; vault CLEAR ≠ Soft-530 CLEAR ≠ unfreeze / **#82** GO) · [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (**first** recovery smoke after extended-OPEN Soft-530 CLEAR — **before** desk / **#79.1** / unfreeze; ≠ **#82** / companions / Bitwarden / unfreeze; **CLEAR ≠ honesty-off** — **#80** Discord honesty + `waitlist.js?v=3` fail-soft stay; vault CLEAR still McKing) · [vault-clear-smoke.md](vault-clear-smoke.md) (**first** recovery smoke after extended-OPEN vault CLEAR — **before** Bitwarden / desk; `/alive` **200** + `/api/config` **200** / McKing **2026.6.0** class not Doc **2025.12.0** + `docker.service` **enabled** + **active**; `/alive` **200** + docker **disabled/inactive** = **NOT CLEAR / recurrence risk**; living OPEN subclass is **530 / CF 1033** connector-down (was 502); classify **1033→200** vs **502→200** by living subclass — do **not** run docker permanence as the 1033 first hop; vault CLEAR alone ≠ Soft-530 CLEAR ≠ **#82** ≠ unfreeze ≠ companions; Bitwarden stays Ben GO, **never auto-fire**; ≠ Soft-530 CLEAR smoke `502ab2e`; forensics still `@d88cacb`) · [post-dual-clear-go.md](post-dual-clear-go.md) (post-dual-CLEAR **Ben GO sequencer** — **never auto-fire** on CLEAR; KeepAlive → **#82** is the *default ordered menu after dual CLEAR*; Soft-530 **OPEN ≠ #82 blocked**; **#82** Worker-live (upload + **mandatory** purge/freshness) → **brochure lane** **#83** ∥ **Bulk Phase1** (not Bulk→#83; Member after Bulk) — **never #81**; unfreeze+#79.1 is **Shop OS parallel**, not “next after **#82**” — lane-done = **unfreeze + #79.1 acceptance smoke**; forensics `@55e10d0` + `@d88cacb` already ack’d) · [shop-os-ci.md](shop-os-ci.md) (git-only Shop OS CI — **not** Doc unfreeze) · [brochure-worker-deploy.md](brochure-worker-deploy.md) · [brochure-worker-ci.md](brochure-worker-ci.md) (paper CI + Option A receipt **after #82 Ben GO**, Soft-530-independent — Soft-530 **OPEN ≠ #82 blocked**; Direct Upload → **mandatory** purge apex+www → body freshness; **never #81**) · [brochure-redirect-watch.md](brochure-redirect-watch.md) (Lookout Option A watch **spec** — living watch **GONE**; Auto-review blocked recreate; weekday Chief `*/20` **+ Soft-530 UX**; overnight/weekend Dynamic redirect **UNCOVERED**; **Redirect-only PASS ≠ Soft-530 UX PASS**) · [lookout-rearm-sop.md](lookout-rearm-sop.md) (**never delete** an armed Lookout watch to fix stalled cron; **Lookout stall ≠ vault HTTP flip** — `enabled:true` + cron-stalled is a coverage class; **re-arm success ≠ first fire** — stickiness ≥3 consecutive `*/5` windows; chronic ≤1 pause/resume per calendar-day, clock resets **America/Edmonton midnight**; Lead **may GO** in-place re-arm of **both** api + vault when stalled **if** the day's 1× is unused; **HOLD** Ben recreate; until stickiness, interim = Chief `*/20` + Lead probes (accept **≤~20m**) — not a Ben ping; Soft-530 api remaining OK-ish is **not** living contrast; **HOLD** after one Ben ask; weekday `*/20` + Soft-530 UX; overnight/weekend **UNCOVERED** — accept that risk; **next** Option A smoke = first weekday `*/20` after ~06:00 or Ben-approved recreate; after Ben approves: enabled + first fire ≤ **10m** + baseline **LIVE 10/10**; Soft-530 companions **HOLD**; **#78 lookout-resume** is **LIVE-SUPERSEDED**; **chronic-HOLD exception** on quiet-ops: both watches calendar-day chronic HOLD → “Lookout continues” is **false for flip detection**; Chronic Lookout ≠ quiet-ops failure ≠ Ben ping) · [brochure-security-headers.md](brochure-security-headers.md) (P2-4 **LIVE**). Member host cutover (plan only): [member-host-cutover.md](member-host-cutover.md). Member edge / path-split (plan only; **capacity-blocked** by Option A **FULL 10/10** — Ben GO only after **#82** Worker-live + Bulk Phase1; **#83** is CI-only after **#82** base — **not** Bulk-gated; **never #81**; `/member*` ranked **BEFORE** brochure Redirect pack; `/member/login` must **not** 301 into `membership.html`): [member-zone-edge.md](member-zone-edge.md). Temporary `app.` alias cut (plan only): [app-alias-cut.md](app-alias-cut.md). Brochure → Classic Pages git (plan only; skipped until CF ↔ GitHub auth; frozen Redirect inventory = **hard cutover gate**; outranked by Next #1 + Member edge): [brochure-pages-cutover.md](brochure-pages-cutover.md). Proposed public-MVP cut-vs-keep (draft, not a Ben lock): [ship-mvp-cut.md](ship-mvp-cut.md). Google Calendar OAuth / two-way sync (plan only; STATUS Next #6 — not a GO): [google-calendar-oauth.md](google-calendar-oauth.md). Ops demo hardening (**#79** — Soft-530-CLEAR **Next Ben GO**, **parallel to #82**; Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull** — merge alone will **not** clear public login while freeze holds (`4cf8924` / **`5swmVz`**); plan only — do **not** execute Garage/Zone from this tip-fold): [ops-demo-hardening.md](ops-demo-hardening.md). McKing shop-host cutover (**PARKED for camp weeks** / **desk/lab optional** — **not camp SSOT**; hop OPEN ≠ cutover GO — **not** Next #1, **not** a GO): [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md). Dual-tunnel ownership (**separate** living-ops row — **not** the shop CF cutover): **Home/lab** Doc tunnel = `cloud.` + `api.` + `app.` + `ops.` only; McKing-only `vault.projectcar.ca` is **McKing-owned** (`cloudflared` → `localhost:8222`; `/api/config` **2026.6.0** when CLEAR). **Camp SSOT** (after **Ben GO**): [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) — Doc hosts Soft-530 shop + vault + NC; McKing may sleep. Zone/Doc follow **that** card at camp GO — **not** the parked McKing shop-host / vault-stay-up cards. **Living:** vault independently **OPEN** subclass **530 / CF 1033** connector-down (Lead-verified **~16:28** America/Edmonton; **was 502** since ~19:45 MT 2026-09-13). Soft-530 **OPEN 1033** + vault **1033** is dual OPEN (half-state **OVER**). Fri `/alive` **200** is historical. Dual-Nextcloud lock: public `cloud.projectcar.ca` stays on **Doc** until explicit Ben GO; McKing `/opt/mission-control` NC remains **lab/hub loopback-only** (unpublished). Paper path = Tailscale Serve HTTPS mirroring VW Serve at `lightning.tailbe8f55.ts.net` + `NEXTCLOUD_TRUSTED_DOMAINS` MagicDNS — **not** a Cloudflare cutover of `cloud.*`. Do **not** point `cloud.*` tunnel/CNAME at McKing until a dual-run acceptance gate (parity vs Doc `status.php`) **and** Ben GO. Dual-tunnel matrix **home/lab** stays Doc=`cloud`/`api`/`app`/`ops`; McKing=`vault` (+ unpublished NC). **Camp** (after **Ben GO**): Doc hosts shop + vault (+ NC); McKing may sleep — [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md). Never auto-cutover. Sequencer does **not** assume McKing vault wake first. Lookout vault flip watch is **LIVE/armed** (`enabled:true`, Lookout confirmed) on `/alive` every **5m** (fallback `/api/config` if `/alive` 404s) — Soft-530 / `api.` `/health` can stay green while vault dies (vault watch ≠ Soft-530 / Doc lid-restore). Soft-530 companion watches on `ops.` / `app.` `/login` (optional `cloud.` `/login` for five-row parity) are **HOLD / not armed** (Ben skipped companion-watch approval ~14:35 America/Edmonton — do **not** re-ask). Do **not** claim `enabled:true`. Soft-530 five-row is Doc shop hosts `cloud.` / `api.` / `app.` / `ops.`, but Lookout Soft-530 coverage is **api-only** (`/health`); `ops.` / `app.` (and `cloud.`) can **530 while `api.` stays 200** (same class as pre-vault-watch). Companion watches ≠ vault `/alive` watch ≠ Doc lid-restore. **Living Soft-530 OPEN + vault OPEN dual-OPEN both 1033 (supersedes dual-OPEN / 7209c1b CLEAR half-state):** Soft-530 **OPEN 530 / CF 1033** (`api.` / `app.` / `ops.`; Lookout + Lead **~16:36**; **was CLEAR**). Vault independently **OPEN** subclass **530 / CF 1033** connector-down (Lead-verified **~16:28** America/Edmonton; **was 502** since ~19:45 MT Sep 13). Soft-530 **OPEN 1033** + vault **1033** is dual OPEN (half-state **OVER**). ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED — `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning`. Freeze intact **`4cf8924`** / BUILD_ID **`5swmVz`**. **#82** still Ben GO. Soft-530 **OPEN ≠ #82 blocked**. Apex deferred. Fri 2026-09-11 ~06:50 CLEAR is **historical**. **Brochure Reality quarantine:** until **#82** Worker-live + Zone Direct Upload, **#70 Reality tip / Option A** is brochure live SSOT — do **not** execute Zone/Garage from **`main` Reality tip** (`main` still pre-Option-A Worker `/shop` 302 + `styles.css?v=35`). After **#82**, smoke is Direct Upload → **mandatory purge** → body freshness (Live checklist). Hop OPEN ≠ shop cutover GO ≠ Doc unfreeze. Machine map: [home-lab-specification.md](home-lab-specification.md) (host table + anti-goals match this dual-tunnel + vault LIVE lock — McKing is **not** offline `lil-cachy`; VW is **not** Doc compose-down; McKing Tailnet Frigate **0.17.2** + Jellyfin lab ≠ Shop OS `/cameras` live and does **not** flip `pc.cameras` — Later cameras stays **Later**). Cockpit spec [mission-control-architecture.md](mission-control-architecture.md) matches the same lock (public VW on McKing, not Doc compose host; shop CF cutover paper; Doc freeze + Soft-530 five-row = Doc shop hosts only; Frigate/Jellyfin lab ≠ Shop OS cameras). (Locks below; [vault-stay-up.md](vault-stay-up.md), [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md), [api-stay-up.md](api-stay-up.md), [deployment-guide.md](deployment-guide.md), [home-lab-specification.md](home-lab-specification.md), [ops-demo-hardening.md](ops-demo-hardening.md), [mission-control-architecture.md](mission-control-architecture.md)).

**Brochure Reality quarantine (held #70):** Until **#82** Worker-live + Zone Direct Upload, treat **this PR’s Reality tip / Option A** as brochure + living-ops SSOT. **Camp host SSOT** ([camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md)) is **#70 SSOT** at camp GO — Doc hosts Soft-530 shop + vault + NC; McKing may sleep. [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md) + McKing-centric [vault-stay-up.md](vault-stay-up.md) are **PARKED for camp weeks** / **desk/lab optional** — Zone/Doc do **not** follow those cards at camp GO. Living dual-tunnel is still **home/lab** (McKing vault **OPEN 530 / CF 1033** connector-down — **was 502**); camp cutover is **not** LIVE and is **not** a Zone execute. **Home vs camp Doc posture** ([home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md)) is **#70 SSOT** too — living Soft-530 **OPEN 530 / CF 1033** + vault **OPEN 530 / CF 1033** (dual OPEN both 1033; half-state **OVER** ~16:36). Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED — **never** `Mac.lan` as Doc. Prior home-awake CLEAR half-state is **superseded** (not dual CLEAR, not camp GO); brochure Option A **Redirect+UX PASS** (rungs 1–2 of the completeness ladder — **≠** Worker body freshness / **#82**); camp cutover paper **ready, pending Ben GO**. **Public STATUS tip honesty:** `main` / public STATUS still stamps brochure Worker live at P4-2 **`2b772ff` / #67** (`styles.css?v=35`, Worker `_redirects` `/shop` **302**). That stamp is **stale** vs live Option A Redirect pack **FULL 10/10** + Soft-530 Discord waitlist (**#80** `waitlist.js?v=3` + `styles.css?v=36`) + McKing vault **LIVE (when CLEAR)** — living vault is **OPEN 530 / CF 1033** (was 502). **Reinforce (no Ben ask / no tip-only PR):** `main` Reality tip is still that **`2b772ff` / ?v=35 / Worker `/shop` 302** row — quarantine stands; **#70** is SSOT until **#82**. Same class: `main` `doc-lid-restore.md` Soft-530 table still says Lookout api-health **paused** vs living **`enabled:true`** (**#78 LIVE-SUPERSEDED**). Do **not** open a tip-only PR to reconcile `main` — keep this quarantine; **#70** is SSOT until **#82**. Do **not** execute Zone or Garage from **`main` Reality tip** — `main` STATUS still documents the pre-Option-A Worker (`_redirects` `/shop` **302** + `styles.css?v=35`). After **#82** upload + **mandatory purge**, **one** tip-fold reconciles `main` Reality to live (Live checklist — body freshness after purge). **Living Soft-530 OPEN + vault OPEN dual-OPEN both 1033:** Soft-530 **OPEN 530 / CF 1033** (`api.` / `app.` / `ops.`; Lookout + Lead **~16:36**; **was CLEAR**); vault independently **OPEN** subclass **530 / CF 1033** connector-down (Lead-verified **~16:28** America/Edmonton; **was 502** since ~19:45 MT Sep 13 — **not** CLEAR). Soft-530 **OPEN 1033** + vault **1033** is dual OPEN (half-state **OVER**). ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED. Half-state ≠ dual CLEAR — do **not** enter [post-dual-clear-go.md](post-dual-clear-go.md) until vault CLEAR. Soft-530 CLEAR at home ≠ camp GO. Soft-530 **CLEAR ≠ #82** ≠ unfreeze ≠ Bitwarden. Lookout: api `ok=false` expected; vault `ok=false` expected (dual OPEN both 1033). Brochure Option A is **LIVE**: Zone Redirect **FULL 10/10**; `waitlist.js?v=3`; `styles.css?v=36`; Soft-530 Discord honesty; Home still **STALE** until **#82** (canonical/`og:url` + nav Home bare `/`; sitemap loc bare `/`, lastmod **2026-09-08**). Redirect+UX **PASS** ≠ Worker body freshness **PASS** / **#82** done. Soft-530 companion watches are **HOLD / not armed** (Ben skipped companion-watch approval ~14:35 America/Edmonton — do **not** re-ask). **Weekend Soft-530 coverage (plan-improve off Sat/Sun):** flip watches = Lookout `api.` `/health` + vault `/alive` **only**; control plane = Doc KeepAlive / lid-close — **not** Doc unfreeze, **not** **#82**. Friday last weekday `*/20` (~17:40 MT) hands **into** weekend coverage. **Mon–Thu nights** after last `*/20` until next weekday first fire (~06:00): [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) — Option A Dynamic redirect **UNCOVERED** while watch **GONE** (same class as weekend `84107e7`). Fri Soft-530 **CLEAR** is historical — living dual-OPEN ≠ unfreeze GO ≠ companion re-ask ≠ **#82** auto-GO. Soft-530 **OPEN ≠ #82 blocked**. Receipt: [brochure-worker-deploy.md](brochure-worker-deploy.md) · [brochure-worker-ci.md](brochure-worker-ci.md) · [brochure-redirect-watch.md](brochure-redirect-watch.md) · [lookout-rearm-sop.md](lookout-rearm-sop.md) (Option A watch **GONE** — **HOLD** recreate). Keep `/member*` Worker **404** until Member edge Ben GO after Bulk Phase1 — never **301** into `membership.html` ([member-zone-edge.md](member-zone-edge.md)). This fold does **not** execute #82 / Bulk / Zone / Garage. **#79** Soft-530-CLEAR Next Ben GO stays **parallel to #82** (not blocked on Worker SEO / Option A upload). Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull**. Frigate anti-goal unchanged.

**Fleet Shared cards (non-SSOT):** Fleet Shared `shop-os-mc-plan.md` is **rewritten** and remains **non-SSOT**. `Coombzy/Project-Car` `Docs/` (held **#70** → `main`) is SSOT. Shared cards are Chief standing notes only — if they contradict Docs tip / live, **discard Shared language**. Chief rewrite **DONE** ~15:42 America/Edmonton on Doc `~/Desktop/Fleet-Nextcloud/Memory/Shared/shop-os-mc-plan.md` — **not** still “in progress”. Dropped false brochure **502 lock** + Garage-blocked-on-502; marked non-SSOT vs Docs/#70; aligned Soft-530 **CLEAR** half-state (Shared ~15:42 — living now **OPEN** both 1033; half-state **OVER**) / camp vault-on-Doc / freeze **`4cf8924`/`5swmVz`** / **#82** not auto. Public `projectcar.ca` brochure is **LIVE** (Option A) since the **2026-09-06** unlock. Living: Soft-530 **OPEN 530 / CF 1033**; vault **OPEN 530 / CF 1033** (was 502); brochure **LIVE**; Shared rewritten. Half-state **OVER**. Essay: [shared-fleet-cards.md](shared-fleet-cards.md). **Not** a Ben ask. No Garage/Zone fan-out. No unfreeze / **#82** / camp GO. Do **not** restore 502 language.

Reality tip: **git tip** `main` **`2dd61a2`** / **#74** ([doc-unfreeze.md](doc-unfreeze.md) Ben GO pull checklist — **on `main`**). Checklist on git is **not** a pull. Green **shop-os-ci** and lid-restore process-wake are **not** Ben GO unfreeze. [shop-os-ci.md](shop-os-ci.md) + `.github/workflows/shop-os-ci.yml` are on `main` — **git-only** quality gate (**#73** / `babdc58`), **not** a Doc unfreeze. **#72** lid-restore no-auto-pull (`a2ec088`) and **#71** google-calendar-oauth plan stay on `main` (STATUS Next #6 — not a GO). OwnerShell **#69** (`b9f9019`) stays **git-only**, not LIVE on Doc (Doc checkout **frozen** until **Ben GO**; same **git-only** bucket as **#36**). OwnerShell chrome + bay-hero dashboard are **on git** (merged, not LIVE on Doc): primary nav Dashboard / Schedule / Chat / Members; secondary clusters Inventory (Parts/Tools), Floor (Jobs/Cameras/Hoists), Admin (Payments/Tiers/Fill/Waitlist); compact dismissible demo banner; dashboard 3 primary metrics + 3×2 bay grid + Parts/To-dos split. **Public STATUS tip honesty:** that P4-2 **`2b772ff` / #67** Worker stamp (below) is the last public/`main` brochure SHA — it is **stale** vs live Option A Redirect pack + Soft-530 Discord waitlist (**#80**) + McKing vault **LIVE (when CLEAR)** — living vault is **OPEN 530 / CF 1033** (was 502). Keep Reality tip quarantine; **#70** is SSOT until **#82**. Do **not** open a tip-only PR. P4-2 is **LIVE** on Worker `projectcar-brochure` (2026-09-07 Worker live verify — Garage PR **#67** → `2b772ff`; Zone Direct Upload ~19:15 America/Regina; smoke PASS apex+www: Home equal `cta-hero` Join the waitlist + Join Discord (`discord.gg/projectcar`); contact email · Discord; Membership waitlist form unchanged; Discord contact-primary + alongside-waitlist copy; `styles.css?v=35` at that upload; sitemap lastmod bumps (2026-09-08 present); P2-4 headers still; P3-4 Shop facility bullets still). **Live URL SSOT (Option A):** Zone Redirect pack is **FULL 10/10** Active Dynamic **301**s (apex+www) — capacity lock; no new Dynamic rules for brochure pretty-URLs. Owns pretty URLs: brochure-root-to-index (`/`→`/index.html`), brochure-shop-to-the-shop, brochure-shop-html-to-the-shop (`/shop.html`→`/the-shop.html`), membership/about/the-shop/contact/roadmap-to-html (+ trailing-slash), brochure-chat-to-contact, brochure-chat-slash-to-contact. Garage must **not** invent new brochure extensionless pretty-URLs until **#82** Worker-live + Bulk Phase1 frees Dynamic slots (Redirect plan A parked). Worker Assets **`html_handling: none`** — `*.html` is canonical. Worker `_redirects` is **thin**: **chat → contact.html only** (no `/` or `/shop` in Worker `_redirects`). Soft-530 is **Worker-live** (PR **#80**): `waitlist.js?v=3` + `styles.css?v=36` + Soft-530 Discord honesty on Membership/Contact. **Option A completeness ladder:** Redirect **PASS** + Soft-530 UX **LIVE** ≠ Worker body freshness **PASS**. Live Home still **STALE** until **#82**: canonical/`og:url` and nav Home still bare `/`; sitemap loc bare `/` (lastmod **2026-09-08**). Soft-530 **CLEAR** does not change that and **never auto-GOs #82**. Home canonical/og/sitemap must be `/index.html` after **#82** — **#82 not yet Worker-live**. Redirect+UX **PASS** is **never** #82 done. P3-4 is **LIVE** on Worker `projectcar-brochure` (2026-09-07 Worker live verify — Garage PR **#65** → `f191ad5`; Zone Direct Upload ~18:46 America/Regina; smoke PASS apex+www: Shop 200 facility bullets (bays/hoists, shared fab, safety/cleanliness, structured shop) + honesty “intended shop capability — not live yet”; no 24/7 packaging on Shop; Membership still “24/7 shop access (when operational)”; Home still P3-1 pitch + `cta-hero` waitlist + Project Underway; no capability-bullet dump; `styles.css?v=34`; P2-4 headers still present). P3-5 is **LIVE** on Worker `projectcar-brochure` (2026-09-07 Worker live verify — Garage PR **#62** → `9d83bfc`; Zone Direct Upload; smoke PASS: Shop/Membership/Roadmap use “24/7 shop access (when operational)”; Home (and About/Contact) have no absolute 24/7). P3-1 is **LIVE** on Worker `projectcar-brochure` (2026-09-07 Worker live verify — Garage PR **#61** → `88c9820`; Zone Direct Upload ~16:50 America/Regina; smoke PASS Home: pitch + `cta-hero` Join the waitlist + Project Underway + Shop/Membership pointers; no feature-bullet dump). P2-4 is **LIVE** on Worker `projectcar-brochure` (2026-09-07 smoke PASS — Transform Rules `brochure-security-headers` / `brochure-html-no-cache` / `brochure-asset-immutable`; apex+www). This tip is the last **product/Docs** SHA — not finance tip-of-main `de290e2` (or later CCJ / Health Audit stamps). Shop-web host allowlist **#36** (`f952cd3`) and OwnerShell **#69** (`b9f9019`) are **git-only**. Docs **#30–#74** already on `main` (website **#52–#67**; Soft-530 **#80** Worker-live; **#71** google-calendar-oauth plan; **#72** lid-restore no-auto-pull; **#73** Shop OS CI; **#74** doc-unfreeze checklist). Last full product walk remains 2026-09-06 ~16:16 (Dashboard **#28** / `afb37f9` **LIVE on Doc**). Doc checkout is **frozen** at **`4cf8924`** / BUILD_ID **`5swmVz-T2CqKEQzTk1ifU`** (Dashboard **#28**) until **Ben GO**. **#36** + **#69** stay **git-only**. plan-improve / Chief / Lead must **not** re-propose Doc pull/rebuild. Do **not** claim #36 or #69 is live on Doc. After **Ben GO**, ordered pull: [doc-unfreeze.md](doc-unfreeze.md). **Fri Soft-530 restore (historical)** 2026-09-11 ~06:50 America/Edmonton (Lead verified then): public `GET https://api.projectcar.ca/health` **200**; `ops.` + `app.` **307** → `/login`; waitlist **OPTIONS 200**. **Living dual OPEN both 1033 supersedes CLEAR half-state / dual-OPEN:** Soft-530 **OPEN 530 / CF 1033** (`api.` / `app.` / `ops.`; Lookout + Lead **~16:36**; **was CLEAR**). Vault independently **OPEN** subclass **530 / CF 1033** (Lead-verified **~16:28**; **was 502** since ~19:45 MT Sep 13). ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED. Product tip stays **`2dd61a2` / #74** — origin-up/down is not a new product SHA. Freeze intact **`4cf8924`** / BUILD_ID **`5swmVz`**. **#82** still Ben GO. Apex deferred. Ordered restore (process wake only — **not** a pull): [doc-lid-restore.md](doc-lid-restore.md). Distinguishes **Live** vs **Next** vs **Later**, and **build-breadth (now)** vs **ship-MVP cut (later)**. Do not invent Stripe, a shop opening, a shipped Member host migration, a removed `app.` alias, or Matrix / Apex / Grok chat. **Chat v1** (human / polling / Owner-starts-rooms) and **Dashboard** (24h bay strips + todos + parts POs) are **LIVE on Doc**.

---

## Host split (LOCKED — Ben GO ~12:22; ops LIVE at edge ~12:55)

| Host | Role |
|------|------|
| **projectcar.ca / www** | **Customer app.** Brochure + waitlist are live today. Member self-serve booking / balance **migrates here** as a follow-up slice. |
| **ops.projectcar.ca** | **LIVE management hostname** at the edge (2026-09-06 ~12:55 America/Edmonton). Staff-on-shift / ops management UI. Owner uses it too. Staff login via OIDC later. **Not Owner-only.** Some clients still have **local DNS cache** that misses `ops.` — use `app.` or flush cache; do not treat that as “ops is not live.” |
| **app.projectcar.ca** | **Temporary alias** until Ben cuts this DNS. Still live on the same Doc shop UI (`:3000`). Do not treat `app.` as the intended name. **Not removed.** |
| **api.projectcar.ca** | Shop API. Public waitlist + authenticated Owner / Member. Tunnel → Doc `:8000`. Lead owns Doc `:8000`. |

Do **not** describe the management host as Owner-only. Staff will use `ops.` (OIDC later); Owner uses the same host.

**Today** the Member demo still lives on the shop UI at `/member` (reachable on **`ops.`** and the temporary **`app.`** alias). Do **not** claim the customer-host migration is shipped. “Member UI on projectcar.ca” is **Next**.

Private Mission Control stays off these hosts.

---

## Build breadth (now) vs Ship-MVP cut (later gate)

Ben / Chief lock. Two horizons — do **not** collapse them into one “v1 includes everything forever.”

| Horizon | Meaning |
|---------|---------|
| **Build breadth (now)** | Breadth-first **placeholders** / rough IA so layout can be ironed out. Put planned features in the app (Parts, Tools, job board, cams, calendar / fill, member surfaces, Payments, Chat v1 demo). Do **not** leave a planned surface out of the **build** for polish. A placeholder is not shipped product. |
| **Ship-MVP cut (later gate)** | At **public MVP release**, cut unfinished and unnecessary features. Do **not** ship every placeholder as the public product. What ships then is a later Ben cut — not “everything we sketched now.” Draft cut-vs-keep (proposed candidates, not a Ben lock): [ship-mvp-cut.md](ship-mvp-cut.md). |
| **Shop OS CI (git-only)** | GitHub Actions on PRs (and push to `main`) that touch `apps/project-car/**`: shop-api `pytest`, shop-web lint / typecheck / `next build`. **Not** a Doc deploy. Green CI is **not** Doc unfreeze GO. Doc checkout stays frozen at `4cf8924` / `5swmVz` until **Ben GO** ([doc-unfreeze.md](doc-unfreeze.md)). Runbook: [shop-os-ci.md](shop-os-ci.md). |

Do not invent extra product from this table. Calendar heat-map / weekly per-hoist is on `main` (PR #18). Next-day fill is on `main` (PR #20). Breadth placeholders are on `main` (PR #21). Inventory prefixes are locked on `main` (PR #26: B1–B6 / TC / PT; CM later) — demo lists, not live checkout. Member parts is a **request desk** now; **full** purchasing is **Later**. **Chat v1** (human / polling / Owner-starts-rooms) is **LIVE on Doc** (PR #27) — not Matrix, not Apex, not Grok. **Dashboard** 24h strips + todos + parts POs are **LIVE on Doc** (PR #28). OwnerShell + bay-hero polish is **on git** (PR #69) — keep-path polish, not a new breadth slice; **not LIVE on Doc**.

---

## Live

- **Brochure:** https://projectcar.ca and https://www.projectcar.ca. Origin: Cloudflare Worker **`projectcar-brochure`** Direct Upload of `apps/website/html` from `main`. Worker Assets **`html_handling: none`** — `*.html` is canonical. Classic Pages git is **skipped** pending Cloudflare ↔ GitHub auth — plan only: [brochure-pages-cutover.md](brochure-pages-cutover.md). Do not start Pages git from a docs PR. Not the Doc `:8088` tunnel. **Apex sidecar deferred** (brochure stays Pages/Worker). Standing re-deploy: `brochure-worker-deploy.md`.
- **Live URL SSOT (Option A):** Zone Redirect pack is **FULL 10/10** Active Dynamic **301**s (apex+www) — capacity lock; no new Dynamic rules for brochure pretty-URLs. **Docs SSOT** is the frozen Sep 10 2026 inventory (box parking, pasted as-is — do **not** invent rules): [brochure-worker-ci.md](brochure-worker-ci.md) Option A matrix · [brochure-pages-cutover.md](brochure-pages-cutover.md) **hard cutover gate**. Owns the 10 named rules: brochure-root-to-index, brochure-shop-to-the-shop, brochure-shop-html-to-the-shop, membership/about/the-shop/contact/roadmap-to-html, brochure-chat-slash-to-contact, brochure-chat-to-contact. `html_handling: none` is Worker Assets — **not** a Redirect Rule. Garage must **not** invent new brochure extensionless pretty-URLs that need another Dynamic Redirect Rule until **#82** Worker-live + Bulk Phase1 frees Dynamic slots. Redirect plan A is parked — Bulk Phase1 pretty-URL pack already drafted: `membership` / `about` / `the-shop` / `contact` / `roadmap` ± slash → `*.html`; `root` / `index` / `shop` / `chat` stay Dynamic until migrate. Worker `_redirects` is **thin**: **chat → contact.html only** (no `/` or `/shop` in Worker `_redirects`). Do **not** claim Worker honors `_redirects` `/shop` → 302 `/the-shop`. Prefer merge **#83** after **#82** base (CI-only thin `_redirects` assert) — **parallel with upload OK**; **#83** does **not** need Bulk Phase1. **Never #81.** Bulk Phase1 stays after upload only for Member-edge slots. Soft-530 is **Worker-live** (PR **#80**): **root** `waitlist.js?v=3` + `styles.css?v=36` + Soft-530 Discord honesty on Membership/Contact. Live HTML is root-relative — probe **`/waitlist.js?v=3`** + **`/styles.css?v=36`** (**200** apex+www); `/assets/waitlist.js?v=3` or `/assets/styles.css?v=36` **404** is **NOT** a regression. **Option A completeness ladder:** Redirect **PASS** + Soft-530 UX **LIVE** ≠ Worker body freshness **PASS**. Live Home still **STALE** until **#82**: canonical/`og:url` and nav Home still bare `/`; sitemap loc bare `/` (lastmod **2026-09-08**). Soft-530 **CLEAR** does not change that and **never auto-GOs #82**. Body asserts stay on the post-#82 Direct Upload smoke gate (index.html canonical/og/nav/sitemap — Live **#82** smoke gate below). Home canonical/og/sitemap must be `/index.html` after **#82** — **#82 not yet Worker-live**. Paper only — no Zone live apply from this fold. See [brochure-worker-ci.md](brochure-worker-ci.md) · [brochure-worker-deploy.md](brochure-worker-deploy.md). Post-dual-CLEAR **brochure lane** after **#82** Worker-live: [post-dual-clear-go.md](post-dual-clear-go.md) (**#83** ∥ **Bulk Phase1**, not Bulk→#83; Member after Bulk; unfreeze+#79.1 is Shop OS parallel). **#82 Worker-live ≠ retire Dynamic (`49ccf90`):** PASS (upload + purge + freshness: canonical / og / sitemap + nav `/index.html`, no `href="/"`) does **not** delete the **10/10** pack. Keep full Option A Dynamic (`root`/`shop` + pretty-URL + `chat`) until Bulk Phase1 is **LIVE** and smoke-equal; then drop pretty-URL Dynamic per parked Redirect A; keep `root`/`shop` unless Bulk covers them. Do **not** migrate Dynamic on upload day.
- **Option A completeness ladder (2026-09-15 stamp — #70 SSOT):** Three rungs — do **not** collapse. (1) **Redirect PASS** = Zone Dynamic **FULL 10/10** apex+www **301**s — **LIVE**. (2) **Soft-530 UX LIVE** = root `styles.css?v=36` + `waitlist.js?v=3` + Discord honesty — **LIVE**. (3) **Worker body freshness PASS** = Home canonical / `og:url` / nav Home / sitemap loc = `https://projectcar.ca/index.html` (not bare `/`) — **STALE until #82**. **Lock:** Redirect PASS + Soft-530 UX LIVE ≠ Worker body freshness PASS. Live Home still **STALE**: canonical/`og:url` and nav Home still bare `/`; sitemap loc bare `/` (lastmod **2026-09-08**). Soft-530 **CLEAR** does not change that and **never auto-GOs #82**. Redirect+UX **PASS** is **never** #82 done. Body asserts stay on the post-#82 Direct Upload smoke gate ([brochure-worker-ci.md](brochure-worker-ci.md) — index.html canonical/og/nav/sitemap after upload + **mandatory** purge). Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) · [website-improvements.md](website-improvements.md) · [brochure-redirect-watch.md](brochure-redirect-watch.md) (**Redirect-only PASS ≠ Soft-530 UX PASS** is rungs 1≠2; this ladder adds 1+2 ≠ 3). This fold does **not** execute Zone Direct Upload / purge / **#82**.
- **Living Soft-530 OPEN + vault OPEN dual-OPEN both 1033 (2026-09-15 stamp — #70 SSOT; supersedes dual-OPEN `5f2fd1c`):** Soft-530 **OPEN 530 / CF 1033**: `api.` / `app.` / `ops.` (Lookout + Lead **~16:36**; **was CLEAR**). Vault independently **OPEN** subclass **530 / CF 1033** connector-down (Lead-verified **~16:28** America/Edmonton; **was 502** since ~19:45 MT Sep 13) on `/alive` + `/api/config` — **not** CLEAR. Soft-530 **OPEN 1033** + vault **1033** is dual OPEN (half-state **OVER**). Recovery fork: **1033** → lightning Healthy / cloudflared first; **502** → `docker.service` enabled+active. Subclass flip = Lead/Lookout field DIFF ≠ Ben re-nag if already asked once. ListMachines: Docs `95a229f5` **CONNECTED** / Shell **unreachable** (lid-restore **blocked**); lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED (`Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning`). Ben asked once Doc awake/desktop — **no re-nag**. Dual OPEN both 1033 — **not** camp GO, **not** dual CLEAR. Soft-530 **CLEAR ≠ #82** ≠ unfreeze ≠ Bitwarden. Lookout: api `ok=false` **expected**; vault `ok=false` **expected** (dual OPEN both 1033). Vault still **OPEN 530 / CF 1033**. **No Ben re-nag.** Chief checking lightning cloudflared/tunnel separately. Brochure **Option A completeness ladder:** (1) Redirect **PASS** Zone **FULL 10/10** — **LIVE**; (2) Soft-530 UX **LIVE** (`waitlist.js?v=3` + `styles.css?v=36` + Discord honesty); (3) Worker body freshness **STALE** until **#82** — Home canonical/`og:url` + nav Home still bare `/`; sitemap loc bare `/` (lastmod **2026-09-08**). Redirect+UX **PASS** ≠ **#82** done. Soft-530 **CLEAR** does not change that and **never auto-GOs #82**. Freeze intact **`4cf8924`** / BUILD_ID **`5swmVz`**. **#82** still Ben GO. Soft-530 **OPEN ≠ #82 blocked** — Ben may GO Worker-live (Direct Upload + **mandatory** purge + freshness) while Soft-530 and/or vault are still **OPEN** (brochure is CF Worker/Zone, not Doc tunnel). Apex deferred. Fri 2026-09-11 ~06:50 Soft-530 CLEAR is **historical**. Index pointers stay: [vault-stay-up.md](vault-stay-up.md), [dual-host-outage.md](dual-host-outage.md), [soft-530-extended-open.md](soft-530-extended-open.md) (quiet-ops — duration ≠ GO), [doc-reappear-first-hop.md](doc-reappear-first-hop.md) (first hop when `Docs-MacBook-Pro` reappears — machineId `95a229f5-9296-4a18-aa98-70fd300dabdf`; **cold-boot** if Docs **ABSENT ≥1 calendar day** / Soft-530 **OPEN** 1033 **≥24h** with Docs **ABSENT** — cloudflared + KeepAlive + CDM note **then** smoke; never treat multi-day lid as overnight sleep; living ABSENT since Fri 2026-09-11 ~16:48 **~4d**; never `Mac.lan` / Porsche), [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (first hop when `lightning` / McKing reappears — machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` label **lightning**; never `Mac.lan`), [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (first Soft-530 recovery smoke after extended-OPEN CLEAR — **before** desk / **#79.1** / unfreeze), [vault-clear-smoke.md](vault-clear-smoke.md) (first vault recovery smoke after extended-OPEN CLEAR — **before** Bitwarden / desk; HTTP **+** `docker.service` permanence on the **502** path; living subclass **530 / CF 1033** first hop is lightning Healthy / cloudflared — classify **1033→200** vs **502→200**), Soft-530 post-CLEAR ([doc-lid-restore.md](doc-lid-restore.md) `@55e10d0`), vault post-CLEAR (`@d88cacb`), [post-dual-clear-go.md](post-dual-clear-go.md). **Camp SSOT** is paper only ([camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md)) — **Ben GO**, never auto-cutover; camp recovery ≠ McKing docker; McKing shop-host + vault-stay-up are **PARKED for camp weeks**; sequencer does **not** assume McKing vault wake first. **Doc local VW/NC healthy ≠ public vault CLEAR** — Doc loopback `:8222` `/alive` + `:8080` after cold-boot = **Z1 preflight only**; public `vault.projectcar.ca` still **530 / CF 1033** = still vault **OPEN** (not partial CLEAR / not dual CLEAR / not camp progress %); [vault-clear-smoke.md](vault-clear-smoke.md) SSOT stays the public hostname. **Home-awake** ([home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md)): Soft-530 **OPEN 530 / CF 1033** + vault **OPEN 530 / CF 1033** is living **dual OPEN** (prior home-awake CLEAR half-state **superseded ~16:36**); do **not** enter [post-dual-clear-go.md](post-dual-clear-go.md) until vault CLEAR; Soft-530 CLEAR at home ≠ camp GO; freeze live-verify **now allowed** (last_known **`4cf8924` / `5swmVz` still unverified**); Lookout api `ok=false` / vault `ok=false` **expected** (dual OPEN both 1033). Home-off (Docs ABSENT + Soft-530 OPEN) remains EXPECTED when Doc is off again. Soft-530 **CLEAR ≠ #82**. Soft-530 **OPEN ≠ #82 blocked**. This paper does **not** invent vault CLEAR, unfreeze, **#82**, Zone execute, companion re-ask, Ben Doc-wake / docker re-ask, or Bitwarden.
- **#82 Worker Direct Upload smoke gate (paper — not executed):** Body freshness asserts **live here** (index.html canonical / `og:url` / nav Home / sitemap loc = `https://projectcar.ca/index.html`). Redirect **PASS** + Soft-530 UX **LIVE** ≠ this gate **PASS**. Soft-530 **CLEAR** ≠ this GO. **#82** Ben GO unchanged. Soft-530-independent — Soft-530 **OPEN ≠ #82 blocked**. Soft-530 companions **HOLD**. After GO, Zone smoke is **ordered**: (1) **Direct Upload** `apps/website/html` onto Worker **`projectcar-brochure`**; (2) **mandatory purge** `projectcar-brochure` Worker/Cache (**apex + www**); (3) **then** assert body freshness. Do **not** treat `Cache-Control: no-cache, must-revalidate` as sufficient — live HTML steady-state is `cf-cache-status: HIT` + no-cache. Freshness: Home canonical/og/sitemap = `https://projectcar.ca/index.html`; nav/logo not `href="/"`; `/shop.html`→`/the-shop.html` **301 via Zone**; Worker `_redirects` **thin** (chat→contact only); Soft-530 assets `waitlist.js?v=3` + `styles.css?v=36`. Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) (assert list) · [brochure-worker-deploy.md](brochure-worker-deploy.md) (upload click-path). This fold does **not** execute the upload or purge. **Never** Direct Upload from **#81** (draft + superseded). **PASS ≠ delete Option A Dynamic.** Keep the **10/10** pack until Bulk Phase1 is **LIVE** and smoke-equal — do **not** migrate Dynamic on upload day.
- **Hard lock / prefer merge (paper):** **Never #81.** Wrong lock was `#82 → Zone Direct Upload → Bulk Phase1 → #83`. Correct after **#82** Ben GO + Zone Direct Upload + mandatory purge/freshness: **#83** and **Bulk Phase1** are **parallel** (not Bulk→#83). (A) **#83** merge anytime on the **#82** base — CI-only thin `_redirects` assert; **may run in parallel with the upload**; does **not** need Bulk Phase1. (B) Bulk Phase1 **only** to free Dynamic slots when Member needs capacity — **after** Bulk is **LIVE** and smoke-equal to today’s **10/10** (keep `root`/`shop`/`chat` Dynamic until then). **#82 PASS ≠ wipe Dynamic.** Do **not** migrate Dynamic on upload day. **Member edge only after Bulk.** Prefer merge **#83** on the **#82** base — do **not** wait for Bulk. Post-dual-CLEAR menu ([post-dual-clear-go.md](post-dual-clear-go.md)): after **#82** Worker-live this is the **brochure lane** — unfreeze+#79.1 is **Shop OS parallel**, not “next after **#82**.” Soft-530 companions **HOLD**. Reality quarantine until **#82** unchanged. Paper only — no Zone/Garage execute.
- **Post-#82 reconcile (one tip-fold — paper now):** After Zone Direct Upload of **#82** **and** the **mandatory purge** (smoke gate above), fold `main` Reality to live: (1) Home canonical / og / sitemap = `https://projectcar.ca/index.html`; (2) nav / logo not `href="/"`; (3) Zone Redirect pack SSOT (**FULL 10/10** / Bulk Phase1 path); (4) Worker `_redirects` **thin** (chat→contact only); (5) assets `waitlist.js?v=3` + `styles.css?v=36`; (6) `/shop.html` → `/the-shop.html` **301 via Zone** — never reintroduce Worker `/shop` **302** fight; (7) keep `/member*` Worker **404** until Member edge Ben GO after Bulk Phase1 — never **301** into `membership.html`. Do **not** treat HTML `Cache-Control: no-cache, must-revalidate` as proof freshness — assert **after** purge. Until that upload, **#70 Reality tip / Option A** is brochure live SSOT — do **not** execute from `main` Reality tip. See [brochure-worker-ci.md](brochure-worker-ci.md), [brochure-worker-deploy.md](brochure-worker-deploy.md), [member-zone-edge.md](member-zone-edge.md). This fold does **not** execute the upload.
- **Pages:** Home, About, The Shop, Membership, Roadmap, Contact. Chat nav/page stripped (PR #5). Apex deferred.
- **Brochure hygiene:** P0-3…P0-7 is in git (`apps/website/html`: robots, sitemap, `404.html`, favicon set, Home progress bar removed) **and already live** on the Worker (re-uploaded 2026-09-06). Classic Pages git still skipped (plan: `brochure-pages-cutover.md`).
- **Brochure P1-1 / P1-3:** **LIVE** on Worker `projectcar-brochure` (2026-09-07). Garage PR #52 → `5361212`. Home `cta-hero` waitlist CTA weight; Contact Phone/IG/TikTok/YouTube/Skool collapsed to one `contact-soon` line. Zone Direct Upload; smoke PASS (Home CTA + Contact; apex/www 200). P1-6 stays deferred.
- **Brochure P2-1:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #55 → `453e44d`. Absolute `og:url` / `og:image` + `twitter:card`. Zone smoke PASS.
- **Brochure P2-2:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #56 → `ed41d23`. Zone Direct Upload ~15:48 America/Regina. Apex `<link rel="canonical">` matches P2-1 `og:url` (host `https://projectcar.ca`, not www). Zone smoke PASS: Home + Membership `rel=canonical` matches `og:url` apex; www serves apex canonicals.
- **Brochure P2-3:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #57 → `be60a01`. Zone Direct Upload ~16:01 America/Regina. Cache `?v=` SSOT: `styles.css?v=34` + `banner-logo.png?v=30` on public HTML. Smoke PASS: Home/About/Contact at minimum; apex+www.
- **Brochure P2-4:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Zone Transform Rules on apex+www: **`brochure-security-headers`** (`X-Content-Type-Options: nosniff`; `Referrer-Policy: strict-origin-when-cross-origin`; `X-Frame-Options: DENY`; `Permissions-Policy` static defaults); **`brochure-html-no-cache`** (`/` + `*.html`: `Cache-Control: no-cache, must-revalidate`); **`brochure-asset-immutable`** (`?v=` `.css`/`.js`/`.png`: `Cache-Control: public, max-age=31536000, immutable`). Smoke PASS (Worker **200**s, not challenge 403): `/` + www 200 + required security + HTML no-cache; `styles.css?v=34` 200 + security + immutable long-cache; `robots.txt` 200 `text/plain` + security + `max-age=0`. DNS/tunnels untouched. Record: [brochure-security-headers.md](brochure-security-headers.md). Do **not** re-apply from a docs PR.
- **Brochure P3-1:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #61 → `88c9820`. Zone Direct Upload ~16:50 America/Regina. Home is pitch + `cta-hero` Join the waitlist + Project Underway + Shop/Membership pointers; no feature-bullet dump. Smoke PASS. P3-2 / P3-3 About stay **held / deferred**.
- **Brochure P3-4:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #65 → `f191ad5`. Zone Direct Upload ~18:46 America/Regina. Shop 200: facility bullets (bays/hoists, shared fab, safety/cleanliness, structured shop) + honesty “intended shop capability — not live yet”; no 24/7 packaging on Shop. Membership still “24/7 shop access (when operational)”. Home still P3-1 pitch + `cta-hero` waitlist + Project Underway; no capability-bullet dump. `styles.css?v=34`; P2-4 headers still present. Smoke PASS apex+www. P3-2 / P3-3 About stay **held / deferred**.
- **Brochure P3-5:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #62 → `9d83bfc`. Zone Direct Upload. Shop/Membership/Roadmap use “24/7 shop access (when operational)”. Home (and About/Contact) have no absolute 24/7. Smoke PASS. P3-2 / P3-3 About stay **held / deferred**.
- **Brochure P4-2:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #67 → `2b772ff`. Zone Direct Upload ~19:15 America/Regina. Home: equal `cta-hero` Join the waitlist + Join Discord (`discord.gg/projectcar`); contact email · Discord. Membership: waitlist form unchanged; Discord contact-primary + alongside-waitlist copy. sitemap lastmod bumps (2026-09-08 present). `styles.css?v=35` at that upload; later Soft-530 **#80** is `styles.css?v=36` + `waitlist.js?v=3` (Worker-live). Pretty URLs are the Zone Redirect pack (Live URL SSOT — **FULL 10/10** capacity lock) — not Worker `_redirects` `/shop` 302. P2-4 headers still; P3-4 Shop facility bullets still. Smoke PASS apex+www. P3-2 / P3-3 About stay **held / deferred**. P1-6 stays deferred.
- **Waitlist:** Membership / Contact `POST` JSON to `https://api.projectcar.ca/waitlist`. Soft-530 is **Worker-live** (PR **#80**): `waitlist.js?v=3` + `styles.css?v=36`; Membership/Contact fail-soft to Discord (`discord.gg/projectcar`) + mailto on 502 / 530 / 1033 — not “try again.” Fail-soft stays **permanent #80**. Living Soft-530 is **OPEN 530 / CF 1033** (`api.` / `app.` / `ops.`; Lookout + Lead **~16:36**; **was CLEAR**). Vault still **OPEN 530 / CF 1033**. Fri 2026-09-11 **OPTIONS 200** / Chief **POST 201** + CORS is **historical**. CORS allowlist includes brochure origins (`https://projectcar.ca`, `https://www.projectcar.ca`), localhost, **`https://ops.projectcar.ca`**, and the temporary alias **`https://app.projectcar.ca`**. **e2e PASS** (brochure → api) when public **GET /health** is **200**. See `cors-origins.md`. **Permanent #80 product behavior** (overnight sleep / tunnel flaps — **not** temporary outage banners): Membership/Contact Soft-530 Discord honesty intro + `waitlist.js?v=3` fail-soft stay after CLEAR. Soft-530 **CLEAR** recovery smoke ([soft-530-clear-smoke.md](soft-530-clear-smoke.md)) ≠ Garage GO to strip that copy. **CLEAR ≠ honesty-off.** Tone tweak only with Ben GO. Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) Soft-530 Discord · [website-improvements.md](website-improvements.md) P1-5.
- **Waitlist Owner desk (LIVE when origin up):** Already shipped — **retired** from the post–`brochure-worker-ci` parked queue. Do **not** open `Docs/waitlist-owner-desk.md`. `apps/project-car/web/app/waitlist/page.tsx` — `OwnerShell` `current=waitlist`; GET `/waitlist` list + mark contacted. OwnerShell nav Waitlist → `/waitlist`. Live when Doc origin is up: `ops.` / `app.` `/waitlist` **307** → `/login?next=/waitlist` (route on frozen BUILD_ID **`5swmVz`**); `api` GET `/waitlist` **401** unauthorized without Owner (endpoint live **when origin up**; living Soft-530 **OPEN 530 / CF 1033**). Optional CSV export stays **Later**. No email blast / Stripe / Member host. Anti-goal: Soft-530 **CLEAR** or desk existence ≠ Doc unfreeze ≠ **#79.1** rebuild GO. **#82** Ben GO / Soft-530 companions HOLD / weekend coverage (`84107e7`) / dual-Nextcloud / `brochure-worker-ci` (`6e6efe8`) / **#79.1** git-only until Doc unfreeze — **unchanged**. This fold does **not** execute Zone or Garage.
- **Lookout `projectcar-api-health-watch`:** **resumed** (`enabled:true`) 2026-09-11 ~06:52 America/Edmonton (Lookout confirmed). Living dual OPEN: api `ok=false` **expected** (Soft-530 **OPEN 530 / CF 1033**); vault `ok=false` **expected** (vault **OPEN 530 / CF 1033**). **`enabled:true` ≠ firing.** Living 2026-09-15: **calendar-day chronic HOLD** (day's 1× spent; stalled ~**56m** since ~**08:21** — same class as vault). Quiet-ops “Lookout continues” is **false for flip detection** while vault is also chronic HOLD ([soft-530-extended-open.md](soft-530-extended-open.md)). Interim = Chief `*/20` + Lead coding-progress probes (accept **≤~20m**). Next local day after **America/Edmonton midnight** Lead **may** try **one** in-place re-arm; **re-arm success ≠ first fire** — require stickiness (≥3 consecutive `*/5` windows) before healthy ([lookout-rearm-sop.md](lookout-rearm-sop.md) row 8). **HOLD** Ben recreate. Lead interim probe ended. **#78 lookout-resume** is **LIVE-SUPERSEDED** — api-health is already **`enabled:true`** while Doc checkout stays frozen at `4cf8924` / `5swmVz`. Soft-530 companion watches stay **HOLD / not armed** (Ben skipped ~14:35 America/Edmonton — do **not** re-ask). Future lid-close **530 / 1033** on `api.` / `ops.` / `app.` is still expected. This Soft-530 watch is **api-only** — `ops.` / `app.` (and `cloud.`) can **530 while `/health` stays 200** (same class as pre-vault-watch; companion watches **HOLD / not armed**, Live below). It also does **not** cover vault — `api.` `/health` can stay green while vault dies (vault flip watch is a **separate LIVE/armed** watch, Live below). Soft-530 companion watches ≠ vault `/alive` watch ≠ Doc lid-restore. Weekend flip coverage (plan-improve off Sat/Sun) = this api `/health` + vault `/alive` **only**. See P4-5 in [website-improvements.md](website-improvements.md).
- **Lookout brochure redirect watch (paper — GONE / HOLD recreate):** While Soft-530 **OPEN**, Option A is the only public **LIVE** surface. Living watch `brochure-option-a-redirect-watch` was **deleted** to fix stalled cron; Auto-review **blocked** recreate — watch **GONE**. **Never delete** an armed Lookout watch to fix cron — prefer in-place update / pause-resume / re-save. **HOLD** after one Ben ask. Weekday interim coverage = Chief plan-improve **`*/20` redirect smoke + Soft-530 UX acceptance** until recreate is enabled. Overnight (Mon–Thu after last `*/20` ~17:40 MT until next weekday first fire ~06:00) **and** weekend (Sat/Sun plan-improve-off, `84107e7`) Option A Dynamic redirect is **explicitly uncovered — accept that risk**. Do **not** invent overnight/weekend plan-improve, Zone, or Garage coverage. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **HOLD** a second Ben recreate ask ([lookout-rearm-sop.md](lookout-rearm-sop.md) already one-ask). **Redirect-only PASS ≠ Soft-530 UX PASS** — `membership.html` + `contact.html` must still show Soft-530 Discord honesty; **root** `/waitlist.js?v=3` must still fail-soft **502 / 530 / 1033** (`0705b37`). After Ben approves: assert routine **enabled** + first fire within **10m** + baseline still **LIVE 10/10**. Flip **Chief + Lead only**; **never Ben**. **Not** the Soft-530 ops/app companion watches (Ben skipped ~14:35 America/Edmonton — do **not** re-ask). **#78 lookout-resume** is **LIVE-SUPERSEDED** (api-health already `enabled:true`) — this is **not** a #78 re-run. Redirect asserts (apex+www): `/` **301** → `/index.html`; `/shop` + `/shop.html` **301** → `/the-shop.html`; extensionless pretty pack **301** → matching `*.html`; **root** `/styles.css?v=36` + `/waitlist.js?v=3` **200** — **not** `/assets/` (`/assets/styles.css?v=36` or `/assets/waitlist.js?v=3` **404** is **NOT** a regression). Sep 9 Lookout `brochure-routing-baseline.json` still says extensionless **IN FLIGHT** — stamp living **LIVE 10/10** (same **root** CSS/JS URLs). This fold does **not** recreate the watch and does **not** execute Zone/Garage. Essay: [brochure-redirect-watch.md](brochure-redirect-watch.md). SOP: [lookout-rearm-sop.md](lookout-rearm-sop.md). Quiet-ops: brochure-redirect **paper** continues; continuous watch **GONE** ([soft-530-extended-open.md](soft-530-extended-open.md)). Living dual-OPEN (`5f2fd1c`) **unchanged**.
- **Lookout rearm SOP (paper — never delete to fix cron):** Living incident: Option A watch **GONE** (delete-to-fix-cron → Auto-review blocked recreate). **Living class (2026-09-15):** **Lookout stall ≠ vault HTTP flip** — vault **or** api can be `enabled:true` but cron-stalled; that is **not** vault OPEN 502. **Re-arm success ≠ first fire** — require stickiness (`lastRun` advances across **≥3 consecutive** `*/5` windows, ≥10–15m). Living ~09:14 MT: vault re-arm ~08:50 → first fire ~08:55 → stall ~**22m**; api stalled ~**56m** since ~08:21. **Chronic:** stall again within 24h → **≤1** pause/resume per calendar-day (clock resets **America/Edmonton midnight**); Lead paper only after today's 1×. Living 2026-09-15: **both api + vault chronic HOLD today** — quiet-ops “Lookout continues” is **false for flip detection**; interim Soft-530+vault SSOT smoke = Chief `*/20` + Lead probes (accept **≤~20m**); Chronic Lookout ≠ quiet-ops failure ≠ Ben ping; chronic HOLD does **not** reopen Ben recreate; next local day Lead **may** try one in-place re-arm then stickiness ≥3 `*/5`; Soft-530 companions + brochure recreate stay **HOLD**. (1) **Never delete** an armed Project Car Lookout watch to fix stalled cron. (2) Prefer in-place update / pause-resume / re-save schedule. (3) If delete already happened and recreate needs Auto-review: **HOLD** after one Ben ask. (4) Weekday Option A coverage = Chief plan-improve `*/20` redirect smoke **+ Soft-530 UX** until recreate is enabled — overnight/weekend Dynamic redirect **UNCOVERED** (accept that risk; do **not** invent overnight plan-improve / Zone / Garage coverage) — **Redirect-only PASS ≠ Soft-530 UX PASS**. Last weekday `*/20` writes [overnight-baseline-stamp.md](overnight-baseline-stamp.md) (**refresh only on field DIFF**); first weekday `*/20` after ~06:00 diffs that stamp — **same-class = stay quiet**; Lead/Ben only on class DIFF. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **HOLD** a second Ben recreate ask. (5) After Ben approves: assert routine enabled + first fire within **10m** + baseline still **LIVE 10/10**. (6) Soft-530 companions stay **HOLD**. (7) `enabled:true` + cron-stalled ≠ missing watch ≠ HTTP flip — Lead **may GO** in-place re-arm of **both** watches; **HOLD** Ben recreate; Soft-530 api remaining **OK-ish** is **not** living contrast. (8) First fire ≠ healthy; stickiness before calling healthy; chronic ≤1 pause/resume per calendar-day. Until stickiness, interim = Chief `*/20` + Lead coding-progress probes — **not** a Ben ping. Docker permanence `@367172d` is a **different** tip. Anti-goals: not **#82** merge nag, not Dynamic wipe, not honesty-off, not companions rearm, not unfreeze, not Zone execute, not invent overnight redirect smoke, not Ben recreate/docker/Doc-wake from a stall, not call healthy on first fire, not loop pause/resume on chronic. **#78 lookout-resume** is **LIVE-SUPERSEDED** — do **not** collapse this SOP into #78. Essay: [lookout-rearm-sop.md](lookout-rearm-sop.md). Spec: [brochure-redirect-watch.md](brochure-redirect-watch.md). Plane: [vault-stay-up.md](vault-stay-up.md). Quiet-ops: [soft-530-extended-open.md](soft-530-extended-open.md). Honesty permanence: [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (`0705b37`). Paper only — no Lookout/Zone/Garage execute.
- **Lookout Soft-530 companion watches (HOLD / not armed):** Soft-530 five-row is Doc shop hosts `cloud.` / `api.` / `app.` / `ops.`, but Lookout Soft-530 coverage is **api-only** (`/health`). `ops.` / `app.` (and `cloud.`) can **530 while `api.` stays 200**. Companion flip watches are **HOLD / not armed** (Ben skipped companion-watch approval ~14:35 America/Edmonton — do **not** re-ask). Do **not** claim `enabled:true`: (1) `https://ops.projectcar.ca/login` — ok = **200** (or **307** → `/login` then **200**); down = **502 / 530 / 1033 / timeout**. (2) `https://app.projectcar.ca/login` — same. Optional `https://cloud.projectcar.ca/login` for five-row parity (Lead probed **200**). Cadence ≈ api watch. Baselines (Lookout-owned): `/workspace/lookout/projectcar-ops-health-baseline.json` + `projectcar-app-health-baseline.json` (+ cloud if later armed). Flip-only alerts: **Chief + Lead only**; **never Ben**; never restart / mutate. Soft-530 companion watches ≠ vault `/alive` watch ≠ Doc lid-restore. Soft-530 living is **OPEN** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton) — Fri CLEAR is historical. Api + vault watches continue. Vault flip watch stays **LIVE/armed**. Weekend does **not** arm these companions — do **not** re-ask. See [shop-web-stay-up.md](shop-web-stay-up.md), [api-stay-up.md](api-stay-up.md).
- **Lid-close / morning 530:** **Current: Soft-530 OPEN 530 / CF 1033** (`api.` / `app.` / `ops.`; Lookout + Lead **~16:36**). Vault still **OPEN 530 / CF 1033**. Fri 2026-09-11 restore and the 2026-09-13 dual-OPEN stamp are **historical**. Soft **530 / 1033** on `api.` / `ops.` / `app.` after lid-close is still expected. Amphetamine / plugged-in no-sleep is mitigation, not a guarantee (`doc-software-baseline.md`). Stay quiet and restore when Doc is reachable; ping Ben only if Doc will not wake / Grok Bot desktop is offline / the outage is prolonged. Ordered Lead sequence: [doc-lid-restore.md](doc-lid-restore.md) (process wake only — **not** a pull). Doc pull after **Ben GO**: [doc-unfreeze.md](doc-unfreeze.md). Green **shop-os-ci** is **not** that GO. Zone only if local origin is healthy but public is still 1033. No edge flip, no CF↔GitHub re-ask, no Member GO.
- **Soft-530 extended OPEN — quiet-ops lock (paper):** Soft-530 **OPEN** has crossed **~24h** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton) **and** vault is **OPEN** (**502** since ~19:45 MT Sep 13) **and** ListMachines is **Mac.lan only**. **Quiet-ops:** (1) **no Ben re-nag** — Doc-wake + McKing wake already asked; (2) **Lead** stays armed for `Docs-MacBook-Pro` reappear → [doc-reappear-first-hop.md](doc-reappear-first-hop.md) (identity `95a229f5-9296-4a18-aa98-70fd300dabdf` **then** [doc-lid-restore.md](doc-lid-restore.md) process wake); **Chief** stays armed for `lightning` reappear → [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (identity `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` **then** [vault-stay-up.md](vault-stay-up.md) vault wake); (3) Lookout `api.` + vault stay armed (**never delete** to fix cron; **`enabled:true` ≠ firing**; re-arm success ≠ first fire — stickiness ≥3 consecutive `*/5` windows; Lead may GO in-place re-arm of **both** when stalled **if** the calendar-day 1× is unused; chronic ≤1 pause/resume per calendar-day, clock resets **America/Edmonton midnight**); **chronic-HOLD exception:** living 2026-09-15 both watches **chronic HOLD today** → “Lookout continues” is **false for flip detection**; interim Soft-530+vault SSOT smoke = Chief `*/20` + Lead probes (accept **≤~20m**); Chronic Lookout ≠ quiet-ops failure ≠ Ben ping; chronic HOLD does **not** reopen Ben recreate; next local day Lead **may** try one in-place re-arm again then stickiness ≥3 `*/5`; Soft-530 companions + brochure recreate stay **HOLD**; brochure-redirect **paper** **continues** ([brochure-redirect-watch.md](brochure-redirect-watch.md)); living Option A watch **GONE** — **HOLD** recreate after one Ben ask; weekday Chief `*/20`; overnight/weekend **UNCOVERED** ([lookout-rearm-sop.md](lookout-rearm-sop.md)); Soft-530 companions stay **HOLD until Ben reopens** — **not** weekend-only; (4) Brochure Option A is public SSOT during extended OPEN; (5) **duration alone ≠** **#82** GO ≠ unfreeze ≠ Zone Direct Upload ≠ Bitwarden ≠ companion re-ask. CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md). Next Soft-530 CLEAR after this lock: [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (**before** desk / **#79.1** / unfreeze). Essay: [soft-530-extended-open.md](soft-530-extended-open.md). Weekday overnight twin (Mon–Thu after last `*/20`): [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md). Wake order stays [dual-host-outage.md](dual-host-outage.md). First hop when Doc reappears: [doc-reappear-first-hop.md](doc-reappear-first-hop.md). First hop when McKing / `lightning` reappears: [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md). This fold does **not** invent CLEAR or execute Zone/Garage.
- **Doc first reappear — first hop (paper — after quiet-ops):** When `Docs-MacBook-Pro` **first reappears** on ListMachines after extended Soft-530 OPEN / quiet-ops: (1) assert identity machineId **`95a229f5-9296-4a18-aa98-70fd300dabdf`** — **never** treat `Mac.lan` or Porsche as Doc; (1a) classify wake — **cold-boot** if Docs **ABSENT ≥1 calendar day** **or** Soft-530 **OPEN** CF **1033 ≥24h** with Docs **ABSENT**; overnight **warm wake** only if **<24h ABSENT after a prior CLEAR**; (1b) on **cold-boot**, **before** smoke: verify cloudflared LaunchAgent/service **up** + shop-api + shop-web KeepAlive **loaded** + **note** CDM/caffeinate posture; (2) run [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (`api.` `/health` **200**, waitlist OPTIONS CORS apex+www, POST **422**/**201**) after that pre-smoke (warm wake: shorter [doc-lid-restore.md](doc-lid-restore.md) process-wake); (3) **only if CLEAR** → Soft-530 post-CLEAR forensic baseline on lid-restore (`55e10d0`); (4) then follow [post-dual-clear-go.md](post-dual-clear-go.md) — **never auto-fire** **#82** / unfreeze / **#79.1** / companions / Zone upload / Bitwarden; (5) Chief may write a **chief-to-doc** hop **only after** CLEAR smoke. Vault CLEAR stays independent (Chief `lightning` / McKing lane). Anti-goals: `Mac.lan` Shell ≠ Doc evidence; **multi-day lid ≠ overnight sleep**; duration alone ≠ Ben wake; **CLEAR ≠ honesty-off**; not Lookout companion re-ask. Living this fold: Soft-530 **OPEN 530 / CF 1033**; vault still **OPEN** **530 / CF 1033** connector-down (was 502); ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED — home-awake pre-camp half-state. Lookout api `ok=false` / vault `ok=false` **expected** (dual OPEN both 1033). Option A Redirect+UX **PASS** (rungs 1–2 — **≠ #82**). **#82** Ben GO only. Soft-530 **CLEAR ≠ #82**. **No Ben re-nag.** Essay: [doc-reappear-first-hop.md](doc-reappear-first-hop.md). Monday map: [deployment-guide.md](deployment-guide.md). Paper only — no Zone/Garage execute. Does **not** invent a reappear or CLEAR.
- **McKing first reappear — first hop (paper — after quiet-ops / extended vault OPEN):** When **`lightning` / McKing first reappears** on ListMachines after extended vault OPEN / quiet-ops: (1) assert identity machineId **`9067d14b-46e5-4ef0-82d5-fce0febdc8f7`** (label **`lightning`**) — **never** treat `Mac.lan` as McKing; (2) run [vault-clear-smoke.md](vault-clear-smoke.md) (`/alive` **200** + `/api/config` VW **2026.6.0** + `docker.service` **enabled** + **active** + classify **502→200**; HTTP-only ≠ CLEAR) after Chief vault wake on the **asserted** host ([vault-stay-up.md](vault-stay-up.md)); (3) **only if CLEAR** → vault post-CLEAR forensic baseline on vault-stay-up (`d88cacb`); (4) Soft-530 independence — Bitwarden gate is **vault CLEAR** (Ben GO after `/alive` + VW **2026.6.0** + docker permanence), **not** Soft-530 CLEAR; Soft-530 **OPEN** can coexist with vault CLEAR + Bitwarden work; **never auto Bitwarden**; **never** treat vault CLEAR as Soft-530 CLEAR, unfreeze, or **#82** GO. Anti-goals: `Mac.lan` Shell ≠ McKing evidence; duration alone ≠ Ben wake; name-only ≠ identity; vault CLEAR ≠ sequencer; morning `*/20` ≠ re-ask after this hop. Living this fold: Soft-530 **OPEN** `api.`/`app.`/`ops.`/`cloud.` **530** CF **1033**; waitlist OPTIONS **530**; vault `/alive` + `/api/config` **502** (docker dead — **not** CLEAR); ListMachines **`Mac.lan` + `lightning`** (`docs=ABSENT`); hop already ran ~19:10 — still blocked on Ben sudo; freeze last_known **`4cf8924`** / **`5swmVz`**; **#82** still Ben GO; quiet-ops **HOLD**. Overnight ListMachines DIFF is stamp-absorbed ([overnight-baseline-stamp.md](overnight-baseline-stamp.md)) — **no morning re-ask**. Essay: [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md). Monday map: [deployment-guide.md](deployment-guide.md). Paper only — no Zone/Garage execute. Does **not** invent a reappear or CLEAR.
- **Soft-530 CLEAR recovery smoke (paper — after extended OPEN):** After Soft-530 **CLEAR** following **extended OPEN** (>~24h), run this **first** recovery smoke **before** any Owner desk / **#79.1** / unfreeze talk: (1) `api.` `/health` **200**; (2) waitlist OPTIONS **200** + CORS Origin `https://projectcar.ca` **and** `https://www.projectcar.ca`; (3) waitlist POST validation **422** or success **201** (prove route alive, not just tunnel-up); (4) `app.`+`ops.` may still **307**→`/login` with print `` `changeme` `` — that is freeze (`4cf8924` / **`5swmVz`**), **not** Soft-530; (5) when Doc public is up, `cloud.` `status.php` **200**. Anti-goals: this smoke ≠ **#82** GO ≠ companion re-ask ≠ Bitwarden; Soft-530 CLEAR alone ≠ unfreeze; vault CLEAR still separate McKing lane ([vault-clear-smoke.md](vault-clear-smoke.md)); this smoke ≠ Garage GO to strip **#80** Discord honesty / `waitlist.js?v=3` fail-soft (**CLEAR ≠ honesty-off**; overnight sleep / tunnel flaps, not temporary banners; tone tweak only with Ben GO). CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire**). Essay: [soft-530-clear-smoke.md](soft-530-clear-smoke.md). Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) Soft-530 Discord · STATUS Waitlist Live · [website-improvements.md](website-improvements.md) P1-5. Living this fold: Soft-530 **OPEN 530 / CF 1033**; vault still **OPEN 530 / CF 1033**; **Docs + lightning CONNECTED**. Waitlist OPTIONS = Origin CORS preflight — **bare 405 ≠ OPEN**. Post-CLEAR forensics + freeze live-verify **now allowed**; last_known **`4cf8924` / `5swmVz` unverified**. Half-state ≠ dual CLEAR. Soft-530 **CLEAR** + Doc VW/NC loopback healthy ≠ public vault CLEAR / camp GO. Paper only — no Zone/Garage execute. Does **not** invent vault CLEAR.
- **Vault CLEAR recovery smoke (paper — after extended OPEN):** After vault **CLEAR** following **extended OPEN** (**502** since ~19:45 MT Sep 13), run this **first** recovery smoke **before** any Bitwarden import/rotate or desk talk: (1) `https://vault.projectcar.ca/alive` **200**; (2) `https://vault.projectcar.ca/api/config` **200** with VW version present (expect McKing **2026.6.0** class, **not** Doc **2025.12.0**); (3) on `lightning` `systemctl is-enabled docker` == **enabled** **and** `is-active` == **active** (optional: `mcking` in `docker` group) — `/alive` **200** + docker **disabled/inactive** = **NOT CLEAR / recurrence risk** (reboot re-OPENs like ~19:45 Sep 13 disabled + SIGKILL stop); HTTP-only ≠ CLEAR; (4) classify recovery path — classify by living subclass — **530 / CF 1033** (living) → **1033→200** after lightning Healthy / cloudflared; **502** → **502→200** + docker permanence (Soft-530 tunnel class stays separate); (5) Soft-530 may still be **OPEN** — vault CLEAR alone ≠ Soft-530 CLEAR ≠ **#82** ≠ unfreeze ≠ companions; (6) Bitwarden import/rotate stays **Ben GO** after vault CLEAR + this smoke, **never auto-fire** — Soft-530 **OPEN** can coexist; do **not** block Bitwarden on Soft-530 CLEAR. Anti-goals: not Zone Direct Upload, not **#82**, not Doc unfreeze / **#79.1** execute, not companion re-ask, not Ben docker / Doc-wake re-nag, not Soft-530 CLEAR smoke (already `502ab2e`). CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire**). Capture forensics still [vault-stay-up.md](vault-stay-up.md) (`d88cacb`). Essay: [vault-clear-smoke.md](vault-clear-smoke.md). Living this fold: Soft-530 **OPEN 530 / CF 1033**; vault still **OPEN** **530 / CF 1033** (was 502 since ~19:45 MT Sep 13); ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED; Option A Redirect+UX **PASS** (rungs 1–2 — **≠ #82**); Home **STALE** until **#82**; freeze `` `changeme` `` / **`5swmVz`**; **#82** still Ben GO. Soft-530 **CLEAR ≠** this card. **SSOT stays `https://vault.projectcar.ca`** — **never** substitute Doc loopback; Doc VW/NC healthy = camp Z1 preflight only, **never** this smoke / vault CLEAR. Paper only — no Zone/Garage execute. Does **not** invent vault CLEAR or re-ask Ben docker.
- **Soft-530 weekday overnight (paper — Mon–Thu nights only):** After last weekday plan-improve `*/20` until next weekday first fire (~06:00). Soft-530 + vault coverage = Lookout `api.` `/health` + vault `/alive` **only** (ops/app companions **HOLD / not armed** — do **not** re-ask). While `brochure-option-a-redirect-watch` is **GONE**, overnight Option A Dynamic redirect is **explicitly uncovered — accept that risk** (same class as weekend `84107e7`; interim = plan-improve `*/20`). Do **not** invent overnight plan-improve, Zone, or Garage coverage. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **HOLD** a second Ben recreate ask ([lookout-rearm-sop.md](lookout-rearm-sop.md) already one-ask). No overnight Zone Direct Upload, no Garage execute, no Ben re-nag from duration alone. Friday last `*/20` hands **into weekend coverage** (next Live row) — **not** this card. Mid-overnight Soft-530 **CLEAR→OPEN** / Doc reappear still [doc-reappear-first-hop.md](doc-reappear-first-hop.md); McKing still [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (**never** `Mac.lan`; **never auto-fire**; **never** wake McKing docker as camp path). Complements weekend coverage (`84107e7`) + [soft-530-extended-open.md](soft-530-extended-open.md). Last weekday `*/20` writes [overnight-baseline-stamp.md](overnight-baseline-stamp.md) (**refresh only on field DIFF**); first weekday `*/20` after ~06:00 diffs that stamp — **same-class = stay quiet** (`611c267`); surface **Lead/Ben only on stamp class DIFF**; first-hop absorbed DIFF → no Ben re-surface + refresh living stamp. Living this fold: Soft-530 **OPEN 530 / CF 1033** + vault **OPEN 530 / CF 1033** **dual OPEN both 1033** (half-state **OVER**) (first Mon–Thu overnight after Sep 13–15 recovery; subclass Lead-verified **~16:28** — **was 502**); ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED; freeze **`last_known`** `` `changeme` `` / **`5swmVz`** / **`4cf8924`**; Lookout api `ok=false` / vault `ok=false` **expected** (dual OPEN both 1033); Option A Redirect+UX **PASS**; Home **STALE** until **#82**. Soft-530 **OPEN 1033** + vault **1033** is dual OPEN (half-state **OVER**). Soft-530 **CLEAR** overnight **≠** camp CDM soak **PASS** **≠** companions **≠** **#82** **≠** unfreeze **≠** Zone vault retarget **≠** Bitwarden auto. Same half-state ≠ dual CLEAR ≠ “incident closed.” Essay: [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md). Stamp: [overnight-baseline-stamp.md](overnight-baseline-stamp.md). SOP: [lookout-rearm-sop.md](lookout-rearm-sop.md). Monday map: [deployment-guide.md](deployment-guide.md). Paper only — no Zone/Garage execute.
- **Overnight baseline stamp (paper — last weekday `*/20` one-liner):** Last weekday plan-improve `*/20` writes a **one-line** baseline **before** the Lookout-only window. **Soft-530 CLEAR + vault OPEN is a first-class half-state class** — ≠ dual OPEN ≠ dual CLEAR; morning must **not** score it as “incident closed.” Tonight **~17:20** America/Edmonton (Tue 2026-09-15) is the **first Mon–Thu overnight** after Sep 13–15 recovery: Soft-530 **OPEN 530 / CF 1033** (Lookout + Lead **~16:36**; **was CLEAR**); vault **OPEN 530 / CF 1033** connector-down (Lead-verified **~16:28**; **was 502**); ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT**; freeze **`last_known`** `` `changeme` `` + BUILD_ID **`5swmVz`** / HEAD **`4cf8924`** (Soft-530 OPEN → unverifiable ≠ DIFF); Option A Redirect **PASS** + Soft-530 UX **LIVE**; Lookout api `ok=false` / vault `ok=false` **expected** (dual OPEN both 1033); brochure `*/5` watch still **GONE** (interim = plan-improve `*/20`). Home **STALE** until **#82**. Morning DIFF from this class: Soft-530 **CLEAR→OPEN** = DIFF (Lead wake / doc-reappear); vault still **530 / CF 1033** = **SAME**; vault **502 ↔ 530/1033** = Lead/Lookout field DIFF ≠ Ben re-nag; Docs **CONNECTED→ABSENT** = DIFF; `changeme` flipping off = freeze DIFF (unfreeze signal). Soft-530 **OPEN 1033** + vault **1033** is dual OPEN (half-state **OVER**). Soft-530 **CLEAR** overnight **≠** camp CDM soak **PASS** **≠** companions re-arm **≠** **#82** **≠** unfreeze **≠** Zone vault retarget **≠** Bitwarden auto. **Never** `Mac.lan` as Doc. **No** second Ben Lookout brochure recreate. **No** overnight Zone/Garage. **Don’t** wake McKing docker as camp path. **Refresh only on field DIFF**; else prior stamp **stands**. First weekday `*/20` after ~06:00 **diffs** that stamp. **Same-class = stay quiet** (`611c267`). **First-hop absorbed DIFF** → no Ben re-surface + refresh living stamp. While Soft-530 is OPEN, unverifiable freeze ≠ DIFF. Mon 2026-09-14 ~17:25 / ~17:42 dual-OPEN lines **stand** (historical). Essay: [overnight-baseline-stamp.md](overnight-baseline-stamp.md). Coverage: [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md). SOP: [lookout-rearm-sop.md](lookout-rearm-sop.md). Monday map: [deployment-guide.md](deployment-guide.md). Paper only — no Zone/Garage execute.
- **Weekend Soft-530 coverage (plan-improve off Sat/Sun):** plan-improve is **off Sat/Sun**. Soft-530 companion ops/app watches stay **HOLD / not armed** (Ben skipped ~14:35 America/Edmonton — do **not** re-ask). Weekend flip coverage = Lookout `api.` `/health` + vault `/alive` **only**. Weekend Soft-530 control plane = Doc KeepAlive / lid-close (`com.projectcar.cloudflared` + uvicorn `:8000` + `next start` `:3000` LaunchAgents) — **not** Doc unfreeze, **not** **#82**. If Lookout `api.` `/health` flips non-200 → Chief/Lead run [doc-lid-restore.md](doc-lid-restore.md) **process wake only** (no pull while freeze `4cf8924` / `5swmVz` intact). Vault flips stay on the **separate** McKing watch (Live above) — [vault-stay-up.md](vault-stay-up.md). Dual-OPEN (Soft-530 OPEN + vault OPEN) recovery glue: [dual-host-outage.md](dual-host-outage.md) — **home/lab/pre-cutover only** wake **Doc first, then McKing** (**SUPERSEDED for camp**; after Ben GO, **`lightning` ABSENT must not trigger McKing wake**); parallel only if both hosts appear on ListMachines; `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning` (no wrong-host Shell; `Mac.lan` is not Doc shop host). Independent CLEAR: Soft-530 = `api.` `/health` **200** + `ops.`/`app.` → `/login` + waitlist OPTIONS **200** + `cloud.` `status.php`; vault = `/alive` **200** + VW `/api/config`. After Soft-530 CLEAR, stamp **post-CLEAR stay-up evidence** (cloudflared lastExit / KeepAlive loaded / caffeinate·CDM / Tailscale) on [doc-lid-restore.md](doc-lid-restore.md). After vault CLEAR, stamp **vault post-CLEAR stay-up evidence** (cloudflared.service ActiveState/Result/ExecMainStatus · VW healthy + DOMAIN · Tailscale · 1033→200 vs 502→200) on [vault-stay-up.md](vault-stay-up.md). Wake order stays [dual-host-outage.md](dual-host-outage.md). Living OPEN class: Soft-530 = CF **1033** since 2026-09-13 ~11:57 America/Edmonton; vault = **502** since ~19:45 MT Sep 13 on `/alive` + `/api/config` (**not** 1033). Soft-530 still **OPEN** this fold; vault independently **OPEN**; ListMachines **Mac.lan only**; this paper does **not** invent CLEAR. Bitwarden import/rotate **blocked** until vault CLEAR. Do **not** schedule weekend Zone Direct Upload / Worker work — first Monday plan-improve resumes Soft-530 smoke. While `brochure-option-a-redirect-watch` is **GONE**, weekend Option A Dynamic redirect is **explicitly uncovered — accept that risk** (same class as [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md)). Do **not** invent weekend plan-improve, Zone, or Garage Option A coverage. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **HOLD** a second Ben recreate ask ([lookout-rearm-sop.md](lookout-rearm-sop.md) already one-ask). Anti-goal: Fri Soft-530 **CLEAR** is historical — living dual-OPEN ≠ unfreeze GO ≠ companion re-ask ≠ **#82** auto-GO. Soft-530 **OPEN ≠ #82 blocked**. **#82** Ben GO / Soft-530 companions HOLD / dual-Nextcloud (`4cde204`) / `brochure-worker-ci` (`6e6efe8`) / waitlist-owner-desk **retired** (Owner desk LIVE; optional CSV Later) / **#79.1** git-only until Doc unfreeze — **unchanged**. Friday last weekday `*/20` (~17:40 MT) hands **into this** weekend coverage. **Mon–Thu nights** after last `*/20` until next weekday first fire (~06:00) are [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) — **not** this Sat/Sun card. Paper: [api-stay-up.md](api-stay-up.md), [shop-web-stay-up.md](shop-web-stay-up.md), [doc-lid-restore.md](doc-lid-restore.md), [vault-stay-up.md](vault-stay-up.md), [dual-host-outage.md](dual-host-outage.md), [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md), [lookout-rearm-sop.md](lookout-rearm-sop.md).
- **Shop OS CI (git-only, PR #73 / `babdc58`):** GitHub Actions workflow `.github/workflows/shop-os-ci.yml` — shop-api `pytest` + shop-web lint / typecheck / `next build` on PRs (and push to `main`) that touch `apps/project-car/**`. **Not** a Doc deploy, **not** a Worker upload, **not** tunnel/live secrets, **not** a Doc unfreeze. Green CI is **not** Ben GO. Doc checkout stays frozen at `4cf8924` / `5swmVz` until **Ben GO** ([doc-unfreeze.md](doc-unfreeze.md)). Runbook: [shop-os-ci.md](shop-os-ci.md).
- **Shop API:** `api.projectcar.ca` = Cloudflare tunnel → Doc `:8000` KeepAlive. Primary stay-up: LaunchAgent `com.projectcar.shop-api` → `~/hermes-tools/mission-control/shop-api/run-shop-api.sh` → uvicorn `:8000`. **Lead owns Doc `:8000`.** Do **not** hand uvicorn restarts to Chief. Public `GET /health` is **200 when Doc origin is up**. **Living:** Soft-530 **OPEN 530 / CF 1033**. Vault independently **OPEN 530 / CF 1033**. Lid-close / sleep still can return CF **530 / 1033** later — not a permanent guarantee. Lid-close / sleep (mornings included) still kills the Mac; the public edge then returns Cloudflare **502** or **530 / error 1033**. Lookout **`projectcar-api-health-watch`** **resumed** (`enabled:true`) 2026-09-11 ~06:52 America/Edmonton (Lookout confirmed; resume-time `/health` **200**; living Soft-530 is CF **1033**). Lead interim probe ended. See [api-stay-up.md](api-stay-up.md). Ordered restore: [doc-lid-restore.md](doc-lid-restore.md).
- **Ops management UI (LIVE at edge):** **`https://ops.projectcar.ca` is LIVE** (2026-09-06 ~12:55 America/Edmonton) **when Doc origin is up**. Zone Cloudflare tunnel v8 hostname `ops` → `http://127.0.0.1:3000` (same Doc shop UI as `app.`). KeepAlive `com.projectcar.shop-web` runs **`next start`** (not `next dev`) on Doc. Shop-web **BUILD** last recorded on Doc after Dashboard **#28** — Doc checkout **frozen** at **`4cf8924`** / BUILD_ID **`5swmVz-T2CqKEQzTk1ifU`**. **`main` tip `2dd61a2` (#74 doc-unfreeze checklist on `main`).** OwnerShell **#69** (`b9f9019`) and host allowlist **#36** (`f952cd3`) are **git-only** — do not call #36 or #69 live on Doc; do **not** re-propose pull/rebuild. Aligns with [doc-lid-restore.md](doc-lid-restore.md) (**#72**): wake is kickstart-if-down only. Green **shop-os-ci** / lid-restore process-wake are **not** Ben GO. After **Ben GO**, pull: [doc-unfreeze.md](doc-unfreeze.md). `shop-web-stay-up` rebuild/pull is **Ben GO** while frozen. Tunnel origin preferred `http://127.0.0.1:3000` (not bare localhost) after IPv6 `[::1]` 502. Public probes **when Doc origin is up**: `/` → `Location: https://ops.projectcar.ca/login` (no localhost hop); `/login` **200**. **Fri LIVE (historical)** 2026-09-11 ~06:50: `ops.` + `app.` **307** → `/login`. **Living:** CF **1033** since 2026-09-13 ~11:57 America/Edmonton. Soft-530 **CLEAR** (when origin is up) re-exposes public demo credentials: Lead/Chief verified live `https://ops.projectcar.ca/login` (and `app.` alias) still shows banner Password `changeme` and prefills the password input **on frozen Doc BUILD_ID `5swmVz`** — Soft-530-CLEAR **Next Ben GO** **#79** (**parallel to #82**), plan [ops-demo-hardening.md](ops-demo-hardening.md). Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull** — merge alone will **not** clear public login while freeze holds (`4cf8924` / `5swmVz`). Do **not** execute Garage/Zone from this tip-fold. Lid-close / sleep (mornings included) still kills the Mac; the public edge then returns Cloudflare **502** or **530 / error 1033** — tunnel / lid-close, not a new product break. Lookout **`projectcar-api-health-watch`** **resumed** (`enabled:true`) 2026-09-11 ~06:52 America/Edmonton (Lookout confirmed; resume-time `/health` **200**; living Soft-530 is CF **1033**). Lead interim probe ended. That api watch is **api-only** — `ops.` / `app.` can **530 while `/health` stays 200**. Soft-530 companion watches on `ops.` / `app.` `/login` are **HOLD / not armed** (Live above; Ben skipped ~14:35 America/Edmonton — do **not** re-ask); ≠ vault `/alive` watch ≠ Doc lid-restore. CORS includes `https://ops.projectcar.ca`. Some clients still have **flaky local DNS cache** for `ops.` — **use the `app.` alias**. Still the Doc demo — not a shop opening. Stay-up / rebuild / 1033-vs-flap: [shop-web-stay-up.md](shop-web-stay-up.md). Ordered restore: [doc-lid-restore.md](doc-lid-restore.md).
- **Temporary `app.` alias (still live, not removed):** **`https://app.projectcar.ca`** remains a **temporary alias** on the same Doc `:3000` origin until Ben cuts that DNS. **Fri LIVE (historical)** 2026-09-11 (307 → `/login`). **Living:** CF **1033** since 2026-09-13 ~11:57 America/Edmonton. Reachable **when Doc origin is up** (same lid-close **530 / 1033** class as ops — not a new product break). Do not treat the alias as gone.
- **Vault (`vault.projectcar.ca`) — McKing-owned at home/lab; independently **OPEN** (separate from paper shop CF cutover):** McKing `cloudflared` → `localhost:8222`. **Living:** vault **OPEN** subclass **530 / CF 1033** connector-down (Lead-verified **~16:28** America/Edmonton; **was 502** since ~19:45 MT 2026-09-13) on `/alive` **and** `/api/config`. Soft-530 **OPEN 1033** + vault **1033** is dual OPEN (half-state **OVER**). First check: lightning Healthy / cloudflared — Chief checking separately. Fri 2026-09-11 `/alive` **200** + `/api/config` **2026.6.0** is **historical** (when CLEAR, prefer `/alive`). Soft-530 five-row dual-run gate is **Doc shop hosts only** (`cloud.` / `api.` / `app.` / `ops.`) — **vault is EXCLUDED** **at home/lab**. Doc lid-restore / LaunchAgent KeepAlive must **not** recreate `vault.` ingress on Doc **without camp Ben GO**. Hop OPEN ≠ shop cutover GO ≠ Doc unfreeze. Anti-goals: never move the Doc mission-control token for `vault.` **at home/lab**; vault LIVE ≠ shop hostname leave ≠ Doc unfreeze; **never** `Mac.lan` as Doc/vault; **never** leave `vault.` pointed at dead McKing docker while Doc VW is live. Weekend dual-outage proved Soft-530 (Doc) and vault (McKing) fail independently **at home/lab**. **Camp** (paper — [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md)): Doc hosts shop + vault (+ NC); McKing may sleep; independence **breaks**; recovery = Doc VW + Zone retarget (**not** McKing docker); **Ben GO**, never auto-cutover. Dual-OPEN recovery glue: [dual-host-outage.md](dual-host-outage.md) (**home/lab/pre-cutover only** Doc first, then McKing; **SUPERSEDED for camp** — **`lightning` ABSENT must not trigger McKing wake**; ListMachines identity lock). After quiet-ops, **home/lab** first hop when `lightning` reappears is [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` — **never** `Mac.lan`) — **not** the camp vault path. After **extended OPEN**, first card is [vault-clear-smoke.md](vault-clear-smoke.md) — **before** Bitwarden / desk (**home/lab** docker permanence; **camp** = `/alive` **200** + `/api/config` VW class). After next McKing vault CLEAR, stamp **vault post-CLEAR stay-up evidence** on [vault-stay-up.md](vault-stay-up.md) (`d88cacb`). Bitwarden import/rotate **blocked** until vault CLEAR, then still **Ben GO** (**never auto-fire**). Essay: [vault-stay-up.md](vault-stay-up.md). Camp essay: [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md). Shop cutover stays paper and is **PARKED for camp weeks**: [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md). Lock: Locks below.
- **Camp vault on Doc (paper — camp SSOT — Ben GO, never auto-cutover):** **Camp host SSOT.** Zone/Doc follow [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) at camp GO — **not** [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md) or McKing-centric [vault-stay-up.md](vault-stay-up.md) (those are **PARKED for camp weeks** / desk/lab optional). Ben locked camp posture — Docs Mac plugged at camp for Nextcloud + Vaultwarden so McKing need not be 24/7. Cutover when ready: Doc CDM on → VW (+ NC) up on Doc → **Zone** retarget `vault.projectcar.ca` to Doc → smoke `/alive` **200** + `/api/config` VW **2026.6.0** class (classify **502→200** vs **1033→200**). Soft-530 shop tunnel stays on Doc too — independence is **home/lab only**; at camp they couple on Doc lid / CDM / KeepAlive (one fail = dual-OPEN). Zone `vault.` checklist is hop **4** **in that file** (preflight / inventory McKing origin / Doc ingress ready / cut / smoke / rollback — **not** a second appendix; **no live Zone execute until Ben GO**). **Never** `Mac.lan` as Doc/vault. **No dual origins.** Do **not** leave vault pointed at dead McKing docker while Doc VW is live. Do **not** treat a McKing docker ask as the camp vault path. Do **not** retarget shop Soft-530 hostnames. [post-dual-clear-go.md](post-dual-clear-go.md) does **not** assume McKing vault wake first. [dual-host-outage.md](dual-host-outage.md) is **SUPERSEDED for camp** — **`lightning` ABSENT must not trigger McKing wake**. Living this fold: Soft-530 **OPEN 530 / CF 1033**; vault **OPEN** 530 / CF 1033 (was 502); Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED — home-awake pre-camp half-state (**not a Ben surface**); Soft-530 CLEAR at home ≠ this camp GO; Option A Redirect+UX **PASS** (rungs 1–2 — **≠ #82**). Soft-530 **CLEAR ≠** camp GO. **Doc local VW/NC healthy ≠ public vault CLEAR:** Doc NC (`:8080`) / VW (`:8222` `/alive`) loopback **200** + compose healthy after cold-boot while public `vault.projectcar.ca` still **530 / CF 1033** on McKing (was 502) = camp **Z1 preflight only** (partial green). Still vault **OPEN** — not partial CLEAR / not dual CLEAR / not camp progress %. [vault-clear-smoke.md](vault-clear-smoke.md) SSOT stays the public hostname; **never** substitute Doc loopback. Soft-530 **CLEAR** alone still **≠** this camp GO. **Camp CDM lid-close soak:** Soft-530 **CLEAR** lid-open / Doc awake at home **never scores** CDM surviving lid-close. Soak **PASS** (lid closed / CDM engaged, named window suggest **≥15–30 min**; Soft-530 still CLEAR + waitlist CORS; local VW/NC up if applicable; cloudflared/KeepAlive still running) is required paper **before** Zone **Z5** retarget. Fail (530/1033 or local VW dies) → **HOLD** Zone retarget; fix CDM/KeepAlive before Ben GO. Still **Ben GO** + Zone `vault.` retarget — do **not** invent LIVE cutover. Soft-530 **OPEN ≠** block this paper. This fold does **not** execute Zone / Garage / Hatch, does **not** retarget, and does **not** re-ask Ben Doc-wake / docker. Essay: [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md). Matrix: [home-lab-specification.md](home-lab-specification.md). Complement posture (home-off expected, **not** a re-cutover): [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md).
- **Home vs camp Doc posture (paper — complement to `fb86f90`, not a re-cutover):** Ben: Doc is intentionally **off at home** (Soft-530 **OPEN** expected) and **on+Amphetamine/CDM at camp** (after vault cutover, Soft-530+vault expected **CLEAR** on one Doc origin). **HOME-awake (living):** Soft-530 **OPEN 530 / CF 1033** + vault **OPEN 530 / CF 1033** is living **dual OPEN** (prior home-awake CLEAR half-state **superseded ~16:36**); do **not** re-nag; Soft-530 CLEAR at home ≠ camp GO. Home Soft-530 **CLEAR** lid-open **never scores** the [camp CDM lid-close soak](camp-vault-on-doc-cutover.md#camp-cdm-lid-close-soak-gate). Home-off (Docs ABSENT + Soft-530 OPEN) remains EXPECTED when Doc is off. Overnight stamp / morning `*/20` = **SAME-CLASS expected**. Lookout Soft-530 `ok=false` while Docs **ABSENT** is **expected baseline** — do **not** re-arm companions. **CAMP (after Ben GO):** dual **OPEN** = coupled Doc Amphetamine/CDM/KeepAlive/cloudflared first-hop — **not** McKing docker, **not** independent mcking-reappear. Away weeks **supersede** standing McKing=`vault.`→`:8222`; McKing 24/7 + WOL/remote LUKS **deferred**. Pi/VPS still better long-term if vault must stay up while Doc is home-off (paper — **not** a Pi GO). [dual-host-outage.md](dual-host-outage.md) is **SUPERSEDED for camp** — **`lightning` ABSENT must not trigger McKing wake**. **Never** `Mac.lan` as Doc. **Never** treat home Soft-530 as camp cutover failure. **Never** auto Ben nag on home Soft-530. Soft-530 **OPEN ≠ #82 blocked**. Living Reality: Soft-530 **OPEN 530 / CF 1033** + vault **OPEN 530 / CF 1033** dual OPEN (half-state **OVER**); ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED — home-awake pre-camp, **not** camp GO, **not a Ben surface**; brochure Option A Redirect+UX **PASS** (rungs 1–2 — **≠ #82** / Worker body freshness); camp cutover paper **ready, pending Ben GO** (Zone `vault.` checklist paper only). Soft-530 **CLEAR ≠ #82** ≠ unfreeze ≠ Bitwarden. Do **not** enter [post-dual-clear-go.md](post-dual-clear-go.md) until vault CLEAR. Essay: [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md). Align: [soft-530-extended-open.md](soft-530-extended-open.md) · [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) · [overnight-baseline-stamp.md](overnight-baseline-stamp.md) · [doc-reappear-first-hop.md](doc-reappear-first-hop.md). Paper only — no Zone/Garage/Hatch, no Ben Doc-wake/docker re-ask.
- **Dual-host outage (weekend dual-OPEN glue):** Soft-530 OPEN + vault OPEN is two restores, not one **at home/lab / pre-cutover**. Wake **Doc first, then McKing** — **SUPERSEDED for camp**. After camp Ben GO ([camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) · [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md)), Doc is the **sole** public Soft-530+vault(+NC) origin; McKing sleep is intentional — **`lightning` ABSENT must not trigger dual-outage McKing wake**. Do **not** walk McKing-second as the camp vault path. Parallel only if both hosts appear on ListMachines. Identity lock: `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning` — no wrong-host Shell (`Mac.lan` is **not** Doc shop host). Independent CLEAR: Soft-530 = `api.` `/health` **200** + `ops.`/`app.` → `/login` + waitlist OPTIONS **200** + `cloud.` `status.php` (after **extended OPEN**, first card is [soft-530-clear-smoke.md](soft-530-clear-smoke.md) — OPTIONS CORS + POST **422**/**201** **before** desk / **#79.1**; `changeme` on `/login` is freeze); vault = `/alive` **200** + VW `/api/config` (after quiet-ops, first hop when `lightning` reappears is [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) — machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7`; after **extended OPEN**, first card is [vault-clear-smoke.md](vault-clear-smoke.md) — `/alive` **200** + `/api/config` McKing **2026.6.0** class + **502→200** **before** Bitwarden / desk). This file is **wake order**. Soft-530 post-CLEAR = **Doc forensics** ([doc-lid-restore.md](doc-lid-restore.md)). Vault post-CLEAR = **McKing forensics** ([vault-stay-up.md](vault-stay-up.md)). After **both** CLEARs + both forensic papers (`55e10d0` / `d88cacb`), Ben GO menu is [post-dual-clear-go.md](post-dual-clear-go.md) — **never auto-fire**. Soft-530 OPEN **~24h+** + vault OPEN + **Mac.lan only** = quiet-ops ([soft-530-extended-open.md](soft-530-extended-open.md)) — **no Ben re-nag**; duration ≠ GO. Anti-goals: not Doc unfreeze, not **#82**, not Zone Direct Upload; Soft-530 companions **HOLD**; Bitwarden import/rotate **blocked** until vault CLEAR. Essay: [dual-host-outage.md](dual-host-outage.md). Plane essays: [api-stay-up.md](api-stay-up.md), [vault-stay-up.md](vault-stay-up.md), [doc-lid-restore.md](doc-lid-restore.md), [shop-web-stay-up.md](shop-web-stay-up.md).
- **Soft-530 post-CLEAR stay-up evidence (paper — next CF 1033):** Soft-530 is **OPEN 530 / CF 1033** this fold (`api.` / `app.` / `ops.`; Lookout + Lead **~16:36**). **Now allowed** (was deferred while OPEN): stamp Doc forensics + freeze live-verify. Freeze live-verify **PASS** this fold (confirmed `` `changeme` `` / **`4cf8924` / `5swmVz`** — freeze **holds**). After that **PASS**, Lead may **draft** gates on [unfreeze-readiness.md](unfreeze-readiness.md) — **draft ≠ GO**. Vault independently **OPEN** subclass **530 / CF 1033** (Lead-verified **~16:28**; **was 502** since ~19:45 MT Sep 13) on `/alive` **and** `/api/config`. ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT**. Brochure Option A + Soft-530 Discord assets still **LIVE**. **#82** unchanged (**never #81**). This fold does **not** invent CLEAR. After the **next** lid-restore Soft-530 CLEAR, stamp forensic baseline before walking away: cloudflared LaunchAgent lastExit, KeepAlive loaded, caffeinate / CDM, Tailscale. [dual-host-outage.md](dual-host-outage.md) is wake order; this is **Doc forensics** (why it died after Fri→Sun flip-flops). Vault twin: [vault-stay-up.md](vault-stay-up.md). Essay: [doc-lid-restore.md](doc-lid-restore.md) · [api-stay-up.md](api-stay-up.md). Anti-goals: not Doc unfreeze, not **#82**, not Zone/Garage execute, not companion re-ask.
- **Vault post-CLEAR stay-up evidence (paper — next vault flip):** Twin of Soft-530 post-CLEAR. Soft-530 still **OPEN** as CF **1033** since 2026-09-13 ~11:57 America/Edmonton; vault independently **OPEN** subclass **530 / CF 1033** connector-down (Lead-verified **~16:28**; **was 502** since ~19:45 MT Sep 13) on `/alive` + `/api/config`. ListMachines only `Mac.lan` — `Docs-MacBook-Pro` + `lightning` absent. Different first checks when `lightning` returns: Soft-530 **1033** → Doc tunnel; vault **530 / CF 1033** → lightning Healthy / cloudflared first; vault **502** → VW origin / docker permanence. This fold does **not** invent CLEAR. After **extended OPEN**, first card is [vault-clear-smoke.md](vault-clear-smoke.md) — **before** Bitwarden / desk. After the **next** McKing vault CLEAR, stamp forensic baseline before walking away: `cloudflared.service` ActiveState/Result/ExecMainStatus (or lastExit); VW container healthy + `DOMAIN=https://vault.projectcar.ca`; Tailscale `lightning` up; whether recovery was **1033→200** vs **502→200**. [dual-host-outage.md](dual-host-outage.md) is wake order; Soft-530 post-CLEAR is Doc forensics; this is **McKing forensics**. Essay: [vault-stay-up.md](vault-stay-up.md). Anti-goals: not Doc unfreeze, not **#82**, not Zone/Garage execute, not companion re-ask.
- **Post-dual-CLEAR Ben GO sequencer (paper — never auto-fire):** After **extended OPEN**, Soft-530 CLEAR first runs [soft-530-clear-smoke.md](soft-530-clear-smoke.md) — **before** this menu / desk / **#79.1**. Vault CLEAR first runs [vault-clear-smoke.md](vault-clear-smoke.md) — **before** Bitwarden / desk. **Does not assume McKing vault wake first.** **Home/lab** vault CLEAR = McKing. **Camp** (after Ben GO) = Doc VW + Zone retarget ([camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md)). After Soft-530 CLEAR (Doc lid-restore) **and** vault CLEAR (**host-posture-aware**) **and** both forensic papers already ack’d (`55e10d0` Soft-530 post-CLEAR + `d88cacb` vault post-CLEAR), Ben’s GO menu is **ordered**: (1) keep Soft-530 healthy (KeepAlive / CDM / caffeinate) as ops baseline; (2) **#82** Worker-live (Direct Upload + **mandatory** purge/freshness — **not only** Direct Upload). After **#82** Worker-live the **brochure lane** is **parallel**: (A) **#83** CI thin `_redirects` anytime on the **#82** base (not Bulk-gated; parallel with upload OK); (B) **Bulk Phase1** frees Option A **FULL 10/10** Dynamic slots when Member needs capacity (keep `root`/`shop`/`chat` Dynamic until then). **Member edge only after Bulk.** **Never #81.** Never Bulk→#83. (3) Doc unfreeze + pull + Garage **#79.1** strip-changeme is **Shop OS parallel** — not “next after **#82**” in place of Bulk / **#83**. Lane-done = [post-dual-clear-go.md](post-dual-clear-go.md) **unfreeze + #79.1 acceptance smoke**: public `app.`/`ops.` `/login` without Password `` `changeme` ``; tip / `BUILD_ID` ≠ `4cf8924` / `5swmVz`; `api` `/health` **200** + waitlist OPTIONS CORS Origin `https://projectcar.ca` (+ www); **#79.1** merge-alone without Doc unfreeze+pull does **NOT** clear public `changeme`. Soft-530 CLEAR alone ≠ that card. Not **#82**, not Zone Direct Upload, not companions re-ask, not Bitwarden. **Never** auto-fire on CLEAR. Anti-goals: CLEAR ≠ companion re-ask; CLEAR ≠ Bitwarden import/rotate; Soft-530 CLEAR alone ≠ Doc unfreeze; vault CLEAR ≠ Soft-530 work; dual CLEAR ≠ **#82** auto-go; Soft-530 **OPEN ≠ #82 blocked**; **#82** ≠ end of brochure. KeepAlive→**#82** stays the *default ordered menu after dual CLEAR*. Living this fold: Soft-530 **OPEN 530 / CF 1033**; vault still **OPEN 530 / CF 1033**; ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED. Half-state ≠ dual CLEAR — **do not enter** this menu until vault CLEAR. Soft-530 CLEAR at home ≠ camp GO. Soft-530 **CLEAR ≠ #82** ≠ unfreeze ≠ Bitwarden. Essay: [post-dual-clear-go.md](post-dual-clear-go.md). Wake order: [dual-host-outage.md](dual-host-outage.md). **#82** gates + Option A receipt: [brochure-worker-ci.md](brochure-worker-ci.md). Member-edge capacity: [member-zone-edge.md](member-zone-edge.md). Unfreeze: [doc-unfreeze.md](doc-unfreeze.md). **#79.1**: [ops-demo-hardening.md](ops-demo-hardening.md). This fold does **not** invent CLEAR or execute Zone/Garage.
- **Unfreeze readiness — draft Ben-GO gates (paper — draft ≠ GO):** Soft-530 **CLEAR** + freeze live-verify **PASS** + Doc HEAD **`4cf8924`** **~89** behind `origin/main` means Lead may **draft** the Ben-GO gates **before** any pull/unfreeze. Draft: `origin/main` delta skim · alembic/migrations · Owner seed/CORS · rollback to **`4cf8924` / `5swmVz`** · Garage **#79.1** **parallel**, not next-after. Locks: **draft ≠ GO**; vault **OPEN 530 / CF 1033** does **not** block drafting; Soft-530 **CLEAR** duration ≠ auto-unfreeze ≠ auto **#82**. Living: Soft-530 **OPEN 530 / CF 1033**; vault **OPEN 530 / CF 1033**; freeze `` `changeme` `` / **`5swmVz`**; Option A Redirect+UX **PASS**; Home **STALE** until **#82**. **#79.1** lane-done after a later GO+rebuild stays [post-dual-clear-go.md](post-dual-clear-go.md) **unfreeze + #79.1 acceptance smoke**. Do **not** enter that menu while vault is OPEN. **No Ben re-nag.** Essay: [unfreeze-readiness.md](unfreeze-readiness.md). Smoke first: [soft-530-clear-smoke.md](soft-530-clear-smoke.md). Pull after GO: [doc-unfreeze.md](doc-unfreeze.md). Paper only — no Garage/Zone/Hatch execute, no pull/unfreeze.
- **Dual-Nextcloud ownership (paper — not a `cloud.*` CF cutover):** Public `cloud.projectcar.ca` stays on **Doc** until explicit **Ben GO**. McKing `/opt/mission-control` NC remains **lab/hub loopback-only** (unpublished — not on the McKing CF tunnel) **at home/lab**. Dual-tunnel matrix **home/lab**: Doc=`cloud`/`api`/`app`/`ops`; McKing=`vault` (+ unpublished NC). **Camp** (after Ben GO): Doc hosts shop + vault (+ NC); McKing may sleep — [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md). Paper path = Tailscale Serve HTTPS mirroring VW Serve at `lightning.tailbe8f55.ts.net` + `NEXTCLOUD_TRUSTED_DOMAINS` MagicDNS — **not** a Cloudflare cutover of `cloud.*`. Anti-goal: do **not** point `cloud.*` tunnel/CNAME at McKing until a dual-run acceptance gate (parity vs Doc `status.php`) **and** Ben GO. Hub dual-run NC+VW healthy / hop OPEN / vault LIVE / Serve stand-up do **not** pass that gate. #82 unchanged. Soft-530 companions **HOLD**. Vault LIVE unchanged. Paper: [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) (**camp SSOT**), [home-lab-specification.md](home-lab-specification.md), [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md) (**PARKED for camp weeks**).
- **Lookout vault flip watch (LIVE/armed — Lookout confirmed `enabled:true`; McKing-only):** Soft-530 / `https://api.projectcar.ca/health` can stay **green while vault dies**. **Lookout stall ≠ vault HTTP flip:** `projectcar-vault-health-watch` can stay `enabled:true` while cron stalls (example ~**104m**, `lastRun`/baseline stuck ~**07:05** on a `*/5`) — that is **not** this OPEN **502**. **Re-arm success ≠ first fire:** living ~09:14 MT 2026-09-15 vault re-arm ~08:50 → first fire ~08:55 → stall ~**22m**; api stalled ~**56m** since ~08:21 — same class. Soft-530 api remaining **OK-ish** is **not** living contrast. Lead **may GO** in-place re-arm of **both** watches (pause/resume or re-save) — **never delete**; then verify **stickiness** (≥3 consecutive `*/5` windows) before healthy; **HOLD** Ben recreate ([lookout-rearm-sop.md](lookout-rearm-sop.md) rows 7–8). Chronic stall (again within 24h) = **≤1** pause/resume per calendar-day (clock resets **America/Edmonton midnight**); Lead paper only after today's 1×. Living 2026-09-15: **both chronic HOLD today** — “Lookout continues” is **false for flip detection**; Chronic Lookout ≠ quiet-ops failure ≠ Ben ping; chronic HOLD does **not** reopen Ben recreate. Until stickiness, interim = Chief `*/20` + Lead coding-progress probes — **not** a Ben ping. Watch URL: `https://vault.projectcar.ca/alive` (fallback `/api/config` if `/alive` 404s). **Living:** vault **OPEN** subclass **530 / CF 1033** connector-down (Lead-verified **~16:28**; **was 502** since ~19:45 MT Sep 13). Fri Lead `/alive` **200** + `/api/config` **2026.6.0** is **historical**. Cadence: every **5m**. Baseline (Lookout-owned): `/workspace/lookout/projectcar-vault-health-baseline.json`. Flip-only alerts: **Chief + Lead only** on **200↔non-200**; **never Ben**; never mutate. Vault watch ≠ Soft-530 / Soft-530 companion watches / Doc lid-restore. Vault is McKing-only (`cloudflared` → `:8222`). Soft-530 five-row gate remains **Doc shop hosts only** (`cloud.` / `api.` / `app.` / `ops.`). Soft-530 companion watches (`ops.` / `app.` `/login`) are **HOLD / not armed** (Live above; Ben skipped ~14:35 America/Edmonton — do **not** re-ask) — **not** this vault watch. Api + vault watches continue. Weekend flip coverage (plan-improve off Sat/Sun) includes this vault `/alive` watch as the **McKing** half — vault flips stay here, not on Doc lid-restore. Vault flip ≠ Doc lid-restore; Doc Soft-530 ≠ McKing vault wake. After **extended OPEN** vault CLEAR, first card is [vault-clear-smoke.md](vault-clear-smoke.md) — **before** Bitwarden / desk. After vault CLEAR, stamp **vault post-CLEAR stay-up evidence** on [vault-stay-up.md](vault-stay-up.md) (`d88cacb`). Essay: [vault-stay-up.md](vault-stay-up.md). Shop CF cutover stays paper.
- **Owner + Member booking + tokens (live):** duration × 100 × band × advance overlay in `America/Regina` is on `main` (PR #12 / #14) and **live on Doc** — shop UI `:3000` — **and reachable via `https://ops.projectcar.ca` and `https://app.projectcar.ca`**. Shop OS seed: **6 hoists** (one shop-priority, Owner-only bay) and **Basic 1000 / Premium 1500**. Demo session cookies — **not OIDC**. HTTPS ops/app demo requires **Secure** cookies (`COOKIE_SECURE` / `SHOP_COOKIE_SECURE`, PR #22). No Stripe. The shop is not open.
- **Member self-serve (demo, still `/member` on the shop UI):** PR #14 merged → `main` `9baf3c4` and walked on Doc; reachable via `https://ops.projectcar.ca/member` and `https://app.projectcar.ca/member`. Member login (seed `ada.reyes@example.com`); `/member/me` balance + ledger; `/member/hoists` + schedule on **Bays 1–5 only**; shop hoist Owner-only (`400 shop_hoist_owner_only`). Demo session cookie (`pc_member_session`) — **not OIDC**. **Not** migrated to projectcar.ca.
- **Calendar redesign (on `main`, PR #18):** Monthly = hoist-density heat-map vs 08:00–21:00. Weekly = **separate per-hoist hour grids**. Owner includes the shop hoist (Owner-only); Member is Bays 1–5. Create / quote / cancel unchanged.
- **Next-day fill (on `main`, PR #20):** leftover customer-bay hours tomorrow get a **10–25%** fill factor; urgency drives the cut. Notification outbox (email stub / later push / SMS). Management `/fill` on Doc demo (`ops.` + temporary `app.` alias). Explicit extra multiplier — not a change to the locked v1 band / overlay tables. See `token-pricing.md`. Not a public price. No Stripe.
- **Breadth-first placeholders (on `main`, PR #21 — demo UI only; Tools / Parts deepened in #26):** Ops on **`ops.`** and the temporary `app.` alias: `/parts` (PT stock + qty / reorder + POs — **not** the member catalog), `/tools` (B1–B6 bay kits + TC crib checkout stub + orders / requests / planned), `/jobs` (sample tasks **with token bounties**), `/cameras` (every cam + door logs + AI stubs), `/payments` (membership + parts billing stubs; **AI tracks by default**, humans on exceptions). Member (customer-facing `/member`): Parts **request desk** (PT / TC SKUs, not commerce), job board with token amounts, **primary camera only**. **Not** live purchasing, QR checkout, Frigate, Stripe, or claim/complete. Demo SKUs are labeled placeholder. McKing Tailnet Frigate **0.17.2** + Jellyfin lab ≠ this `/cameras` stub and does **not** flip `pc.cameras`.
- **Schedule harden (on `main`, PR #24):** Owner `/schedule` and Member `/member/schedule` **fail-soft** if bookings list 500s — month heat-map + weekly grids still render. Windows are sent as **America/Regina instants (UTC ISO)** so FastAPI cannot 422 on naive wall-clock strings.
- **Inventory prefixes (on `main`, PR #26):** **B1–B6** / **TC** / **PT** locked (CM later). **B6 = shop hoist bay**, not `SH`. Demo lists only.
- **Shop OS Chat v1 (LIVE on Doc, PR #27):** Owner-start rooms; Member↔Ops; human polling. Owner/Ops `/chat` — see-all threads, **Owner-only create room**, mute. Member `/member/chat` — own rooms, reply, poll every few seconds while a thread is open. Logged-out `/chat` and `/member/chat` **auth-gate 307**. Seeded demo threads. **Not** live Matrix, **not** Apex, **not** Grok. Demo cookies. The shop is not open.
- **Dashboard 24h + todos (LIVE on Doc, PR #28):** Owner `/` shows **Bays 1–6** (Bay 6 = shop hoist) with every booked hour in the next **24 hours** (`America/Regina`) plus member + vehicle/notes. **No “Today’s bookings” list.** Personal to-dos (CRUD) + ICS / copy-event. Parts orders strip (PT SKU / ordered–shipped–in transit–received stubs). Member `/member` gets the same todos + only bays that member booked. Google / Apple **Connect** stubs only. Not a shop opening. Doc still serves this **#28** layout (checkout **frozen** until **Ben GO**).
- **OwnerShell + bay-hero dashboard (on git, PR #69 — not LIVE on Doc):** `main` **`b9f9019`** (product SHA; later docs tip is **`2dd61a2` / #74**). Primary nav Dashboard / Schedule / Chat / Members; secondary Inventory (Parts/Tools), Floor (Jobs/Cameras/Hoists), Admin (Payments/Tiers/Fill/Waitlist); compact dismissible demo banner. Dashboard: 3 primary metrics (booked hours, open to-dos, open POs) + 3×2 bay grid + Parts/To-dos split. Same #28 data — layout polish on the keep path, not a new breadth slice. OwnerDemoBanner honesty: **ops.projectcar.ca is LIVE**; **app.** is the temporary alias — not “naming only, no DNS yet.” Banner copy is on git with this follow-up (same **#36** / **#69** **git-only** bucket). Doc checkout **frozen** at **`4cf8924`** / BUILD_ID **`5swmVz-T2CqKEQzTk1ifU`** until **Ben GO**. Do **not** invent #69 LIVE on Doc. Do **not** re-propose pull/rebuild.

---

## Next (not shipped)

Two gates — do **not** collapse them into “v1 includes everything forever.” Calendar (#18), fill (#20), placeholders (#21), cookies (#22), Docs (#23 / #25 / **#30–#74**, website **#52–#67**), schedule harden (#24), inventory (#26), Chat (#27), Dashboard (#28), shop-web host allowlist (**#36**, `f952cd3`), OwnerShell + bay-hero dashboard (**#69**, `b9f9019`), google-calendar-oauth plan (**#71**), lid-restore no-auto-pull (**#72**, `a2ec088`), Shop OS CI (**#73**, `babdc58` — git-only, not a Doc unfreeze), doc-unfreeze checklist (**#74**, `2dd61a2` — on `main`; green shop-os-ci / lid-restore process-wake are **not** Ben GO), brochure P1-1 / P1-3 (**#52**, `5361212`), brochure P2-1 (**#55**, `453e44d`), brochure P2-2 (**#56**, `ed41d23`), brochure P2-3 (**#57**, `be60a01`), brochure P3-1 (**#61**, `88c9820`), brochure P3-5 (**#62**, `9d83bfc`), brochure P3-4 (**#65**, `f191ad5`), brochure P4-2 (**#67**, `2b772ff`), and Soft-530 waitlist honesty (**#80**, Worker-live) are **on `main`** — they are not Next. P2-1 / P2-2 / P2-3 / P2-4 / P3-1 / P3-4 / P3-5 / P4-2 / Soft-530 **#80** are **LIVE** on Worker. Live URL SSOT is the Zone Redirect pack (Option A) — **FULL 10/10** capacity lock; no new Dynamic rules for brochure pretty-URLs until **#82** Worker-live + Bulk Phase1. Not Worker `_redirects` `/shop` 302. **#82** Home canonical/og/sitemap `/index.html` is **not yet Worker-live**. When GO'd, smoke is Direct Upload → **mandatory purge** → body freshness ([brochure-worker-ci.md](brochure-worker-ci.md)). Prefer merge **#83** after **#82** base (CI-only thin `_redirects` assert) — **parallel with upload OK**; **#83** does **not** need Bulk Phase1. **Never #81.** Bulk Phase1 stays after upload only for Member-edge Dynamic slots. P3-2 / P3-3 About stay **held / deferred**. P1-6 stays deferred. **#36 and #69 are git-only** — Doc checkout **frozen** until **Ben GO**; do **not** re-propose pull/rebuild. After **Ben GO**, pull: [doc-unfreeze.md](doc-unfreeze.md). **`ops.projectcar.ca` is LIVE** at the edge. `app.` remains the temporary alias — do not cut it here.

| Gate | Meaning |
|------|---------|
| **Build breadth (now)** | Put planned features into the app as placeholders / rough IA (Parts, Tools, job board, cams, calendar / fill, member surfaces, Chat v1 demo, …) so layout can be ironed out. Placeholders are OK. Do **not** leave a planned surface out of the **build** for polish. |
| **Ship-MVP cut (later gate)** | Before the **public MVP** ship, cut unfinished and unnecessary features. Do **not** ship every placeholder as the public product. Draft table: [ship-mvp-cut.md](ship-mvp-cut.md). |

**Soft-530-CLEAR Next Ben GO — #79 ops demo hardening (parallel to #82).** Soft-530 CLEAR + live `ops.` / `app.` `/login` still print `changeme` on frozen Doc BUILD_ID **`5swmVz`** (`4cf8924`). Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull** — merge alone will **not** clear public login while the freeze holds. Plan: [ops-demo-hardening.md](ops-demo-hardening.md). (1) Garage **#79.1**: strip plaintext password from public Owner/Member login banners + stop prefilling password (email ok) — **git-only** until [doc-unfreeze.md](doc-unfreeze.md). (2) optional Zone Cloudflare Access on `ops` (+ optional `app`) until OIDC — brochure stays public; exclude `api` waitlist paths. After Ben GO unfreeze + pull + **#79.1** rebuild, lane-done = [post-dual-clear-go.md](post-dual-clear-go.md) **unfreeze + #79.1 acceptance smoke** (public `app.`/`ops.` `/login` without `changeme`; tip / `BUILD_ID` ≠ `4cf8924` / `5swmVz`; `/health` **200** + waitlist OPTIONS CORS Origin `https://projectcar.ca` (+ www); **#79.1** merge-alone does **not** clear public `changeme`). Soft-530 CLEAR alone ≠ that card. Not **#82**, not Zone Direct Upload, not companions re-ask, not Bitwarden. **#79 is parallel to #82** — not blocked on Worker SEO / Option A upload / Bulk Phase1. **#82** sequence stays **#82 → Zone upload+purge; #83 after #82; Bulk for Member**. **Never #81.** Do **not** execute Garage/Zone from this tip-fold. **Ben GO** required. Soft-530 CLEAR makes this more urgent than when Doc was 530. Hardening GO ≠ unfreeze GO. After Soft-530 **CLEAR** + freeze live-verify **PASS**, Lead may **draft** the Ben-GO gates on [unfreeze-readiness.md](unfreeze-readiness.md) (**draft ≠ GO**; vault OPEN does **not** block drafting; **#79.1** stays parallel, not next-after) — still **not** a pull.

1. **Host migration — Member UI on projectcar.ca.** Customer surface (self-serve booking + balance) moves to **projectcar.ca**. **Canonical Member cookie host = apex `projectcar.ca`.** **Capacity-blocked** by Option A — CF Dynamic Redirect pack is **FULL 10/10**. Member cutover needs Dynamic (or equivalent) for `/member*` path/tunnel ranking **BEFORE** brochure Redirect Rules, plus www→apex `/member*` **301** without fighting brochure dual-host. **Hard gate:** Member edge **Ben GO** only after **#82** Worker-live + Bulk Phase1 frees pretty-URL Dynamic slots (Redirect SSOT pick A already parked — receipt: [brochure-worker-ci.md](brochure-worker-ci.md) Option A matrix; upload click-path [brochure-worker-deploy.md](brochure-worker-deploy.md)). **#83** is **not** this gate — prefer merge after **#82** base (CI-only thin `_redirects` assert); **parallel with upload OK**; does **not** need Bulk Phase1. **Never #81.** Bulk Phase1 stays after the **#82** upload **only** to free those Member slots. Keep `root` / `shop` Dynamic until Member needs those slots. Smoke anti-collision: apex+www `/member/login` must **NOT** 301 into `membership.html` (today `/member*` is Worker **404** — preserve that). Zone plan after that GO: **301** `www.projectcar.ca/member*` → `https://projectcar.ca/member*` so host-only `pc_member_session` cannot drop across www↔apex (brochure static stays dual-host). Management stays on **`ops.`** (temporary `app.` alias still live). Today Member demo still lives on shop-UI `/member`. Cutover checklist (plan only — not shipped, no DNS, no `app.` cut, no live edge flip): **`member-host-cutover.md`**. Zone path-split checklist (plan only — `/member*` → Doc `:3000`, everything else stays Worker): **`member-zone-edge.md`**. Garage (site) / Zone (path rules / tunnel hostname / CORS edge) wait for these docs, **#82** + Bulk Phase1 slot-free, **and Ben GO**. Do **not** execute the edge plan from a docs PR. Do **not** execute **#82** / Bulk Phase1 / Option A rewrite from this tip-fold. Prefer merge **#83** after **#82** base — not after Bulk. **Never #81.** Cutover planning outranks new breadth placeholders **and** Classic Pages git (`brochure-pages-cutover.md`). Do **not** claim this is shipped. After that GO, brochure Membership secondary CTA is `website-improvements.md` **P1-6** (apex `https://projectcar.ca/member/login` only — never www, never ops/app).
2. **Ben cuts the `app.` alias.** `ops.` is already LIVE at the edge and **stays the management host**. `app.` stays until Ben cuts that DNS. Member cutover (Next #1) does **not** require cutting `app.` first. Plan only (not executed — no DNS / tunnel / CORS / allowlist from a docs PR): **`app-alias-cut.md`**. Do not remove the alias from docs or edge config in a docs PR.
3. **Chat follow-ons (not Chat v1).** Chat v1 human/polling/Owner-starts-rooms is **LIVE on Doc** (Live above). Later: Grok on **projectcar.ca**, assign / staff notes / escalate, websockets. **Apex sidecar stays deferred.** Do not revive Matrix.
4. **Mission Control cockpit** still needs **Ben GO** before start. Owner + Member booking are already live on Doc **and** the public ops / app hosts — that earlier hold is satisfied. Do not start the cockpit from a docs PR.
5. Staff **OIDC** later (on **`ops.`**). **Full Stripe later.** Public Apex chat **deferred (Ben)** — not P0; do not revive. Do not dump the rest of v2 here.
6. **Google Calendar two-way sync.** PR #28 shipped ICS download + “copy event” + a Connect Google / Apple stub (`GET /calendar/google/start` is `501` until `GOOGLE_OAUTH_*` is set). Token exchange and two-way event sync stay **Next**. Apple stays ICS import (no public OAuth in this slice). Standing plan (not a GO): [google-calendar-oauth.md](google-calendar-oauth.md).

McKing Docker shop-host (api / ops / app origin move; brochure stays Worker) — path **OPEN** 2026-09-11 ~10:32 America/Edmonton (sshd / docker / `/opt/mission-control`; hub dual-run NC+VW healthy on Doc+McKing). Doc KeepAlive still owns public `api.` / `ops.` / `app.`. Hop OPEN ≠ shop cutover GO ≠ Doc unfreeze. Soft-530 dual-run five-row gate still blocks any public **shop** CF hostname leave (`cloud.` / `api.` / `app.` / `ops.` only — **vault EXCLUDED**; vault is **LIVE verified** on McKing). Shop cutover paper: [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md). **Not** this Next list. **Not** GO’d. Paper only for shop.

Placeholder pages (Parts, Tools, Job board, Cameras, Payments) are **demo UI** (Live above). Item 5 still means **live Stripe / OIDC** — not the Payments placeholder. They do not rewrite calendar (#18) or drop fill (#20). Do not cut the `app.` alias here.

---

## Later (parked)

- Member-to-member hoist time trades/offers — bookings should not be glued to one member forever (transferable booking or trade-offer entity). Design note only; do not design the trade system now.
- Member booking assistant (Ben later-want) — a bot that helps members book hoist dates. Same Later bucket as trades. After Member UI is solid; likely Grok / Apex-replacement public chat lane, **not** Owner admin. Do **not** build in current Shop OS slices. Related to the planned chat dual surface (Next) — still not an implementation.
- **Member parts purchasing (full).** Member `/member/parts` is a **request desk** (build-breadth now) — PT stock or TC crib tool, not a cart. Ops Parts is PT qty / reorder + POs, not the member catalog. Full member purchasing is **Later**. Do not claim a shop is selling parts. Flag `pc.marketplace` stays **off**.
- **Job board claim / complete / assign** — workflow is Later. Placeholder board with sample token-paid chores is what shipped.
- **Live Frigate (or equivalent NVR)** — flag `pc.cameras` stays **off** until wired. Occupancy is a **hint, not source of truth**. Door logs ops-only. **Anti-goal:** today’s McKing Tailnet Frigate **0.17.2** + Jellyfin lab ≠ Shop OS `/cameras` live and does **not** flip `pc.cameras`. No camera wiring from this Frigate/Jellyfin stand-up. This Later row stays **Later**.
- **Live tool inventory / QR / checkout / hardware** — prefix model is locked (below); ops `/tools` lists are demo SKUs. Return mail, overdue workflow, and readers stay Later.
- **Consumables (`CM`)** — prefix reserved. Full CM desk Later. Trivial stub note only.
- **Stripe / live billing** — ops Payments placeholder (membership + parts; AI default, human on exceptions) is IA only. `pc.payments` stays **off**. Full Stripe is Later. AI tracks payments by default; humans get alerts on exceptions.
- **Waitlist optional CSV export** — Owner desk (list + mark contacted) is **LIVE** when origin is up. Optional CSV export stays **Later**. No email blast. No Stripe. No Member host. Do **not** draft `waitlist-owner-desk.md`. Soft-530 **CLEAR** or desk existence ≠ Doc unfreeze ≠ **#79.1** rebuild GO.

---

## Locks

- **Host split** is locked (table above). Customer = `projectcar.ca` / www. Management = **`ops.projectcar.ca`** (LIVE at edge; staff-on-shift, not Owner-only). `app.` is a **temporary alias** until Ben cuts that DNS — **not removed**. Local DNS cache on some clients is not a product rollback — use `app.`.
- **Soft-530-CLEAR Next Ben GO (#79):** Public Owner/Member `/login` still prints and prefills demo password `changeme` on frozen BUILD_ID **`5swmVz`** after Soft-530 CLEAR (Lead/Chief live). Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull** — merge alone will **not** clear public login while freeze holds (`4cf8924` / `5swmVz`). Lane-done after Ben GO unfreeze + pull + rebuild = [post-dual-clear-go.md](post-dual-clear-go.md) **unfreeze + #79.1 acceptance smoke**. Soft-530 CLEAR alone ≠ that card. Not **#82**, not Zone Direct Upload, not companions re-ask, not Bitwarden. **#79 is parallel to #82** — not blocked on Worker SEO / Option A upload. Plan [ops-demo-hardening.md](ops-demo-hardening.md). Do **not** execute Garage/Zone from this tip-fold. Hardening GO ≠ Doc unfreeze GO ≠ shop CF cutover. **#82** → Zone upload+purge; **#83** after **#82**; Bulk for Member — **unchanged**. **Never #81.**
- **Doc checkout frozen (LOCKED — until Ben GO):** Doc working tree / shop-web BUILD stay at **`4cf8924`** / BUILD_ID **`5swmVz-T2CqKEQzTk1ifU`** (Dashboard **#28**). **#36** (`f952cd3`) and **#69** (`b9f9019`) are **git-only** — OwnerShell + bay-hero are **on git**, not LIVE on Doc. plan-improve / Chief / Lead must **not** re-propose Doc pull/rebuild. Only **Ben GO** lifts this. Aligns with [doc-lid-restore.md](doc-lid-restore.md) (**#72**, `a2ec088`): lid-restore is process wake / kickstart-if-down only — **not** auto-`git pull`. After **Ben GO**, ordered pull: [doc-unfreeze.md](doc-unfreeze.md). Green **shop-os-ci** / lid-restore process-wake are **not** that GO. **#78 lookout-resume** is **LIVE-SUPERSEDED** — `projectcar-api-health-watch` is already **`enabled:true`**. Soft-530 companion watches are **HOLD / not armed** (Ben skipped ~14:35 America/Edmonton — do **not** re-ask). Api + vault watches continue. Freeze does **not** invent a companion GO. Garage **#79.1** strip-changeme is **git-only** in the same freeze bucket as **#36** / **#69** until unfreeze+pull. Fri Soft-530 **CLEAR** is historical — living dual-OPEN ≠ unfreeze GO ≠ companion re-ask. After Soft-530 **CLEAR** + freeze live-verify **PASS**, Lead may **draft** gates on [unfreeze-readiness.md](unfreeze-readiness.md) — **draft ≠ GO**.
- **Unfreeze readiness draft (LOCKED — this fold):** Soft-530 **CLEAR** + freeze live-verify **PASS** + HEAD **`4cf8924`** **~89** behind `origin/main` unlocks Lead **drafting** Ben-GO gates — **not** a pull. **draft ≠ GO.** Vault **OPEN** does **not** block drafting. Soft-530 **CLEAR** duration ≠ auto-unfreeze ≠ auto **#82**. Gates: delta skim · alembic · Owner seed/CORS · rollback to **`4cf8924` / `5swmVz`** · **#79.1** parallel not next-after. **#79.1** acceptance stays [post-dual-clear-go.md](post-dual-clear-go.md). Essay: [unfreeze-readiness.md](unfreeze-readiness.md). Living: Soft-530 **OPEN 530 / CF 1033**; vault **OPEN 530 / CF 1033**; freeze `` `changeme` `` / **`5swmVz`**; Option A Redirect+UX **PASS**; Home **STALE** until **#82**. Paper only — no pull, no Ben re-nag.
- **Fleet Shared cards (LOCKED — non-SSOT; this fold):** `Coombzy/Project-Car` `Docs/` (held **#70** → `main`) is SSOT. Fleet Shared `shop-os-mc-plan.md` is **rewritten** and remains **non-SSOT** — Chief standing notes only. If Shared contradicts Docs tip / live, **discard Shared language**. Chief rewrite **DONE** ~15:42 America/Edmonton on Doc `~/Desktop/Fleet-Nextcloud/Memory/Shared/shop-os-mc-plan.md` — **not** still “in progress”. Dropped false brochure **502 lock** + Garage-blocked-on-502; marked non-SSOT vs Docs/#70; aligned Soft-530 **CLEAR** half-state (Shared ~15:42 — living now **OPEN** both 1033; half-state **OVER**) / camp vault-on-Doc / freeze **`4cf8924`/`5swmVz`** / **#82** not auto. Public `projectcar.ca` brochure is **LIVE** (Option A) since **2026-09-06**. Living: Soft-530 **OPEN 530 / CF 1033**; vault **OPEN 530 / CF 1033** (was 502); brochure **LIVE**; Shared rewritten. Half-state **OVER**. **Not** a Ben ask. No Garage/Zone fan-out. No unfreeze / **#82** / camp GO. Do **not** restore 502 language. Essay: [shared-fleet-cards.md](shared-fleet-cards.md).
- **Option A completeness ladder (LOCKED):** Redirect **PASS** + Soft-530 UX **LIVE** ≠ Worker body freshness **PASS**. Rungs: (1) Zone **FULL 10/10** **LIVE**; (2) `styles.css?v=36` + `waitlist.js?v=3` + Discord honesty **LIVE**; (3) Home canonical/`og:url`/nav/sitemap `/index.html` **STALE** until **#82** — live Home still bare `/` (sitemap loc bare `/`, lastmod **2026-09-08**). Soft-530 **CLEAR** does not change that and **never auto-GOs #82**. Redirect+UX **PASS** is **never** #82 done. Body asserts stay on [brochure-worker-ci.md](brochure-worker-ci.md) post-#82 Direct Upload smoke gate. Paper only — no Zone/Garage execute.
- **Doc local VW/NC healthy ≠ public vault CLEAR (LOCKED — this fold):** Doc loopback VW (`:8222` `/alive`) + NC (`:8080`) after cold-boot = camp **preflight only** (Z1 local ready). Public vault CLEAR requires `https://vault.projectcar.ca/alive` **200** + `/api/config` VW class after Zone retarget (**Z5**) — local healthy **never scores** CLEAR. Living Doc VW up + public **530 / CF 1033** (or **502**) = still vault **OPEN** (not partial CLEAR / not dual CLEAR / not camp progress %). [vault-clear-smoke.md](vault-clear-smoke.md) SSOT stays the public hostname — **never** substitute Doc loopback. Anti-goals: no auto Zone retarget; no Bitwarden; don’t wake McKing docker as camp path; never `Mac.lan` as Doc/vault; Soft-530 **CLEAR** alone still **≠** camp GO. Essay: [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md). Cross: [vault-clear-smoke.md](vault-clear-smoke.md) · [soft-530-clear-smoke.md](soft-530-clear-smoke.md) · [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md) · [post-dual-clear-go.md](post-dual-clear-go.md). Living: Soft-530 **OPEN 530 / CF 1033**; vault **OPEN 530 / CF 1033** (was 502); freeze `` `changeme` `` / **`5swmVz`**; Option A Redirect+UX **LIVE** / Home **STALE** until **#82**. Paper only — no Zone/Garage execute, no Ben re-nag.
- **Living Soft-530 OPEN + vault OPEN dual-OPEN both 1033 anti-goals (LOCKED):** Soft-530 **OPEN 530 / CF 1033** + vault **OPEN 530 / CF 1033** + ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED is **not** dual CLEAR, **not** [post-dual-clear-go.md](post-dual-clear-go.md), **not** camp GO, **not** Doc unfreeze, **not** **#82** auto-GO, **not** Zone execute, **not** Soft-530 companions re-ask, **not** Bitwarden import/rotate. **Doc local VW/NC healthy ≠ public vault CLEAR** — Doc loopback **200** + public **530 / CF 1033** (or **502**) is still vault **OPEN** (not partial CLEAR / not camp progress %); [vault-clear-smoke.md](vault-clear-smoke.md) SSOT stays `vault.projectcar.ca`. Waitlist OPTIONS class = Origin CORS preflight — **bare 405 ≠ OPEN**. Lookout: api `ok=false` expected; vault `ok=false` expected (dual OPEN both 1033). Freeze live-verify **now allowed** and **PASS** this fold (confirmed `` `changeme` `` / **`4cf8924` / `5swmVz`** — freeze **holds**, not lifted). Lead may **draft** unfreeze gates ([unfreeze-readiness.md](unfreeze-readiness.md)) — **draft ≠ GO**; vault OPEN does **not** block drafting. Freeze intact **`4cf8924`** / BUILD_ID **`5swmVz`**. **#82** still Ben GO. Soft-530 **OPEN ≠ #82 blocked** — Ben may GO Worker-live while Soft-530/vault still **OPEN** (brochure is CF Worker/Zone, not Doc tunnel). Quiet-ops duration still ≠ auto-GO. Dual CLEAR ≠ auto-GO. Apex deferred. Index pointers stay: [vault-stay-up.md](vault-stay-up.md), [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) (camp vs home/lab; sequencer ≠ McKing-first), [dual-host-outage.md](dual-host-outage.md), [soft-530-extended-open.md](soft-530-extended-open.md) (quiet-ops), [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) (Mon–Thu overnight Lookout-only), [overnight-baseline-stamp.md](overnight-baseline-stamp.md) (last weekday `*/20` one-liner; Soft-530 **CLEAR** + vault **OPEN** = **first-class half-state class**; tonight **~17:20**; freeze **`last_known`** `` `changeme` `` / **`5swmVz`** / **`4cf8924`**; morning same half-state ≠ dual CLEAR / “incident closed”; Soft-530 **CLEAR→OPEN** = DIFF; vault still **530 / CF 1033** = SAME; vault **502 ↔ 530/1033** = subclass field DIFF (Lead/Lookout ≠ Ben re-nag); Docs **CONNECTED→ABSENT** = DIFF; `changeme` off = freeze DIFF), [doc-reappear-first-hop.md](doc-reappear-first-hop.md) (Doc first hop — **cold-boot** if Docs **ABSENT ≥1 calendar day**; living since Fri 2026-09-11 ~16:48 **~4d**; never treat multi-day lid as overnight sleep), [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (McKing first hop — machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` label **lightning**; never `Mac.lan`), [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (first Soft-530 recovery smoke after extended-OPEN CLEAR — **before** desk / **#79.1** / unfreeze), [vault-clear-smoke.md](vault-clear-smoke.md) (first vault recovery smoke after extended-OPEN CLEAR — **before** Bitwarden / desk; HTTP **+** `docker.service` permanence on the **502** path; living subclass **530 / CF 1033** first hop is lightning Healthy / cloudflared — classify **1033→200** vs **502→200**), Soft-530 post-CLEAR ([doc-lid-restore.md](doc-lid-restore.md) `@55e10d0`), vault post-CLEAR (`@d88cacb`), [post-dual-clear-go.md](post-dual-clear-go.md). Paper only — no Zone/Garage execute.
- **Soft-530 extended OPEN — quiet-ops (LOCKED — this fold):** Soft-530 OPEN **~24h+** + vault OPEN + **Mac.lan only** = quiet-ops. **Home-off HOLD** ([home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md)): Docs **ABSENT** + Soft-530 **OPEN** is **EXPECTED** — do **not** re-nag lid-restore / KeepAlive; hop waits on actual reappear. **No Ben re-nag** (Doc-wake + McKing wake already asked). Lead stays armed for `Docs-MacBook-Pro` reappear → [doc-reappear-first-hop.md](doc-reappear-first-hop.md) (machineId `95a229f5-9296-4a18-aa98-70fd300dabdf` **then** lid-restore); Chief stays armed for `lightning` reappear → [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` **then** vault wake). Lookout api+vault stay armed (**never delete** to fix cron; **`enabled:true` ≠ firing**; re-arm success ≠ first fire — [lookout-rearm-sop.md](lookout-rearm-sop.md) row 8); **chronic-HOLD exception:** when **both** are calendar-day chronic HOLD, “Lookout continues” is **false for flip detection** — living 2026-09-15 **both chronic HOLD today**; interim = Chief `*/20` + Lead probes (accept **≤~20m**); clock resets **America/Edmonton midnight**; next local day Lead **may** try one in-place re-arm then stickiness ≥3 `*/5`; Chronic Lookout ≠ quiet-ops failure ≠ Ben ping; chronic HOLD does **not** reopen Ben recreate; Soft-530 companions + brochure recreate stay **HOLD**; brochure-redirect **paper** **continues**; living Option A watch **GONE** — **HOLD** recreate; weekday Chief `*/20`; overnight/weekend **UNCOVERED**; companions **HOLD until Ben reopens** (not weekend-only). Option A is public SSOT. **Duration alone ≠** **#82** GO ≠ unfreeze ≠ Zone Direct Upload ≠ Bitwarden ≠ companion re-ask. CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md). Next Soft-530 CLEAR after this lock: [soft-530-clear-smoke.md](soft-530-clear-smoke.md). Next vault CLEAR after this lock: [vault-clear-smoke.md](vault-clear-smoke.md). Essay: [soft-530-extended-open.md](soft-530-extended-open.md). Weekday overnight twin: [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md). First hop Doc: [doc-reappear-first-hop.md](doc-reappear-first-hop.md). First hop McKing: [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md). Paper only — no Zone/Garage execute.
- **Doc first reappear — first hop (LOCKED — this fold):** **Home-off ABSENT does not queue this hop** ([home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md)). When `Docs-MacBook-Pro` **first reappears** after quiet-ops: assert machineId **`95a229f5-9296-4a18-aa98-70fd300dabdf`** (**never** `Mac.lan` / Porsche as Doc) → classify **cold-boot** vs overnight **warm wake** → on **cold-boot** (Docs **ABSENT ≥1 calendar day** **or** Soft-530 **OPEN** 1033 **≥24h** with Docs **ABSENT**) verify cloudflared LaunchAgent/service up + shop-api/shop-web KeepAlive loaded + **note** CDM/caffeinate **before** smoke → [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (`/health` **200** + OPTIONS CORS apex+www + POST **422**/**201**) → **only if CLEAR** stamp lid-restore forensics → [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire** **#82** / unfreeze / **#79.1** / companions / Zone upload / Bitwarden). Overnight **warm wake** (`<24h` ABSENT after a prior CLEAR) stays the shorter process-wake. **Never** treat a multi-day lid as overnight sleep. Chief-to-doc hop **only after** CLEAR smoke. Vault CLEAR stays Chief `lightning` / McKing. Anti-goals: `Mac.lan` Shell ≠ Doc evidence; multi-day lid ≠ overnight sleep; duration ≠ Ben wake; **CLEAR ≠ honesty-off**; not companion re-ask. Living this fold: Soft-530 **OPEN 530 / CF 1033**; vault still **OPEN** **530 / CF 1033** connector-down (was 502); ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED — home-awake pre-camp half-state. Lookout api `ok=false` / vault `ok=false` **expected** (dual OPEN both 1033). Option A Redirect+UX **PASS** (rungs 1–2 — **≠ #82**). **#82** Ben GO only. Soft-530 **CLEAR ≠ #82**. **No Ben re-nag.** Essay: [doc-reappear-first-hop.md](doc-reappear-first-hop.md). Monday map: [deployment-guide.md](deployment-guide.md). Paper only — no Zone/Garage execute. Does **not** invent a reappear or CLEAR.
- **McKing first reappear — first hop (LOCKED — this fold):** When `lightning` / McKing **first reappears** after quiet-ops / extended vault OPEN: assert machineId **`9067d14b-46e5-4ef0-82d5-fce0febdc8f7`** (label **`lightning`**) — **never** `Mac.lan` as McKing → [vault-clear-smoke.md](vault-clear-smoke.md) (`/alive` **200** + `/api/config` VW **2026.6.0** + `docker.service` **enabled** + **active** + **502→200**; HTTP-only ≠ CLEAR) → **only if CLEAR** stamp vault-stay-up forensics → Soft-530 independence (Bitwarden gate is **vault CLEAR** + Ben GO after `/alive` + VW **2026.6.0** + docker permanence — **not** Soft-530 CLEAR; Soft-530 **OPEN** can coexist with vault CLEAR + Bitwarden work; **never auto Bitwarden**; vault CLEAR ≠ Soft-530 CLEAR ≠ unfreeze / **#82** GO). Anti-goals: `Mac.lan` Shell ≠ McKing evidence; duration ≠ Ben wake; name-only ≠ identity; morning `*/20` ≠ re-ask after this hop. Living this fold: Soft-530 **OPEN** `api.`/`app.`/`ops.`/`cloud.` **530** CF **1033**; waitlist OPTIONS **530**; vault `/alive` + `/api/config` **502** (docker dead); ListMachines **`Mac.lan` + `lightning`** (`docs=ABSENT`); hop already ran ~19:10 — still blocked on Ben sudo; freeze last_known; **#82** still Ben GO; quiet-ops **HOLD**. Overnight ListMachines DIFF is stamp-absorbed — **no morning re-ask**. Essay: [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md). Monday map: [deployment-guide.md](deployment-guide.md). Paper only — no Zone/Garage execute. Does **not** invent CLEAR or a second Ben sudo / docker page.
- **Soft-530 CLEAR recovery smoke (LOCKED — this fold):** After Soft-530 **CLEAR** following **extended OPEN** (>~24h), **first** recovery smoke **before** Owner desk / **#79.1** / unfreeze talk: `api.` `/health` **200**; waitlist OPTIONS **200** + CORS Origin `https://projectcar.ca` **and** www; waitlist POST **422** or **201** (route alive, not just tunnel-up); `app.`/`ops.` may still **307**→`/login` with print `` `changeme` `` (freeze, **not** Soft-530); `cloud.` `status.php` **200** when Doc public is up. ≠ **#82** GO ≠ companion re-ask ≠ Bitwarden; Soft-530 CLEAR alone ≠ unfreeze; vault CLEAR still McKing ([vault-clear-smoke.md](vault-clear-smoke.md)); **CLEAR ≠ honesty-off** — **#80** Membership/Contact Discord honesty intro + `waitlist.js?v=3` fail-soft (502/530/1033→Discord+mailto) stay (overnight sleep / tunnel flaps, **not** temporary banners; tone tweak only with Ben GO; this smoke ≠ Garage GO to strip). CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire**). Essay: [soft-530-clear-smoke.md](soft-530-clear-smoke.md). Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) Soft-530 Discord · STATUS Waitlist Live · [website-improvements.md](website-improvements.md) P1-5. Living this fold: Soft-530 **OPEN 530 / CF 1033**; vault still **OPEN 530 / CF 1033**; **Docs + lightning CONNECTED**. Half-state ≠ dual CLEAR. Paper only — no Zone/Garage execute. Does **not** invent vault CLEAR.
- **Vault CLEAR recovery smoke (LOCKED — this fold):** After vault **CLEAR** following **extended OPEN** (**502** since ~19:45 MT Sep 13), **first** recovery smoke **before** Bitwarden import/rotate or desk talk: `vault.` `/alive` **200**; `/api/config` **200** + McKing **2026.6.0** class (**not** Doc **2025.12.0**); on `lightning` `docker.service` **enabled** + **active** (optional: `mcking` in `docker` group); `/alive` **200** + docker **disabled/inactive** = **NOT CLEAR / recurrence risk** (reboot re-OPENs like ~19:45 Sep 13); HTTP-only ≠ CLEAR; classify **502→200** (origin-down), **not** CF **1033→200**; Soft-530 may still be **OPEN** — vault CLEAR alone ≠ Soft-530 CLEAR ≠ **#82** ≠ unfreeze ≠ companions; Bitwarden gate is **vault CLEAR** + this smoke, then **Ben GO**, **never auto-fire**. Soft-530 **OPEN** can coexist — do **not** block Bitwarden on Soft-530 CLEAR. ≠ Zone Direct Upload ≠ **#82** ≠ unfreeze / **#79.1** execute ≠ companion re-ask ≠ Ben docker / Doc-wake re-nag ≠ Soft-530 CLEAR smoke (`502ab2e`). CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire**). Forensics still [vault-stay-up.md](vault-stay-up.md) (`d88cacb`). Essay: [vault-clear-smoke.md](vault-clear-smoke.md). Living this fold: Soft-530 **OPEN 530 / CF 1033**; vault still **OPEN** **530 / CF 1033**; ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED; Option A Redirect+UX **PASS** (rungs 1–2 — **≠ #82**); Home **STALE** until **#82**; freeze `` `changeme` `` / **`5swmVz`**; **#82** still Ben GO. Soft-530 **CLEAR ≠** this card. **SSOT stays `https://vault.projectcar.ca`** — **never** substitute Doc loopback; Doc VW/NC healthy = camp Z1 preflight only. Paper only — no Zone/Garage execute. Does **not** invent vault CLEAR or re-ask Ben docker.
- **Soft-530 weekday overnight (LOCKED — Mon–Thu nights only):** After last weekday plan-improve `*/20` until next weekday first fire (~06:00). **Home-off** Soft-530 overnight is **EXPECTED** ([home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md)) — not a mid-overnight incident / morning Ben DIFF. Lookout `api.` `/health` + vault `/alive` **only** (companions **HOLD**). While `brochure-option-a-redirect-watch` is **GONE**, overnight Option A Dynamic redirect is **UNCOVERED — accept that risk** (same class as weekend `84107e7`; interim = plan-improve `*/20`). Do **not** invent overnight plan-improve, Zone, or Garage coverage. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **HOLD** a second Ben recreate ask. No overnight Zone Direct Upload, no Garage execute, no Ben re-nag from duration. Friday last `*/20` → weekend coverage (next lock). Mid-overnight Soft-530 **CLEAR→OPEN** / Doc reappear still [doc-reappear-first-hop.md](doc-reappear-first-hop.md); McKing still [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (**never** `Mac.lan`; **never auto-fire**; **never** wake McKing docker as camp path). Complements weekend coverage (`84107e7`) + [soft-530-extended-open.md](soft-530-extended-open.md). Last weekday `*/20` writes [overnight-baseline-stamp.md](overnight-baseline-stamp.md) (**refresh only on field DIFF**); first weekday `*/20` after ~06:00 diffs that stamp — **same-class = stay quiet** (`611c267`); Lead/Ben only on class DIFF; first-hop absorbed DIFF → no Ben re-surface + refresh living stamp. Living this fold: Soft-530 **OPEN 530 / CF 1033** + vault **OPEN 530 / CF 1033** **dual OPEN both 1033** (half-state **OVER**) (first Mon–Thu overnight after Sep 13–15 recovery; subclass **was 502**); Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED; freeze **`last_known`** `` `changeme` `` / **`5swmVz`** / **`4cf8924`**; Lookout api `ok=false` / vault `ok=false` **expected** (dual OPEN both 1033); Option A Redirect+UX **PASS**; Home **STALE** until **#82**. Soft-530 **CLEAR** overnight **≠** camp CDM soak **PASS** **≠** companions **≠** **#82** **≠** unfreeze **≠** Zone vault retarget **≠** Bitwarden auto. Same half-state ≠ dual CLEAR ≠ “incident closed.” Essay: [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md). Stamp: [overnight-baseline-stamp.md](overnight-baseline-stamp.md). SOP: [lookout-rearm-sop.md](lookout-rearm-sop.md). Paper only — no Zone/Garage execute.
- **Overnight baseline stamp (LOCKED — last weekday `*/20` one-liner):** Soft-530 **CLEAR** + vault **OPEN** is a **first-class half-state class** — ≠ dual OPEN ≠ dual CLEAR; morning must **not** score it as “incident closed.” Tonight **~17:20** (Tue 2026-09-15) is the first Mon–Thu overnight after Sep 13–15 recovery: Soft-530 **OPEN 530 / CF 1033** (Lookout + Lead **~16:36**; **was CLEAR**); vault **OPEN 530 / CF 1033** connector-down (public; **was 502**); ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT**; freeze **`last_known`** `` `changeme` `` + **`5swmVz`** / **`4cf8924`** (Soft-530 OPEN → unverifiable ≠ DIFF); Option A Redirect **PASS** + Soft-530 UX **LIVE**; Lookout api `ok=false` / vault `ok=false` **expected** (dual OPEN both 1033); brochure `*/5` watch **GONE** (interim = plan-improve `*/20`). Home **STALE** until **#82**. Morning DIFF: Soft-530 **CLEAR→OPEN** = DIFF (Lead wake / doc-reappear); vault still **530 / CF 1033** = **SAME**; vault **502 ↔ 530/1033** = subclass field DIFF (Lead/Lookout ≠ Ben re-nag); Docs **CONNECTED→ABSENT** = DIFF; `changeme` off = freeze DIFF (unfreeze signal). Soft-530 **CLEAR** overnight **≠** camp CDM soak **PASS** **≠** companions re-arm **≠** **#82** **≠** unfreeze **≠** Zone vault retarget **≠** Bitwarden auto. **Never** `Mac.lan` as Doc. **No** second Ben Lookout recreate. **No** overnight Zone/Garage. **Don’t** wake McKing docker as camp path. **Refresh only on field DIFF.** **Same-class = stay quiet** (`611c267`). **Home-off Soft-530 + Docs ABSENT = SAME-CLASS expected.** **First-hop absorbed DIFF** → no Ben re-surface. While Soft-530 is OPEN, unverifiable freeze ≠ DIFF. Mon 2026-09-14 ~17:25 / ~17:42 dual-OPEN lines **stand** (historical). Essay: [overnight-baseline-stamp.md](overnight-baseline-stamp.md). Paper only — no Zone/Garage execute.
- **Weekend Soft-530 coverage (LOCKED — plan-improve off Sat/Sun):** Weekend flip coverage = Lookout `api.` `/health` + vault `/alive` **only** (companions stay **HOLD / not armed** — do **not** re-ask). Weekend control plane = Doc KeepAlive / lid-close (`com.projectcar.cloudflared` + uvicorn `:8000` + `next start` `:3000` LaunchAgents) — **not** Doc unfreeze, **not** **#82**. If `api.` `/health` flips non-200 → Chief/Lead [doc-lid-restore.md](doc-lid-restore.md) **process wake only** (no pull while freeze intact). Vault flips stay on the separate McKing watch — [vault-stay-up.md](vault-stay-up.md). Dual-OPEN glue: [dual-host-outage.md](dual-host-outage.md) — **home/lab/pre-cutover only** **Doc first, then McKing** (**SUPERSEDED for camp**; **`lightning` ABSENT must not trigger McKing wake** after Ben GO); parallel only if both hosts on ListMachines; `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning`; independent CLEAR gates; Bitwarden import/rotate **blocked** until vault CLEAR. After Soft-530 CLEAR, stamp **post-CLEAR stay-up evidence** (lastExit / KeepAlive / caffeinate·CDM / Tailscale) on [doc-lid-restore.md](doc-lid-restore.md) — wake order stays dual-host-outage. Soft-530 still **OPEN** this fold (CF **1033** since 2026-09-13 ~11:57 America/Edmonton); vault independently **OPEN** (**502** since ~19:45 MT Sep 13); ListMachines **Mac.lan only**; this paper does **not** invent CLEAR. Do **not** schedule weekend Zone Direct Upload / Worker work — first Monday plan-improve resumes Soft-530 smoke. While `brochure-option-a-redirect-watch` is **GONE**, weekend Option A Dynamic redirect is **UNCOVERED — accept that risk**. Do **not** invent weekend plan-improve, Zone, or Garage Option A coverage. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **HOLD** a second Ben recreate ask. Anti-goal: Fri Soft-530 **CLEAR** is historical — living dual-OPEN ≠ unfreeze GO ≠ companion re-ask ≠ **#82** auto-GO. Soft-530 **OPEN ≠ #82 blocked**. **#82** Ben GO / dual-Nextcloud (`4cde204`) / `brochure-worker-ci` (`6e6efe8`) / waitlist-owner-desk **retired** (Owner desk LIVE; optional CSV Later) / **#79.1** git-only until Doc unfreeze — **unchanged**. Friday last weekday `*/20` (~17:40 MT) hands **into this** weekend coverage. **Mon–Thu nights** after last `*/20` until next weekday first fire (~06:00) are [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) — **not** this Sat/Sun card. Paper: [api-stay-up.md](api-stay-up.md), [shop-web-stay-up.md](shop-web-stay-up.md), [doc-lid-restore.md](doc-lid-restore.md), [vault-stay-up.md](vault-stay-up.md), [dual-host-outage.md](dual-host-outage.md), [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md), [lookout-rearm-sop.md](lookout-rearm-sop.md).
- **Camp vault on Doc (LOCKED — camp SSOT; paper; Ben GO, never auto-cutover):** **Camp host SSOT.** Zone/Doc follow [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) at camp GO. [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md) + McKing-centric [vault-stay-up.md](vault-stay-up.md) are **PARKED for camp weeks** / desk/lab optional. Camp posture = Docs Mac plugged for NC + VW; McKing may sleep. Recovery = Doc VW + Zone `vault.` retarget — **not** McKing docker. Dual-tunnel **camp**: Doc hosts shop + vault (+ NC). Soft-530 independence is **home/lab only**; at camp they couple on Doc lid/CDM/KeepAlive. Zone `vault.` checklist is hop **4** **in that file** (preflight / inventory McKing origin / Doc ingress ready / cut / **2026.6.0** smoke **502→200** vs **1033→200** / rollback to McKing if Doc smoke fails; Soft-530 shop hostnames stay on Doc). Smoke: `/alive` **200** + `/api/config` VW **2026.6.0** class. Bitwarden still vault CLEAR + Ben GO. Soft-530 **OPEN ≠** block this paper. Anti-goals: never `Mac.lan` as Doc/vault; no dual origins; don’t leave `vault.` on dead McKing docker while Doc VW is live; don’t treat McKing docker ask as camp vault path; don’t retarget shop Soft-530 hostnames; **no live Zone execute until Ben GO**. [post-dual-clear-go.md](post-dual-clear-go.md) does **not** assume McKing vault wake first. [dual-host-outage.md](dual-host-outage.md) is **SUPERSEDED for camp** — **`lightning` ABSENT must not trigger McKing wake**. Living: Soft-530 **OPEN 530 / CF 1033**; vault **OPEN** 530 / CF 1033 (was 502); Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED — home-awake pre-camp half-state (**not a Ben surface**); Soft-530 CLEAR at home ≠ camp GO; Option A Redirect+UX **PASS** (rungs 1–2 — **≠ #82**). **Doc local VW/NC healthy ≠ public vault CLEAR** — Doc loopback `:8222` `/alive` + `:8080` after cold-boot = **Z1 preflight only**; public `vault.projectcar.ca` still **530 / CF 1033** (McKing origin; was 502) = still vault **OPEN** (not partial CLEAR / not dual CLEAR / not camp progress %); [vault-clear-smoke.md](vault-clear-smoke.md) SSOT stays the public hostname; **never** substitute Doc loopback; Soft-530 **CLEAR** alone still **≠** camp GO; still **Ben GO** + Zone retarget — **not** LIVE cutover. No Zone/Garage/Hatch execute. No Ben Doc-wake/docker re-ask. Essay: [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md). Complement: [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md) (home-off expected — **not** this cutover failing).
- **Home vs camp Doc posture (LOCKED — paper; complement to `fb86f90`, not a re-cutover):** **HOME-off:** Docs **ABSENT** + Soft-530 **OPEN** is **EXPECTED**. **HOME-awake (living):** Soft-530 **OPEN 530 / CF 1033** + vault **OPEN 530 / CF 1033** is living **dual OPEN** (prior home-awake CLEAR half-state **superseded ~16:36**) — do **not** enter [post-dual-clear-go.md](post-dual-clear-go.md); Soft-530 CLEAR at home ≠ camp GO; Soft-530 **CLEAR ≠ #82** ≠ unfreeze ≠ Bitwarden. Quiet-ops **HOLD**; do **not** re-nag. **CAMP** (after Ben GO): Doc on+Amphetamine/CDM → Soft-530+vault expected CLEAR together; dual OPEN = coupled Doc hop (**not** McKing docker). Away weeks **supersede** standing McKing=`vault.`→`:8222`; McKing 24/7 + WOL/remote LUKS **deferred**. Pi/VPS still better long-term if vault must stay up while Doc is home-off (paper — **not** a Pi GO). [dual-host-outage.md](dual-host-outage.md) is **SUPERSEDED for camp** — **`lightning` ABSENT must not trigger McKing wake**. Stamp / morning `*/20`: home Soft-530 is **SAME-CLASS expected**. Lookout `ok=false` while Docs **ABSENT** at home is expected baseline — do **not** re-arm companions. Soft-530 **OPEN ≠ #82 blocked**. Never `Mac.lan` as Doc. Never treat home Soft-530 as camp cutover failure. Never auto Ben nag on home Soft-530. Living: Soft-530 **OPEN 530 / CF 1033** + vault **OPEN 530 / CF 1033** dual OPEN (half-state **OVER**); ListMachines Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED — home-awake pre-camp, **not a Ben surface**; Option A Redirect+UX **PASS** (rungs 1–2 — **≠ #82**); camp cutover paper ready, pending Ben GO (Zone `vault.` checklist paper only). Soft-530 CLEAR at home ≠ camp GO. Soft-530 **CLEAR ≠ #82** ≠ unfreeze ≠ Bitwarden. Essay: [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md). Align: [soft-530-extended-open.md](soft-530-extended-open.md) · [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) · [overnight-baseline-stamp.md](overnight-baseline-stamp.md) · [doc-reappear-first-hop.md](doc-reappear-first-hop.md). Paper only — no Zone/Garage/Hatch, no Ben Doc-wake/docker re-ask.
- **Post-dual-CLEAR Ben GO sequencer (LOCKED — never auto-fire):** After **extended OPEN**, Soft-530 CLEAR first runs [soft-530-clear-smoke.md](soft-530-clear-smoke.md) — **before** this menu / desk / **#79.1**. Vault CLEAR first runs [vault-clear-smoke.md](vault-clear-smoke.md) — **before** Bitwarden / desk. **Does not assume McKing vault wake first.** **Home/lab** vault CLEAR = McKing. **Camp** (after Ben GO) = Doc VW + Zone retarget ([camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md)). After Soft-530 CLEAR (Doc lid-restore) **and** vault CLEAR (**host-posture-aware**) **and** both forensic papers already ack’d (`55e10d0` + `d88cacb`), Ben’s GO menu is **ordered**: (1) keep Soft-530 healthy (KeepAlive / CDM / caffeinate); (2) **#82** Worker-live (upload + **mandatory** purge/freshness). After **#82** the **brochure lane** is **parallel**: (A) **#83** CI thin `_redirects` anytime on the **#82** base (not Bulk-gated; parallel with upload OK); (B) **Bulk Phase1** frees Option A **FULL 10/10** slots when Member needs capacity — **#82 PASS ≠ wipe Dynamic**; keep full Dynamic until Bulk is **LIVE** and smoke-equal (keep `root`/`shop`/`chat` Dynamic until then; do **not** migrate Dynamic on upload day). Member edge **only after Bulk.** **Never #81.** Never Bulk→#83. (3) Doc unfreeze + pull + Garage **#79.1** is **Shop OS parallel** — not “next after **#82**” in place of Bulk / **#83**. Lane-done = [post-dual-clear-go.md](post-dual-clear-go.md) **unfreeze + #79.1 acceptance smoke** (public `app.`/`ops.` `/login` without `changeme`; tip / `BUILD_ID` ≠ `4cf8924` / `5swmVz`; `/health` **200** + waitlist OPTIONS CORS Origin `https://projectcar.ca` (+ www); **#79.1** merge-alone does **not** clear public `changeme`). Soft-530 CLEAR alone ≠ that card. Not **#82**, not Zone Direct Upload, not companions re-ask, not Bitwarden. **Never** auto-fire on CLEAR. Anti-goals: CLEAR ≠ companion re-ask; CLEAR ≠ Bitwarden import/rotate; Soft-530 CLEAR alone ≠ Doc unfreeze; vault CLEAR ≠ Soft-530 work; dual CLEAR ≠ **#82** auto-go; Soft-530 **OPEN ≠ #82 blocked**; **#82** ≠ end of brochure. KeepAlive→**#82** stays the *default ordered menu after dual CLEAR*. Living this fold: Soft-530 **OPEN 530 / CF 1033**; vault still **OPEN 530 / CF 1033**; **Docs + lightning CONNECTED**. Half-state ≠ dual CLEAR — **do not enter** until vault CLEAR. Paper: [post-dual-clear-go.md](post-dual-clear-go.md). Option A / **#82** gates: [brochure-worker-ci.md](brochure-worker-ci.md). Member-edge capacity: [member-zone-edge.md](member-zone-edge.md). **#82** / companions HOLD / **#79.1** git-only until unfreeze — **unchanged**.
- **Waitlist-owner-desk retired:** Owner desk is **LIVE** when origin is up (login + `/waitlist` on frozen **`5swmVz`**). Post–`brochure-worker-ci` parked queue item is done — do **not** open `Docs/waitlist-owner-desk.md`. Optional CSV stays Later. No email blast / Stripe / Member host. Soft-530 **CLEAR** or desk existence ≠ Doc unfreeze ≠ **#79.1** rebuild GO. **#82** Ben GO / Soft-530 companions HOLD / weekend coverage (`84107e7`) / dual-Nextcloud / `brochure-worker-ci` (`6e6efe8`) / **#79.1** git-only until Doc unfreeze — **unchanged**.
- **McKing shop-host cutover (PARKED for camp weeks / desk/lab optional — not camp SSOT):** Later **home/lab** shop-to-McKing plan only. Path **OPEN** 2026-09-11 ~10:32 America/Edmonton — sshd / docker / `/opt/mission-control`; hub dual-run NC+VW healthy on Doc+McKing. **At camp GO, Zone/Doc follow [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md), not this card.** Doc KeepAlive still owns public `api.` / `ops.` / `app.` (Doc stays public CF origin / KeepAlive backup). Soft-530 dual-run five-row gate still blocks any public **shop** CF hostname leave (`cloud.` / `api.` / `app.` / `ops.` only — **vault EXCLUDED**) **at home/lab**. Hop OPEN ≠ shop cutover GO ≠ Doc unfreeze (freeze `4cf8924` / `5swmVz` until **Ben GO**). Vault is **McKing-owned** **at home/lab** (living **OPEN 530 / CF 1033**, was 502) — that is the **separate** living-ops row below, not this shop-cutover pointer. Gate row 2 acceptance artifact when it lands: in-flight Doc dual-run evidence card (Tailscale / private only) — still no public McKing attach for shop. Paper: [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md). **Not** Next #1. This STATUS fold is a pointer, not a live shop cut. Brochure Worker stays. Soft-530 is **OPEN** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton); Fri CLEAR / Doc-up is **historical**. Future lid-close **530 / 1033** still expected while Doc is origin.
- **Dual-tunnel ownership (separate living-ops row — not the shop CF cutover):** **Home/lab:** Doc tunnel = **`cloud.` + `api.` + `app.` + `ops.` only** (shop Soft-530 / KeepAlive). McKing-only tunnel **`vault.projectcar.ca` is McKing-owned** (+ **unpublished** NC, not a tunnel hostname): McKing `cloudflared` → `localhost:8222`. **Camp SSOT** (after **Ben GO**): Doc hosts shop tunnels + vault (+ NC); McKing may sleep — [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md). [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md) + McKing-centric [vault-stay-up.md](vault-stay-up.md) are **PARKED for camp weeks** / desk/lab optional — Zone/Doc do **not** follow those cards at camp GO. Soft-530 independence is **home/lab only**. Never auto-cutover. Zone owns `vault.` hostname retarget. **Living:** vault **OPEN** subclass **530 / CF 1033** (was 502 since ~19:45 MT 2026-09-13). Fri `/alive` **200** + `/api/config` **2026.6.0** is **historical** (when CLEAR, prefer `/alive`). Soft-530 five-row dual-run gate applies to **Doc shop hosts only** (`cloud.` / `api.` / `app.` / `ops.`) — **vault is EXCLUDED**. Soft-530 / `api.` `/health` can stay green while vault dies. Lookout vault flip watch is **LIVE/armed** (`enabled:true`, Lookout confirmed) on `https://vault.projectcar.ca/alive` every **5m** (fallback `/api/config` if `/alive` 404s; baseline Lookout-owned: `/workspace/lookout/projectcar-vault-health-baseline.json`). Flip-only alerts: **Chief + Lead only** on **200↔non-200**; **never Ben**; never mutate. Vault watch ≠ Soft-530 / Soft-530 companion watches / Doc lid-restore. Soft-530 five-row is Doc shop hosts `cloud.` / `api.` / `app.` / `ops.`, but Lookout Soft-530 coverage was **api-only** (`/health`) — `ops.` / `app.` (and `cloud.`) can **530 while `api.` stays 200** (same class as pre-vault-watch). Soft-530 companion watches on `ops.` / `app.` `/login` (optional `cloud.` `/login`; Lead probed **200**) are **HOLD / not armed** (Ben skipped companion-watch approval ~14:35 America/Edmonton — do **not** re-ask). Companion watches ≠ vault `/alive` watch ≠ Doc lid-restore. Soft-530 living is **OPEN** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton) — Fri CLEAR is historical. Doc lid-restore / LaunchAgent KeepAlive must **not** recreate `vault.` ingress on Doc **at home/lab** / **without camp Ben GO**. Anti-goals: never move the Doc mission-control token for `vault.` **at home/lab**; vault LIVE ≠ shop hostname leave ≠ Doc unfreeze. Hop OPEN ≠ shop cutover GO ≠ Doc unfreeze. Shop CF cutover stays paper — no Zone live for shop. Machine map: [home-lab-specification.md](home-lab-specification.md). Cross-links: [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) (**camp SSOT**), [vault-stay-up.md](vault-stay-up.md) (**PARKED for camp weeks**), [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md) (**PARKED for camp weeks**), [api-stay-up.md](api-stay-up.md), [deployment-guide.md](deployment-guide.md), [home-lab-specification.md](home-lab-specification.md).
- **Dual-Nextcloud ownership (paper — not a `cloud.*` CF cutover):** Public `cloud.projectcar.ca` stays on **Doc** until explicit **Ben GO**. McKing `/opt/mission-control` NC remains **lab/hub loopback-only** (unpublished) **at home/lab**. Dual-tunnel matrix **home/lab**: Doc=`cloud`/`api`/`app`/`ops`; McKing=`vault` (+ unpublished NC). **Camp** (after Ben GO): Doc hosts shop + vault (+ NC); McKing may sleep — [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md). Paper path = Tailscale Serve HTTPS mirroring VW Serve at `lightning.tailbe8f55.ts.net` + `NEXTCLOUD_TRUSTED_DOMAINS` MagicDNS — **not** a Cloudflare cutover of `cloud.*`. Do **not** point `cloud.*` tunnel/CNAME at McKing until a dual-run acceptance gate (parity vs Doc `status.php`) **and** Ben GO. Hub dual-run NC+VW healthy / hop OPEN / vault LIVE / Serve stand-up do **not** pass that gate. #82 gate unchanged. Soft-530 companions **HOLD**. Vault LIVE unchanged. Paper: [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) (**camp SSOT**), [home-lab-specification.md](home-lab-specification.md), [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md) (**PARKED for camp weeks**).
- **IA freeze.** Ben is happy with the current app direction. Do **not** start major IA reshuffles.
- **Build breadth (now)** and **Ship-MVP cut (later gate)** are **two locks** (tables above). Build = placeholders in the app for IA. Public MVP = later cut of unfinished / unnecessary. Do **not** read this as “v1 includes everything forever.” Proposed keep vs cut (draft): [ship-mvp-cut.md](ship-mvp-cut.md).
- **Inventory prefixes (LOCKED — Ben + Chief):** **B1–B6** = resident bay hand-tool kits (one kit per hoist/bay). **B6 = shop hoist bay** — same B-scheme as other bays, **not `SH`**. **TC** = tool-crib specialty tools (checkout / return / overdue). **PT** = parts inventory (stock, PO, member requests). **CM** = consumables **later** (do not build a full CM UI now). SKU shape `PREFIX-CATEGORY-NNN` (category may include extra hyphen segments): `B2-WR-014`, `TC-TQ-003`, `PT-OIL-5W30-012`. Bay-resident tools stay on the cart (no crib checkout). Crib tools check out. Parts are qty / reorder + requests — not tool checkout and not member commerce. Demo UI / seed SKUs only until a thin API lands. Existing `tools` table (`name` / `part_number`) stays schema-only in this slice.
- **Ops sections (locked IA, placeholders now):** **Parts** (PT list + POs) and **Tools** (B1–B6 lists + TC checkout stub, plus orders / requests / planned). Not a claim that inventory or purchasing is shipped.
- **Job board (locked IA, placeholder now):** shop chores (cleaning, tool maintenance, random upkeep). Ops posts a **token bounty**; member claim/complete credits the **append-only token ledger** (same tokens as hoist booking, **not Stripe**). Placeholder must show sample amounts. Full workflow Later.
- **Cameras (locked IA, placeholder now):** members = **one primary shop cam**. Ops = **all cams** + door entry logs + Frigate / AI collection. Not a claim that Frigate is wired on `main`. **Anti-goal:** McKing Tailnet Frigate **0.17.2** + Jellyfin lab ≠ Shop OS `/cameras` live and does **not** flip `pc.cameras`. No camera wiring from that stand-up. Later Frigate / cameras placeholder stays **Later**.
- **McKing Frigate / Jellyfin lab ≠ Shop OS cameras (anti-goal):** Today’s McKing Tailnet Frigate **0.17.2** + Jellyfin lab is **not** Shop OS `/cameras` live. It does **not** flip `pc.cameras`. Do **not** wire cameras / Frigate / NVR into Shop OS from this stand-up. Later stays **Later**. Machine map: [home-lab-specification.md](home-lab-specification.md).
- **Member parts / tool requests** = request desk now; **full purchasing Later**. No member Tools nav.
- **Chat v1** = human messaging + polling. **Owners start rooms.** Members reply in rooms they are in. Ops/Owner see-all + mute. Dual surfaces `/chat` and `/member/chat`. No AI/Grok/Matrix bridge. Not the brochure Chat page (still stripped). Apex sidecar **deferred**.
- No Stripe. No “shop is open” claims. No live public pricing until Ben says so.
- Token pricing v1 is locked: bands + overlay in `America/Regina` — `token-pricing.md`. `hours × 100 × band × overlay × fill`. Defaults are Owner-editable placeholders, not public prices. Allotments: **Basic 1000 / Premium 1500** per period. Two tiers only (no Pro, no Weekly). Fill is an explicit extra factor (10–25%), not a rewrite of the overlay table.
- **6 hoists** in calendar/seed. Exactly one is the shop hoist (`is_shop`) — **Bay 6**. **v1 = (A) Owner-only** — customers cannot book that **bay** (`400 shop_hoist_owner_only`). **(B) bumpable** is a later tweak only. See `token-pricing.md`. Owner Dashboard is Bays 1–6 next-24h strips; no “Today’s bookings” list.
- HTTPS ops/app Doc demo requires **Secure** session cookies (`COOKIE_SECURE` / `SHOP_COOKIE_SECURE`). Demo cookies — **not OIDC**.
- Shop-web KeepAlive on Doc uses **`next start`**, not `next dev`. See [shop-web-stay-up.md](shop-web-stay-up.md).
- Mission Control cockpit needs **Ben GO** before start. Do not start the cockpit from a docs PR.
- Do not revive Apex. Brochure stays Worker / Pages — no Apex sidecar.
- **Live URL SSOT (Option A):** Zone Redirect pack is **FULL 10/10** Active Dynamic **301**s (apex+www) — capacity lock; no new Dynamic rules for brochure pretty-URLs. Owns pretty URLs, including brochure-shop-html-to-the-shop (`/shop.html`→`/the-shop.html`). Worker Assets **`html_handling: none`** — `*.html` is canonical. Worker `_redirects` is **thin**: chat → contact.html only (no `/` or `/shop`). Do **not** claim Worker `/shop` → 302 `/the-shop`. **#83** is the CI-only thin `_redirects` assert on the **#82** base — **not** Bulk-gated. **Never #81.**
- **Hard lock / prefer merge (brochure sequence):** **Never #81.** Wrong lock was `#82 → Zone Direct Upload → Bulk Phase1 → #83`. After **#82** Ben GO + Zone Direct Upload + mandatory purge/freshness, **#83** and **Bulk Phase1** are **parallel** (not Bulk→#83). (A) **#83** merge anytime on the **#82** base (CI-only thin `_redirects` assert; **parallel with upload OK**; **not** Bulk-gated). (B) Bulk Phase1 **only** to free Dynamic slots when Member needs capacity — **after** Bulk is **LIVE** and smoke-equal to today’s **10/10** (keep `root`/`shop`/`chat` Dynamic until then). **#82 PASS ≠ wipe Dynamic.** Do **not** migrate Dynamic on upload day. **Member edge only after Bulk.** Do **not** write `#82 → upload → Bulk Phase1 → #83`. Option A **FULL 10/10** still capacity-blocks **new Dynamic pretty-URLs** and **Member edge** until Bulk Phase1 — that lock is **not** a **#83** gate. Soft-530 companions **HOLD**. Reality quarantine until **#82** unchanged. Paper only.
- **Brochure Reality quarantine (held #70):** Until **#82** Worker-live + Zone Direct Upload, **#70 Reality tip / Option A** is brochure + living-ops SSOT. **Public STATUS tip honesty:** `main` still stamps P4-2 **`2b772ff` / #67** — **stale** vs live Option A Redirect pack + Soft-530 Discord waitlist + McKing vault **LIVE (when CLEAR)** — living vault is **OPEN 530 / CF 1033** (was 502). **Reinforce (no Ben ask / no tip-only PR):** `main` Reality tip is still **`2b772ff` / `styles.css?v=35` / Worker `/shop` 302** — quarantine stands; **#70** is SSOT until **#82**. Same class: `main` `doc-lid-restore.md` Soft-530 table still says Lookout api-health **paused** vs living **`enabled:true`** (**#78 LIVE-SUPERSEDED**). **Living half-state:** Soft-530 **CLEAR** + vault **OPEN 530 / CF 1033** (was 502); Docs `95a229f5` **CONNECTED** / Shell **unreachable**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED. Home still bare `href="/"` + canonical/og apex until **#82**. Option A is public SSOT. Half-state ≠ dual CLEAR. Do **not** open a tip-only PR; keep this quarantine until **#82**. Do **not** execute Zone/Garage from **`main` Reality tip** (`main` still pre-Option-A Worker `/shop` **302** + `styles.css?v=35`). After **#82** upload + **mandatory purge** (`projectcar-brochure` Worker/Cache, apex+www), one tip-fold reconciles `main` Reality to live (Live post-#82 checklist: Home canonical/og/sitemap `https://projectcar.ca/index.html`; nav/logo not `href="/"`; Zone Redirect pack SSOT FULL 10/10 / Bulk Phase1 path; Worker `_redirects` thin chat→contact only; `waitlist.js?v=3` + `styles.css?v=36`; `/shop.html`→`/the-shop.html` 301 via Zone — never reintroduce Worker `/shop` 302). Do **not** treat `Cache-Control: no-cache, must-revalidate` as sufficient — live HTML steady-state is `cf-cache-status: HIT` + no-cache. `/member*` stays Worker **404** until Member edge Ben GO after Bulk Phase1 — never 301 into `membership.html`. Soft-530 companion watches **HOLD / not armed** (Ben skipped ~14:35 America/Edmonton — do **not** re-ask). Paper only. Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) · [brochure-worker-deploy.md](brochure-worker-deploy.md) · [brochure-redirect-watch.md](brochure-redirect-watch.md) · [lookout-rearm-sop.md](lookout-rearm-sop.md). Edge paper: [member-zone-edge.md](member-zone-edge.md).
- **Lookout brochure redirect watch (paper — GONE / HOLD recreate):** While Soft-530 **OPEN**, Option A is the only public **LIVE** surface. Living watch **GONE** (deleted to fix stalled cron; Auto-review blocked recreate). **Never delete** to fix cron. **HOLD** after one Ben ask. Weekday Chief `*/20` **+ Soft-530 UX**. Overnight/weekend Option A Dynamic redirect **UNCOVERED — accept that risk**. Do **not** invent overnight plan-improve, Zone, or Garage coverage. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **Redirect-only PASS ≠ Soft-530 UX PASS.** After Ben approves: enabled + first fire ≤ **10m** + baseline **LIVE 10/10**. Flip **Chief + Lead only**; **never Ben**. **Not** companion re-ask. **#78 lookout-resume** is **LIVE-SUPERSEDED**. Essay: [brochure-redirect-watch.md](brochure-redirect-watch.md). SOP: [lookout-rearm-sop.md](lookout-rearm-sop.md). Overnight: [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md). Weekend: `84107e7`. Quiet-ops: paper continues; continuous watch **GONE** ([soft-530-extended-open.md](soft-530-extended-open.md)). Living dual-OPEN (`5f2fd1c`) **unchanged**. Paper only — no Zone/Garage execute.
- **Lookout rearm SOP (LOCKED — this fold):** (1) **Never delete** an armed Project Car Lookout watch to fix stalled cron. (2) Prefer in-place update / pause-resume / re-save. (3) Delete already happened + Auto-review blocked recreate → **HOLD** after one Ben ask. (4) Weekday Option A = Chief `*/20` redirect smoke **+ Soft-530 UX**; overnight/weekend Dynamic redirect **UNCOVERED** (accept that risk; do **not** invent overnight plan-improve / Zone / Garage coverage) — **Redirect-only PASS ≠ Soft-530 UX PASS**; Soft-530 UX smoke URL SSOT = **root** `/styles.css?v=36` + `/waitlist.js?v=3` (not `/assets/` — those 404s are **not** a regression). **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **HOLD** a second Ben recreate ask. (5) After Ben approves: routine enabled + first fire within **10m** + baseline **LIVE 10/10** (same **root** CSS/JS). (6) Soft-530 companions stay **HOLD**. (7) **Lookout stall ≠ vault HTTP flip** — `enabled:true` + cron-stalled (vault or api) is a coverage class, not OPEN 502; Lead **may GO** in-place re-arm of **both**; **HOLD** Ben recreate; Soft-530 api remaining OK-ish is **not** living contrast; docker permanence `@367172d` is a **different** tip. (8) **Re-arm success ≠ first fire** — stickiness ≥3 consecutive `*/5` windows (≥10–15m); living ~09:14 MT vault first fire then ~22m stall / api stalled ~56m; **chronic** ≤1 pause/resume per calendar-day (clock resets **America/Edmonton midnight**); next local day Lead **may** try one in-place re-arm then stickiness ≥3 `*/5`; Lead paper only after today's 1×; until stickiness, interim = Chief `*/20` + Lead probes (accept **≤~20m**) — **not** a Ben ping. Living 2026-09-15: **both chronic HOLD today** — quiet-ops “Lookout continues” is **false for flip detection**; Chronic Lookout ≠ quiet-ops failure ≠ Ben ping; chronic HOLD does **not** reopen Ben recreate; Soft-530 companions + brochure recreate stay **HOLD** ([soft-530-extended-open.md](soft-530-extended-open.md)). Anti-goals: not **#82** merge nag, not Dynamic wipe, not honesty-off, not companions rearm, not unfreeze, not Zone execute, not invent overnight redirect smoke, not Ben recreate/docker/Doc-wake from a stall, not call healthy on first fire, not loop pause/resume on chronic. **#78** is **LIVE-SUPERSEDED** (api-health already `enabled:true` — can still cron-stall). Essay: [lookout-rearm-sop.md](lookout-rearm-sop.md). Plane: [vault-stay-up.md](vault-stay-up.md). Overnight: [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md). Weekend: `84107e7`. Honesty permanence: [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (`0705b37`). Paper only — no Lookout/Zone/Garage execute.
- **Interim Chief `*/20` Soft-530 UX (LOCKED — this fold):** While Lookout redirect watch is **GONE**, weekday Chief `*/20` must assert Soft-530 UX, not only pretty-URL **301**s. Overnight/weekend Option A Dynamic redirect is **UNCOVERED — accept that risk**. Do **not** invent overnight plan-improve, Zone, or Garage coverage. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. `membership.html` + `contact.html` still show Soft-530 Discord honesty; **root** `/waitlist.js?v=3` still fail-softs **502 / 530 / 1033** → Discord+mailto; **root** `/styles.css?v=36` **200**. Probe those **root** paths — **not** `/assets/`. `/assets/styles.css?v=36` or `/assets/waitlist.js?v=3` **404** is **NOT** a regression. **Redirect-only PASS ≠ Soft-530 UX PASS.** **CLEAR ≠ honesty-off** (`0705b37`). Not **#82** merge nag, not Dynamic wipe, not companions rearm. Essay: [brochure-redirect-watch.md](brochure-redirect-watch.md) · [lookout-rearm-sop.md](lookout-rearm-sop.md) · [brochure-worker-ci.md](brochure-worker-ci.md) · [brochure-pages-cutover.md](brochure-pages-cutover.md). Paper only.
- **#82 Worker-live ≠ retire Option A Dynamic (LOCKED — this fold) (`49ccf90`):** After Ben GO **#82** → Zone Direct Upload + **mandatory** purge + body freshness (canonical / og / sitemap + nav home = `/index.html`, **no** bare `href="/"`), **PASS** does **not** authorize deleting the CF Dynamic Redirect **10/10** pack. Keep **full** Option A Dynamic (`root` / `shop` + pretty-URL + `chat`) until **Bulk Phase1 is LIVE and smoke-equal** to today’s **10/10**. Then Zone may drop pretty-URL Dynamic per parked Redirect A / Bulk Phase1; keep `root` / `shop` Dynamic unless Bulk covers them too. Frozen Sep 10 2026 inventory is Docs SSOT — do **not** invent rules. Anti-goals: not “Worker-live so wipe Dynamic”; not migrating Dynamic on upload day; not **#81**; not unfreeze / **#79.1** / companions / Bitwarden. Aligns **#83** ∥ Bulk Phase1 after **#82**. Paper: [post-dual-clear-go.md](post-dual-clear-go.md) brochure lane · [brochure-worker-ci.md](brochure-worker-ci.md) Option A matrix · [brochure-pages-cutover.md](brochure-pages-cutover.md) hard cutover gate. Paper only — no Zone/Garage execute.
- **Option A Dynamic pack (FULL 10/10 — LOCKED):** Frozen live inventory (Zone refresh **2026-09-10**, `rule_count: 10`) is Docs SSOT — [brochure-worker-ci.md](brochure-worker-ci.md) Option A matrix · [brochure-pages-cutover.md](brochure-pages-cutover.md) **hard cutover gate**. Do **not** invent rules. Garage must **not** invent new brochure extensionless pretty-URLs that need another Dynamic Redirect Rule until **#82** Worker-live + Bulk Phase1 frees Dynamic slots. Redirect plan A is parked — Bulk Phase1 pretty-URL pack already drafted: `membership` / `about` / `the-shop` / `contact` / `roadmap` ± slash → `*.html`; `root` / `index` / `shop` / `chat` stay Dynamic until migrate (keep `root` / `shop` Dynamic until Member needs those slots, or Bulk covers them). **#82 Worker-live ≠ retire this pack (`49ccf90`):** after Ben GO **#82** → Zone Direct Upload + purge + body freshness (canonical / og / sitemap + nav home = `/index.html`, no bare `href="/"`), **PASS** does **not** authorize deleting the CF Dynamic Redirect **10/10** pack. Keep **full** Option A Dynamic (`root` / `shop` + pretty-URL + `chat`) until **Bulk Phase1 is LIVE and smoke-equal** to today’s **10/10**. Then Zone may drop pretty-URL Dynamic per parked Redirect A; keep `root` / `shop` unless Bulk covers them too. Do **not** migrate Dynamic on upload day. Not “Worker-live so wipe Dynamic.” **Next #1 Member edge is capacity-blocked** until that slot-free — `/member*` must rank **BEFORE** the brochure Redirect pack; apex+www `/member/login` must **not** 301 into `membership.html`. **#83** is CI-only on the **#82** base (thin `_redirects` assert) and is **not** gated by Bulk Phase1 — prefer merge after **#82**; **parallel with upload OK**. **Never #81.** Do **not** write `#82 → upload → Bulk Phase1 → #83`. This lock does **not** touch unfreeze / **#79.1** / companions / Bitwarden. Paper only — no Zone live apply. Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) (Option A matrix + **#82** mandatory purge before body-freshness; **never** Direct Upload from **#81**). Upload click-path: [brochure-worker-deploy.md](brochure-worker-deploy.md). Lane: [post-dual-clear-go.md](post-dual-clear-go.md). Edge paper: [member-zone-edge.md](member-zone-edge.md).
- Shop members never get Nextcloud accounts.
- No n8n.
- **Lead owns Doc `:8000`.** Do not hand uvicorn restarts to Chief.
- **Ops Parts** is PT stock + PO tracker — distinct from `/member/parts` (requests only).
- **Payments (ops):** membership + parts. AI tracks by default; humans on exceptions. Placeholder only. Stripe Later.

Specs: `project-car-application-specification.md` §2 / §13 / §15 / §16 / §17, `token-pricing.md`, `website-webapp-specification.md`, `platform-architecture.md` §5.
