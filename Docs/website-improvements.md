# Project Car — Website Improvements (Living)

**Status:** Living document — update when items ship, get deferred, or new issues are found  
**Last audited:** 2026-09-11 (Soft-530 restore **LIVE** ~06:50 America/Edmonton — Doc origin up; Lookout watch **resumed** (`enabled:true`) ~06:52. Option A URL SSOT unchanged and **FULL 10/10** capacity lock: Zone Redirect pack owns pretty URLs as 10 Active Dynamic **301**s, apex+www — no new Dynamic rules for brochure pretty-URLs until **#82** Worker-live + Bulk Phase1 (Redirect A parked: `membership` / `about` / `the-shop` / `contact` / `roadmap` ± slash → `*.html`; `root` / `index` / `shop` / `chat` stay Dynamic until migrate) — brochure-root-to-index `/`→`/index.html`, brochure-shop-to-the-shop, brochure-shop-html-to-the-shop (`/shop.html`→`/the-shop.html`), membership/about/the-shop/contact/roadmap-to-html (+ trailing-slash), brochure-chat-to-contact, brochure-chat-slash-to-contact. Garage must **not** invent new brochure extensionless pretty-URLs that need another Dynamic Redirect Rule. Paper only — no Zone live apply. Worker Assets **`html_handling: none`** — `*.html` is canonical. Worker `_redirects` is **thin**: **chat → contact.html only** — do **not** claim Worker `/shop` → 302 `/the-shop`. Soft-530 **#80** Worker-live: `waitlist.js?v=3` + `styles.css?v=36` + Soft-530 Discord honesty on Membership/Contact. Home canonical/og/sitemap `/index.html` waits on **#82** — **not yet Worker-live**. P4-2 Discord CTA still **LIVE** from #67 / `2b772ff`. P2-1 / P2-2 / P2-3 / P2-4 / P3-1 / P3-4 / P3-5 already LIVE. P3-2 / P3-3 About stay **held / deferred**).  
**Owner:** Ben (decisions) · Garage (brochure HTML/CSS) · Zone (Cloudflare Worker upload) · Doc (Shop API host)  
**Canonical:** `Coombzy/Project-Car` → `Docs/website-improvements.md`  
**Local clone:** `~/src/Project-Car/Docs/website-improvements.md`  
**Desktop mirror:** `~/Desktop/Project Car/docs/website-improvements.md`  
**Site code:** `apps/website/` (git SSOT). **Live origin:** Cloudflare Worker `projectcar-brochure` Direct Upload of `apps/website/html` (not Doc `:8088` tunnel). Classic Pages git skipped for now. Apex is **deferred** (stripped from this tree).  
**Related:**
- `website-webapp-specification.md` — domain, tunnel, email, architecture (SSOT; do not dual-author a second plan under `apps/website/`)
- `brochure-worker-deploy.md` — standing Zone Direct Upload of `apps/website/html` onto Worker `projectcar-brochure`; Option A Dynamic pack **FULL 10/10** lock + parked Bulk Phase1 pointer; **#83** CI-only after **#82** base — **not** Bulk-gated
- `brochure-worker-ci.md` — paper CI + Option A receipt **after #82 Ben GO** (Soft-530-independent; **OPEN ≠ #82 blocked**): frozen Sep 10 2026 Redirect inventory is Docs SSOT; Direct Upload → **mandatory** Worker/Cache purge (apex+www) → body freshness (`HIT`+no-cache steady-state); **#82** PASS ≠ retire Dynamic **10/10** (`49ccf90`); Soft-530 **root** `/waitlist.js?v=3` / `/styles.css?v=36` / Discord fail-soft (**not** `/assets/` — those 404s are **not** a regression); **never** Direct Upload from **#81**
- `brochure-redirect-watch.md` — Lookout Option A watch **spec**. Living watch **GONE** (deleted to fix stalled cron; Auto-review blocked recreate). **HOLD** recreate. Weekday Chief `*/20` **+ Soft-530 UX**. Overnight/weekend Dynamic redirect **UNCOVERED — accept that risk**. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **Redirect-only PASS ≠ Soft-530 UX PASS.** Soft-530 UX smoke URL SSOT = **root** `/styles.css?v=36` + `/waitlist.js?v=3` (not `/assets/`). Flip Chief+Lead only when armed. **Not** companion re-ask. Sep 9 baseline still **IN FLIGHT** → stamp **LIVE 10/10** (same root CSS/JS).
- `lookout-rearm-sop.md` — **Never delete** an armed Lookout watch to fix stalled cron. Prefer in-place update / pause-resume / re-save. **Lookout stall ≠ vault HTTP flip** — `enabled:true` + cron-stalled is a coverage class. **Re-arm success ≠ first fire** — stickiness = `lastRun` advances across **≥3 consecutive** `*/5` windows (≥10–15m). Living ~09:14 MT 2026-09-15: vault re-arm ~08:50 → first fire ~08:55 → stall ~22m; api stalled ~56m since ~08:21. **Chronic:** stall again within 24h → **≤1** pause/resume per calendar-day (clock resets **America/Edmonton midnight**); Lead paper only after today's 1×. Living 2026-09-15: **both chronic HOLD today** — quiet-ops “Lookout continues” is **false for flip detection**; interim Soft-530+vault SSOT smoke = Chief `*/20` + Lead probes (accept **≤~20m**); Chronic Lookout ≠ quiet-ops failure ≠ Ben ping; chronic HOLD does **not** reopen Ben recreate. Lead **may GO** in-place re-arm of **both** api + vault when stalled **if** the day's 1× is unused; next local day Lead **may** try one again then stickiness ≥3 `*/5`; **HOLD** Ben recreate; until stickiness, interim = Chief `*/20` + Lead probes — not a Ben ping; Soft-530 api remaining OK-ish is **not** living contrast. **HOLD** after one Ben ask. Weekday `*/20` + Soft-530 UX (root CSS/JS URLs). Overnight/weekend **UNCOVERED**. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. After Ben approves: enabled + first fire ≤ **10m** + baseline **LIVE 10/10**. Soft-530 companions **HOLD**. **#78 lookout-resume** is **LIVE-SUPERSEDED**.
- `soft-530-weekday-overnight.md` — Mon–Thu nights after last weekday `*/20` (~17:40 MT) until next weekday first fire (~06:00). Lookout api+vault only. Option A Dynamic redirect **UNCOVERED** while watch **GONE** — accept that risk. Complements weekend coverage (`84107e7`). Last weekday `*/20` writes `overnight-baseline-stamp.md`.
- `overnight-baseline-stamp.md` — Last weekday `*/20` (~17:40 MT) one-liner before Lookout-only: Soft-530 class+since · vault class+since · Option A Redirect+UX PASS/FAIL/UNCOVERED+timestamp · ListMachines IDs (**never** `Mac.lan` as Doc/McKing) · freeze/`BUILD_ID`/`changeme` **`last_known` from last Soft-530 CLEAR only** · Lookout api+vault armed / **chronic HOLD** + `brochure-option-a-redirect-watch` GONE/armed. Living 2026-09-15: **both chronic HOLD today** — “Lookout continues” is **false for flip detection**; Chronic Lookout ≠ quiet-ops failure ≠ Ben ping. **Refresh only on field DIFF**; else prior stamp stands. First weekday `*/20` after ~06:00 diffs that stamp. **Same-class = stay quiet** (`611c267`). Surface Lead/Ben only on stamp class DIFF. **First-hop absorbed DIFF** (overnight first-hop completed + Ben already asked once + Soft-530 / vault HTTP class unchanged) → morning **must not** re-surface Ben; **refresh** living stamp; optional Lead-only ack. Cross-link `mcking-reappear-first-hop.md` / `doc-reappear-first-hop.md`. **Couldn’t verify freeze tip / changeme / BUILD_ID while Soft-530 is OPEN (530/1033) is NOT a freeze DIFF**. True-last `*/20` **~17:42** SAME-CLASS vs ~17:25 — **do NOT rewrite last-weekday**. Living morning-refresh: **`lightning` CONNECTED only** (`Mac.lan` dropped vs ~12:24) / Docs **ABSENT** / vault still **502** (docker dead) / Soft-530 still **1033** / HTTP class unchanged — **no Ben surface** / McKing hop already ran ~19:10 — still blocked on Ben sudo — **no morning re-ask**.
- `brochure-pages-cutover.md` — Classic Pages git plan (blocked on CF ↔ GitHub auth). Frozen Redirect inventory is a **hard cutover gate**. **#82 Worker-live ≠ retire Dynamic** (`49ccf90`). Soft-530 UX smoke URL SSOT = **root** `/styles.css?v=36` + `/waitlist.js?v=3` (not `/assets/`).
- `soft-530-extended-open.md` — quiet-ops once Soft-530 OPEN **~24h** + vault OPEN + **Mac.lan only**. No Ben re-nag. Option A public SSOT. Api+vault stay armed; brochure **paper** continues (continuous watch **GONE**). **Chronic-HOLD exception:** when **both** watches are calendar-day chronic HOLD, “Lookout continues” is **false for flip detection**. Living 2026-09-15: **both chronic HOLD today**. Interim Soft-530+vault SSOT smoke = Chief `*/20` + Lead probes (accept **≤~20m**). Chronic HOLD does **not** reopen Ben recreate. Clock resets **America/Edmonton midnight** — next local day Lead **may** try one in-place re-arm then stickiness ≥3 `*/5`. Soft-530 companions + brochure recreate stay **HOLD**. **Chronic Lookout ≠ quiet-ops failure ≠ Ben ping.** Duration ≠ **#82** auto-GO / unfreeze / Zone / Bitwarden / companion re-ask. Soft-530 **OPEN ≠ #82 blocked**. Next Soft-530 CLEAR: `soft-530-clear-smoke.md`. First hop when Doc reappears: `doc-reappear-first-hop.md`. First hop when McKing reappears: `mcking-reappear-first-hop.md`. Stamp: `overnight-baseline-stamp.md`. SOP: `lookout-rearm-sop.md`.
- `post-dual-clear-go.md` — KeepAlive→**#82** is the *default ordered menu after dual CLEAR* (**never auto-fire**). Soft-530 **OPEN** does **not** defer **#82**. Dual CLEAR ≠ auto-GO. **Never #81.** Option A Dynamic **10/10** until Bulk Phase1 **LIVE**.
- `doc-reappear-first-hop.md` — **first hop** when `Docs-MacBook-Pro` reappears after quiet-ops: assert machineId `95a229f5-9296-4a18-aa98-70fd300dabdf` (**never** `Mac.lan` / Porsche as Doc) → classify **cold-boot** vs overnight **warm wake** → on **cold-boot** (Docs **ABSENT ≥1 calendar day** or Soft-530 **OPEN** 1033 **≥24h** with Docs **ABSENT**) verify cloudflared + shop KeepAlive + note CDM **before** smoke → CLEAR smoke → only if CLEAR → forensics → sequencer (**never auto-fire**). Overnight **warm wake** (`<24h` ABSENT after a prior CLEAR) stays shorter process-wake. **Never** treat multi-day lid as overnight sleep. Living: Docs **ABSENT** since Fri 2026-09-11 ~16:48 (**~4d** — **cold-boot**). Overnight first-hop absorb is the same rule if this hop later completes before morning `*/20`.
- `mcking-reappear-first-hop.md` — **first hop** when `lightning` / McKing reappears after extended vault OPEN / quiet-ops: assert machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` (label **lightning**; **never** `Mac.lan`) → vault-clear-smoke (`/alive` **200** + `/api/config` VW **2026.6.0** + `docker.service` **enabled** + **active** + **502→200**; HTTP-only ≠ CLEAR) → only if CLEAR → vault post-CLEAR forensics; Soft-530 independence — Bitwarden gate is **vault CLEAR** (Ben GO after `/alive` + VW **2026.6.0** + docker permanence), **not** Soft-530 CLEAR; Soft-530 **OPEN** can coexist with vault CLEAR + Bitwarden work; **never auto Bitwarden**; vault CLEAR ≠ Soft-530 CLEAR ≠ unfreeze / **#82** GO. Living: hop already ran ~19:10 — still blocked on Ben sudo; vault still **502** (docker dead). Morning `*/20` **must not** re-ask (`overnight-baseline-stamp.md` first-hop absorb).
- `soft-530-clear-smoke.md` — **first** recovery smoke after extended-OPEN Soft-530 CLEAR (**before** desk / **#79.1** / unfreeze): `/health` **200** + OPTIONS CORS (apex **and** www) + POST **422**/**201**; `changeme` on `/login` is freeze, not Soft-530. **CLEAR ≠ honesty-off** — **#80** Discord honesty + `waitlist.js?v=3` fail-soft stay (overnight sleep / tunnel flaps, not temporary banners; tone tweak only with Ben GO).
- `vault-clear-smoke.md` — **first** recovery smoke after extended-OPEN vault CLEAR (**before** Bitwarden / desk): `/alive` **200** + `/api/config` **200** / McKing **2026.6.0** class (not Doc **2025.12.0**) **and** `docker.service` **enabled** + **active** on `lightning`; `/alive` **200** + docker **disabled/inactive** = **NOT CLEAR / recurrence risk** (reboot re-OPENs like ~19:45 Sep 13); this OPEN is **502→200**, not CF **1033→200**; Bitwarden gate is **vault CLEAR** + this smoke, then **Ben GO**, **never auto-fire**. Soft-530 **OPEN** can coexist — do **not** block Bitwarden on Soft-530 CLEAR.
- `brochure-security-headers.md` — P2-4 Zone Transform Rules **LIVE** (2026-09-07 smoke PASS; do not re-apply from a docs PR)
- `api-stay-up.md` / `cors-origins.md` — public Shop API stay-up + waitlist CORS
- `member-host-cutover.md` — Member UI on projectcar.ca (plan only; **Ben GO**). P1-6 waits on that cutover.
- Skill `project-car-web-copy` — public copy rules (tighten, no fake prices/dates)
- Skill `project-car` — fleet / product context (public site ≠ Mission Control)

---

## How to maintain this doc

1. **Audit block** — when re-checking the live site, bump **Last audited** and add a short Changelog entry.
2. **Status values** (use exactly these in tables):
   - `open` — agreed worth doing, not started
   - `in_progress` — actively being worked
   - `done` — shipped and verified on https://projectcar.ca
   - `blocked` — waiting on Ben / dependency / secret / hardware
   - `wontfix` — explicit decision not to do (note why)
   - `deferred` — not now; revisit later
3. **When shipping** — mark `done`, add date, one-line verification (URL or command).
4. **Do not invent** prices, open dates, street addresses, or “live” features that are not live. Match public honesty on About / Membership.
5. **Copy changes** — follow `project-car-web-copy` page lanes (Home ≠ product dump; Chat must not overpromise).
6. **After edits** — commit + push `Coombzy/Project-Car`; rsync/copy to Desktop mirror without `--delete` unless intentionally pruning.

---

## Snapshot (2026-09-07)

| Check | Result |
|--------|--------|
| https://projectcar.ca / www | Worker `projectcar-brochure` Direct Upload of `apps/website/html` (P4-2 PR #67 / `2b772ff` **LIVE**; P3-4 PR #65 / `f191ad5` **LIVE**; P3-5 PR #62 / `9d83bfc` **LIVE**; P3-1 PR #61 / `88c9820`; P2-4 **LIVE**; P2-3 PR #57 / `be60a01`; P2-2 PR #56 / `ed41d23`; P2-1 PR #55 / `453e44d`; P1-1 / P1-3 PR #52 / `5361212`). HTML from this agent often hits CF challenge (403); SSL in front is Cloudflare. |
| robots.txt / sitemap / favicon / Home bar | Pre-upload snapshot from earlier 2026-09-06 is **stale**. Worker was re-uploaded later that day with P0-3…P0-7. Do not treat hygiene as pending. |
| Waitlist | **e2e PASS** — Membership/Contact `POST` to `https://api.projectcar.ca/waitlist` |
| Pages | Home, About, The Shop, Membership, Roadmap, Contact. Chat nav stripped (PR #5). |
| Discord invite | `https://discord.gg/projectcar` |
| Soft-404 | Live Worker 404s missing static files. Git has `404.html` + nginx `error_page 404`. Do not add SPA `/* /index.html 200`. |
| Mission Control | Separate; ops + Member booking are live on Doc demo, **`https://ops.projectcar.ca`**, and the temporary alias `https://app.projectcar.ca`. Cockpit still needs **Ben GO** — do not start it from brochure work. |

**Overall:** Waitlist is live. P1-1 Home `cta-hero` waitlist CTA weight and P1-3 Contact `contact-soon` collapse are **LIVE** on Worker `projectcar-brochure` (PR #52 / `5361212`; 2026-09-07 smoke PASS). P2-1 is **LIVE** on that Worker (PR #55 / `453e44d`; absolute `og:url` / `og:image` + `twitter:card`; Zone smoke PASS). P2-2 canonicals are **LIVE** on that Worker (PR #56 / `ed41d23`; Zone Direct Upload ~2026-09-07 15:48 America/Regina; Zone smoke PASS: `rel=canonical` matches `og:url` apex on Home + Membership; www serves apex canonicals). P2-3 cache `?v=` SSOT is **LIVE** on that Worker (PR **#57** / `be60a01`; Zone Direct Upload ~2026-09-07 16:01 America/Regina; smoke PASS: `styles.css?v=34` + `banner-logo.png?v=30` on public HTML — Home/About/Contact at minimum; apex+www). P2-4 security + cache-split headers are **LIVE** on that Worker (2026-09-07 smoke PASS — Transform Rules `brochure-security-headers` / `brochure-html-no-cache` / `brochure-asset-immutable`; apex+www; Worker **200**s). P3-1 Home pitch + CTA (not a feature-bullet dump) is **LIVE** on that Worker (PR **#61** / `88c9820`; Zone Direct Upload ~16:50 America/Regina; smoke PASS: pitch + `cta-hero` Join the waitlist + Project Underway + Shop/Membership pointers). P3-4 Shop vs Home split is **LIVE** on that Worker (PR **#65** / `f191ad5`; Zone Direct Upload ~18:46 America/Regina; smoke PASS apex+www: Shop 200 facility bullets (bays/hoists, shared fab, safety/cleanliness, structured shop) + honesty “intended shop capability — not live yet”; no 24/7 packaging on Shop; Membership still “24/7 shop access (when operational)”; Home still P3-1 pitch + `cta-hero` waitlist + Project Underway; no capability-bullet dump; `styles.css?v=34`; P2-4 headers still present). P3-5 24/7 tense is **LIVE** on that Worker (PR **#62** / `9d83bfc`; Zone Direct Upload; smoke PASS: Shop/Membership/Roadmap use “24/7 shop access (when operational)”; Home (and About/Contact) have no absolute 24/7). P4-2 Discord equal CTA is **LIVE** on that Worker (PR **#67** / `2b772ff`; Zone Direct Upload ~19:15 America/Regina; smoke PASS apex+www: Home equal `cta-hero` Join the waitlist + Join Discord (`discord.gg/projectcar`); contact email · Discord; Membership waitlist form unchanged; Discord contact-primary + alongside-waitlist copy; sitemap lastmod bumps (2026-09-08 present); `styles.css?v=35` at that upload; P2-4 headers still; P3-4 Shop facility bullets still). **Live URL SSOT (Option A):** Zone Redirect pack is **FULL 10/10** Active Dynamic **301**s (apex+www) — capacity lock; no new Dynamic rules for brochure pretty-URLs. Owns pretty URLs, including brochure-shop-html-to-the-shop (`/shop.html`→`/the-shop.html`). Garage must **not** invent new brochure extensionless pretty-URLs until **#82** Worker-live + Bulk Phase1 (Redirect A parked: `membership` / `about` / `the-shop` / `contact` / `roadmap` ± slash → `*.html`; `root` / `index` / `shop` / `chat` stay Dynamic until migrate). Worker `_redirects` is **thin** (chat → contact.html only — no `/` or `/shop`). Soft-530 **#80** Worker-live: `waitlist.js?v=3` + `styles.css?v=36`. Home canonical/og/sitemap `/index.html` waits on **#82** — **not yet Worker-live**. P3-2 / P3-3 About stay **held / deferred**. Brochure Chat/Apex stay gone (Apex sidecar deferred; public Chat page stays stripped). Shop OS **Chat v1** (human / polling) is **LIVE** on the ops/app demo — not the brochure. Brochure hygiene P0-3…P0-7 is **already live** on the Worker. Ops + Member booking, Chat #27, and Dashboard #28 are live on Doc demo, **`https://ops.projectcar.ca`**, and the temporary alias `https://app.projectcar.ca`. Some clients still have flaky local DNS for `ops.` — use `app.`. Not Mission Control, not Apex. The shop is not open. P1-6 stays deferred.

---

## Priority legend

| Priority | Meaning |
|----------|---------|
| **P0** | Broken, misleading, or actively hurts trust/crawlers — fix first |
| **P1** | Conversion / visitor clarity before open doors |
| **P2** | SEO, share cards, headers, deploy hygiene |
| **P3** | Copy tighten per page-lane skill |
| **P4** | Growth / content once basics are solid |

---

## P0 — Fix now

Apex is **not** in this list. See Deferred — Apex.

| ID | Item | Status | Notes / acceptance |
|----|------|--------|-------------------|
| P0-3 | **Real `robots.txt`** | done | 2026-09-06 — `apps/website/html/robots.txt` is `User-agent: *` / `Allow: /` + `Sitemap:` (no extra Disallow). Local: `curl -sS http://127.0.0.1:8088/robots.txt` is `text/plain`, not Home HTML. Live after next Worker Direct Upload. |
| P0-4 | **Real `sitemap.xml`** | done | 2026-09-06 — `apps/website/html/sitemap.xml` lists canonical public HTML only: `/`, about, the-shop, membership, roadmap, contact. No Chat/Apex. Local: `curl -sSI http://127.0.0.1:8088/sitemap.xml`. |
| P0-5 | **Stop soft-404 SPA fallback for static site** | done | 2026-09-06 — `404.html` + nginx `error_page 404 /404.html`; `_redirects` stays Chat→Contact 301 only (no SPA fallback). Local nginx: unknown path → **404** + branded page. Worker already 404s missing files; Zone should set `not_found_handling = "404-page"` on next upload. |
| P0-6 | **Favicon set** | done | 2026-09-06 — `html/favicon.ico` + `assets/favicon.svg` / `favicon-32.png` / `apple-touch-icon.png`. Pages no longer use `mcking.jpg` as the only icon. Local: `curl -sS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8088/favicon.ico` → 200. |
| P0-7 | **Home “Website progress 10%”** | done | 2026-09-06 — progress-block removed (no owner-approved metric; did not invent a %). Home keeps honest “Project Underway” + waitlist CTA. Local: Home HTML has no “Website progress” / `10%`. |

### Deferred — Apex (not active P0)

IDs kept so old links resolve. Do not treat these as current P0.

| ID | Item | Status | Notes / acceptance |
|----|------|--------|-------------------|
| P0-1 | **Apex auth readable in container** | deferred | Was live `/api/apex/health` 503 (auth mount `600` / user mismatch). `apps/website/` has no Apex sidecar (Pages cutover). Revisit only if Ben/Lead puts chat back in scope. |
| P0-2 | **Apex offline UX if chat stays down** | deferred | Follows P0-1. Contact/Chat must not imply a working assistant while Apex is out of scope. |

---

## P1 — Conversion / UX

| ID | Item | Status | Notes / acceptance |
|----|------|--------|-------------------|
| P1-1 | **Home primary CTAs** | done | 2026-09-07 live verify — Garage PR #52 → main `5361212`. Home `cta-hero` waitlist CTA weight (Join the waitlist is the dominant above-the-fold action). Zone Direct Upload Worker `projectcar-brochure` **LIVE** from that tip; smoke PASS (Home CTA; apex/www 200). |
| P1-2 | **Chat page honesty** | done | 2026-09-06 — Chat page/nav stripped in PR #5. No public chat copy left to overpromise. Do not revive. |
| P1-3 | **Contact: only live channels** | done | 2026-09-07 live verify — Garage PR #52 → main `5361212`. Phone / IG / TikTok / YouTube / Skool collapsed to one `contact-soon` line. Email + Discord + waitlist stay. Zone Direct Upload Worker `projectcar-brochure` **LIVE** from that tip; smoke PASS (Contact; apex/www 200). |
| P1-4 | **Nav weight** | deferred | Optional: Home · About · Shop · Membership · Contact in primary nav; Roadmap/Chat in footer until chat is real. |
| P1-5 | **Interest capture (waitlist)** | done | 2026-09-06 — Membership/Contact `POST` JSON to `https://api.projectcar.ca/waitlist` (`apps/website/html/waitlist.js`). Name, email, optional phone/notes. No pricing / book-now. Ops: `cors-origins.md`, `api-stay-up.md`. Soft-530 is **Worker-live** (PR **#80**): `waitlist.js?v=3` + `styles.css?v=36`; Membership/Contact fail-soft to Discord (`discord.gg/projectcar`) + mailto on 502 / 530 / 1033 — not “try again.” Fail-soft is the **living** waitlist path: Soft-530 **OPEN** CF **1033** since 2026-09-13 ~11:57 America/Edmonton (waitlist OPTIONS **530** / **1033**). Fri 2026-09-11 ~06:50 **GET /health 200** / **OPTIONS 200** / Chief **POST 201** + CORS is **historical**. Public **OPTIONS** / **POST /waitlist** **530 / 1033** while Doc is asleep is expected lid-close — not a CORS regression. e2e only when **GET /health** is **200**. **Permanent #80** (overnight sleep / tunnel flaps — **not** temporary outage banners): Membership/Contact Soft-530 Discord honesty intro + `waitlist.js?v=3` fail-soft stay after CLEAR. Soft-530 **CLEAR** recovery smoke ([soft-530-clear-smoke.md](soft-530-clear-smoke.md)) ≠ Garage GO to strip that copy. **CLEAR ≠ honesty-off.** Tone tweak only with Ben GO. Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) Soft-530 Discord · STATUS Waitlist Live. |
| P1-6 | **Membership secondary CTA: “Already a member? Sign in”** | deferred | After Member cutover — **Ben GO** (not now; not done). Link **only** apex `https://projectcar.ca/member/login`. Never www, never ops/app. Waitlist stays the interest path. No book-now / pricing / shop-open claims. Do **not** implement brochure HTML from a docs PR. P1-1 / P1-3 already shipped (#52); this item still waits on cutover. Plan: `member-host-cutover.md`, STATUS Next #1. |

---

## P2 — SEO / share / tech hygiene

| ID | Item | Status | Notes / acceptance |
|----|------|--------|-------------------|
| P2-1 | **Absolute Open Graph URLs** | done | 2026-09-07 live verify — Garage PR #55 → main `453e44d` already on Worker. Absolute `og:url` / `og:image` + `twitter:card` (`og:type` / `twitter:image` too) at `https://projectcar.ca/...` on all brochure HTML. Zone Direct Upload Worker `projectcar-brochure` **LIVE**; Zone smoke PASS. Dedicated ~1200×630 share crop still later. |
| P2-2 | **Canonical links per page** | done | 2026-09-07 live verify — Garage PR #56 → main `ed41d23` already on Worker (Zone Direct Upload ~15:48 America/Regina). `<link rel="canonical" href="https://projectcar.ca/...">` on every public HTML page (Home `/`, about, the-shop, membership, roadmap, contact, 404). Apex host only (not www). Paths match P2-1 `og:url`. Zone smoke PASS: `rel=canonical` matches `og:url` apex on Home + Membership; www serves apex canonicals. |
| P2-3 | **Asset / CSS cache version single source** | done | 2026-09-07 live verify — Garage PR #57 → main `be60a01` already on Worker (Zone Direct Upload ~16:01 America/Regina). All public HTML uses `styles.css?v=34` and `banner-logo.png?v=30`. Shared `shop-config.js?v=1` + `waitlist.js?v=2` already matched. Page-only images left as-is. Smoke PASS: Home/About/Contact at minimum; apex+www. HTML no-cache / CF header config is **LIVE** (P2-4: `brochure-security-headers.md`). |
| P2-4 | **Security headers at origin** | done | 2026-09-07 live verify — Zone Transform Rules on Worker `projectcar-brochure` (apex+www): **`brochure-security-headers`** (`X-Content-Type-Options: nosniff`; `Referrer-Policy: strict-origin-when-cross-origin`; `X-Frame-Options: DENY`; `Permissions-Policy` static defaults); **`brochure-html-no-cache`** (`/` + `*.html`: `Cache-Control: no-cache, must-revalidate`); **`brochure-asset-immutable`** (`?v=` `.css`/`.js`/`.png`: `Cache-Control: public, max-age=31536000, immutable`). Smoke PASS (Worker **200**s, not challenge 403): `/` + www 200 + required security + HTML no-cache; `styles.css?v=34` 200 + security + immutable long-cache; `robots.txt` 200 `text/plain` + security + `max-age=0`. DNS/tunnels untouched. Record: `brochure-security-headers.md`. Do **not** re-apply from a docs PR. |
| P2-5 | **Image weight / modern formats** | deferred | shop ~444K, mcking ~312K OK; WebP/AVIF + srcset when polishing mobile. |
| P2-6 | **JSON-LD LocalBusiness/Organization** | deferred | Only when real public location/hours exist—no invented NAP. |
| P2-7 | **Put site tree under git** | done | 2026-09-06 — `apps/website/` is the git SSOT (imported from Doc live tree). |

---

## P3 — Copy (page lanes)

Follow skill **project-car-web-copy**. Tighten; don’t rewrite the story. No fake prices/dates.

| ID | Item | Status | Notes / acceptance |
|----|------|--------|-------------------|
| P3-1 | **Home: pitch + CTA, not full feature dump** | done | 2026-09-07 live verify — Garage PR #61 → main `88c9820`. Home is pitch + `cta-hero` Join the waitlist + Project Underway + Shop/Membership pointers; no feature-bullet dump. Zone Direct Upload Worker `projectcar-brochure` **LIVE** from that tip (~16:50 America/Regina); smoke PASS. P3-2 / P3-3 About stay held/deferred. P3-4 is LIVE (#65). P3-5 is LIVE (#62). |
| P3-2 | **About value cards shorter** | deferred | About stays **held**. Not this slice. Target ~one short sentence per card; keep structure (problem → who → pieces → experience → heading → not promising → CTA). |
| P3-3 | **About “Parts” / “Member app” destinations** | deferred | About stays **held**. Not this slice. Listed without pages. Add honest stubs (“not live”) or stop presenting as navigable destinations. |
| P3-4 | **Shop vs Home bullet overlap** | done | 2026-09-07 live verify — Garage PR #65 → main `f191ad5`. Shop emphasizes facility/standards (bays/hoists, shared fab, safety/cleanliness, structured shop) + honesty “intended shop capability — not live yet”; no 24/7 packaging on Shop. Membership still “24/7 shop access (when operational)”. Home stays P3-1 pitch + `cta-hero` waitlist + Project Underway; no capability-bullet dump. Zone Direct Upload Worker `projectcar-brochure` **LIVE** from that tip (~18:46 America/Regina); smoke PASS apex+www (`styles.css?v=34`; P2-4 headers still present). P3-2 / P3-3 About stay held/deferred. |
| P3-5 | **Tense consistency for “24/7”** | done | 2026-09-07 live verify — Garage PR #62 → main `9d83bfc`. Shop/Membership/Roadmap use “24/7 shop access (when operational)”. Home (and About/Contact) have no absolute 24/7. Zone Direct Upload Worker `projectcar-brochure` **LIVE** from that tip; smoke PASS. P3-2 / P3-3 About stay held/deferred. P3-4 is LIVE (#65). |

---

## P4 — Growth / later

| ID | Item | Status | Notes / acceptance |
|----|------|--------|-------------------|
| P4-1 | **Real progress photography** | deferred | One honest build-out photo beats another generated hero for trust. |
| P4-2 | **Discord as equal CTA on Home/Membership** | done | 2026-09-07 live verify — Garage PR #67 → main `2b772ff`. Home equal `cta-hero` Join the waitlist + Join Discord (`discord.gg/projectcar`); contact email · Discord. Membership: waitlist form unchanged; Discord contact-primary + alongside-waitlist copy. sitemap lastmod bumps (2026-09-08 present). Zone Direct Upload Worker `projectcar-brochure` **LIVE** from that tip (~19:15 America/Regina); smoke PASS apex+www (`styles.css?v=35` at that upload; later Soft-530 **#80** is `styles.css?v=36` + `waitlist.js?v=3`). Pretty URLs are the Zone Redirect pack (Option A URL SSOT — **FULL 10/10** capacity lock) — not Worker `_redirects` `/shop` 302. P2-4 headers still; P3-4 Shop facility bullets still. P3-2 / P3-3 About stay held/deferred. P1-6 stays deferred. |
| P4-3 | **YouTube / Skool links** | deferred | Post-MVP documenting the business build—only when channel/community URLs exist. |
| P4-4 | **Analytics** | deferred | Privacy-friendly (e.g. CF Web Analytics) to see which CTA works. |
| P4-5 | **Synthetic monitors** | done | **Partial / resumed.** Lookout **`projectcar-api-health-watch`** **resumed** (`enabled:true`) 2026-09-11 ~06:52 America/Edmonton (Lookout confirmed; live `/health` **200**). **`enabled:true` ≠ firing.** Living ~09:14 MT 2026-09-15: api stalled ~**56m** since ~08:21 — same class as vault. Remaining **OK-ish** is **not** living contrast. **Lookout stall ≠ vault HTTP flip** — vault **or** api can stay `enabled:true` while cron stalls. **Re-arm success ≠ first fire** — stickiness ≥3 consecutive `*/5` windows; Lead **may GO** in-place re-arm of **both** if the day's 1× is unused; chronic ≤1 pause/resume per calendar-day (clock resets **America/Edmonton midnight**). Living 2026-09-15: **both chronic HOLD today** — quiet-ops “Lookout continues” is **false for flip detection**; interim Soft-530+vault SSOT smoke = Chief `*/20` + Lead probes (accept **≤~20m**); Chronic Lookout ≠ quiet-ops failure ≠ Ben ping ([lookout-rearm-sop.md](lookout-rearm-sop.md) · [soft-530-extended-open.md](soft-530-extended-open.md)). **#78 lookout-resume** is **LIVE-SUPERSEDED** (api-health already `enabled:true`). Soft-530 companion watches stay **HOLD / not armed** until **Ben reopens** (Ben skipped — do **not** re-ask; **not** weekend-only — [soft-530-extended-open.md](soft-530-extended-open.md)). Weekend flip coverage (plan-improve off Sat/Sun) = this api `/health` + vault `/alive` **only**. While `brochure-option-a-redirect-watch` is **GONE**, weekend **and** Mon–Thu overnight Option A Dynamic redirect is **UNCOVERED — accept that risk**. Do **not** invent overnight/weekend plan-improve, Zone, or Garage coverage. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **HOLD** a second Ben recreate ask. **Mon–Thu nights** after last weekday `*/20` (~17:40 MT) until next weekday first fire (~06:00) = same Lookout-only set ([soft-530-weekday-overnight.md](soft-530-weekday-overnight.md)); Friday last `*/20` → weekend coverage (`84107e7`). Lookout api+vault **continue** during quiet-ops **except** calendar-day chronic HOLD — then “Lookout continues” is **false for flip detection** (brochure-redirect **paper** continues; continuous watch **GONE**). Lookout owns the watch again (Lead interim probe ended). Soft-530 fail-soft still valuable for future lid-close (502/530). Brochure Option A is the only public **LIVE** surface while Soft-530 is OPEN. Continuous Lookout watch is **GONE** (deleted to fix stalled cron; Auto-review blocked recreate) — **HOLD** after one Ben ask; weekday Chief `*/20` **+ Soft-530 UX**; **Redirect-only PASS ≠ Soft-530 UX PASS**; Soft-530 UX smoke URL SSOT = **root** `/styles.css?v=36` + `/waitlist.js?v=3` (**not** `/assets/` — those 404s are **not** a regression); SOP [lookout-rearm-sop.md](lookout-rearm-sop.md); spec [brochure-redirect-watch.md](brochure-redirect-watch.md). **Never delete** an armed watch to fix cron. Flip Chief+Lead only when armed; **not** companions. Homepage-only 200 is insufficient. Pretty-URL **301**s alone are **not** Soft-530 UX. **Apex dropped** (sidecar deferred; do not monitor `/api/apex/health`). Discord invite check stays optional/deferred. Do not invent extra shipped monitors beyond that paper. Matches STATUS Live Lookout + weekend Soft-530 notes. |

---

## Suggested implementation order

1. P3-2 / P3-3 About — **held / deferred**  
2. P4 remaining as interest grows (P4-2 **done**)  
3. P1-6 only **after Member cutover GO** — still deferred; no brochure HTML from a docs PR.

Brochure hygiene is **already live** on Worker `projectcar-brochure` (re-uploaded 2026-09-06). P1-1 / P1-3 are **LIVE** on that Worker (PR #52 / `5361212`; 2026-09-07 smoke PASS). P2-1 is **LIVE** on that Worker (PR #55 / `453e44d`; Zone smoke PASS). P2-2 is **LIVE** on that Worker (PR #56 / `ed41d23`; Zone Direct Upload ~2026-09-07 15:48 America/Regina; Zone smoke PASS). P2-3 is **LIVE** on that Worker (PR **#57** / `be60a01`; Zone Direct Upload ~2026-09-07 16:01 America/Regina; smoke PASS: `styles.css?v=34` + `banner-logo.png?v=30` on public HTML — Home/About/Contact at minimum; apex+www). P2-4 is **LIVE** on that Worker (2026-09-07 smoke PASS — Transform Rules `brochure-security-headers` / `brochure-html-no-cache` / `brochure-asset-immutable`; apex+www; Worker **200**s). P3-1 is **LIVE** on that Worker (PR **#61** / `88c9820`; Zone Direct Upload ~16:50 America/Regina; smoke PASS: Home pitch + `cta-hero` Join the waitlist + Project Underway + Shop/Membership pointers; no feature-bullet dump). P3-5 is **LIVE** on that Worker (PR **#62** / `9d83bfc`; Zone Direct Upload; smoke PASS: Shop/Membership/Roadmap use “24/7 shop access (when operational)”; Home (and About/Contact) have no absolute 24/7). P3-4 is **LIVE** on that Worker (PR **#65** / `f191ad5`; Zone Direct Upload ~18:46 America/Regina; smoke PASS apex+www: Shop 200 facility bullets (bays/hoists, shared fab, safety/cleanliness, structured shop) + honesty “intended shop capability — not live yet”; no 24/7 packaging on Shop; Membership still “24/7 shop access (when operational)”; Home still P3-1 pitch + `cta-hero` waitlist + Project Underway; no capability-bullet dump; `styles.css?v=34`; P2-4 headers still present). P4-2 is **LIVE** on that Worker (PR **#67** / `2b772ff`; Zone Direct Upload ~19:15 America/Regina; smoke PASS apex+www: Home equal `cta-hero` Join the waitlist + Join Discord (`discord.gg/projectcar`); contact email · Discord; Membership waitlist form unchanged; Discord contact-primary + alongside-waitlist copy; sitemap lastmod bumps (2026-09-08 present); `styles.css?v=35` at that upload; P2-4 headers still; P3-4 Shop facility bullets still). **Live URL SSOT (Option A):** Zone Redirect pack is **FULL 10/10** Active Dynamic **301**s (apex+www) — capacity lock; no new Dynamic rules for brochure pretty-URLs. Owns pretty URLs, including brochure-shop-html-to-the-shop (`/shop.html`→`/the-shop.html`). Garage must **not** invent new brochure extensionless pretty-URLs until **#82** Worker-live + Bulk Phase1 (Redirect A parked: `membership` / `about` / `the-shop` / `contact` / `roadmap` ± slash → `*.html`; `root` / `index` / `shop` / `chat` stay Dynamic until migrate). Worker `_redirects` is **thin** (chat → contact.html only — no `/` or `/shop`). Soft-530 **#80** Worker-live: `waitlist.js?v=3` + `styles.css?v=36`. Home canonical/og/sitemap `/index.html` waits on **#82** — **not yet Worker-live**. Ops + Member booking are live on Doc demo **and** the temporary `app.` alias. Intended management host is **`ops.`**. Mission Control cockpit still needs **Ben GO**. The shop is not open.

Waitlist (P1-5), Chat strip (P1-2), Home CTA weight (P1-1), Contact channel honesty (P1-3), absolute OG (P2-1), canonicals (P2-2), cache `?v=` SSOT (P2-3), security + cache-split headers (P2-4), Home pitch + CTA (P3-1), 24/7 tense (P3-5), Shop vs Home split (P3-4), Discord equal CTA (P4-2), git home (P2-7), and P0-3…P0-7 are **done** (git + live Worker). P3-2 / P3-3 About stay **held / deferred**. P1-6 stays **deferred**. Apex (P0-1 / P0-2) is **deferred** — do not start the queue there.  

---

## Quick verification commands (Doc)

```bash
# Local from apps/website (python http.server 8088 --directory html)
# P2-2: one apex canonical per public HTML page (paths match og:url)
rg -n 'rel="canonical"' html/*.html
# P2-3: one styles.css?v= and one banner-logo.png?v= across public HTML
rg -n 'styles\.css\?v=' html/*.html
rg -n 'banner-logo\.png\?v=' html/*.html
# leftover mismatched cache versions must be empty
rg -n 'styles\.css\?v=(32|33|34)' html/*.html || true
rg -n 'banner-logo\.png\?v=(28|29)' html/*.html || true
curl -sSI http://127.0.0.1:8088/robots.txt | head -8
curl -sS http://127.0.0.1:8088/robots.txt
curl -sSI http://127.0.0.1:8088/sitemap.xml | head -8
curl -sS -o /dev/null -w '%{http_code} %{content_type}\n' http://127.0.0.1:8088/favicon.ico
# nginx (optional): unknown path must be HTTP 404 + 404.html, not Home
curl -sS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8088/this-should-404

# Live (after next Worker Direct Upload). HTML may 403 under CF challenge from datacenters.
curl -sS -o /dev/null -w '%{http_code}\n' https://projectcar.ca
curl -sSI https://projectcar.ca/robots.txt | head -5
curl -sS https://projectcar.ca/robots.txt | head -20
curl -sSI https://projectcar.ca/sitemap.xml | head -5
curl -sS -o /dev/null -w '%{http_code}\n' https://projectcar.ca/this-should-404
# P2-4: Worker 200 headers only (challenge 403 is WAF). Record: brochure-security-headers.md
curl -sSI https://projectcar.ca/
curl -sSI https://www.projectcar.ca/
curl -sSI "https://projectcar.ca/styles.css?v=35"
curl -sSI https://projectcar.ca/robots.txt

# Apex — deferred (not an active P0). Skip unless chat is back in scope.
# curl -sS http://127.0.0.1:8088/api/apex/health
# curl -sS -o /dev/null -w '%{http_code}\n' https://projectcar.ca/api/apex/health

# Waitlist API (Lead owns uvicorn; Zone owns tunnel — see api-stay-up.md / cors-origins.md)
curl -sS -o /dev/null -w '%{http_code}\n' https://api.projectcar.ca/health

# Discord
curl -sS -o /dev/null -w '%{http_code} %{url_effective}\n' -L https://discord.gg/projectcar
```

---

## Explicit non-goals (for now)

- Shipping Member booking onto projectcar.ca **in a brochure / this-docs PR** — that host migration is **Next**, not live  
- Live pricing tables or fake open dates  
- Reintroducing n8n or bolting Mission Control onto projectcar.ca  
- Pixar/Disney McQueen IP in branding assets  
- Treating **brochure** Chat as shipped product / reviving Apex (Shop OS Chat v1 is LIVE on ops/app; that is not this site)  
- Cutting the `app.` alias from a docs PR — `ops.` is already LIVE; alias stays until Ben cuts that DNS  
- Inventing new brochure extensionless pretty-URLs that need another Dynamic Redirect Rule while the Option A pack is **FULL 10/10** (wait for **#82** Worker-live + Bulk Phase1; Redirect A parked). **#83** is CI-only after **#82** base — **not** Bulk-gated. **Never #81.**  

---

## Changelog

| Date | Change |
|------|--------|
| 2026-09-15 | **Zone `vault.projectcar.ca` tunnel-retarget checklist** on held **#70**. Appended to [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) hop **4** (same file — **not** a second Zone appendix). Paper only until **Ben GO**. Steps: preflight Doc identity `95a229f5-9296-4a18-aa98-70fd300dabdf` + CDM/Amphetamine + VW(+NC) local (Soft-530 shop tunnels stay on Doc — do **not** retarget `api.`/`app.`/`ops.`/`cloud.`); inventory live vault origin (McKing/`lightning` expected while **OPEN 502**); Doc `cloudflared`/ingress for vault hostname ready before cut; Zone cutover (Zone owns CF; **never auto**); smoke `/alive` **200** + `/api/config` VW **2026.6.0** class + classify **502→200** vs **1033→200**; rollback `vault.` to prior McKing origin if Doc smoke fails (shop hostnames untouched). Anti-goals: never `Mac.lan` as Doc/vault; never wake McKing docker as camp vault path; never retarget shop Soft-530 hostnames; Bitwarden still vault CLEAR + Ben GO; Soft-530 **OPEN ≠** block this paper; **no live Zone execute until Ben GO**. Stamp honesty: `Mac.lan` flap **reappeared** vs ~12:45 (after ~12:24 drop); HTTP unchanged — **not a Ben surface**. Pointers: STATUS / README / deployment-guide / [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md) / [overnight-baseline-stamp.md](overnight-baseline-stamp.md). Paper only. Tip fold on held Docs **#70**. |
| 2026-09-15 | **Reinforce home-vs-camp gaps** on held **#70**. `0a32a83` / `fb86f90` / `1152131` already cover home-off expected + camp Doc+CDM cutover. Fold only missing locks on [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md) + STATUS one-liner: away weeks **supersede** standing McKing=`vault.`→`:8222`; McKing 24/7 + WOL/remote LUKS **deferred**; Pi/VPS still better long-term if vault must stay up while Doc is home-off (paper — **not** a Pi GO); camp Amphetamine/CDM named. Stamp honesty: ListMachines `Mac.lan` dropped vs ~12:24; only `lightning` CONNECTED; Docs still **ABSENT**; Soft-530 **1033** / vault **502** HTTP unchanged — **no Ben surface**. Overnight living stamp refreshed. Anti-goals: not Zone/Garage/Hatch execute, not Ben Doc-wake / docker / WOL re-ask, not invent CLEAR / Pi GO. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-15 | **Supersede dual-host-outage for camp** on held **#70**. [dual-host-outage.md](dual-host-outage.md) Doc-then-McKing is **home/lab/pre-cutover only** (vault still on McKing). After camp Ben GO ([camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) · [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md)), Doc is the **sole** public Soft-530+vault(+NC) origin; McKing sleep is intentional — **`lightning` ABSENT must not trigger dual-outage McKing wake**. Zone `vault.projectcar.ca` retarget = hop **4** on the camp card — **Ben GO only** (one-liner, not a new Zone appendix). STATUS / README / deployment-guide pointers that still implied Doc-then-McKing at camp aligned. Living: Soft-530 **OPEN** 530/1033; vault **OPEN** 502; Docs **ABSENT** (home EXPECTED); Option A **PASS**. Anti-goals: not Zone/Garage/Hatch execute, not live tunnel retarget, not Ben docker / Doc-wake / **#82** re-nag, never `Mac.lan` as Doc/McKing. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-15 | **Supersede host SSOT** on held **#70**. [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) is **camp SSOT** (Doc hosts Soft-530 shop + vault + NC; McKing may sleep). [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md) + McKing-centric [vault-stay-up.md](vault-stay-up.md) are **PARKED for camp weeks** / **desk/lab optional** — Zone/Doc do **not** follow those cards at camp GO. Soft-530 independence stays **home/lab only** (`0a32a83` + `fb86f90`). STATUS / README / deployment-guide / dual-tunnel pointers updated. Living: Soft-530 **OPEN** 530/1033; vault **OPEN** 502; Docs **ABSENT** (home EXPECTED); Option A **PASS**. Anti-goals: not Zone/Garage/Hatch execute, not live tunnel retarget, not Ben docker / Doc-wake / **#82** re-nag, never `Mac.lan` as Doc/McKing. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-15 | **Home vs camp Doc posture** on held **#70**. Complement to `fb86f90` camp-vault-on-doc-cutover — **not** a re-cutover. Ben: Doc is intentionally **off at home** (Soft-530 **OPEN** expected) and **on+CDM at camp** (after vault cutover, Soft-530+vault expected CLEAR on one Doc origin). **HOME:** Docs **ABSENT** + Soft-530 **OPEN** is **EXPECTED**; quiet-ops **HOLD**; do **not** re-nag lid-restore / KeepAlive; vault may be McKing CLEAR or OPEN independently. **CAMP** (after Ben GO): dual OPEN = coupled Doc CDM/KeepAlive/cloudflared first-hop — **not** McKing docker, **not** independent mcking-reappear. Overnight stamp / morning `*/20`: home Soft-530 is **SAME-CLASS expected**. Lookout `ok=false` while Docs **ABSENT** at home is **expected baseline**, not stickiness panic — do **not** re-arm companions. Soft-530 **OPEN ≠ #82 blocked**. Never `Mac.lan` as Doc. Never treat home Soft-530 as camp cutover failure. Never auto Ben nag on home Soft-530. Primary: [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md). Pointers: [overnight-baseline-stamp.md](overnight-baseline-stamp.md) · [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) · [soft-530-extended-open.md](soft-530-extended-open.md) · [doc-reappear-first-hop.md](doc-reappear-first-hop.md). Living: Soft-530 **1033**; vault **502**; Docs **ABSENT** (home-off); Option A **LIVE**; camp cutover paper ready, pending Ben GO. Anti-goals: not Zone/Garage/Hatch execute, not Ben Doc-wake / docker re-ask, not invent CLEAR. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-15 | **Camp vault on Doc cutover** on held **#70**. Ben locked camp posture — Docs Mac plugged for NC + VW so McKing need not be 24/7. Primary: [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md). Dual-tunnel **camp**: Doc hosts shop + vault (+ NC); McKing may sleep. Soft-530 independence is **home/lab only**; at camp they couple on Doc lid/CDM/KeepAlive. Recovery = Doc VW + Zone `vault.` retarget — **not** McKing docker. **Ben GO**, never auto-cutover. Smoke: `/alive` **200** + `/api/config` VW class. Bitwarden still vault CLEAR + Ben GO. Anti-goals: never `Mac.lan` as Doc/vault; no dual origins; don’t leave `vault.` on dead McKing docker while Doc VW is live; don’t treat McKing docker ask as camp vault path. [post-dual-clear-go.md](post-dual-clear-go.md) does **not** assume McKing vault wake first. Pointers: [vault-stay-up.md](vault-stay-up.md) · [dual-host-outage.md](dual-host-outage.md) · [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) · [home-lab-specification.md](home-lab-specification.md). Living: Soft-530 **OPEN** 530/1033; vault **OPEN** 502; **Mac.lan + lightning** / Docs **ABSENT**; Option A **PASS**; Soft-530 UX **LIVE**; **#82** Ben GO only. Anti-goals: not Zone/Garage/Hatch execute, not Ben Doc-wake / docker re-ask, not invent CLEAR. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-15 | **Doc reappear multi-day cold-boot class** on held **#70**. When Docs **ABSENT ≥1 calendar day** (or Soft-530 **OPEN** 1033 **≥24h** with Docs **ABSENT**), reappear is **cold-boot** — **before** [soft-530-clear-smoke.md](soft-530-clear-smoke.md): (1) assert machineId `95a229f5-9296-4a18-aa98-70fd300dabdf`; (2) verify cloudflared LaunchAgent/service up; (3) verify shop-api + shop-web KeepAlive loaded; (4) note CDM/caffeinate posture — then smoke → forensics → [post-dual-clear-go.md](post-dual-clear-go.md). Overnight **warm wake** (`<24h` ABSENT after a prior CLEAR) stays the shorter process-wake. **Never** treat a multi-day lid as overnight sleep. Living example: Docs **ABSENT** since Fri 2026-09-11 ~16:48 (**~4d**). **Never** `Mac.lan` as Doc. **Never auto-fire.** Primary: [doc-reappear-first-hop.md](doc-reappear-first-hop.md). Cross-link [soft-530-clear-smoke.md](soft-530-clear-smoke.md) / [doc-lid-restore.md](doc-lid-restore.md) / [dual-host-outage.md](dual-host-outage.md) / STATUS. Living: Soft-530 **OPEN** 530/1033; vault **OPEN** 502; **Mac.lan + lightning** / Docs **ABSENT**; Option A **PASS**; Soft-530 UX **LIVE**; Lookout **both chronic HOLD today**; **#82** Ben GO only. Anti-goals: not Zone/Garage execute, not Ben Doc-wake / docker / recreate re-ask, not invent CLEAR. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-15 | **Quiet-ops “Lookout continues” chronic-HOLD exception** on held **#70**. Living quiet-ops (`1e6a0b1`) says Soft-530 extended OPEN → Lookout continues. Live 2026-09-15: both api + vault health watches are **calendar-day chronic HOLD** (`enabled:true` but cron-dead after today's **1×** re-arm budget). In that state “Lookout continues” is **false for flip detection**. Paper: (1) interim Soft-530+vault SSOT smoke = Chief `*/20` + Lead coding-progress probes (accept **≤~20m** flip latency); (2) chronic HOLD does **not** reopen a Ben recreate ask; (3) **1×** pause/resume per calendar-day clock resets **America/Edmonton midnight** — next local day Lead **may** try one in-place re-arm again, then stickiness **≥3** `*/5` before healthy; (4) Soft-530 companions + brochure recreate stay **HOLD**. **Honesty:** Chronic Lookout ≠ quiet-ops failure ≠ Ben ping. Primary: [soft-530-extended-open.md](soft-530-extended-open.md). Cross-link [lookout-rearm-sop.md](lookout-rearm-sop.md) + [overnight-baseline-stamp.md](overnight-baseline-stamp.md). Living: Soft-530 **OPEN** 530/1033; vault **OPEN** 502; **Mac.lan + lightning** / Docs **ABSENT**; Option A **PASS**; Soft-530 UX **LIVE**; Lookout **both chronic HOLD today**; **#82** Ben GO only. Anti-goals: not Zone/Garage execute, not Ben recreate/docker/Doc-wake re-ask, not treat chronic HOLD as quiet-ops failure. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-15 | **Lookout re-arm success ≠ first fire; require stickiness** on held **#70**. After in-place re-arm, success = `lastRun` advances across **≥3 consecutive** `*/5` windows (≥3 fires spanning **≥10–15m**). Living ~09:14 MT 2026-09-15: vault re-arm ~08:50 → first fire ~08:55 OK → stall again (~**22m**); api watch stalled ~**56m** since ~08:21 — same class (`enabled:true` while cron-dead). **Chronic:** stall again within 24h → do **not** loop pause/resume more than **1×/calendar-day**; Lead paper only; interim = Chief `*/20` + Lead coding-progress probes; **HOLD** Ben recreate. Lead **may GO** in-place re-arm of both `projectcar-vault-health-watch` and `projectcar-api-health-watch` when stalled, then verify stickiness before healthy. Cross-link [soft-530-extended-open.md](soft-530-extended-open.md) lock 3. SOP: [lookout-rearm-sop.md](lookout-rearm-sop.md) row 8. Living: Soft-530 **OPEN** 530/1033; vault **OPEN** 502; **Mac.lan + lightning** / Docs **ABSENT**; Option A **PASS**; Soft-530 UX **LIVE**; **#82** Ben GO only. Anti-goals: not Zone/Garage execute, not Ben recreate/docker/Doc-wake re-ask, not call healthy on first fire, not loop pause/resume on chronic. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-15 | **Lookout stall ≠ vault HTTP flip** on held **#70**. `projectcar-vault-health-watch` can be `enabled:true` but cron-stalled (example ~**104m**, `lastRun`/baseline stuck ~**07:05** on a `*/5`) — a **separate coverage class** from vault OPEN **502**. Apply [lookout-rearm-sop.md](lookout-rearm-sop.md): Lead → Lookout **in-place** re-arm (pause/resume or re-save) — **never delete** to fix; **HOLD** any Ben recreate ask (quiet-ops + brochure Auto-review lesson). Until vault watch re-arms, interim = Chief `*/20` smoke + Lead coding-progress probes — **not** a Ben ping. Soft-530 api watch remaining **OK-ish** is contrast only. Pointer: [vault-stay-up.md](vault-stay-up.md). Vault CLEAR docker.service permanence already `@367172d` — **do not redo**. Living: Soft-530 **OPEN** 530/1033; vault **OPEN** 502; **Mac.lan + lightning** / Docs **ABSENT**; Option A **PASS**; Soft-530 UX **LIVE**; **#82** Ben GO only. Anti-goals: not Zone/Garage execute, not Ben recreate/docker/Doc-wake re-ask, not collapse stall into 502. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-15 | **Vault CLEAR must assert `docker.service` permanence, not only HTTP** on held **#70**. After Ben enables docker and compose is up: `/alive` **200** + `/api/config` VW **2026.6.0** class as today; on `lightning` `systemctl is-enabled docker` == **enabled** **and** `is-active` == **active** (optional: `mcking` in `docker` group). `/alive` **200** + docker **disabled/inactive** = **NOT CLEAR / recurrence risk** — reboot re-OPENs vault the same way as ~19:45 Sep 13 (disabled + SIGKILL stop). Cross-link [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md). Bitwarden still vault CLEAR + Ben GO; Soft-530 independent. Living: Soft-530 **OPEN** 530/1033; vault **OPEN** 502; **Mac.lan + lightning** / Docs **ABSENT**; Option A **PASS**; Soft-530 UX **LIVE**; **#82** still Ben GO. Anti-goals: not **#82** upload, not companion re-ask, not Ben docker / Doc-wake re-nag, not Bitwarden auto. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-15 | **Overnight stamp first-hop absorb** on held **#70**. When a ListMachines (or other) class DIFF is handled by first-hop between last weekday `*/20` and morning first `*/20`, morning `*/20` must: (1) **not** re-surface that DIFF to Ben if first-hop completed + Ben already asked once + Soft-530 / vault HTTP class unchanged; (2) **refresh** the living stamp so later same-day `*/20` is same-class quiet (`611c267`); (3) optional short Lead-only ack (stamp refresh + first-hop pointer). Cross-link [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) / [doc-reappear-first-hop.md](doc-reappear-first-hop.md). Living: `lightning` DIFF ~19:06 / ~19:10 absorbed; morning stamp = **Mac.lan + lightning** / vault still **OPEN 502** (docker dead) / Soft-530 still **OPEN 1033** / Docs **ABSENT**; McKing hop already ran ~19:10 — still blocked on Ben sudo — **no morning re-ask**. Live snapshot: Option A Redirect **PASS**; Soft-530 UX **LIVE**; **#82** still Ben GO; quiet-ops **HOLD**. Anti-goals: not **#82** upload, not companion re-ask, not second Ben recreate / docker / Doc-wake re-nag, not invent overnight smoke. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | **Overnight stamp freeze/`BUILD_ID`/`changeme` = `last_known` from last Soft-530 CLEAR only** on held **#70**. While Soft-530 is **OPEN** (530/1033), `app.`+`ops.` `/login` cannot be live-probed — morning first `*/20` must **NOT** treat “couldn’t verify freeze tip / changeme / BUILD_ID” as a stamp class DIFF. Freeze live-verify is deferred to [soft-530-clear-smoke.md](soft-530-clear-smoke.md). Stamp still records last_known tip hashes (`4cf8924` / `5swmVz` / `changeme`) for continuity. True-last `*/20` **~17:42** MT Mon 2026-09-14 was **SAME-CLASS** vs the ~17:25 paper stamp — **do NOT refresh**. Living same-class: Soft-530 **OPEN** 530/1033; vault **OPEN** 502; **Mac.lan only**; Option A Redirect+UX **PASS**; Lookout api+vault **armed**; brochure watch **GONE**. Anti-goals: not **#82** upload, not companion re-ask, not invent overnight smoke, not second Ben recreate ask, not Ben re-nag. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | Extend `overnight-baseline-stamp.md` on held **#70** (`56d5d99`). Morning first `*/20` (~06:00) surfaces **Lead/Ben only on stamp class DIFF** (Soft-530 / vault / ListMachines / Option A / freeze / Lookout). **Same-class = stay quiet.** A true last ~17:40 fire **refreshes the stamp only if any field diffs**; else the prior stamp **stands**. Living example (paper / STATUS pointer — **not** a live ops action) Chief stamp America/Edmonton **2026-09-14 ~17:25** for tomorrow first `*/20` ~06:00: Soft-530 **OPEN** `api.`/`app.`/`ops.`/`cloud.` **530** CF **1033** waitlist OPTIONS **530** since 2026-09-13T11:57 (**~29.5h**); vault **OPEN** `/alive` **502** + `/api/config` **502** since 2026-09-13T19:45 (**~21.7h**); ListMachines **Mac.lan only** (`docs=ABSENT` · `lightning=ABSENT` — lid-restore + vault wake blocked); freeze **last_known** `4cf8924` / `5swmVz` (Doc absent → unverifiable live); Option A Redirect **PASS 10/10** apex+www; Soft-530 UX **LIVE** `styles.css?v=36` + `waitlist.js?v=3` + Discord honesty; Lookout api+vault **armed**; companions **HOLD**; `brochure-option-a-redirect-watch` **GONE** (overnight Dynamic **UNCOVERED** = accept); quiet-ops **HOLD**; **#82** not auto-GO; no Ben re-nag. Last-weekday PASS ≠ overnight coverage — do **not** invent overnight redirect smoke. Anti-goals: not **#82** upload, not companion re-ask, not invent overnight redirect smoke, not second Ben recreate ask. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | Draft `overnight-baseline-stamp.md` on held **#70**. Complements [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) + overnight Option A uncovered (`c200d71`). Last weekday plan-improve `*/20` (~17:40 MT Mon–Thu; Friday stamps then weekend coverage) writes a **one-line** overnight baseline **before** the Lookout-only window: Soft-530 class+since · vault class+since · Option A Redirect+UX PASS/FAIL/UNCOVERED+timestamp · ListMachines IDs (**never** treat `Mac.lan` as Doc/McKing) · freeze tip/`BUILD_ID` if known · Lookout api+vault armed + `brochure-option-a-redirect-watch` GONE/armed. First weekday `*/20` after ~06:00 **diffs** that stamp instead of rediscovering. **No Ben re-nag from stamp alone.** No Zone / Garage execute. Overnight CLEAR still [soft-530-clear-smoke.md](soft-530-clear-smoke.md) / [vault-clear-smoke.md](vault-clear-smoke.md) + first-hop. Option A overnight Dynamic still **UNCOVERED** — accept; do **not** invent overnight redirect smoke. Anti-goals: not **#82** upload, not companion re-ask, not second Ben recreate ask. Living stamp: Soft-530 **OPEN** `api.`/`app.`/`ops.`/`cloud.` **530** CF **1033** since ~11:57 Sep 13; waitlist OPTIONS **530**; vault `/alive`+`/api/config` **502** since ~19:45; ListMachines **Mac.lan only**; freeze intact; **#82** still Ben GO; Soft-530 UX root `styles.css?v=36` + `waitlist.js?v=3` + Discord honesty LIVE. Discoverability: [deployment-guide.md](deployment-guide.md) Monday paper map + STATUS Live / Locks. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | **Option A overnight/weekend Dynamic redirect UNCOVERED** on held **#70**. While `brochure-option-a-redirect-watch` is **GONE**, overnight (Mon–Thu after last `*/20` ~17:40 MT until next weekday first fire ~06:00) **and** weekend (Sat/Sun plan-improve-off, `84107e7` stay-up rows) Option A Dynamic redirect coverage is **explicitly uncovered — accept that risk**. Do **not** invent overnight/weekend plan-improve, Zone, or Garage coverage. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **HOLD** a second Ben recreate ask (`lookout-rearm-sop` already one-ask). Cross-link: [lookout-rearm-sop.md](lookout-rearm-sop.md) · [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) · weekend Soft-530 coverage (`84107e7` / [api-stay-up.md](api-stay-up.md) · [doc-lid-restore.md](doc-lid-restore.md) · [vault-stay-up.md](vault-stay-up.md) · [shop-web-stay-up.md](shop-web-stay-up.md)). Living stamp: Soft-530 **OPEN** `api.`/`app.`/`ops.`/`cloud.` **530** CF **1033**; waitlist OPTIONS **530**; vault `/alive` **502**; ListMachines **Mac.lan only**; Soft-530 UX **LIVE**. Anti-goals: not **#82** upload, not companion re-ask, not Ben Doc-wake re-nag, not invent overnight redirect smoke. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | Draft `soft-530-weekday-overnight.md` on held **#70**. Complements weekend Soft-530 coverage (`84107e7`) + [soft-530-extended-open.md](soft-530-extended-open.md). **Mon–Thu nights only:** after last weekday plan-improve `*/20` (~17:40 MT) until next weekday first fire (~06:00), Soft-530 + vault coverage = Lookout `api.` `/health` + vault `/alive` **only** (ops/app companions HOLD). No overnight Zone Direct Upload, no Garage execute, no Ben re-nag from OPEN duration alone. Friday last `*/20` hands into weekend coverage. Mid-overnight Doc / McKing reappear still [doc-reappear-first-hop.md](doc-reappear-first-hop.md) / [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (**never** `Mac.lan` as either; **never auto-fire**). Living stamp: Soft-530 **OPEN** `api.`/`app.`/`ops.`/`cloud.` **530** CF **1033**; waitlist OPTIONS **530**; vault `/alive` **502**; ListMachines **Mac.lan only**; freeze intact; **#82** still Ben GO; Soft-530 UX assets root-relative LIVE. Discoverability: [deployment-guide.md](deployment-guide.md) Monday paper map + STATUS living-ops / Live / Locks. Anti-goals: not **#82** upload, not unfreeze, not companion re-ask, not Bitwarden, not Ben Doc-wake re-nag. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | **Option A Soft-530 UX smoke URL SSOT (root, not `/assets/`)** on held **#70**. Live HTML uses root-relative `styles.css?v=36` + `waitlist.js?v=3` (**200** apex+www). Interim Chief `*/20` + [brochure-worker-ci.md](brochure-worker-ci.md) + [brochure-pages-cutover.md](brochure-pages-cutover.md) + Lookout Option A baselines lock those **root** paths. A probe of `/assets/styles.css?v=36` or `/assets/waitlist.js?v=3` **404s** and is **NOT** a regression (`assets/` is favicons/images). UX still: `membership.html` + `contact.html` Soft-530 Discord honesty; **root** `waitlist.js?v=3` fail-soft **502 / 530 / 1033**. **Redirect-only PASS ≠ Soft-530 UX PASS.** Anti-goals: not **#82** upload, not Dynamic wipe, not honesty-off, not companions rearm, not Ben Doc-wake re-nag. Living dual-OPEN (`5f2fd1c`) **unchanged**. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | **Paper reconcile — Soft-530 OPEN ≠ #82 blocked** on held **#70**. `brochure-worker-ci.md` was framed **post–Soft-530-CLEAR**. Correct framing: **after #82 Ben GO** (Soft-530-independent). Brochure is CF Worker/Zone, not Doc tunnel. Ben may GO **#82** Worker-live (Direct Upload + **mandatory** purge + freshness) while Soft-530 and/or vault are still **OPEN**. [post-dual-clear-go.md](post-dual-clear-go.md) KeepAlive→**#82** remains the *default ordered menu after dual CLEAR*; Soft-530 **OPEN** does **not** defer **#82**. Locks kept: quiet-ops duration ≠ auto-GO; dual CLEAR ≠ auto-GO; **never #81**; Option A Dynamic **10/10** until Bulk Phase1 **LIVE**. STATUS living dual-OPEN already said “**#82** still Ben GO” — other papers now match. Anti-goals: not Zone Direct Upload, not unfreeze/#79.1 execute, not companion re-ask, not Bitwarden, not Ben Doc-wake re-nag. Pointers: [brochure-worker-ci.md](brochure-worker-ci.md) · [post-dual-clear-go.md](post-dual-clear-go.md) · STATUS Live / Locks · [deployment-guide.md](deployment-guide.md). Living dual-OPEN (`5f2fd1c`) **unchanged**: Soft-530 still **OPEN** (CF **1033**); vault still **OPEN** (**502**); ListMachines only `Mac.lan`. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | **Paper reconcile — Bitwarden gate is vault CLEAR, not Soft-530 CLEAR** on held **#70**. Commit `7277666` / [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) said Bitwarden stayed **blocked** until Soft-530 CLEAR. Correct gate: **vault CLEAR** (Ben GO after `/alive` **200** + VW **2026.6.0** smoke). Soft-530 independence: vault CLEAR ≠ Soft-530 CLEAR ≠ unfreeze / **#82** — **do not** block Bitwarden on Soft-530. Soft-530 **OPEN** can coexist with vault CLEAR + Bitwarden work. **Never auto Bitwarden.** **Never** treat vault CLEAR as Soft-530 CLEAR or unfreeze / **#82** GO. Pointers: [vault-clear-smoke.md](vault-clear-smoke.md) · [vault-stay-up.md](vault-stay-up.md) · [post-dual-clear-go.md](post-dual-clear-go.md) · STATUS Live / Locks · [deployment-guide.md](deployment-guide.md). Living dual-OPEN (`5f2fd1c`) **unchanged**: Soft-530 still **OPEN** (CF **1033**); vault still **OPEN** (**502**); ListMachines only `Mac.lan`. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | Draft `mcking-reappear-first-hop.md` on held **#70** (twin of `doc-reappear-first-hop.md`). When **`lightning` / McKing first reappears** on ListMachines after extended vault OPEN / quiet-ops: (1) assert machineId **`9067d14b-46e5-4ef0-82d5-fce0febdc8f7`** (label **lightning**) — **never** treat `Mac.lan` as McKing; (2) run [vault-clear-smoke.md](vault-clear-smoke.md) (`/alive` **200** + `/api/config` VW **2026.6.0** + classify **502→200**) after vault-stay-up on the **asserted** host; (3) **only if CLEAR** → vault post-CLEAR forensics on [vault-stay-up.md](vault-stay-up.md); (4) Soft-530 independence — Bitwarden gate is **vault CLEAR** (Ben GO after `/alive` + VW **2026.6.0** smoke), **not** Soft-530 CLEAR; Soft-530 **OPEN** can coexist with vault CLEAR + Bitwarden work; **never auto Bitwarden**; **never** treat vault CLEAR as Soft-530 CLEAR, unfreeze, or **#82** GO. Discoverability: [deployment-guide.md](deployment-guide.md) Monday paper map + STATUS Live / Locks. Cross-links: [doc-reappear-first-hop.md](doc-reappear-first-hop.md), [vault-clear-smoke.md](vault-clear-smoke.md), [vault-stay-up.md](vault-stay-up.md), [dual-host-outage.md](dual-host-outage.md), [soft-530-extended-open.md](soft-530-extended-open.md), [post-dual-clear-go.md](post-dual-clear-go.md). Living stamp: Soft-530 **OPEN** `api.`/`app.`/`ops.`/`cloud.` **530** CF **1033**; waitlist OPTIONS **530**; vault `/alive` **502**; ListMachines **Mac.lan only**; freeze intact; **#82** still Ben GO. Paper only — no Zone/Garage execute. Does **not** invent a reappear or CLEAR. Tip fold on held Docs **#70**. |
| 2026-09-14 | Draft `doc-reappear-first-hop.md` on held **#70**. When `Docs-MacBook-Pro` **first reappears** on ListMachines after extended Soft-530 OPEN / quiet-ops: (1) assert machineId **`95a229f5-9296-4a18-aa98-70fd300dabdf`** — **never** treat `Mac.lan` or Porsche as Doc; (2) run [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (`/health` **200** + OPTIONS CORS apex+www + POST **422**/**201**) after lid-restore on the **asserted** host; (3) **only if CLEAR** → Soft-530 post-CLEAR forensics on [doc-lid-restore.md](doc-lid-restore.md); (4) then [post-dual-clear-go.md](post-dual-clear-go.md) — **never auto-fire** **#82** / unfreeze / **#79.1** / companions / Zone upload / Bitwarden; (5) Chief-to-doc hop **only after** CLEAR smoke. Vault CLEAR stays independent (Chief `lightning` / McKing). Anti-goals: `Mac.lan` Shell ≠ Doc evidence; duration alone ≠ Ben wake; **CLEAR ≠ honesty-off**; not Lookout companion re-ask. Discoverability: [deployment-guide.md](deployment-guide.md) Monday paper map + STATUS Live / Locks. Cross-links: [soft-530-extended-open.md](soft-530-extended-open.md), [soft-530-clear-smoke.md](soft-530-clear-smoke.md), [dual-host-outage.md](dual-host-outage.md), [post-dual-clear-go.md](post-dual-clear-go.md), [doc-lid-restore.md](doc-lid-restore.md). Living dual-OPEN (`5f2fd1c`) **unchanged**: Soft-530 still **OPEN** (CF **1033**); vault still **OPEN** (**502**); ListMachines only `Mac.lan`. Paper only — no Zone/Garage execute. Does **not** invent a reappear or CLEAR. Tip fold on held Docs **#70**. |
| 2026-09-14 | **Frozen Option A Redirect inventory + interim Chief `*/20` Soft-530 UX + Monday paper map** on held **#70**. Pasted parked Sep 10 2026 **10/10** Redirect Rules YAML as Docs SSOT (do **not** invent rules) into [brochure-worker-ci.md](brochure-worker-ci.md) Option A matrix and [brochure-pages-cutover.md](brochure-pages-cutover.md) **hard cutover gate**. **#82 Worker-live ≠ retire Dynamic** (`49ccf90`) still holds — Pages bind / Worker-live is **not** a wipe. While Lookout redirect watch is **GONE**, interim Chief `*/20` now includes Soft-530 UX: `membership.html` + `contact.html` still show Discord honesty; `waitlist.js?v=3` still fail-softs **502 / 530 / 1033**. **Redirect-only PASS ≠ Soft-530 UX PASS.** Honesty permanence (`0705b37`) unchanged. Discoverability: [deployment-guide.md](deployment-guide.md) **Soft-530 living paper index** lists Monday **#70** runbooks with when-to-open (vault-stay-up · dual-host-outage · extended-open · **doc-reappear-first-hop** · **mcking-reappear-first-hop** · both CLEAR smokes · lookout-rearm · brochure-redirect-watch · post-dual-clear-go · brochure-worker-ci / pages-cutover). Anti-goals: not **#82** merge nag, not Dynamic wipe, not honesty-off, not companions rearm. Cross-links: [brochure-redirect-watch.md](brochure-redirect-watch.md) · [lookout-rearm-sop.md](lookout-rearm-sop.md) · [soft-530-clear-smoke.md](soft-530-clear-smoke.md). Living dual-OPEN (`5f2fd1c`) **unchanged**. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | **#82 Worker-live ≠ retire Option A Dynamic pack** on held **#70**. After Ben GO **#82** → Zone Direct Upload + **mandatory** purge + body freshness (canonical / og / sitemap + nav home = `/index.html`, no bare `href="/"`), **PASS** does **not** authorize deleting the CF Dynamic Redirect **10/10** pack. Keep **full** Option A Dynamic (`root`/`shop` + pretty-URL + `chat`) until **Bulk Phase1 is LIVE and smoke-equal** to today’s **10/10**. Then Zone may drop pretty-URL Dynamic per parked Redirect A / Bulk Phase1; keep `root`/`shop` unless Bulk covers them too. Anti-goals: not “Worker-live so wipe Dynamic”; not migrating Dynamic on upload day; not **#81**; not unfreeze / **#79.1** / companions / Bitwarden. Aligns **#83** ∥ Bulk Phase1 after **#82**. Canonical: [post-dual-clear-go.md](post-dual-clear-go.md) brochure lane · [brochure-worker-ci.md](brochure-worker-ci.md) · STATUS Locks / Live URL SSOT. Living dual-OPEN (`5f2fd1c`) **unchanged**: Soft-530 still **OPEN** (CF **1033**); vault still **OPEN** (**502**); ListMachines only `Mac.lan`. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | **Soft-530 Discord honesty permanence** on held **#70**. Soft-530 **CLEAR** recovery smoke (`/health` **200** + waitlist OPTIONS CORS + POST **422**/**201**) is **not** a Garage GO to strip Membership/Contact Soft-530 Discord honesty intro or `waitlist.js?v=3` fail-soft (502/530/1033→Discord+mailto). That copy is **permanent #80 product behavior** for overnight sleep / tunnel flaps — **not** temporary outage banners. **CLEAR ≠ honesty-off.** Tone tweak only with Ben GO. Anti-goals: not **#82** upload, not unfreeze/#79.1, not Zone Direct Upload, not companion re-ask, not Bitwarden. Cross-links: [soft-530-clear-smoke.md](soft-530-clear-smoke.md) · [brochure-worker-ci.md](brochure-worker-ci.md) Soft-530 Discord · STATUS Waitlist Live · this file P1-5. Living dual-OPEN (`5f2fd1c`) **unchanged**: Soft-530 still **OPEN** (CF **1033**); vault still **OPEN** (**502**); ListMachines only `Mac.lan`. Paper only — no Zone/Garage execute. Does **not** invent CLEAR. Tip fold on held Docs **#70**. |
| 2026-09-14 | Draft `lookout-rearm-sop.md` on held **#70**. Living: `brochure-option-a-redirect-watch` was **deleted** to fix stalled cron; Auto-review **blocked** recreate — watch **GONE**. SOP: (1) **Never delete** an armed Project Car Lookout watch to fix stalled cron; (2) prefer in-place update / pause-resume / re-save; (3) if delete already happened and recreate needs Auto-review: **HOLD** after one Ben ask; (4) interim Option A coverage = Chief plan-improve `*/20` redirect smoke until recreate is enabled; (5) after Ben approves: assert routine enabled + first fire within **10m** + baseline still **LIVE 10/10**; (6) Soft-530 companions stay **HOLD**. Anti-goals: not **#82**, not unfreeze, not Zone execute, not companion re-ask. **#78 lookout-resume** is **LIVE-SUPERSEDED** (api-health already `enabled:true`) — do **not** collapse this into #78. Cross-links: [brochure-redirect-watch.md](brochure-redirect-watch.md), [soft-530-extended-open.md](soft-530-extended-open.md) quiet-ops, STATUS Live / Locks. Paper only — no Lookout/Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | Draft `vault-clear-smoke.md` on held **#70** (preferred twin of `soft-530-clear-smoke.md` `@502ab2e`). After vault **CLEAR** following **extended OPEN** (**502** since ~19:45 MT Sep 13), **first** recovery smoke **before** Bitwarden import/rotate or desk talk: (1) `https://vault.projectcar.ca/alive` **200**; (2) `https://vault.projectcar.ca/api/config` **200** with VW version present (expect McKing **2026.6.0** class, **not** Doc **2025.12.0**); (3) classify recovery path — this OPEN is origin-down **502→200**, **not** CF **1033→200** (Soft-530 tunnel class stays separate); (4) Soft-530 may still be **OPEN** — vault CLEAR alone ≠ Soft-530 CLEAR ≠ **#82** ≠ unfreeze ≠ companions; (5) Bitwarden import/rotate stays **Ben GO** after CLEAR, **never auto-fire**. Anti-goals: not Zone Direct Upload, not **#82**, not Doc unfreeze / **#79.1** execute, not companion re-ask, not Soft-530 CLEAR smoke (already `502ab2e`). CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire**). Capture forensics still [vault-stay-up.md](vault-stay-up.md) (`d88cacb`). Cross-links: [vault-stay-up.md](vault-stay-up.md), [soft-530-clear-smoke.md](soft-530-clear-smoke.md), [soft-530-extended-open.md](soft-530-extended-open.md), [post-dual-clear-go.md](post-dual-clear-go.md), [dual-host-outage.md](dual-host-outage.md). Living stamps **unchanged**: Soft-530 **OPEN** CF **1033** since 2026-09-13 ~11:57 America/Edmonton; vault **OPEN** **502** since ~19:45 MT Sep 13. Paper only — no Zone/Garage execute. Does **not** invent CLEAR. Tip fold on held Docs **#70**. |
| 2026-09-14 | Draft `soft-530-clear-smoke.md` on held **#70**. After Soft-530 **CLEAR** following **extended OPEN** (>~24h), **first** recovery smoke **before** Owner desk / **#79.1** / unfreeze talk: (1) `api.` `/health` **200**; (2) waitlist OPTIONS **200** + CORS Origin `https://projectcar.ca` **and** `https://www.projectcar.ca`; (3) waitlist POST validation **422** or success **201** (prove route alive, not just tunnel-up); (4) `app.`/`ops.` may still **307**→`/login` with print `` `changeme` `` — freeze (`4cf8924` / **`5swmVz`**), **not** Soft-530; (5) when Doc public is up, `cloud.` `status.php` **200**. Anti-goals: CLEAR smoke ≠ **#82** GO ≠ companion re-ask ≠ Bitwarden; Soft-530 CLEAR alone ≠ unfreeze; vault CLEAR still separate McKing lane. CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire**). Cross-links: [soft-530-extended-open.md](soft-530-extended-open.md), [doc-lid-restore.md](doc-lid-restore.md), [dual-host-outage.md](dual-host-outage.md), [post-dual-clear-go.md](post-dual-clear-go.md) **#79.1** acceptance, [cors-origins.md](cors-origins.md), STATUS living dual-OPEN. Living stamps **unchanged**: Soft-530 **OPEN** CF **1033** since 2026-09-13 ~11:57 America/Edmonton; vault **OPEN** **502** since ~19:45 MT Sep 13. Paper only — no Zone/Garage execute. Does **not** invent CLEAR. Tip fold on held Docs **#70**. |
| 2026-09-14 | Draft `soft-530-extended-open.md` on held **#70**. Soft-530 **OPEN** crossed **~24h** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton) **and** vault **OPEN** (**502** since ~19:45 MT Sep 13) **and** ListMachines **Mac.lan only** → **quiet-ops lock**: (1) no Ben re-nag (Doc-wake + McKing wake already asked); (2) Lead stays armed for `Docs-MacBook-Pro` → lid-restore; Chief stays armed for `lightning` → vault wake; (3) Lookout api+vault+brochure-redirect **continue**; companions **HOLD until Ben reopens** (not weekend-only); (4) Brochure Option A is public SSOT during extended OPEN; (5) duration alone ≠ **#82** GO ≠ unfreeze ≠ Zone Direct Upload ≠ Bitwarden ≠ companion re-ask. CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md). Later note only (not this bounce): Soft-530 CLEAR after extended OPEN should include waitlist OPTIONS CORS + POST smoke before desk / **#79** gates. Cross-links: [dual-host-outage.md](dual-host-outage.md), [vault-stay-up.md](vault-stay-up.md), [brochure-redirect-watch.md](brochure-redirect-watch.md), [post-dual-clear-go.md](post-dual-clear-go.md), STATUS living dual-OPEN. Living dual-OPEN (`5f2fd1c`) **unchanged**. Paper only — no Zone/Garage execute. Does **not** invent CLEAR. Tip fold on held Docs **#70**. |
| 2026-09-14 | Draft `brochure-redirect-watch.md` on held **#70**. While Soft-530 **OPEN**, Option A is the only public **LIVE** surface and has **no** continuous watch (only `*/20` smokes). Lookout watch **to arm** — flip **Chief + Lead only**; **not** the ops/app companion watches Ben skipped (do **not** re-ask). Asserts (apex+www): `/` **301**→`/index.html`; `/shop`+`/shop.html` **301**→`/the-shop.html`; extensionless pretty pack **301**→matching `*.html`; `styles.css?v=36` + `waitlist.js?v=3` **200**. Sep 9 `brochure-routing-baseline.json` still says extensionless **IN FLIGHT** — stamp living **LIVE 10/10**. Fold honesty: `main` `doc-lid-restore.md` Soft-530 table still says Lookout api-health **paused** vs living **`enabled:true`** (**#78 LIVE-SUPERSEDED**). Docs gap (no Ben ask / no tip-only PR): `main` STATUS Reality tip still **`2b772ff` / ?v=35 / Worker `/shop` 302** — stale vs live Option A + **#70** SSOT until **#82** (already in Reality quarantine; reinforce). Living dual-OPEN (`5f2fd1c`) **unchanged**. Anti-goals: not Zone change, not **#82** upload, not unfreeze, not companion re-ask, not Bitwarden. Paper only — no Zone/Garage execute. Does **not** arm the watch. Tip fold on held Docs **#70**. |
| 2026-09-14 | Reconcile **brochure lane after #82**: **#83** ∥ **Bulk Phase1** (not Bulk→#83) on held **#70**. `71a03b0` shorthand “Bulk Phase1 → **#83**” contradicted `brochure-worker-ci` (`6e6efe8`) / **#83** decoupled from Bulk (`584b544`). After **#82** upload + **mandatory** purge/freshness: (A) **#83** CI thin `_redirects` merge anytime on the **#82** base (**not** Bulk-gated; parallel with upload OK); (B) **Bulk Phase1** frees Dynamic slots when Member needs capacity (keep `root`/`shop`/`chat` Dynamic until then). **Member edge only after Bulk.** **Never #81.** Never imply Bulk gates **#83**. Canonical: [post-dual-clear-go.md](post-dual-clear-go.md). Align STATUS / this changelog / pointer files. Living dual-OPEN honesty (`5f2fd1c`) **unchanged**: Soft-530 still **OPEN** (CF **1033**); vault still **OPEN** (**502**); ListMachines only `Mac.lan`. Anti-goals: not Zone Direct Upload, not unfreeze/#79.1 execute, not companion re-ask, not Bitwarden. Paper only — no Zone/Garage execute. Tip fold on held Docs **#70**. |
| 2026-09-14 | Post-dual-CLEAR **Shop OS parallel — unfreeze + #79.1 acceptance smoke** on held **#70**. Sibling of the brochure lane on [post-dual-clear-go.md](post-dual-clear-go.md). After Ben GO Doc unfreeze + pull + Garage **#79.1** rebuild, before calling the lane done assert: (1) public `app.` + `ops.` `/login` HTML no longer prints Password `` `changeme` ``; (2) `BUILD_ID` / freeze tip ≠ `4cf8924` / `5swmVz`; (3) `api` `/health` **200** + waitlist OPTIONS CORS Origin `https://projectcar.ca` (+ www); (4) merge of **#79.1** alone without Doc unfreeze+pull does **NOT** clear public `changeme`. Anti-goals: not **#82**, not Zone Direct Upload, not companions re-ask, not Bitwarden. Soft-530 CLEAR alone ≠ this card. Cross-links: [doc-unfreeze.md](doc-unfreeze.md) · [ops-demo-hardening.md](ops-demo-hardening.md) / **#79** · STATUS living-ops / Next. Living dual-OPEN honesty (`5f2fd1c`) **unchanged**: Soft-530 still **OPEN** (CF **1033**); vault still **OPEN** (**502**); ListMachines only `Mac.lan`. Paper only — no Zone/Garage execute. Does **not** invent CLEAR. Tip fold on held Docs **#70**. |
| 2026-09-14 | **Living dual-OPEN STATUS honesty** on held **#70**. #70 SSOT is **not** still Soft-530→LIVE from Fri CLEAR. Stamp: Soft-530 **OPEN** CF **1033** since 2026-09-13 ~11:57 America/Edmonton (`api.` / `app.` / `ops.` / `cloud.` + waitlist OPTIONS); vault independently **OPEN** **502** since ~19:45 MT Sep 13 (**not** 1033); ListMachines **Mac.lan only** — `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning` (lid-restore + vault wake **blocked**). Brochure Option A **LIVE**: Zone Redirect **FULL 10/10**; `waitlist.js?v=3`; `styles.css?v=36`; Soft-530 Discord honesty; Home still bare `href="/"` + canonical/og apex until **#82**. Freeze intact **`4cf8924`** / BUILD_ID **`5swmVz`**. **#82** still Ben GO. Apex deferred. Index pointers stay: [vault-stay-up.md](vault-stay-up.md), [dual-host-outage.md](dual-host-outage.md), Soft-530/vault post-CLEAR forensics, [post-dual-clear-go.md](post-dual-clear-go.md). Locks anti-goals: not unfreeze, not **#82** merge/upload, not Zone execute, not companions re-ask, not Bitwarden. Paper only — no Zone/Garage execute. Does **not** invent CLEAR. Tip fold on held Docs **#70**. |
| 2026-09-14 | Post-dual-CLEAR **brochure lane after #82** on held **#70**. Sequencer gap: **#82** was Direct Upload only, then unfreeze+#79.1 as “next.” After **#82** Worker-live (upload + **mandatory** purge/freshness) the **brochure lane** is **parallel**: (A) **#83** CI thin `_redirects` anytime on the **#82** base (not Bulk-gated; parallel with upload OK); (B) **Bulk Phase1** frees Option A **FULL 10/10** Dynamic slots when Member needs capacity (keep `root`/`shop`/`chat` Dynamic until then). **Member edge only after Bulk.** **Never #81.** Never Bulk→#83. **Superseded as sequence** by the 2026-09-14 **#83** ∥ Bulk reconcile on this changelog. Unfreeze + **#79.1** is **Shop OS parallel**, not “next after **#82**” in place of Bulk / **#83**. Canonical: [post-dual-clear-go.md](post-dual-clear-go.md). Cross-links: [brochure-worker-ci.md](brochure-worker-ci.md) Option A receipt · STATUS Live URL SSOT / Locks · [member-zone-edge.md](member-zone-edge.md). Living OPEN unchanged: Soft-530 still **OPEN** (CF **1033**); vault still **OPEN** (**502**); ListMachines only `Mac.lan`. Paper only — no Zone/Garage execute. Does **not** invent CLEAR. Tip fold on held Docs **#70**. |
| 2026-09-14 | Post-dual-CLEAR **Ben GO sequencer** on held **#70**. After Soft-530 CLEAR (Doc lid-restore) **and** vault CLEAR (McKing) **and** both forensic papers already ack’d (`55e10d0` Soft-530 post-CLEAR + `d88cacb` vault post-CLEAR), lock an ordered Ben GO menu — **never auto-fire** on CLEAR: (1) keep Soft-530 healthy (KeepAlive / CDM / caffeinate) as ops baseline; (2) **#82** Worker-live (upload + **mandatory** purge/freshness) then **brochure lane** **#83** ∥ **Bulk Phase1** (not Bulk→#83; Member-after-Bulk) — **never #81**; (3) Doc unfreeze + pull + Garage **#79.1** is **Shop OS parallel**. Canonical: [post-dual-clear-go.md](post-dual-clear-go.md). STATUS living-ops / Locks + short section in [dual-host-outage.md](dual-host-outage.md). Cross-links: [doc-lid-restore.md](doc-lid-restore.md), [vault-stay-up.md](vault-stay-up.md), [brochure-worker-ci.md](brochure-worker-ci.md), [doc-unfreeze.md](doc-unfreeze.md), [ops-demo-hardening.md](ops-demo-hardening.md). Living OPEN unchanged: Soft-530 still **OPEN** (CF **1033**); vault still **OPEN** (**502**); ListMachines only `Mac.lan`. Anti-goals: CLEAR ≠ companion re-ask; CLEAR ≠ Bitwarden import/rotate; Soft-530 CLEAR alone ≠ Doc unfreeze; vault CLEAR ≠ Soft-530 work; dual CLEAR ≠ **#82** auto-go; **#82** ≠ end of brochure. Paper only — no Zone/Garage execute. Does **not** invent CLEAR. Tip fold on held Docs **#70**. |
| 2026-09-14 | Vault **post-CLEAR stay-up evidence** on held **#70** (twin of Soft-530 `@55e10d0`). After McKing vault CLEAR, stamp forensic baseline for the next vault flip: `cloudflared.service` ActiveState/Result/ExecMainStatus (or lastExit); VW container healthy + `DOMAIN=https://vault.projectcar.ca`; Tailscale `lightning` up; whether recovery was **1033→200** vs **502→200**. `dual-host-outage.md` is **wake order**; Soft-530 post-CLEAR on [doc-lid-restore.md](doc-lid-restore.md) is **Doc forensics**; this is **McKing forensics**. Canonical: [vault-stay-up.md](vault-stay-up.md); STATUS Live pointer. Living OPEN class honesty: Soft-530 still **OPEN** as CF **1033** tunnel-down; vault independently **OPEN** as **502** origin-down (**not** 1033) on `/alive` + `/api/config`; ListMachines only `Mac.lan` (Docs + lightning absent). Different first checks when `lightning` returns. Brochure Option A + Soft-530 Discord assets still **LIVE**. **#82** unchanged (**never #81**). Does **not** invent CLEAR. Anti-goals: not Doc unfreeze, not **#82**, not Zone/Garage execute, not companion re-ask. Locks unchanged. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-14 | Soft-530 **post-CLEAR stay-up evidence** on held **#70**. After lid-restore CLEAR, stamp forensic baseline for the next CF 1033: cloudflared LaunchAgent lastExit, KeepAlive loaded, caffeinate / CDM, Tailscale. `dual-host-outage.md` (`7bf894b`) is wake order; this is why it died after Fri→Sun flip-flops. Canonical: [doc-lid-restore.md](doc-lid-restore.md); pointer: [api-stay-up.md](api-stay-up.md) + STATUS Live. Living: Soft-530 still **OPEN** (api/app/ops/cloud **530** CF **1033**; waitlist OPTIONS **530**); vault independently **OPEN** (`/alive` **502**); ListMachines only `Mac.lan` (Ben Laptop; Docs + lightning absent). Brochure Option A + Soft-530 Discord assets still **LIVE**. **#82** unchanged (**never #81**). Does **not** invent CLEAR. Anti-goals: not Doc unfreeze, not **#82**, not Zone/Garage execute, not companion re-ask. Locks unchanged: vault-stay-up / dual-host-outage / weekend Soft-530 / brochure-worker-ci / dual-Nextcloud. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-14 | Draft `dual-host-outage.md` on held **#70**. Weekend dual-OPEN recovery glue after `vault-stay-up` (`017f778`). Soft-530 OPEN + vault OPEN + only `Mac.lan` online: wake **Doc first, then McKing** (parallel only if both hosts appear on ListMachines). Identity lock: `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning` — no wrong-host Shell (`Mac.lan` is not Doc shop host). Independent CLEAR: Soft-530 = `api.` `/health` **200** + `ops.`/`app.` → `/login` + waitlist OPTIONS **200** + `cloud.` `status.php`; vault = `/alive` **200** + VW `/api/config`. Anti-goals: not Doc unfreeze, not **#82**, not Zone Direct Upload; Soft-530 companions **HOLD**; Bitwarden import/rotate **blocked** until vault CLEAR. Indexed from `deployment-guide.md` + STATUS living-ops / Locks. Cross-links: `api-stay-up.md`, `vault-stay-up.md`, `doc-lid-restore.md`, `shop-web-stay-up.md`. Locks unchanged: **#82** Ben GO / companions HOLD / dual-Nextcloud / `brochure-worker-ci` / weekend Soft-530 coverage / waitlist-owner-desk retired / vault stay-up (`017f778`). Paper only. Tip fold on held Docs **#70**. |
| 2026-09-14 | Draft `vault-stay-up.md` on held **#70**. Weekend dual-outage proved Soft-530 (Doc) and vault (McKing) fail independently. Docs already had `api-stay-up` / `shop-web-stay-up` / `doc-lid-restore` — this is the missing McKing VW + `cloudflared` stay-up (`vault.` → `:8222`). Lookout `/alive` **separate** from Soft-530 `api.` `/health`. Triage **502** vs CF Tunnel **1033**. Bitwarden import/rotate **blocked** until vault CLEAR. Dual-tunnel anti-goals: never touch Doc mission-control token; McKing = `vault.` (+ unpublished NC); Doc = `cloud.` / `api.` / `app.` / `ops.`. Vault flip ≠ Doc lid-restore; Doc Soft-530 ≠ McKing vault wake. Indexed from `deployment-guide.md` + STATUS living-ops / Locks. **Public STATUS tip honesty** (fold only, no tip-only PR): `main` still stamps P4-2 **`2b772ff` / #67** — **stale** vs live Option A Redirect pack + Soft-530 Discord waitlist + McKing vault **LIVE (when CLEAR)**; keep Reality tip quarantine; **#70** is SSOT until **#82**. Locks unchanged: **#82** Ben GO / Soft-530 companions HOLD / dual-Nextcloud / `brochure-worker-ci` / weekend Soft-530 coverage / waitlist-owner-desk retired. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-11 | Retire parked waitlist-owner-desk on held **#70**. Owner desk is **LIVE** when origin is up (login + `/waitlist`): `apps/project-car/web/app/waitlist/page.tsx` (`OwnerShell` `current=waitlist`; GET `/waitlist` list + mark contacted); OwnerShell nav Waitlist → `/waitlist`; live `ops.` / `app.` `/waitlist` **307** → `/login?next=/waitlist` (route on frozen **`5swmVz`**); `api` GET `/waitlist` **401** without Owner (endpoint live; Soft-530 **CLEAR**). Optional CSV stays Later. No email blast / Stripe / Member host. Do **not** open `Docs/waitlist-owner-desk.md`. Anti-goal: Soft-530 **CLEAR** or desk existence ≠ Doc unfreeze ≠ **#79.1** rebuild GO. **#82** Ben GO / companions HOLD / weekend coverage `84107e7` / dual-Nextcloud / `brochure-worker-ci` `6e6efe8` / **#79.1** git-only — unchanged. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-11 | Weekend Soft-530 coverage (plan-improve off Sat/Sun): flip watches = Lookout `api.` `/health` + vault `/alive` **only**; control plane = Doc KeepAlive / lid-close (cloudflared + uvicorn `:8000` + `next start` `:3000`) — **not** Doc unfreeze, **not** **#82**. `/health` non-200 → Chief/Lead `doc-lid-restore.md` **process wake only**. No weekend Zone Direct Upload / Worker — first Monday plan-improve resumes Soft-530 smoke. Anti-goal: Soft-530 **CLEAR** Friday ≠ unfreeze GO ≠ companion re-ask. **#82** Ben GO / companions HOLD / dual-Nextcloud `4cde204` / `brochure-worker-ci` `6e6efe8` / waitlist-owner-desk parked / **#79.1** git-only — unchanged. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-11 | Draft `brochure-worker-ci.md` on held **#70** (was queued post–Soft-530-CLEAR). Ordered **#82** smoke: Direct Upload → **mandatory** Worker/Cache purge (apex+www) → body freshness (`HIT`+no-cache is steady-state). Soft-530 smoke: `waitlist.js?v=` + `styles.css?v=` + Discord fail-soft. Option A **FULL 10/10** lock + parked Bulk Phase1 pretty-URL pack. **Never** Direct Upload from **#81** (draft + superseded). **#82** Ben GO / Soft-530 companions HOLD / dual-Nextcloud / **#79.1** git-only / waitlist-owner-desk parked — unchanged. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-11 | Soft-530-CLEAR **#79** freeze honesty: Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull** — merge alone will **not** clear public `ops.` / `app.` `/login` while freeze holds (`4cf8924` / **`5swmVz`**). Live login still prints `changeme`. **#79** Next Ben GO stays **parallel to #82**. **#78** lookout-resume **LIVE-SUPERSEDED** (api-health already `enabled:true`; companions **HOLD**). Hard lock **#82** → Zone upload+purge; **#83** after **#82**; Bulk for Member — unchanged. **Never #81.** Paper only. Tip fold on held Docs **#70**. |
| 2026-09-11 | Decouple **#83** from Bulk Phase1. Hard lock / prefer merge: **#82** Ben GO → Zone Direct Upload + mandatory purge/freshness → **#83** merge after **#82** base (CI-only thin `_redirects` assert; **parallel with upload OK**; **not** Bulk-gated) → Bulk Phase1 after upload **only** to free Dynamic slots for Member edge. **Never #81.** Do **not** write `#82 → upload → Bulk Phase1 → #83`. Soft-530 companions HOLD. Reality quarantine until **#82** unchanged. Paper only. Tip fold on held Docs **#70**. |
| 2026-09-11 | Option A Dynamic Redirect pack locked **FULL 10/10**. No new Dynamic rules for brochure pretty-URLs. Garage must **not** invent new extensionless pretty-URLs until **#82** Worker-live + Bulk Phase1 frees slots. Redirect plan A parked: Bulk Phase1 pretty-URL pack already drafted (`membership` / `about` / `the-shop` / `contact` / `roadmap` ± slash → `*.html`; `root` / `index` / `shop` / `chat` stay Dynamic until migrate). Soft-530 **CLEAR** live honesty and **#82** not-Worker-live gate unchanged. Paper only — no Zone live apply. Tip fold on held Docs **#70**. |
| 2026-09-11 | Lookout confirmed `projectcar-api-health-watch` **resumed** (`enabled:true`) ~06:52 America/Edmonton; live `/health` **200**. Lead interim probe ended. Freeze + #82 gates unchanged. Tip fold on held Docs **#70**. |
| 2026-09-11 | Soft-530 restore **LIVE** (Lead verified ~06:50 America/Edmonton): public GET /health 200; ops/app 307 → /login; waitlist OPTIONS 200 (Chief POST 201 + CORS). Soft-530 fail-soft (#80) still valuable for future lid-close. Lookout `projectcar-api-health-watch` resume GO’d; Lead interim probe ended. Doc checkout still frozen at `4cf8924` / `5swmVz`. Product tip stays `2dd61a2` / #74. Tip fold on held Docs **#70**. |
| 2026-09-10 | Option A URL SSOT: Zone Redirect pack (10 Active Dynamic **301**s, apex+www) owns pretty URLs, including brochure-shop-html-to-the-shop (`/shop.html`→`/the-shop.html`) live ~12:04 America/Edmonton. Worker Assets **`html_handling: none`**. Worker `_redirects` is **thin**: chat → contact.html only (no `/` or `/shop`). Do **not** claim Worker `/shop` → 302 `/the-shop`. Soft-530 **#80** Worker-live: `waitlist.js?v=3` + `styles.css?v=36` + Discord honesty on Membership/Contact. Home canonical/og/sitemap `/index.html` waits on **#82** — **not yet Worker-live**. Tip fold on held Docs **#70**. |
| 2026-09-08 | P4-5 Lookout sync: `projectcar-api-health-watch` **paused** (`enabled:false`). Lead owns morning probe. Do **not** re-nag Ben to allow GET. STATUS Live + Locks: Doc checkout **frozen** at `4cf8924` / BUILD_ID `5swmVz-T2CqKEQzTk1ifU` (Dashboard #28) until Ben GO; #36 + #69 stay git-only. |
| 2026-09-07 | P4-2 **done** with Worker live verify. Garage PR #67 → main `2b772ff` already on Worker (Zone Direct Upload ~19:15 America/Regina): Home equal `cta-hero` Join the waitlist + Join Discord (`discord.gg/projectcar`); contact email · Discord; Membership waitlist form unchanged; Discord contact-primary + alongside-waitlist copy; Worker honors `_redirects`: `/shop`,`/shop/` → 302 `/the-shop` (follow 200); `/shop.html` → 302 `/the-shop.html`; sitemap lastmod bumps (2026-09-08 present); `styles.css?v=35`; P2-4 headers still; P3-4 Shop facility bullets still. P2-1 / P2-2 / P2-3 / P2-4 / P3-1 / P3-4 / P3-5 already LIVE. P3-2 / P3-3 About stay **held / deferred**. P1-6 stays deferred. Tip fold: `2b772ff` / **#67**. |
| 2026-09-07 | P3-4 **done** with Worker live verify. Garage PR #65 → main `f191ad5` already on Worker (Zone Direct Upload ~18:46 America/Regina): Shop 200 facility bullets (bays/hoists, shared fab, safety/cleanliness, structured shop) + honesty “intended shop capability — not live yet”; no 24/7 packaging on Shop; Membership still “24/7 shop access (when operational)”; Home still P3-1 pitch + `cta-hero` waitlist + Project Underway; no capability-bullet dump; `styles.css?v=34`; P2-4 headers still present. P2-1 / P2-2 / P2-3 / P2-4 / P3-1 / P3-5 already LIVE. P3-2 / P3-3 About stay **held / deferred**. P1-6 stays deferred. Tip fold: `f191ad5` / **#65**. |
| 2026-09-07 | P3-4 Shop vs Home split **in git** this PR (not Worker-live). Shop facility/standards (bays, fab, safety/cleanliness); Membership keeps access packaging (tiers / 24/7 when operational); Home stays P3-1 pitch + waitlist. Status stays **open** until Zone smoke after merge. P3-2 / P3-3 About stay **held / deferred**. Do not invent P3-4 done. Tip fold: `764eb11` / **#64**. |
| 2026-09-07 | P3-5 **done** with Worker live verify. Garage PR #62 → main `9d83bfc` already on Worker (Zone Direct Upload): Shop/Membership/Roadmap use “24/7 shop access (when operational)”; Home (and About/Contact) have no absolute 24/7. P2-1 / P2-2 / P2-3 / P2-4 / P3-1 already LIVE. P3-2 / P3-3 About stay **held / deferred**. Do not invent P3-4 done. P1-6 stays deferred. Tip fold: `9d83bfc` / **#62**. |
| 2026-09-07 | P3-1 **done** with Worker live verify. Garage PR #61 → main `88c9820` already on Worker (Zone Direct Upload ~16:50 America/Regina): Home pitch + `cta-hero` Join the waitlist + Project Underway + Shop/Membership pointers; no feature-bullet dump. P2-1 / P2-2 / P2-3 / P2-4 already LIVE. P3-2 / P3-3 About stay **held / deferred**. Next brochure slice is **P3-5** (24/7 tense; Membership “when operational” SSOT) — not done. P1-6 stays deferred. Tip fold: `88c9820` / **#61**. |
| 2026-09-07 | P2-4 **done** with Worker live verify. Zone Transform Rules on Worker `projectcar-brochure` (apex+www): `brochure-security-headers` / `brochure-html-no-cache` / `brochure-asset-immutable`. Smoke PASS (Worker **200**s, not challenge 403): `/` + www 200 + required security + HTML `no-cache, must-revalidate`; `styles.css?v=34` 200 + security + immutable long-cache; `robots.txt` 200 `text/plain` + security + `max-age=0`. DNS/tunnels untouched. P1-6 stays deferred. Tip fold: `a1c29ee` / **#59**. |
| 2026-09-07 | P2-3 **done** with Worker live verify. Garage PR #57 → main `be60a01` already on Worker (Zone Direct Upload ~16:01 America/Regina): `styles.css?v=34` + `banner-logo.png?v=30` on public HTML (Home/About/Contact at minimum; apex+www). P2-1 / P2-2 already LIVE. P2-4 stays **open** (checklist on main — not applied). P1-6 stays deferred. Tip fold: `348375e` / **#58**. |
| 2026-09-07 | P2-4 Zone checklist landed (`brochure-security-headers.md`). Target Worker `projectcar-brochure` headers + HTML vs `?v=` cache split. Status stays **open** / plan — not applied, not live. Zone after Lead GO. Do not invent P2-4 done. Tip fold: `be60a01` / **#57**. |
| 2026-09-07 | P2-2 **done** with Worker live verify. Garage PR #56 → main `ed41d23` already on Worker (Zone Direct Upload ~15:48 America/Regina): `rel=canonical` matches `og:url` apex on Home + Membership; www serves apex canonicals. P2-3 **done** with git/`rg` verify PR **#57** / `be60a01` (`styles.css?v=34` + `banner-logo.png?v=30` on all public HTML). P2-4 stays **open**. P1-6 stays deferred. |
| 2026-09-07 | P2-1 **done** with Worker live verify. Garage PR #55 → main `453e44d` already on Worker: absolute `og:url` / `og:image` + `twitter:card`. Zone smoke PASS. P2-2 tags landed in PR #56 / `ed41d23` (later Worker-live; see row above). |
| 2026-09-07 | P1-1 / P1-3 **done** (live verify). Garage PR #52 → main `5361212`. Home `cta-hero` waitlist CTA weight; Contact Phone/IG/TikTok/YouTube/Skool collapsed to one `contact-soon` line. Zone Direct Upload Worker `projectcar-brochure` **LIVE** from that tip; smoke PASS (Home CTA + Contact; apex/www 200). P1-6 stays deferred. P2 not invented as done. |
| 2026-09-07 | P1-6 stub: Membership secondary “Already a member? Sign in” → apex `https://projectcar.ca/member/login` only. **Deferred** until Member cutover GO. Never www / ops / app. Waitlist stays the interest path. No brochure HTML in this docs PR. |
| 2026-09-07 | P1-5 note: lid-close public **530 / 1033** on OPTIONS / POST /waitlist is expected — not a CORS regression. `waitlist.js` mailto fallback while origin is down; e2e only when GET /health is 200. See `cors-origins.md`. |
| 2026-09-07 | P4-5 synthetic monitors **done / partial**: Lookout `projectcar-api-health-watch` live on `api.projectcar.ca/health`. Apex dropped (deferred). Brochure homepage 200 + Discord invite optional/deferred. Spec roadmap row 2 **Done** on Worker `projectcar-brochure`. |
| 2026-09-06 | Reality sync ~16:16 America/Edmonton (`main` `afb37f9`): **`ops.projectcar.ca` LIVE** → Doc `:3000`; `app.` = temporary alias. Calendar #18 / fill #20 / placeholders #21 / inventory #26 / Chat #27 / Dashboard #28 are Live on Doc. Brochure Chat/Apex stay stripped. MC cockpit still needs Ben GO. No Stripe. The shop is not open. |
| 2026-09-06 | Reality sync ~14:10 America/Edmonton (`main` `91c547e`): **`ops.projectcar.ca` LIVE** → Doc `:3000`; `app.` = temporary alias. Calendar #18 / fill #20 / placeholders #21 / schedule harden #24 are Live. Chat planned, not live (superseded ~16:16). MC cockpit still needs Ben GO. No Stripe. The shop is not open. |
| 2026-09-06 | Reality sync ~12:28 America/Edmonton: host split locked — customer = projectcar.ca; management = **`ops.projectcar.ca`**; `app.` = temporary alias (live). Member UI on projectcar.ca is Next. No DNS cut. MC cockpit still needs Ben GO. No Stripe. The shop is not open. |
| 2026-09-06 | Reality sync ~11:52 America/Edmonton: `https://app.projectcar.ca` **LIVE** (Ben GO ~11:41) pointing at Doc demo. Owner + Member booking reachable on the public app host. MC cockpit still needs Ben GO. No Stripe. The shop is not open. |
| 2026-09-06 | Reality sync ~11:20 America/Edmonton: P0-3…P0-7 **already live** on Worker (re-uploaded). No pending hygiene upload. Owner + Member booking live on Doc demo; `app.projectcar.ca` not claimed; MC cockpit still needs Ben GO. |
| 2026-09-06 | P0-3…P0-7 **done** in `apps/website/` (robots, sitemap, 404.html, favicon set, Home progress bar removed). Live origin noted as Worker Direct Upload, not Doc `:8088`. P1-2 Chat strip **done** (PR #5). Next product path: Owner booking; MC held. |
| 2026-09-06 | Status sync: P1-5 waitlist **done**; P2-7 site-under-git **done**; Apex P0-1 / P0-2 **deferred** (not active P0). |
| 2026-08-12 | Initial living doc from full live + local audit (Doc). P0–P4 backlog, maintain rules, verify commands. Linked from `website-webapp-specification.md`. |

---

## Decision log

| Date | Decision | By |
|------|----------|-----|
| 2026-09-06 | Host split: customer = `projectcar.ca` / www; management = **`ops.projectcar.ca` LIVE** (staff on shift + Owner, not Owner-only); `app.` = temporary alias until Ben cuts that DNS. | Ben (via Master Chief) |
| 2026-09-06 | Brochure live host is Worker Direct Upload (`projectcar-brochure`), not Doc `:8088`. Classic Pages git skipped. Hygiene P0s are in git **and already live** (Worker re-uploaded 2026-09-06). | Ben (task) · Garage (site) · Zone (CF) |
| 2026-09-06 | Waitlist on brochure is **done**. Apex is **deferred**, not active P0. `Docs/` is SSOT; `apps/website/WEBSITE-*` files are pointers only. | Status sync (living ops) |
| 2026-08-12 | Keep improvements as a **living git doc** in Project-Car `Docs/`, separate from architecture spec. | Ben (request) · Doc (author) |
