# Deployment guide — living-ops index

**Status:** Index (filename kept so old links resolve)  
**Updated:** 2026-09-16  
**Canonical:** `Coombzy/Project-Car` → `Docs/deployment-guide.md`

This file is **not** a runbook. Use the living docs below.

---

## Soft-530 living paper index (held #70 — Monday map)

Discoverability only. Monday’s **#70** tip-commits are paper. This table is the map so those essays are not tip-only. **No new product.** No Zone / Garage execute. **Camp SSOT** at camp GO is [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) — [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md) + McKing-centric [vault-stay-up.md](vault-stay-up.md) are **PARKED for camp weeks**. Living half-state after short OPEN blip: Soft-530 **CLEAR** (Lead `api.` `/health` **200**; `app.`/`ops.` **307**; **`soft530_clear_since` ~07:25**; **`soft530_open_blip` ~07:06–~07:25**); vault independently **OPEN 530 / CF 1033** connector-down (was 502 since ~19:45 MT Sep 13 — **EXPECTED**, **not** CLEAR); prior overnight CLEAR dwell **~22:00–07:06** ended at the blip; half-state **≠** dual CLEAR / **≠** unfreeze / **≠** **#82**; camp / post-dual-clear-go **must not reopen** while Docs **EMPTY** / **CONNECTED≠Shellable**; **Living Reality ≠ frozen tip SHA** (still Sep-7 **`2b772ff`**); ListMachines **EMPTY**; **CONNECTED ≠ Shellable ≠ tunnel healthy**; companions **HOLD**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED — **never** `Mac.lan` as Doc. Half-state ≠ dual CLEAR — do **not** enter [post-dual-clear-go.md](post-dual-clear-go.md). Soft-530 CLEAR at home ≠ camp GO. Soft-530 **CLEAR ≠ #82** ≠ unfreeze ≠ Bitwarden. Lookout: api `ok=false` expected; vault `ok=false` expected (dual OPEN). Option A Redirect+UX **PASS** (rungs 1–2 — **≠** Worker body freshness / **#82**). Soft-530 **OPEN** never auto-GOs **#82**. Body asserts stay on [brochure-worker-ci.md](brochure-worker-ci.md).

Open **one** card for the moment you are in. Do **not** invent CLEAR, **#82**, unfreeze, Dynamic wipe, honesty-off, or companion rearm from this index.

| When to open | Paper |
|--------------|-------|
| **Camp SSOT** — vault cutover / camp vault recovery — Doc hosts Soft-530 shop + vault + NC; McKing may sleep; Doc VW + Zone retarget (**not** McKing docker). Hop **4** Zone `vault.` checklist **in that file** (preflight / inventory McKing origin / Doc ingress ready / cut / **2026.6.0** smoke **502→200** vs **1033→200** / rollback). **Ben GO**, never auto-cutover. Soft-530 shop hostnames stay on Doc. Soft-530 independence is **home/lab only**. **Doc local VW/NC healthy ≠ public vault CLEAR** — loopback `:8222` / `:8080` **200** = Z1 preflight only; public CLEAR is Z5 on `vault.projectcar.ca`; [vault-clear-smoke.md](vault-clear-smoke.md) SSOT stays the public hostname. **Camp CDM lid-close soak** (hop **3a**) — Soft-530 **CLEAR** lid-open / home-awake **never scores**; soak **PASS** (lid closed / CDM engaged, named window suggest **≥15–30 min**) is required paper **before** Zone **Z5**; fail → **HOLD** Zone retarget. [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md) + McKing-centric [vault-stay-up.md](vault-stay-up.md) are **PARKED for camp weeks** / desk/lab optional — Zone/Doc follow **this** card at camp GO | [camp-vault-on-doc-cutover.md](camp-vault-on-doc-cutover.md) |
| **Home vs camp Doc posture** — home-off Docs **ABSENT** + Soft-530 **OPEN** is **EXPECTED** (quiet-ops HOLD; no lid-restore / KeepAlive re-nag; vault independent on McKing). Camp after Ben GO: Doc on+Amphetamine/CDM → Soft-530+vault expected CLEAR together; dual OPEN = coupled Doc hop (**not** McKing docker). Away weeks **supersede** standing McKing=`vault.`→`:8222`; McKing 24/7 + WOL/remote LUKS **deferred**. Pi/VPS still better long-term if vault must stay up while Doc is home-off (paper — **not** a Pi GO). Stamp / morning `*/20`: home Soft-530 is **SAME-CLASS expected**. Lookout `ok=false` while Docs **ABSENT** at home is expected baseline — do **not** re-arm companions. Complement to `fb86f90`, **not** a re-cutover. Soft-530 **OPEN ≠ #82 blocked**. Never `Mac.lan` as Doc. Never treat home Soft-530 as camp cutover failure. Living honesty: Soft-530 **CLEAR** + vault **OPEN 530 / CF 1033** half-state (`soft530_clear_since` **~07:25**; **`soft530_open_blip` ~07:06–~07:25**; was 502); ListMachines **EMPTY**; **CONNECTED ≠ Shellable ≠ tunnel healthy**; lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED — home-awake recover, **not a Ben surface**. Camp / post-dual-clear-go **must not reopen**. Soft-530 short-blip recover ≠ camp GO. Home Soft-530 **CLEAR** lid-open **never scores** the camp CDM soak | [home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md) |
| McKing vault is **OPEN** / `/alive` flipped — **home/lab** stay-up + post-CLEAR forensics (**PARKED for camp weeks** / desk/lab optional — **not camp SSOT**; not Doc lid-restore; **not** the camp path). Lookout stall (`enabled:true` + stale `lastRun`) is **not** this 502 wake — [lookout-rearm-sop.md](lookout-rearm-sop.md) in-place re-arm | [vault-stay-up.md](vault-stay-up.md) |
| Soft-530 **OPEN** **and** vault **OPEN** — **home/lab/pre-cutover only** wake order (Doc first, then McKing; `Mac.lan` ≠ Docs ≠ `lightning`). **SUPERSEDED for camp** — after Ben GO, Doc is sole Soft-530+vault(+NC) origin; **`lightning` ABSENT must not trigger McKing wake** | [dual-host-outage.md](dual-host-outage.md) |
| Soft-530 **OPEN ~24h+** + vault **OPEN** + **Mac.lan only** — quiet-ops (no Ben re-nag; duration ≠ GO). **Home-off HOLD:** Docs **ABSENT** + Soft-530 **OPEN** is **EXPECTED** — do **not** re-nag lid-restore / KeepAlive ([home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md)). **Chronic-HOLD exception:** when both api + vault are calendar-day chronic HOLD, “Lookout continues” is **false for flip detection**; interim = Chief `*/20` + Lead probes (accept **≤~20m**); Chronic Lookout ≠ quiet-ops failure ≠ Ben ping | [soft-530-extended-open.md](soft-530-extended-open.md) |
| After last weekday plan-improve `*/20` until next weekday first fire (~06:00) — **Mon–Thu nights only** — Lookout `api.` `/health` + vault `/alive` only (companions HOLD); brochure `*/5` watch **GONE** — overnight Option A Dynamic **UNCOVERED** (interim = plan-improve `*/20`); **HOLD** second Ben recreate ask; no overnight Zone / Garage; last weekday `*/20` writes the overnight baseline stamp. Living: Soft-530 **CLEAR** + vault **OPEN 530 / CF 1033** **half-state** (`soft530_clear_since` **~22:00**; first Mon–Thu overnight after Sep 13–15 recovery; **was 502**). Soft-530 **CLEAR** overnight **≠** camp CDM soak **PASS** **≠** companions **≠** auto Doc wake **≠** **#82** **≠** unfreeze **≠** Zone vault retarget **≠** Bitwarden auto. Same half-state ≠ dual CLEAR ≠ “incident closed.” **Never** `Mac.lan` as Doc. Don’t wake McKing docker as camp path | [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) |
| Last weekday plan-improve `*/20` — write the **one-line** overnight baseline **before** the Lookout-only window. **Soft-530 CLEAR + vault OPEN is a first-class half-state class** — ≠ dual OPEN ≠ dual CLEAR; morning must **not** score “incident closed.” Tonight **~22:00** (Tue 2026-09-15; last `*/20` **~17:20** dual OPEN is **historical**) is the first Mon–Thu overnight after Sep 13–15 recovery: Soft-530 **CLEAR** **`soft530_clear_since` ~22:00** (Lookout **530→200** @10:00pm MT; Lead public clear-smoke **PASS**) + same-day OPEN dwell **~16:36–22:00**; vault **OPEN 530 / CF 1033** connector-down (public; **was 502**); ListMachines Docs `95a229f5` **CONNECTED+Shellable** (KeepAlive **UP** ~22:07–22:08; EMPTY flap **~16:51–22:00** ended); lightning **ABSENT**; `Mac.lan` may CONNECTED; freeze **`last_known`** `` `changeme` `` + **`5swmVz`** / **`4cf8924`** (not last_known-only); Option A Redirect **PASS** + Soft-530 UX **LIVE**; Lookout api+vault **armed**; quiet-ops **HOLD**; **#82** not auto-GO; **`shared_fleet_card_stale=false`**. Shared living-host **amend DONE ~22:08**. Shared still **non-SSOT**. Shared never overrides Docs/#70 SSOT. Home **STALE** until **#82**. Morning DIFF: age OPEN from last relapse (**~16:36**); Soft-530 still OPEN + same `soft530_open_since` = SAME; Soft-530 **CLEAR→OPEN** = DIFF; vault still **530 / CF 1033** = SAME; vault **502 ↔ 530/1033** = subclass field DIFF (Lead/Lookout ≠ Ben re-nag); Docs **CONNECTED→ABSENT** = DIFF; `changeme` off = freeze DIFF (unfreeze signal). Soft-530 **CLEAR** overnight **≠** camp CDM soak **PASS** **≠** companions **≠** **#82** **≠** unfreeze **≠** Zone vault retarget **≠** Bitwarden auto. **Never** `Mac.lan` as Doc. **No** second Ben Lookout recreate. **No** overnight Zone/Garage. Don’t wake McKing docker as camp path. **Same-class = stay quiet** (`611c267`). | [overnight-baseline-stamp.md](overnight-baseline-stamp.md) |
| Soft-530 **CLEAR→OPEN** while Docs ListMachines **CONNECTED** but Shell **unreachable** — **flap-while-connected** (desktop/local-exec dead). **CONNECTED ≠ Shellable ≠ tunnel healthy.** Only Shellable runs lid-restore / KeepAlive / clear-smoke. Assert `95a229f5` ≠ `Mac.lan`; retry Shell sparingly; park; Ben ask once; quiet-ops **HOLD**. No-ABSENT-gap Shell return = warm path (cloudflared + KeepAlive → clear-smoke). CONNECTED+Shell-unreachable → **ABSENT** = absorb; next CONNECTED = [doc-reappear-first-hop.md](doc-reappear-first-hop.md). Living recover **EMPTY ~07:25**. Stamp must **not** keep Soft-530=OPEN after a live short-blip CLEAR. Never CONNECTED ≠ CLEAR. Never auto unfreeze/#82/camp Zone/Bitwarden | [soft-530-flap-while-connected.md](soft-530-flap-while-connected.md) |
| Short Soft-530 **OPEN** blip then **CLEAR** with ListMachines **EMPTY** (or **CONNECTED≠Shellable**) — **does not** reopen camp GO / post-dual-clear-go. Stamp **`soft530_open_blip` ~07:06–~07:25** + **`soft530_clear_since` ~07:25**. Re-offer camp vault-on-Doc Ben GO only after **CONNECTED+Shellable** + clear-smoke + ≥30m SAME-CLASS dwell. Shared amend **never mid-flap**; cancel OPEN-since-**~07:06** queue. ≠ auto camp/#82/#79.1/unfreeze/Bitwarden; vault **OPEN 1033 EXPECTED**; behind freeze intact | [soft-530-blip-recover.md](soft-530-blip-recover.md) |
| `Docs-MacBook-Pro` **first reappears** after quiet-ops — **home-off ABSENT does not queue this hop** ([home-vs-camp-doc-posture.md](home-vs-camp-doc-posture.md)); assert machineId `95a229f5-9296-4a18-aa98-70fd300dabdf` (**never** `Mac.lan` / Porsche as Doc) → classify **cold-boot** vs overnight **warm wake** → on **cold-boot** (Docs **ABSENT ≥1 calendar day** or Soft-530 **OPEN** 1033 **≥24h** with Docs **ABSENT**) verify cloudflared + shop KeepAlive + note CDM **before** smoke → CLEAR smoke → only if CLEAR → forensics → sequencer (**never auto-fire**); overnight **warm wake** `<24h` after a prior CLEAR stays shorter process-wake; **never** treat multi-day lid as overnight sleep; living Docs **ABSENT** since Fri 2026-09-11 ~16:48 **~4d**; Chief-to-doc hop only after CLEAR smoke; vault stays Chief `lightning` | [doc-reappear-first-hop.md](doc-reappear-first-hop.md) |
| `lightning` / McKing **first reappears** after extended vault OPEN / quiet-ops — assert machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` (label **lightning**; **never** `Mac.lan`) → vault-clear-smoke (`/alive` **200** + `/api/config` VW **2026.6.0** + `docker.service` **enabled** + **active** + **502→200**; HTTP-only ≠ CLEAR) → only if CLEAR → vault post-CLEAR forensics; Soft-530 independence — Bitwarden gate is **vault CLEAR** (Ben GO after `/alive` + VW **2026.6.0** + docker permanence), **not** Soft-530 CLEAR; Soft-530 **OPEN** can coexist with vault CLEAR + Bitwarden work; **never auto Bitwarden**; vault CLEAR ≠ Soft-530 CLEAR ≠ unfreeze / **#82** GO | [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) |
| Soft-530 just **CLEAR**ed after extended OPEN — first recovery smoke **before** desk / **#79.1** / unfreeze (**CLEAR ≠ honesty-off**; waitlist OPTIONS = Origin CORS preflight — **bare 405 ≠ OPEN**; on CLEAR + Shellable, freeze live-verify is **required** from Doc `/Users/dochak/src/Project-Car` — **`freeze_git_verified`**, SHA, disk BUILD_ID, behind-count; `` `changeme` `` print alone ≠ verify; living **~06:57** **verified** `true` / `4cf8924` / `5swmVz-T2CqKEQzTk1ifU` / behind 89; SHA/BUILD_ID mismatch → freeze **DRIFT** Lead lane ≠ auto-unfreeze / ≠ Ben nag; never Desktop/Project Car / ProjectCar-App; behind N expected — never auto-pull) | [soft-530-clear-smoke.md](soft-530-clear-smoke.md) |
| Soft-530 **CLEAR** + freeze live-verify **PASS** + HEAD **`4cf8924`** **~89** behind `origin/main` — Lead **draft** Ben-GO gates **before** any pull/unfreeze (**draft ≠ GO**; vault OPEN does **not** block drafting; Soft-530 CLEAR duration ≠ auto-unfreeze ≠ auto **#82**; **#79.1** parallel, not next-after; lane-done later = [post-dual-clear-go.md](post-dual-clear-go.md) acceptance smoke) | [unfreeze-readiness.md](unfreeze-readiness.md) |
| Fleet Shared `shop-os-mc-plan.md` looks like living-ops SSOT / still says brochure “stays 502” / Garage-blocked-on-502 / living-host Soft-530 **CLEAR** after a same-day live **OPEN** — **discard Shared**; `Coombzy/Project-Car` `Docs/` (held **#70** → `main`) is SSOT; Shared = Chief standing notes only; rewrite **DONE** ~15:42 America/Edmonton on Doc `~/Desktop/Fleet-Nextcloud/Memory/Shared/shop-os-mc-plan.md` — **not** still in progress; living-host lines **must** carry Soft-530 **class + as-of**; Shared rewrite **DONE** ~15:42; living-host **amend DONE ~22:08** — living **CLEAR** since **~22:00** / Docs **KeepAlive UP / Shellable**; same-day **CLEAR↔OPEN** after Shared rewrite still **queues** a later amend on next Docs CONNECTED+**Shellable** (warm companion; **never** `Mac.lan`; amend ≠ Ben ask ≠ **#82** / unfreeze / camp); dropped false 502 lock + Garage-blocked-on-502; aligned Soft-530 **CLEAR** half-state (Shared ~15:42 as-of stale vs living **CLEAR ~22:00**; half-state **living**) / camp vault-on-Doc / freeze **`4cf8924`/`5swmVz`** / **#82** not auto. Public brochure is **LIVE** (Option A) since **2026-09-06**. **Not** a Ben ask. No Garage/Zone fan-out. No unfreeze / **#82** / camp GO. Do **not** restore 502 language | [shared-fleet-cards.md](shared-fleet-cards.md) |
| Vault just **CLEAR**ed after extended OPEN — first recovery smoke **before** Bitwarden / desk (HTTP **+** `docker.service` **enabled** + **active**; **502→200**, not 1033; HTTP-only ≠ CLEAR). **SSOT = public `vault.projectcar.ca`** — never substitute Doc loopback; Doc VW/NC healthy ≠ this card | [vault-clear-smoke.md](vault-clear-smoke.md) |
| Option A Lookout watch is **GONE** / cron stalled — never delete to fix cron; **HOLD** recreate (not a second Ben ask); weekday Chief `*/20`; overnight/weekend Dynamic redirect **UNCOVERED** — accept that risk; **next** Option A smoke = first weekday `*/20` after ~06:00 or Ben-approved Lookout recreate. **Vault / api watch stall ≠ vault HTTP 502** — `enabled:true` + stale `lastRun` is a coverage class; **re-arm success ≠ first fire** (stickiness ≥3 consecutive `*/5` windows); Lead **may GO** in-place re-arm of **both** if the day's 1× is unused; chronic ≤1 pause/resume per calendar-day (clock resets **America/Edmonton midnight**); when **both** are chronic HOLD, quiet-ops “Lookout continues” is **false for flip detection**; interim = Chief `*/20` + Lead probes (accept **≤~20m**) — not a Ben ping | [lookout-rearm-sop.md](lookout-rearm-sop.md) |
| Option A watch **spec** while Soft-530 **OPEN** — weekday `*/20` Redirect **+ Soft-530 UX**; overnight/weekend **UNCOVERED**; **Redirect-only PASS ≠ Soft-530 UX PASS**; Soft-530 UX smoke URL SSOT = **root** `/styles.css?v=36` + `/waitlist.js?v=3` (not `/assets/` — those 404s are **not** a regression) | [brochure-redirect-watch.md](brochure-redirect-watch.md) |
| **Both** CLEARs + forensics already ack’d — ordered Ben GO menu (**never auto-fire**; KeepAlive→**#82** default after dual CLEAR; dual CLEAR ≠ **#82** auto-go; Soft-530 **CLEAR ≠ #82**; living is **half-state after short-blip recover** — **do not enter**; **must not reopen** camp GO while Docs **EMPTY** / **CONNECTED≠Shellable**; Soft-530 CLEAR at home ≠ camp GO; Soft-530 **CLEAR ≠ #82** ≠ unfreeze ≠ Bitwarden; **does not** assume McKing vault wake first — camp vault CLEAR is Doc VW + Zone retarget) | [post-dual-clear-go.md](post-dual-clear-go.md) |
| Need the frozen Sep 10 **10/10** Redirect inventory (Docs SSOT) or Pages **hard cutover gate** — **after #82 Ben GO** (Soft-530-independent; **OPEN ≠ #82 blocked**); **#82** PASS ≠ retire Dynamic (`49ccf90`); Soft-530 UX smoke URL SSOT = **root** `/styles.css?v=36` + `/waitlist.js?v=3` (not `/assets/`). STATUS **Option A completeness ladder**: Redirect+UX **PASS** ≠ Worker body freshness **PASS**; body asserts stay here (index.html canonical/og/nav/sitemap). Live Home still **STALE** until **#82**. Soft-530 **CLEAR** never auto-GOs **#82**. | [brochure-worker-ci.md](brochure-worker-ci.md) · [brochure-pages-cutover.md](brochure-pages-cutover.md) |

Living one-pager stays [STATUS.md](STATUS.md). Product / Next list stays there — this section is **not** a second STATUS.

---

## Stay-up / deploy

| Need | Doc |
|------|-----|
| Doc lid-close / morning 530 restore (ordered Lead sequence — process wake only). Weekend `api.` `/health` flip (plan-improve off Sat/Sun) uses this same wake — **not** unfreeze, **not** **#82**. After Soft-530 CLEAR: **post-CLEAR stay-up evidence** (cloudflared lastExit / KeepAlive loaded / caffeinate·CDM / Tailscale) for the next CF 1033. Living Soft-530 is **CLEAR** this fold (`soft530_clear_since` **~22:00**) — post-CLEAR forensics + freeze live-verify **now allowed**; **CLEAR ≠ ABSENT**; companions **HOLD** forensics. Paper does **not** invent vault CLEAR / dual CLEAR. | `doc-lid-restore.md` |
| Doc unfreeze after **Ben GO** (pull / migrate / rebuild / `BUILD_ID` / smoke). Soft-530 **CLEAR** Friday ≠ this GO. Shop OS **#79.1** lane-done = `post-dual-clear-go.md` acceptance smoke. Draft the Ben-GO gates **before** any pull: `unfreeze-readiness.md` (**draft ≠ GO**). | `doc-unfreeze.md` |
| Draft Ben-GO unfreeze gates **before** any pull (Soft-530 **CLEAR** + freeze live-verify **PASS** + HEAD **`4cf8924`** **~89** behind `origin/main`): `origin/main` delta skim · alembic · Owner seed/CORS · rollback to **`4cf8924` / `5swmVz`** · Garage **#79.1** parallel not next-after. **draft ≠ GO**. Vault OPEN does **not** block drafting. Soft-530 CLEAR duration ≠ auto-unfreeze ≠ auto **#82**. Living: Soft-530 **CLEAR** (`soft530_clear_since` **~22:00**); vault **OPEN 530 / CF 1033** (was 502); freeze `` `changeme` `` / **`5swmVz`**; Option A Redirect+UX **PASS**; Home **STALE** until **#82**. | `unfreeze-readiness.md` |
| Fleet Shared `shop-os-mc-plan.md` is **rewritten / non-SSOT** — Docs (held **#70** → `main`) is SSOT; Shared = Chief standing notes only; rewrite **DONE** ~15:42 America/Edmonton on Doc `~/Desktop/Fleet-Nextcloud/Memory/Shared/shop-os-mc-plan.md` — **not** still in progress; living-host lines **must** carry Soft-530 **class + as-of**; Shared rewrite **DONE** ~15:42; living-host **amend DONE ~22:08** — living **CLEAR** since **~22:00** / Docs **KeepAlive UP / Shellable**; same-day **CLEAR↔OPEN** after Shared rewrite still **queues** a later amend on next Docs CONNECTED+**Shellable** (warm companion; **never** `Mac.lan`; amend ≠ Ben ask ≠ **#82** / unfreeze / camp). Dropped false brochure 502 lock + Garage-blocked-on-502; marked non-SSOT vs Docs/#70; aligned Soft-530 CLEAR half-state / camp vault-on-Doc / freeze `4cf8924`/`5swmVz` / #82 not auto. Public brochure **LIVE** (Option A) since **2026-09-06**. Not a Ben ask. No Garage/Zone fan-out. | `shared-fleet-cards.md` |
| Shop UI stay-up on Doc (`ops.` / temporary `app.`, `:3000`, **`next start`**). Companion watches **HOLD**; weekend control plane = KeepAlive / lid-close. | `shop-web-stay-up.md` |
| Shop API stay-up on Doc (`api.`, `:8000`, uvicorn). Weekend flip coverage = Lookout `api.` `/health` + vault `/alive` **only**. | `api-stay-up.md` |
| **Home/lab** McKing Vaultwarden + `cloudflared` stay-up (`vault.projectcar.ca` → `:8222`) — **PARKED for camp weeks** / desk/lab optional, **not camp SSOT**. Lookout `/alive` **separate** from Soft-530 `api.` `/health`. Weekend dual-outage proved Soft-530 (Doc) and vault (McKing) fail independently **at home/lab**. Vault flip ≠ Doc lid-restore; Doc Soft-530 ≠ McKing vault wake. After quiet-ops, first hop when `lightning` reappears is `mcking-reappear-first-hop.md` (machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` — **never** `Mac.lan`). After **extended OPEN**, first card is `vault-clear-smoke.md`. After vault CLEAR: **post-CLEAR stay-up evidence** (cloudflared.service / VW+DOMAIN / Tailscale / 1033→200 vs 502→200). Bitwarden gate is **vault CLEAR** (Ben GO after `/alive` + VW **2026.6.0** smoke) — **not** Soft-530 CLEAR. Soft-530 **OPEN** can coexist with vault CLEAR + Bitwarden work. **Never auto-fire.** **Camp SSOT** is `camp-vault-on-doc-cutover.md`. | `vault-stay-up.md` · `vault-clear-smoke.md` · `mcking-reappear-first-hop.md` · `camp-vault-on-doc-cutover.md` |
| **Camp SSOT** — vault on Doc (paper — **Ben GO**, never auto-cutover): Zone/Doc follow this card at camp GO. McKing shop-host + vault-stay-up are **PARKED for camp weeks**. Docs Mac plugged for NC + VW; McKing may sleep. Cutover: Doc CDM → VW (+ NC) on Doc → **CDM lid-close soak** (hop **3a**) → Zone retarget `vault.` → `/alive` **200** + `/api/config` VW **2026.6.0** class (classify **502→200** vs **1033→200**). Soft-530 **CLEAR** lid-open / home-awake **never scores** the soak; soak **PASS** is required paper **before** Zone **Z5**; fail → **HOLD** Zone retarget. Hop **4** Zone checklist **in this file** (inventory McKing origin; Doc ingress ready; rollback to McKing if Doc smoke fails; Soft-530 shop hostnames stay on Doc). Dual-tunnel camp: Doc hosts Soft-530 shop + vault (+ NC). Soft-530 independence **home/lab only**. Sequencer does **not** assume McKing vault wake first. Never `Mac.lan`. No dual origins. Not McKing docker. Soft-530 **OPEN ≠** block this paper. **No live Zone execute until Ben GO.** No Garage/Hatch execute. No Ben Doc-wake/docker re-ask. Living: Soft-530 **CLEAR** + vault **OPEN** 530 / CF 1033 half-state (was 502); ListMachines Docs `95a229f5` **CONNECTED+Shellable** (KeepAlive **UP** ~22:07–22:08; EMPTY flap **~16:51–22:00** ended); lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED (home-awake pre-camp — **not** camp GO, **not a Ben surface**); Option A **PASS**; Soft-530 UX **LIVE**. Soft-530 CLEAR at home ≠ this camp GO. **Doc local VW/NC healthy ≠ public vault CLEAR** — Z1 loopback is preflight only; [vault-clear-smoke.md](vault-clear-smoke.md) SSOT stays `vault.projectcar.ca`. | `camp-vault-on-doc-cutover.md` |
| Weekend dual-OPEN recovery glue (Soft-530 OPEN + vault OPEN): **home/lab/pre-cutover only** wake **Doc first, then McKing**; **SUPERSEDED for camp** — after Ben GO, Doc is sole Soft-530+vault(+NC) origin; **`lightning` ABSENT must not trigger McKing wake**; parallel only if both hosts appear on ListMachines. Identity lock: `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning` — no wrong-host Shell (`Mac.lan` is not Doc shop host). After quiet-ops: Doc first hop `doc-reappear-first-hop.md`; McKing first hop `mcking-reappear-first-hop.md` (machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7`). Independent CLEAR: Soft-530 = `api.` `/health` **200** + `ops.`/`app.` → `/login` + waitlist OPTIONS **200** + `cloud.` `status.php`; vault = `/alive` **200** + VW `/api/config`. Not Doc unfreeze, not **#82**, not Zone Direct Upload. Companions **HOLD**. Bitwarden gate is **vault CLEAR** (Ben GO after `/alive` + VW **2026.6.0** smoke) — **not** Soft-530 CLEAR. Soft-530 **OPEN** can coexist with vault CLEAR + Bitwarden work. Wake order only — Soft-530 post-CLEAR is `doc-lid-restore.md`; vault post-CLEAR is `vault-stay-up.md`. After both CLEARs + forensics: `post-dual-clear-go.md`. Extended OPEN quiet-ops: `soft-530-extended-open.md`. | `dual-host-outage.md` |
| Soft-530 extended OPEN — quiet-ops lock (Soft-530 OPEN **~24h** + vault OPEN + **Mac.lan only**): no Ben re-nag (Doc-wake + McKing wake already asked); Lead armed for `Docs-MacBook-Pro` reappear → `doc-reappear-first-hop.md` (machineId `95a229f5-9296-4a18-aa98-70fd300dabdf` **then** lid-restore); Chief armed for `lightning` reappear → `mcking-reappear-first-hop.md` (machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` **then** vault wake); Lookout api+vault stay armed (never delete to fix cron); **chronic-HOLD exception:** when **both** are calendar-day chronic HOLD, “Lookout continues” is **false for flip detection** — living 2026-09-15 **both chronic HOLD today**; interim Soft-530+vault SSOT smoke = Chief `*/20` + Lead probes (accept **≤~20m**); clock resets **America/Edmonton midnight**; next local day Lead **may** try one in-place re-arm then stickiness ≥3 `*/5`; Chronic Lookout ≠ quiet-ops failure ≠ Ben ping; chronic HOLD does **not** reopen Ben recreate; Soft-530 companions + brochure recreate stay **HOLD**; brochure-redirect paper continues (living Option A watch **GONE** — HOLD recreate, weekday Chief `*/20`; overnight/weekend **UNCOVERED**; `lookout-rearm-sop.md` · `overnight-baseline-stamp.md`); companions HOLD until Ben reopens (**not** weekend-only); Option A public SSOT; duration ≠ **#82** / unfreeze / Zone Direct Upload / Bitwarden / companion re-ask. CLEAR path still `post-dual-clear-go.md`. Next Soft-530 CLEAR after this lock: `soft-530-clear-smoke.md`. Next vault CLEAR after this lock: `vault-clear-smoke.md`. First hop when Doc reappears: `doc-reappear-first-hop.md`. First hop when McKing reappears: `mcking-reappear-first-hop.md`. Weekday overnight twin (Mon–Thu after last `*/20`): `soft-530-weekday-overnight.md`. | `soft-530-extended-open.md` |
| Soft-530 weekday overnight (Mon–Thu after last plan-improve `*/20` until next weekday first fire ~06:00): Lookout `api.` `/health` + vault `/alive` **only** (companions HOLD). Brochure `*/5` watch **GONE** — overnight Option A Dynamic **UNCOVERED** (interim = plan-improve `*/20`). **HOLD** a second Ben recreate ask. No overnight Zone / Garage. Last weekday `*/20` writes `overnight-baseline-stamp.md`. Living this fold: Soft-530 **OPEN 530 / CF 1033** + vault **OPEN 530 / CF 1033** **dual OPEN both 1033** (half-state **OVER**) (first Mon–Thu overnight after Sep 13–15 recovery; **was 502**); ListMachines Docs `95a229f5` **CONNECTED+Shellable** (KeepAlive **UP** ~22:07–22:08; EMPTY flap **~16:51–22:00** ended); lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED; freeze **`last_known`** `` `changeme` `` / **`5swmVz`** / **`4cf8924`**; Lookout api `ok=false` / vault `ok=false` **expected** (dual OPEN both 1033); Option A Redirect+UX **PASS**; Home **STALE** until **#82**. Soft-530 **CLEAR** overnight **≠** camp CDM soak **PASS** **≠** companions **≠** **#82** **≠** unfreeze **≠** Zone vault retarget **≠** Bitwarden auto. Same half-state ≠ dual CLEAR ≠ “incident closed.” **Never** `Mac.lan` as Doc. Don’t wake McKing docker as camp path. Paper only. | `soft-530-weekday-overnight.md` |
| Overnight baseline stamp (last weekday `*/20` one-liner before Lookout-only): **Soft-530 CLEAR + vault OPEN is a first-class half-state class** — ≠ dual OPEN ≠ dual CLEAR; morning must **not** score “incident closed.” Tonight **~22:00** (Tue 2026-09-15; last `*/20` **~17:20** dual OPEN is **historical**) is the first Mon–Thu overnight after Sep 13–15 recovery: Soft-530 **CLEAR** **`soft530_clear_since` ~22:00** (Lookout **530→200** @10:00pm MT; Lead public clear-smoke **PASS**) + same-day OPEN dwell **~16:36–22:00**; vault **OPEN 530 / CF 1033** connector-down (public; **was 502**); ListMachines Docs `95a229f5` **CONNECTED+Shellable** (KeepAlive **UP** ~22:07–22:08; EMPTY flap **~16:51–22:00** ended); lightning **ABSENT**; `Mac.lan` may CONNECTED; freeze **`last_known`** `` `changeme` `` + **`5swmVz`** / **`4cf8924`** (not last_known-only); Option A Redirect **PASS** + Soft-530 UX **LIVE**; Lookout api+vault **armed**; quiet-ops **HOLD**; **#82** not auto-GO; **`shared_fleet_card_stale=false`**. Shared living-host **amend DONE ~22:08**. Shared still **non-SSOT**. Shared never overrides Docs/#70 SSOT. Home **STALE** until **#82**. Morning DIFF: age OPEN from last relapse (**~16:36**); Soft-530 still OPEN + same `soft530_open_since` = SAME; Soft-530 **CLEAR→OPEN** = DIFF; vault still **530 / CF 1033** = SAME; vault **502 ↔ 530/1033** = subclass field DIFF (Lead/Lookout ≠ Ben re-nag); Docs **CONNECTED→ABSENT** = DIFF; `changeme` off = freeze DIFF (unfreeze signal). Soft-530 **CLEAR** overnight **≠** camp CDM soak **PASS** **≠** companions **≠** **#82** **≠** unfreeze **≠** Zone vault retarget **≠** Bitwarden auto. **Never** `Mac.lan` as Doc. **No** second Ben Lookout recreate. **No** overnight Zone/Garage. Don’t wake McKing docker as camp path. **Same-class = stay quiet** (`611c267`). Complements `soft-530-weekday-overnight.md`. Paper only. | `overnight-baseline-stamp.md` |
| Doc first reappear after quiet-ops — **first hop** (paper): when `Docs-MacBook-Pro` returns on ListMachines, (1) assert machineId **`95a229f5-9296-4a18-aa98-70fd300dabdf`** — **never** `Mac.lan` / Porsche as Doc; (1a) classify **cold-boot** (Docs **ABSENT ≥1 calendar day** **or** Soft-530 **OPEN** 1033 **≥24h** with Docs **ABSENT**) vs overnight **warm wake** (`<24h` ABSENT after a prior CLEAR); (1b) on **cold-boot**, **before** smoke: verify cloudflared LaunchAgent/service up + shop-api/shop-web KeepAlive loaded + **note** CDM/caffeinate; (2) run `soft-530-clear-smoke.md` (`/health` **200** + OPTIONS CORS apex+www + POST **422**/**201**) after that pre-smoke on the **asserted** host; (3) **only if CLEAR** → Soft-530 post-CLEAR forensics on `doc-lid-restore.md`; (4) then `post-dual-clear-go.md` — **never auto-fire** **#82** / unfreeze / **#79.1** / companions / Zone upload / Bitwarden; (5) Chief-to-doc hop **only after** CLEAR smoke. **Never** treat a multi-day lid as overnight sleep. Vault CLEAR stays Chief `lightning` / McKing (`mcking-reappear-first-hop.md` · `vault-clear-smoke.md`). Anti-goals: `Mac.lan` Shell ≠ Doc evidence; multi-day lid ≠ overnight sleep; duration ≠ Ben wake; **CLEAR ≠ honesty-off**; not companion re-ask. Living this fold: Soft-530 still **OPEN** CF **1033**; vault still **OPEN** **502**; **Mac.lan + lightning** / Docs **ABSENT** since Fri 2026-09-11 ~16:48 (**~4d** — **cold-boot**); Option A **PASS**; Soft-530 UX **LIVE**; Lookout **both chronic HOLD today**; **#82** Ben GO only. Paper only — does **not** invent a reappear or CLEAR. | `doc-reappear-first-hop.md` |
| McKing first reappear after quiet-ops / extended vault OPEN — **first hop** (paper): when `lightning` returns on ListMachines, (1) assert machineId **`9067d14b-46e5-4ef0-82d5-fce0febdc8f7`** (label **lightning**) — **never** `Mac.lan`; (2) run `vault-clear-smoke.md` (`/alive` **200** + `/api/config` VW **2026.6.0** + `docker.service` **enabled** + **active** + classify **502→200**; HTTP-only ≠ CLEAR) after vault-stay-up on the **asserted** host; (3) **only if CLEAR** → vault post-CLEAR forensics on `vault-stay-up.md` (`d88cacb`); (4) Soft-530 independence — Bitwarden gate is **vault CLEAR** (Ben GO after `/alive` + VW **2026.6.0** + docker permanence), **not** Soft-530 CLEAR; Soft-530 **OPEN** can coexist with vault CLEAR + Bitwarden work; **never auto Bitwarden**; **never** treat vault CLEAR as Soft-530 CLEAR, unfreeze, or **#82** GO. Twin of `doc-reappear-first-hop.md`. Living this fold: Soft-530 **OPEN** `api.`/`app.`/`ops.`/`cloud.` **530** CF **1033**; waitlist OPTIONS **530**; vault `/alive` **502** (docker dead); ListMachines **`Mac.lan` + `lightning`** (`docs=ABSENT`); hop already ran ~19:10 — still blocked on Ben sudo; **#82** still Ben GO; quiet-ops **HOLD**. Morning `*/20` **must not** re-ask (`overnight-baseline-stamp.md` first-hop absorb). Paper only — does **not** invent CLEAR or a second Ben sudo / docker page. | `mcking-reappear-first-hop.md` |
| Soft-530 CLEAR recovery smoke after **extended OPEN** (>~24h) — **first** public card **before** Owner desk / **#79.1** / unfreeze talk: `api.` `/health` **200**; waitlist OPTIONS **200** + CORS Origin `https://projectcar.ca` **and** www; waitlist POST **422** or **201** (route alive, not just tunnel-up); `app.`/`ops.` may still **307**→`/login` with print `` `changeme` `` (freeze, **not** Soft-530); `cloud.` `status.php` **200** when Doc public is up. ≠ **#82** GO ≠ companion re-ask ≠ Bitwarden; Soft-530 CLEAR alone ≠ unfreeze; vault CLEAR still McKing (`vault-clear-smoke.md`). CLEAR path still `post-dual-clear-go.md` (**never auto-fire**). Living this fold: Soft-530 **OPEN 530 / CF 1033** (`/health` **200**; Origin CORS OPTIONS — **bare 405 ≠ OPEN**); vault still **OPEN** **530 / CF 1033**; ListMachines Docs `95a229f5` **CONNECTED+Shellable** (KeepAlive **UP** ~22:07–22:08; EMPTY flap **~16:51–22:00** ended); lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED. Half-state ≠ dual CLEAR. Paper only — does **not** invent vault CLEAR. | `soft-530-clear-smoke.md` |
| Vault CLEAR recovery smoke after **extended OPEN** (**502** since ~19:45 MT Sep 13) — **first** public card **before** Bitwarden import/rotate or desk talk: `/alive` **200**; `/api/config` **200** + McKing **2026.6.0** class (not Doc **2025.12.0**); on `lightning` `docker.service` **enabled** + **active** (optional: `mcking` in `docker` group); `/alive` **200** + docker **disabled/inactive** = **NOT CLEAR / recurrence risk** (reboot re-OPENs like ~19:45 Sep 13); HTTP-only ≠ CLEAR; living subclass **530 / CF 1033** (was 502); classify **1033→200** vs **502→200** by living subclass — do **not** run docker permanence as the 1033 first hop; Soft-530 may still be **OPEN** — vault CLEAR alone ≠ Soft-530 CLEAR ≠ **#82** ≠ unfreeze ≠ companions; Bitwarden gate is **vault CLEAR** + this smoke, then **Ben GO**, **never auto-fire**. Soft-530 **OPEN** can coexist — do **not** block Bitwarden on Soft-530 CLEAR. ≠ Zone Direct Upload ≠ **#82** ≠ unfreeze / **#79.1** ≠ companion re-ask ≠ Ben docker / Doc-wake re-nag ≠ Soft-530 CLEAR smoke (`502ab2e`). Forensics still `vault-stay-up.md` (`d88cacb`). CLEAR path still `post-dual-clear-go.md` (**never auto-fire**). Living this fold: Soft-530 **OPEN 530 / CF 1033**; vault still **OPEN** **530 / CF 1033**; ListMachines Docs `95a229f5` **CONNECTED+Shellable** (KeepAlive **UP** ~22:07–22:08; EMPTY flap **~16:51–22:00** ended); lightning **ABSENT** (McKing sleep intentional); `Mac.lan` may CONNECTED; Option A **PASS**; Soft-530 UX **LIVE**; **#82** still Ben GO. Half-state ≠ this card. Paper only — does **not** invent vault CLEAR. | `vault-clear-smoke.md` |
| Post-dual-CLEAR Ben GO sequencer (paper — **never auto-fire**): after Soft-530 CLEAR + vault CLEAR + forensic papers `55e10d0` + `d88cacb`, KeepAlive → **#82** Worker-live (upload + **mandatory** purge/freshness) → **brochure lane** **#83** ∥ **Bulk Phase1** (not Bulk→#83; Member after Bulk) (**never #81**). KeepAlive→**#82** is the *default ordered menu after dual CLEAR*. Soft-530 **OPEN ≠ #82 blocked** — Ben may GO Worker-live while Soft-530/vault still **OPEN** (brochure is CF Worker/Zone). After **extended OPEN**, Soft-530 first card is `soft-530-clear-smoke.md`; vault first card is `vault-clear-smoke.md`. Unfreeze + **#79.1** is **Shop OS parallel**, not “next after **#82**.” Shop OS **acceptance smoke** (lane-done): public `app.`/`ops.` `/login` without `changeme`; tip / `BUILD_ID` ≠ `4cf8924` / `5swmVz`; `/health` **200** + waitlist OPTIONS CORS Origin `https://projectcar.ca` (+ www); **#79.1** merge-alone does **not** clear public `changeme`. Soft-530 CLEAR alone ≠ that card. Not **#82**, not Zone Direct Upload, not companions re-ask, not Bitwarden. Dual CLEAR ≠ **#82** auto-go. Quiet-ops duration still ≠ auto-GO. Living this fold: Soft-530 **OPEN 530 / CF 1033** + vault **OPEN 530 / CF 1033** is **half-state** — **do not enter** until vault CLEAR. Soft-530 CLEAR at home ≠ camp GO. Soft-530 **CLEAR ≠ #82** ≠ unfreeze ≠ Bitwarden. | `post-dual-clear-go.md` |
| Soft-530 post-CLEAR stay-up evidence (next CF 1033): after lid-restore CLEAR, stamp cloudflared lastExit / KeepAlive loaded / caffeinate·CDM / Tailscale before walking away. Dual-host-outage is wake order; this is **Doc forensics**. Living Soft-530 is **OPEN 530 / CF 1033** this fold — post-CLEAR forensics wait on CLEAR. Paper does **not** invent vault CLEAR / dual CLEAR. | `doc-lid-restore.md` · `api-stay-up.md` · `STATUS.md` Live |
| Vault post-CLEAR stay-up evidence (next vault flip): after McKing vault CLEAR, stamp cloudflared.service ActiveState/Result/ExecMainStatus (or lastExit) / VW container healthy + `DOMAIN=https://vault.projectcar.ca` / Tailscale / whether recovery was **1033→200** vs **502→200**. Dual-host-outage is wake order; Soft-530 post-CLEAR is Doc forensics; this is **McKing forensics**. Living class: Soft-530 **CLEAR** (`/health` **200**); vault still **OPEN 530 / CF 1033** connector-down on `/alive` + `/api/config` (**was 502**). Recovery fork: **1033** → lightning Healthy / cloudflared first; **502** → `docker.service` enabled+active. Subclass flip = Lead/Lookout field DIFF ≠ Ben re-nag. Paper does **not** invent vault CLEAR. | `vault-stay-up.md` · `STATUS.md` Live |
| Weekend Soft-530 coverage (plan-improve off Sat/Sun): KeepAlive / lid-close control plane; no weekend Zone Direct Upload / Worker; first Monday plan-improve resumes Soft-530 smoke. While `brochure-option-a-redirect-watch` is **GONE**, weekend Option A Dynamic redirect is **UNCOVERED — accept that risk**. Do **not** invent weekend plan-improve / Zone / Garage Option A coverage. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **HOLD** a second Ben recreate ask. Friday last weekday `*/20` (~17:40 MT) hands **into this** weekend coverage. **Mon–Thu nights** after last `*/20` until next weekday first fire (~06:00) are `soft-530-weekday-overnight.md` — **not** the Sat/Sun card. Vault flips stay on `vault-stay-up.md`. Dual-OPEN glue: `dual-host-outage.md`. Soft-530 post-CLEAR: `doc-lid-restore.md`. Vault post-CLEAR: `vault-stay-up.md`. After both CLEARs: `post-dual-clear-go.md` (**never auto-fire**). | `STATUS.md` Live / Locks · `api-stay-up.md` · `shop-web-stay-up.md` · `doc-lid-restore.md` · `vault-stay-up.md` · `dual-host-outage.md` · `soft-530-weekday-overnight.md` · `lookout-rearm-sop.md` · `post-dual-clear-go.md` |
| Shop OS GitHub Actions quality gate (pytest + web typecheck / `next build`; git-only — **not** Doc deploy, **not** unfreeze GO) | `shop-os-ci.md` |
| Member UI → projectcar.ca (plan only; **Ben GO**) | `member-host-cutover.md` |
| Member path-split on projectcar.ca / www (Zone; plan only; **capacity-blocked** by Option A **FULL 10/10** — Ben GO only after **#82** Worker-live + Bulk Phase1) | `member-zone-edge.md` |
| Brochure Worker CI / Option A **FULL 10/10** receipt (paper — **after #82 Ben GO**, Soft-530-independent; Soft-530 **OPEN ≠ #82 blocked**; frozen Sep 10 2026 Redirect inventory is Docs SSOT; Direct Upload → **mandatory** purge apex+www → body freshness; **#82** PASS ≠ retire Dynamic (`49ccf90`); **never #81**) | `brochure-worker-ci.md` (upload click-path stays `brochure-worker-deploy.md`) |
| Brochure Option A Lookout watch **spec** while Soft-530 OPEN. Living watch **GONE** (deleted to fix stalled cron; Auto-review blocked recreate). **HOLD** after one Ben ask. Weekday Chief `*/20` **+ Soft-530 UX**. Overnight/weekend Dynamic redirect **UNCOVERED**. **Redirect-only PASS ≠ Soft-530 UX PASS.** Soft-530 UX smoke URL SSOT = **root** `/styles.css?v=36` + `/waitlist.js?v=3` (not `/assets/` — those 404s are **not** a regression). Flip Chief+Lead only when armed. **Not** companion re-ask. **#78 LIVE-SUPERSEDED**. Sep 9 baseline still **IN FLIGHT** → stamp **LIVE 10/10**. | `brochure-redirect-watch.md` |
| Lookout rearm SOP: **never delete** an armed watch to fix stalled cron. Prefer in-place update / pause-resume / re-save. **Lookout stall ≠ vault HTTP flip** — `enabled:true` + cron-stalled is a coverage class (vault or api). **Re-arm success ≠ first fire** — stickiness ≥3 consecutive `*/5` windows (≥10–15m). Living ~09:14 MT: vault first fire then ~22m stall; api stalled ~56m. **Chronic** ≤1 pause/resume per calendar-day (clock resets **America/Edmonton midnight**). Living 2026-09-15: **both chronic HOLD today** — quiet-ops “Lookout continues” is **false for flip detection**. Lead **may GO** in-place re-arm of **both** if the day's 1× is unused; next local day Lead **may** try one again then stickiness ≥3 `*/5`; **HOLD** Ben recreate; until stickiness, interim = Chief `*/20` + Lead probes (accept **≤~20m**) — not a Ben ping; Soft-530 api remaining OK-ish is **not** living contrast. Delete already happened + Auto-review blocked recreate → **HOLD** after one Ben ask. Weekday Option A = Chief `*/20` redirect **+ Soft-530 UX**. Overnight/weekend Dynamic redirect **UNCOVERED — accept that risk**. Do **not** invent overnight plan-improve / Zone / Garage coverage. **Next Option A smoke** = first weekday `*/20` after ~06:00, **or** Ben-approved Lookout recreate. **Redirect-only PASS ≠ Soft-530 UX PASS.** After Ben approves: enabled + first fire ≤ **10m** + baseline **LIVE 10/10**. Soft-530 companions **HOLD**. Not **#82** merge nag / Dynamic wipe / honesty-off / companions rearm / invent overnight redirect smoke / Ben recreate-docker-Doc-wake from a stall / call healthy on first fire / loop pause/resume on chronic. **#78** is **LIVE-SUPERSEDED**. | `lookout-rearm-sop.md` |
| Temporary `app.` alias cut (plan only; STATUS Next #2 — do **not** execute) | `app-alias-cut.md` |
| Public MVP cut-vs-keep (draft; not a Ben lock) | `ship-mvp-cut.md` |
| Brochure Worker Direct Upload (`projectcar-brochure`) | `brochure-worker-deploy.md` |
| Brochure security + cache headers (P2-4; **LIVE** — do **not** re-apply) | `brochure-security-headers.md` |
| Brochure → Classic Pages git (plan only; blocked on CF ↔ GitHub auth; frozen Redirect inventory = **hard cutover gate**; **#82** PASS ≠ retire Dynamic) | `brochure-pages-cutover.md` |
| Google Calendar OAuth / two-way sync (plan only; STATUS Next #6 — not a GO) | `google-calendar-oauth.md` |
| McKing shop-host cutover (**PARKED for camp weeks** / desk/lab optional — **not camp SSOT**; later home/lab Docker + tunnel hostname reuse; Soft-530 dual-run gate before public CF cut; **not** Next #1, **not** GO). Camp GO follows `camp-vault-on-doc-cutover.md` | `mcking-shop-host-cutover.md` |
| Ops demo hardening — Soft-530-CLEAR **Next Ben GO** (**#79**, **parallel to #82**): Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull** (merge ≠ public `/login` clear while freeze holds `4cf8924` / **`5swmVz`**) + optional Access on `ops` / `app` until OIDC (plan only; do **not** execute Garage/Zone from this tip-fold) | `ops-demo-hardening.md` |
| Dual-tunnel ownership (**separate** from shop CF cutover): **Home/lab** Doc = `cloud.` + `api.` + `app.` + `ops.` only; McKing-only `vault.` → `:8222` (`/alive` Lead **200** prefer; `/api/config` **2026.6.0**, Chief verified) + **unpublished** NC. **Camp SSOT** (after Ben GO): Doc hosts Soft-530 shop + vault (+ NC); McKing may sleep — `camp-vault-on-doc-cutover.md`. McKing shop-host + vault-stay-up are **PARKED for camp weeks** / desk/lab optional — Zone/Doc do **not** follow those cards at camp GO. Soft-530 independence is **home/lab only**. Public `cloud.` stays on **Doc** until Ben GO. McKing `/opt/mission-control` NC is lab/hub loopback-only **at home/lab**. Paper path = Tailscale Serve HTTPS (mirror VW Serve at `lightning.tailbe8f55.ts.net`) + `NEXTCLOUD_TRUSTED_DOMAINS` MagicDNS — **not** a `cloud.*` CF cutover. Do **not** point `cloud.*` tunnel/CNAME at McKing until `status.php` parity vs Doc **and** Ben GO. Soft-530 five-row gate = shop hosts only (vault EXCLUDED) **at home/lab**. Lookout vault flip watch **LIVE/armed** (`enabled:true`, Lookout confirmed) on `/alive` every **5m** (fallback `/api/config` if `/alive` 404s) — Soft-530 / `api.` `/health` can stay green while vault dies **at home/lab**; vault watch ≠ Soft-530 / Doc lid-restore. Weekend dual-outage proved the planes fail independently **at home/lab**. Never touch the Doc mission-control tunnel token for `vault.` **without camp Ben GO**. | `camp-vault-on-doc-cutover.md` · `vault-stay-up.md` · `STATUS.md` Live / Locks · `home-lab-specification.md` · `api-stay-up.md` · `mcking-shop-host-cutover.md` |

---

## Elsewhere

| Need | Doc |
|------|-----|
| What’s live / host split | `STATUS.md` |
| Stack, host plan, bans | `platform-architecture.md` |
| Nextcloud / Vaultwarden / cockpit (parked). Dual-tunnel + vault LIVE on McKing (VW not Doc compose host); shop CF cutover paper; Soft-530 five-row = Doc shop hosts only | `mission-control-architecture.md` |
| Live hub / dual-tunnel machine map (Doc shop hosts + McKing vault LIVE **at home/lab**; camp = Doc shop + vault). Vault stay-up essay: `vault-stay-up.md`. Camp cutover: `camp-vault-on-doc-cutover.md`. Dual-OPEN glue: `dual-host-outage.md` | `home-lab-specification.md` |
| Older hub probe on Doc (2026-08-16 rows can be stale) | `nextcloud-progress.md` |
| Doc apps / LaunchAgents / sleep | `doc-software-baseline.md` |
| Public site origin + tunnel | `website-webapp-specification.md` |
| Shop app first code slice | `project-car-application-specification.md` §13 |
