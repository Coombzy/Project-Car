# Plan-improve weekday coverage hole — mid-weekday Option A UNCOVERED

**Status:** Paper — coverage class lock, **not** a GO  
**Updated:** 2026-09-16 ~11:30 America/Edmonton  
**Related:** `STATUS.md` (Reality · living Soft-530 **OPEN** **`soft530_open_since` ~08:41** + vault **OPEN 530 / CF 1033** + ListMachines **Mac.lan only** + **`shared_fleet_card_stale=true`** + this pointer), [lookout-rearm-sop.md](lookout-rearm-sop.md) (Option A watch **GONE**; **HOLD** second Ben recreate ask; weekday interim = Chief `*/20`), [brochure-redirect-watch.md](brochure-redirect-watch.md) (watch **spec**; Redirect+UX PASS ≠ overnight/mid-weekday coverage), [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md) (**Mon–Thu overnight** gap — **not** this mid-weekday hole), [overnight-baseline-stamp.md](overnight-baseline-stamp.md) (last weekday `*/20` one-liner; Lookout watch **GONE** is a stamp field), weekend Soft-530 coverage (`84107e7` — Sat/Sun plan-improve-off; same Option A **UNCOVERED** class), [shared-fleet-cards.md](shared-fleet-cards.md) (Shared last DONE **CLEAR ~22:00** **stale** vs living **OPEN ~08:41**; amend **HOLD** until Docs Shellable), [doc-lid-restore.md](doc-lid-restore.md) (**never** treat `Mac.lan` / `f74bffa2` as Docs lid-restore), [deployment-guide.md](deployment-guide.md) (Monday paper map)

When Soft-530 is **OPEN** **and** brochure Lookout Option A Dynamic watch is **GONE**, interim Option A smoke is Chief plan-improve **`*/20` only**. A **weekday** fire gap **>~40m** means Option A is **mid-weekday UNCOVERED** — **not** overnight/weekend-only.

**Next Option A smoke** = the recovering plan-improve fire (Redirect **10/10** + Soft-530 UX **root** assets) **or** Ben-approved Lookout recreate. **HOLD** a second Ben recreate ask (`lookout-rearm-sop` already one-ask).

This fold executes **none** of Lookout recreate, Zone upload, Garage, **#82**, unfreeze, companions, Bitwarden, Shared write, or a Ben re-nag.

---

## Lock — weekday `*/20` hole ≠ overnight/weekend hole

| This is | This is not |
|---------|-------------|
| Soft-530 **OPEN** + Lookout `brochure-option-a-redirect-watch` **GONE** | An armed Option A `*/5` flip watch |
| Interim Option A smoke = Chief plan-improve **`*/20` only** | Overnight Lookout-only coverage ([soft-530-weekday-overnight.md](soft-530-weekday-overnight.md)) |
| A **weekday** fire gap **>~40m** | Overnight after last `*/20` ~17:40, or Sat/Sun plan-improve-off |
| Option A **mid-weekday UNCOVERED** | Overnight/weekend **UNCOVERED** only (`c200d71`) |
| Recovering `*/20` Redirect **10/10** + Soft-530 UX **root** PASS | Worker body freshness / **#82** done |
| **HOLD** second Ben Lookout recreate ask | A new Ben page because the gap happened |

**Cadence honesty.** Chief `*/20` is every **20m**. Two missed windows ≈ **>~40m**. That is the weekday hole threshold. Overnight/weekend uncovered stays the **accepted** Lookout-GONE class. This card names the **same-class hole inside weekday hours** when `*/20` itself goes dark.

---

## Living proof (~11:30 America/Edmonton)

| Field | Living |
|-------|--------|
| **Soft-530** | **OPEN SAME-CLASS** ~**2.8h** since **`soft530_open_since` ~08:41**. `api.`/`app.`/`ops.` **530 / CF 1033**. |
| **Vault** | still **OPEN 530 / CF 1033** — **EXPECTED**. |
| **Combined class** | Dual OPEN both **1033**. **≠ dual CLEAR.** |
| **Lookout Option A Dynamic** | **GONE.** Recreate **HOLD** until Ben GO. |
| **Fire gap** | **~08:49→~11:29** (~**2.5h**) — weekday hours, Soft-530 **OPEN**, watch **GONE**. **>~40m** → **mid-weekday UNCOVERED**. |
| **This Chief fire ~11:29** | Option A Redirect **PASS** + Soft-530 UX **LIVE** (Redirect **10/10**; root `/styles.css?v=36` + `/waitlist.js?v=3` + Discord honesty). Home still **STALE** until **#82**. |
| **ListMachines** | **Mac.lan only** (not Docs). **EMPTY→Mac.lan-only is non-progress.** **Never** treat `f74bffa2` as Docs lid-restore. Docs still **ABSENT**. **Never** `Mac.lan` as Doc (`95a229f5`) or McKing (`9067d14b`). |
| **Shared** | Last DONE still **CLEAR since ~22:00** Sep 15. Living is **OPEN ~08:41**. **`shared_fleet_card_stale=true`**. Amend **HOLD** until next CLEAR recover + ≥30m SAME-CLASS + Docs **CONNECTED+Shellable**. **No backfill.** |
| **Camp / post-dual-clear-go** | **HOLD.** Coverage hole **≠** camp GO. |

Receipt: [overnight_2026-09-16_1130_weekday_coverage_hole_verify.md](overnight_2026-09-16_1130_weekday_coverage_hole_verify.md). Prior missed CLEAR: [missed-clear-window-shared-supersede.md](missed-clear-window-shared-supersede.md). Overnight complement: [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md).

---

## When Soft-530 OPEN + Option A watch GONE

1. Stamp interim Option A coverage as Chief **`*/20` only**. Do **not** invent `enabled:true`.
2. If a **weekday** gap between `*/20` fires is **>~40m**, stamp Option A **mid-weekday UNCOVERED**. Do **not** collapse that into overnight/weekend-only.
3. **Next smoke** = the recovering plan-improve fire (Redirect **10/10** + Soft-530 UX **root** assets) **or** Ben-approved Lookout recreate.
4. **HOLD** a second Ben recreate ask. The hole is accepted risk while the watch stays **GONE**.
5. Absorb ListMachines **EMPTY→Mac.lan-only** as **non-progress**. `f74bffa2` is **not** Docs. Do **not** run [doc-lid-restore.md](doc-lid-restore.md) on it.
6. Keep **`shared_fleet_card_stale=true`** while Shared living-host still says **CLEAR ~22:00** Sep 15 and living Soft-530 is **OPEN ~08:41**. Shared amend waits Docs **CONNECTED+Shellable**. **No backfill.**

---

## ListMachines EMPTY→Mac.lan-only (non-progress)

| Hit | Meaning | Do not |
|-----|---------|--------|
| **EMPTY** | Local-exec **ABSENT**. Docs **ABSENT**. | Treat EMPTY as Soft-530 class. |
| **Mac.lan only** (`f74bffa2`) | Ben laptop. **Not** Docs. **Not** McKing. | Treat as Docs lid-restore. Shell as Doc. Score progress vs EMPTY. |
| **Docs `95a229f5` CONNECTED+Shellable** | The only host that may run lid-restore / Shared amend / freeze git. | Skip identity. Treat `Mac.lan` as that host. |

**EMPTY→Mac.lan-only** is a laptop flap, not a Doc wake. Same identity lock as [overnight-baseline-stamp.md](overnight-baseline-stamp.md) `Mac.lan` flap vs ~12:45 — **refresh** the stamp, **no Ben surface**, **no lid-restore**.

---

## Shared Reality (`shared_fleet_card_stale=true`)

STATUS Reality stamps **`shared_fleet_card_stale=true`** while Fleet Shared `shop-os-mc-plan.md` living-host still says Soft-530 **CLEAR ~22:00** Sep 15 and living is **OPEN ~08:41**. Shared amend waits Docs **CONNECTED+Shellable**. **Never** `Mac.lan`. **Do not** backfill OPEN-since-**~08:41** or the missed CLEAR **~07:25–~08:41**.

Docs/#70 is SSOT. Shared **never** overrides. Essay: [shared-fleet-cards.md](shared-fleet-cards.md) · [missed-clear-window-shared-supersede.md](missed-clear-window-shared-supersede.md).

---

## Anti-goals

| Anti-goal | Why |
|-----------|-----|
| **Not unfreeze** | Freeze stays **`last_known`** `` `4cf8924` `` / **`5swmVz`**. Coverage hole ≠ pull. |
| **Not #82 auto-GO** | Redirect+UX **PASS** ≠ Worker body freshness. Home **STALE** until **#82**. Soft-530 **OPEN ≠ #82 blocked**, but this card is **not** a GO. |
| **Not Zone upload** | No Direct Upload / purge from a `*/20` hole. |
| **Not companions re-ask** | Soft-530 ops/app stay **HOLD**. Gap ≠ companion GO. |
| **Not a second Ben Lookout recreate** | One ask already. Hole **accepted** while watch **GONE**. |
| **Not overnight/weekend-only** | This hole is **mid-weekday**. Overnight card stays [soft-530-weekday-overnight.md](soft-530-weekday-overnight.md). |
| **Not `f74bffa2` as Docs lid-restore** | Mac.lan only. Non-progress. |
| **Not Shared backfill / stale=false** | Shared still **CLEAR ~22:00**. Living **OPEN ~08:41**. **`shared_fleet_card_stale=true`**. |

**#82** Ben GO / companions **HOLD** / freeze `4cf8924` / `5swmVz` / Option A overnight uncovered (`c200d71`) — **unchanged**. This file executes **none** of those.

---

## Do not

- Treat a weekday `*/20` gap **>~40m** as overnight/weekend-only uncovered
- Invent Lookout Option A `enabled:true` while the watch is **GONE**
- Re-ask Ben to recreate Lookout Option A from this hole
- Treat Redirect+UX **PASS** on the recovering fire as **#82** / Worker freshness
- Treat ListMachines **EMPTY→Mac.lan-only** / `f74bffa2` as Docs lid-restore
- Flip **`shared_fleet_card_stale=false`** while Shared still says **CLEAR ~22:00** and living is **OPEN ~08:41**
- Backfill Shared OPEN-since-**~08:41** or the missed CLEAR
- Auto-fire unfreeze / **#82** / Zone upload / companions
- Re-nag Ben
