# Status — 2026-09-14

Living one-pager: what’s live, what’s next, locks. Product detail stays in the start-here specs. Stay-up / CORS / brochure-deploy runbooks live elsewhere — this file is not a runbook. **Monday #70 paper map** (when-to-open): [deployment-guide.md](deployment-guide.md) Soft-530 living paper index. Living ops: [doc-lid-restore.md](doc-lid-restore.md) (process wake only · **post-CLEAR stay-up evidence** for the next CF 1033) · [doc-unfreeze.md](doc-unfreeze.md) (Ben GO pull) · [shop-web-stay-up.md](shop-web-stay-up.md) · [api-stay-up.md](api-stay-up.md) · [vault-stay-up.md](vault-stay-up.md) (McKing VW + cloudflared — weekend dual-outage proved Soft-530 ≠ vault · **post-CLEAR stay-up evidence** for the next vault flip) · [dual-host-outage.md](dual-host-outage.md) (weekend dual-OPEN **wake order** — Doc first, then McKing; `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning`; Doc forensics = lid-restore; McKing forensics = vault-stay-up) · [soft-530-extended-open.md](soft-530-extended-open.md) (quiet-ops lock once Soft-530 OPEN **~24h** + vault OPEN + **Mac.lan only** — no Ben re-nag; Lead/Chief stay armed; companions HOLD until Ben reopens, **not** weekend-only; Option A public SSOT; duration ≠ **#82** / unfreeze / Zone / Bitwarden / companion re-ask; CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md)) · [doc-reappear-first-hop.md](doc-reappear-first-hop.md) (**first hop** when `Docs-MacBook-Pro` reappears after quiet-ops — assert machineId `95a229f5-9296-4a18-aa98-70fd300dabdf`; **never** `Mac.lan` / Porsche as Doc; then CLEAR smoke; only if CLEAR → forensics; then sequencer — **never auto-fire**; Chief-to-doc hop only after CLEAR smoke; vault stays Chief `lightning`) · [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (**first hop** when `lightning` / McKing reappears after extended vault OPEN / quiet-ops — assert machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` label **lightning**; **never** `Mac.lan`; then vault-clear-smoke `/alive` **200** + `/api/config` VW **2026.6.0** + **502→200**; only if CLEAR → vault post-CLEAR forensics; Soft-530 independence — Bitwarden stays blocked until Soft-530 CLEAR separate; **never auto Bitwarden**; vault CLEAR ≠ Soft-530 CLEAR ≠ unfreeze / **#82** GO) · [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (**first** recovery smoke after extended-OPEN Soft-530 CLEAR — **before** desk / **#79.1** / unfreeze; ≠ **#82** / companions / Bitwarden / unfreeze; **CLEAR ≠ honesty-off** — **#80** Discord honesty + `waitlist.js?v=3` fail-soft stay; vault CLEAR still McKing) · [vault-clear-smoke.md](vault-clear-smoke.md) (**first** recovery smoke after extended-OPEN vault CLEAR — **before** Bitwarden / desk; `/alive` **200** + `/api/config` **200** / McKing **2026.6.0** class not Doc **2025.12.0**; this OPEN is **502→200** not CF **1033→200**; vault CLEAR alone ≠ Soft-530 CLEAR ≠ **#82** ≠ unfreeze ≠ companions; Bitwarden stays Ben GO, **never auto-fire**; ≠ Soft-530 CLEAR smoke `502ab2e`; forensics still `@d88cacb`) · [post-dual-clear-go.md](post-dual-clear-go.md) (post-dual-CLEAR **Ben GO sequencer** — **never auto-fire** on CLEAR; KeepAlive → **#82** Worker-live (upload + **mandatory** purge/freshness) → **brochure lane** **#83** ∥ **Bulk Phase1** (not Bulk→#83; Member after Bulk) — **never #81**; unfreeze+#79.1 is **Shop OS parallel**, not “next after **#82**” — lane-done = **unfreeze + #79.1 acceptance smoke**; forensics `@55e10d0` + `@d88cacb` already ack’d) · [shop-os-ci.md](shop-os-ci.md) (git-only Shop OS CI — **not** Doc unfreeze) · [brochure-worker-deploy.md](brochure-worker-deploy.md) · [brochure-worker-ci.md](brochure-worker-ci.md) (paper CI + Option A receipt — Direct Upload → **mandatory** purge apex+www → body freshness; **never #81**) · [brochure-redirect-watch.md](brochure-redirect-watch.md) (Lookout Option A watch **spec** — living watch **GONE**; Auto-review blocked recreate; interim Chief `*/20` **+ Soft-530 UX**; **Redirect-only PASS ≠ Soft-530 UX PASS**) · [lookout-rearm-sop.md](lookout-rearm-sop.md) (**never delete** an armed Lookout watch to fix stalled cron; **HOLD** after one Ben ask; interim `*/20` + Soft-530 UX; after Ben approves: enabled + first fire ≤ **10m** + baseline **LIVE 10/10**; Soft-530 companions **HOLD**; **#78 lookout-resume** is **LIVE-SUPERSEDED**) · [brochure-security-headers.md](brochure-security-headers.md) (P2-4 **LIVE**). Member host cutover (plan only): [member-host-cutover.md](member-host-cutover.md). Member edge / path-split (plan only; **capacity-blocked** by Option A **FULL 10/10** — Ben GO only after **#82** Worker-live + Bulk Phase1; **#83** is CI-only after **#82** base — **not** Bulk-gated; **never #81**; `/member*` ranked **BEFORE** brochure Redirect pack; `/member/login` must **not** 301 into `membership.html`): [member-zone-edge.md](member-zone-edge.md). Temporary `app.` alias cut (plan only): [app-alias-cut.md](app-alias-cut.md). Brochure → Classic Pages git (plan only; skipped until CF ↔ GitHub auth; frozen Redirect inventory = **hard cutover gate**; outranked by Next #1 + Member edge): [brochure-pages-cutover.md](brochure-pages-cutover.md). Proposed public-MVP cut-vs-keep (draft, not a Ben lock): [ship-mvp-cut.md](ship-mvp-cut.md). Google Calendar OAuth / two-way sync (plan only; STATUS Next #6 — not a GO): [google-calendar-oauth.md](google-calendar-oauth.md). Ops demo hardening (**#79** — Soft-530-CLEAR **Next Ben GO**, **parallel to #82**; Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull** — merge alone will **not** clear public login while freeze holds (`4cf8924` / **`5swmVz`**); plan only — do **not** execute Garage/Zone from this tip-fold): [ops-demo-hardening.md](ops-demo-hardening.md). McKing shop-host cutover (path **OPEN** ~10:32; still plan only — hop OPEN ≠ cutover GO — **not** Next #1, **not** a GO): [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md). Dual-tunnel ownership (**separate** living-ops row — **not** the shop CF cutover): Doc tunnel = `cloud.` + `api.` + `app.` + `ops.` only; McKing-only `vault.projectcar.ca` is **McKing-owned** (`cloudflared` → `localhost:8222`; `/api/config` **2026.6.0** when CLEAR). **Living:** vault independently **OPEN** as **502** since ~19:45 MT 2026-09-13 — **not** 1033. Fri `/alive` **200** is historical. Dual-Nextcloud lock: public `cloud.projectcar.ca` stays on **Doc** until explicit Ben GO; McKing `/opt/mission-control` NC remains **lab/hub loopback-only** (unpublished). Paper path = Tailscale Serve HTTPS mirroring VW Serve at `lightning.tailbe8f55.ts.net` + `NEXTCLOUD_TRUSTED_DOMAINS` MagicDNS — **not** a Cloudflare cutover of `cloud.*`. Do **not** point `cloud.*` tunnel/CNAME at McKing until a dual-run acceptance gate (parity vs Doc `status.php`) **and** Ben GO. Dual-tunnel matrix stays Doc=`cloud`/`api`/`app`/`ops`; McKing=`vault` (+ unpublished NC). Lookout vault flip watch is **LIVE/armed** (`enabled:true`, Lookout confirmed) on `/alive` every **5m** (fallback `/api/config` if `/alive` 404s) — Soft-530 / `api.` `/health` can stay green while vault dies (vault watch ≠ Soft-530 / Doc lid-restore). Soft-530 companion watches on `ops.` / `app.` `/login` (optional `cloud.` `/login` for five-row parity) are **HOLD / not armed** (Ben skipped companion-watch approval ~14:35 America/Edmonton — do **not** re-ask). Do **not** claim `enabled:true`. Soft-530 five-row is Doc shop hosts `cloud.` / `api.` / `app.` / `ops.`, but Lookout Soft-530 coverage is **api-only** (`/health`); `ops.` / `app.` (and `cloud.`) can **530 while `api.` stays 200** (same class as pre-vault-watch). Companion watches ≠ vault `/alive` watch ≠ Doc lid-restore. **Living dual-OPEN (supersedes Fri Soft-530→LIVE):** Soft-530 **OPEN** as CF **1033** since 2026-09-13 ~11:57 America/Edmonton (`api.` / `app.` / `ops.` / `cloud.` + waitlist OPTIONS). Vault independently **OPEN** as **502** since ~19:45 MT Sep 13 (**not** 1033). ListMachines **Mac.lan only** — `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning` (lid-restore + vault wake **blocked** until the right hosts appear). Freeze intact **`4cf8924`** / BUILD_ID **`5swmVz`**. **#82** still Ben GO. Apex deferred. Fri 2026-09-11 ~06:50 CLEAR is **historical**. **Brochure Reality quarantine:** until **#82** Worker-live + Zone Direct Upload, **#70 Reality tip / Option A** is brochure live SSOT — do **not** execute Zone/Garage from **`main` Reality tip** (`main` still pre-Option-A Worker `/shop` 302 + `styles.css?v=35`). After **#82**, smoke is Direct Upload → **mandatory purge** → body freshness (Live checklist). Hop OPEN ≠ shop cutover GO ≠ Doc unfreeze. Machine map: [home-lab-specification.md](home-lab-specification.md) (host table + anti-goals match this dual-tunnel + vault LIVE lock — McKing is **not** offline `lil-cachy`; VW is **not** Doc compose-down; McKing Tailnet Frigate **0.17.2** + Jellyfin lab ≠ Shop OS `/cameras` live and does **not** flip `pc.cameras` — Later cameras stays **Later**). Cockpit spec [mission-control-architecture.md](mission-control-architecture.md) matches the same lock (public VW on McKing, not Doc compose host; shop CF cutover paper; Doc freeze + Soft-530 five-row = Doc shop hosts only; Frigate/Jellyfin lab ≠ Shop OS cameras). (Locks below; [vault-stay-up.md](vault-stay-up.md), [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md), [api-stay-up.md](api-stay-up.md), [deployment-guide.md](deployment-guide.md), [home-lab-specification.md](home-lab-specification.md), [ops-demo-hardening.md](ops-demo-hardening.md), [mission-control-architecture.md](mission-control-architecture.md)).

**Brochure Reality quarantine (held #70):** Until **#82** Worker-live + Zone Direct Upload, treat **this PR’s Reality tip / Option A** as brochure + living-ops SSOT. **Public STATUS tip honesty:** `main` / public STATUS still stamps brochure Worker live at P4-2 **`2b772ff` / #67** (`styles.css?v=35`, Worker `_redirects` `/shop` **302**). That stamp is **stale** vs live Option A Redirect pack **FULL 10/10** + Soft-530 Discord waitlist (**#80** `waitlist.js?v=3` + `styles.css?v=36`) + McKing vault **LIVE (when CLEAR)** — living vault is **OPEN 502**. **Reinforce (no Ben ask / no tip-only PR):** `main` Reality tip is still that **`2b772ff` / ?v=35 / Worker `/shop` 302** row — quarantine stands; **#70** is SSOT until **#82**. Same class: `main` `doc-lid-restore.md` Soft-530 table still says Lookout api-health **paused** vs living **`enabled:true`** (**#78 LIVE-SUPERSEDED**). Do **not** open a tip-only PR to reconcile `main` — keep this quarantine; **#70** is SSOT until **#82**. Do **not** execute Zone or Garage from **`main` Reality tip** — `main` STATUS still documents the pre-Option-A Worker (`_redirects` `/shop` **302** + `styles.css?v=35`). After **#82** upload + **mandatory purge**, **one** tip-fold reconciles `main` Reality to live (Live checklist — body freshness after purge). **Living dual-OPEN:** Soft-530 **OPEN** CF **1033** since 2026-09-13 ~11:57 America/Edmonton; vault independently **OPEN** **502** since ~19:45 MT Sep 13. Soft-530 has crossed **~24h** + vault OPEN + **Mac.lan only** → quiet-ops ([soft-530-extended-open.md](soft-530-extended-open.md)). Brochure Option A is **LIVE**: Zone Redirect **FULL 10/10**; `waitlist.js?v=3`; `styles.css?v=36`; Soft-530 Discord honesty; Home still bare `href="/"` + canonical/og apex until **#82**. Soft-530 companion watches are **HOLD / not armed** (Ben skipped companion-watch approval ~14:35 America/Edmonton — do **not** re-ask). **Weekend Soft-530 coverage (plan-improve off Sat/Sun):** flip watches = Lookout `api.` `/health` + vault `/alive` **only**; control plane = Doc KeepAlive / lid-close — **not** Doc unfreeze, **not** **#82**. Fri Soft-530 **CLEAR** is historical — living dual-OPEN ≠ unfreeze GO ≠ companion re-ask ≠ **#82**. Receipt: [brochure-worker-deploy.md](brochure-worker-deploy.md) · [brochure-worker-ci.md](brochure-worker-ci.md) · [brochure-redirect-watch.md](brochure-redirect-watch.md) · [lookout-rearm-sop.md](lookout-rearm-sop.md) (Option A watch **GONE** — **HOLD** recreate). Keep `/member*` Worker **404** until Member edge Ben GO after Bulk Phase1 — never **301** into `membership.html` ([member-zone-edge.md](member-zone-edge.md)). This fold does **not** execute #82 / Bulk / Zone / Garage. **#79** Soft-530-CLEAR Next Ben GO stays **parallel to #82** (not blocked on Worker SEO / Option A upload). Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull**. Frigate anti-goal unchanged.

Reality tip: **git tip** `main` **`2dd61a2`** / **#74** ([doc-unfreeze.md](doc-unfreeze.md) Ben GO pull checklist — **on `main`**). Checklist on git is **not** a pull. Green **shop-os-ci** and lid-restore process-wake are **not** Ben GO unfreeze. [shop-os-ci.md](shop-os-ci.md) + `.github/workflows/shop-os-ci.yml` are on `main` — **git-only** quality gate (**#73** / `babdc58`), **not** a Doc unfreeze. **#72** lid-restore no-auto-pull (`a2ec088`) and **#71** google-calendar-oauth plan stay on `main` (STATUS Next #6 — not a GO). OwnerShell **#69** (`b9f9019`) stays **git-only**, not LIVE on Doc (Doc checkout **frozen** until **Ben GO**; same **git-only** bucket as **#36**). OwnerShell chrome + bay-hero dashboard are **on git** (merged, not LIVE on Doc): primary nav Dashboard / Schedule / Chat / Members; secondary clusters Inventory (Parts/Tools), Floor (Jobs/Cameras/Hoists), Admin (Payments/Tiers/Fill/Waitlist); compact dismissible demo banner; dashboard 3 primary metrics + 3×2 bay grid + Parts/To-dos split. **Public STATUS tip honesty:** that P4-2 **`2b772ff` / #67** Worker stamp (below) is the last public/`main` brochure SHA — it is **stale** vs live Option A Redirect pack + Soft-530 Discord waitlist (**#80**) + McKing vault **LIVE (when CLEAR)** — living vault is **OPEN 502**. Keep Reality tip quarantine; **#70** is SSOT until **#82**. Do **not** open a tip-only PR. P4-2 is **LIVE** on Worker `projectcar-brochure` (2026-09-07 Worker live verify — Garage PR **#67** → `2b772ff`; Zone Direct Upload ~19:15 America/Regina; smoke PASS apex+www: Home equal `cta-hero` Join the waitlist + Join Discord (`discord.gg/projectcar`); contact email · Discord; Membership waitlist form unchanged; Discord contact-primary + alongside-waitlist copy; `styles.css?v=35` at that upload; sitemap lastmod bumps (2026-09-08 present); P2-4 headers still; P3-4 Shop facility bullets still). **Live URL SSOT (Option A):** Zone Redirect pack is **FULL 10/10** Active Dynamic **301**s (apex+www) — capacity lock; no new Dynamic rules for brochure pretty-URLs. Owns pretty URLs: brochure-root-to-index (`/`→`/index.html`), brochure-shop-to-the-shop, brochure-shop-html-to-the-shop (`/shop.html`→`/the-shop.html`), membership/about/the-shop/contact/roadmap-to-html (+ trailing-slash), brochure-chat-to-contact, brochure-chat-slash-to-contact. Garage must **not** invent new brochure extensionless pretty-URLs until **#82** Worker-live + Bulk Phase1 frees Dynamic slots (Redirect plan A parked). Worker Assets **`html_handling: none`** — `*.html` is canonical. Worker `_redirects` is **thin**: **chat → contact.html only** (no `/` or `/shop` in Worker `_redirects`). Soft-530 is **Worker-live** (PR **#80**): `waitlist.js?v=3` + `styles.css?v=36` + Soft-530 Discord honesty on Membership/Contact. Home canonical/og/sitemap must be `/index.html` after **#82** — **#82 not yet Worker-live**. P3-4 is **LIVE** on Worker `projectcar-brochure` (2026-09-07 Worker live verify — Garage PR **#65** → `f191ad5`; Zone Direct Upload ~18:46 America/Regina; smoke PASS apex+www: Shop 200 facility bullets (bays/hoists, shared fab, safety/cleanliness, structured shop) + honesty “intended shop capability — not live yet”; no 24/7 packaging on Shop; Membership still “24/7 shop access (when operational)”; Home still P3-1 pitch + `cta-hero` waitlist + Project Underway; no capability-bullet dump; `styles.css?v=34`; P2-4 headers still present). P3-5 is **LIVE** on Worker `projectcar-brochure` (2026-09-07 Worker live verify — Garage PR **#62** → `9d83bfc`; Zone Direct Upload; smoke PASS: Shop/Membership/Roadmap use “24/7 shop access (when operational)”; Home (and About/Contact) have no absolute 24/7). P3-1 is **LIVE** on Worker `projectcar-brochure` (2026-09-07 Worker live verify — Garage PR **#61** → `88c9820`; Zone Direct Upload ~16:50 America/Regina; smoke PASS Home: pitch + `cta-hero` Join the waitlist + Project Underway + Shop/Membership pointers; no feature-bullet dump). P2-4 is **LIVE** on Worker `projectcar-brochure` (2026-09-07 smoke PASS — Transform Rules `brochure-security-headers` / `brochure-html-no-cache` / `brochure-asset-immutable`; apex+www). This tip is the last **product/Docs** SHA — not finance tip-of-main `de290e2` (or later CCJ / Health Audit stamps). Shop-web host allowlist **#36** (`f952cd3`) and OwnerShell **#69** (`b9f9019`) are **git-only**. Docs **#30–#74** already on `main` (website **#52–#67**; Soft-530 **#80** Worker-live; **#71** google-calendar-oauth plan; **#72** lid-restore no-auto-pull; **#73** Shop OS CI; **#74** doc-unfreeze checklist). Last full product walk remains 2026-09-06 ~16:16 (Dashboard **#28** / `afb37f9` **LIVE on Doc**). Doc checkout is **frozen** at **`4cf8924`** / BUILD_ID **`5swmVz-T2CqKEQzTk1ifU`** (Dashboard **#28**) until **Ben GO**. **#36** + **#69** stay **git-only**. plan-improve / Chief / Lead must **not** re-propose Doc pull/rebuild. Do **not** claim #36 or #69 is live on Doc. After **Ben GO**, ordered pull: [doc-unfreeze.md](doc-unfreeze.md). **Fri Soft-530 restore (historical)** 2026-09-11 ~06:50 America/Edmonton (Lead verified then): public `GET https://api.projectcar.ca/health` **200**; `ops.` + `app.` **307** → `/login`; waitlist **OPTIONS 200**. **Living dual-OPEN supersedes that stamp:** Soft-530 **OPEN** CF **1033** since 2026-09-13 ~11:57 America/Edmonton (`api.` / `app.` / `ops.` / `cloud.` + waitlist OPTIONS). Vault independently **OPEN** **502** since ~19:45 MT Sep 13. ListMachines **Mac.lan only** — lid-restore + vault wake **blocked**. Product tip stays **`2dd61a2` / #74** — origin-up/down is not a new product SHA. Freeze intact **`4cf8924`** / BUILD_ID **`5swmVz`**. **#82** still Ben GO. Apex deferred. Ordered restore (process wake only — **not** a pull): [doc-lid-restore.md](doc-lid-restore.md). Distinguishes **Live** vs **Next** vs **Later**, and **build-breadth (now)** vs **ship-MVP cut (later)**. Do not invent Stripe, a shop opening, a shipped Member host migration, a removed `app.` alias, or Matrix / Apex / Grok chat. **Chat v1** (human / polling / Owner-starts-rooms) and **Dashboard** (24h bay strips + todos + parts POs) are **LIVE on Doc**.

---

## Host split (LOCKED — Ben GO ~12:22; ops LIVE at edge ~12:55)

| Host | Role |
|------|------|
| **projectcar.ca / www** | **Customer app.** Brochure + waitlist are live today. Member self-serve booking / balance **migrates here** as a follow-up slice. |
| **ops.projectcar.ca** | **LIVE management hostname** at the edge (2026-09-06 ~12:55 America/Edmonton). Staff-on-shift / ops management UI. Owner uses it too. Staff login via OIDC later. **Not Owner-only.** Some clients still have **local DNS cache** that misses `ops.` — use `app.` or flush cache; do not treat that as “ops is not live.” |
| **app.projectcar.ca** | **Temporary alias** until Ben cuts this DNS. Still live on the same Doc shop UI (`:3000`). Do not treat `app.` as the intended name. **Not removed.** |
| **api.projectcar.ca** | Shop API. Public waitlist + authenticated Owner / Member. Tunnel → Doc `:8000`. Lead owns Doc `:8000`. |

Do **not** describe the management host as Owner-only. Staff will use `ops.` (OIDC later); Owner uses the same host.

**Today** the Member demo still lives on the shop UI at `/member` (reachable on **`ops.`** and the temporary **`app.`** alias). Do **not** claim the customer-host migration is shipped. “Member UI on projectcar.ca” is **Next**.

Private Mission Control stays off these hosts.

---

## Build breadth (now) vs Ship-MVP cut (later gate)

Ben / Chief lock. Two horizons — do **not** collapse them into one “v1 includes everything forever.”

| Horizon | Meaning |
|---------|---------|
| **Build breadth (now)** | Breadth-first **placeholders** / rough IA so layout can be ironed out. Put planned features in the app (Parts, Tools, job board, cams, calendar / fill, member surfaces, Payments, Chat v1 demo). Do **not** leave a planned surface out of the **build** for polish. A placeholder is not shipped product. |
| **Ship-MVP cut (later gate)** | At **public MVP release**, cut unfinished and unnecessary features. Do **not** ship every placeholder as the public product. What ships then is a later Ben cut — not “everything we sketched now.” Draft cut-vs-keep (proposed candidates, not a Ben lock): [ship-mvp-cut.md](ship-mvp-cut.md). |
| **Shop OS CI (git-only)** | GitHub Actions on PRs (and push to `main`) that touch `apps/project-car/**`: shop-api `pytest`, shop-web lint / typecheck / `next build`. **Not** a Doc deploy. Green CI is **not** Doc unfreeze GO. Doc checkout stays frozen at `4cf8924` / `5swmVz` until **Ben GO** ([doc-unfreeze.md](doc-unfreeze.md)). Runbook: [shop-os-ci.md](shop-os-ci.md). |

Do not invent extra product from this table. Calendar heat-map / weekly per-hoist is on `main` (PR #18). Next-day fill is on `main` (PR #20). Breadth placeholders are on `main` (PR #21). Inventory prefixes are locked on `main` (PR #26: B1–B6 / TC / PT; CM later) — demo lists, not live checkout. Member parts is a **request desk** now; **full** purchasing is **Later**. **Chat v1** (human / polling / Owner-starts-rooms) is **LIVE on Doc** (PR #27) — not Matrix, not Apex, not Grok. **Dashboard** 24h strips + todos + parts POs are **LIVE on Doc** (PR #28). OwnerShell + bay-hero polish is **on git** (PR #69) — keep-path polish, not a new breadth slice; **not LIVE on Doc**.

---

## Live

- **Brochure:** https://projectcar.ca and https://www.projectcar.ca. Origin: Cloudflare Worker **`projectcar-brochure`** Direct Upload of `apps/website/html` from `main`. Worker Assets **`html_handling: none`** — `*.html` is canonical. Classic Pages git is **skipped** pending Cloudflare ↔ GitHub auth — plan only: [brochure-pages-cutover.md](brochure-pages-cutover.md). Do not start Pages git from a docs PR. Not the Doc `:8088` tunnel. **Apex sidecar deferred** (brochure stays Pages/Worker). Standing re-deploy: `brochure-worker-deploy.md`.
- **Live URL SSOT (Option A):** Zone Redirect pack is **FULL 10/10** Active Dynamic **301**s (apex+www) — capacity lock; no new Dynamic rules for brochure pretty-URLs. **Docs SSOT** is the frozen Sep 10 2026 inventory (box parking, pasted as-is — do **not** invent rules): [brochure-worker-ci.md](brochure-worker-ci.md) Option A matrix · [brochure-pages-cutover.md](brochure-pages-cutover.md) **hard cutover gate**. Owns the 10 named rules: brochure-root-to-index, brochure-shop-to-the-shop, brochure-shop-html-to-the-shop, membership/about/the-shop/contact/roadmap-to-html, brochure-chat-slash-to-contact, brochure-chat-to-contact. `html_handling: none` is Worker Assets — **not** a Redirect Rule. Garage must **not** invent new brochure extensionless pretty-URLs that need another Dynamic Redirect Rule until **#82** Worker-live + Bulk Phase1 frees Dynamic slots. Redirect plan A is parked — Bulk Phase1 pretty-URL pack already drafted: `membership` / `about` / `the-shop` / `contact` / `roadmap` ± slash → `*.html`; `root` / `index` / `shop` / `chat` stay Dynamic until migrate. Worker `_redirects` is **thin**: **chat → contact.html only** (no `/` or `/shop` in Worker `_redirects`). Do **not** claim Worker honors `_redirects` `/shop` → 302 `/the-shop`. Prefer merge **#83** after **#82** base (CI-only thin `_redirects` assert) — **parallel with upload OK**; **#83** does **not** need Bulk Phase1. **Never #81.** Bulk Phase1 stays after upload only for Member-edge slots. Soft-530 is **Worker-live** (PR **#80**): `waitlist.js?v=3` + `styles.css?v=36` + Soft-530 Discord honesty on Membership/Contact. **Living Option A:** Home still bare `href="/"` + canonical/og apex until **#82**. Home canonical/og/sitemap must be `/index.html` after **#82** — **#82 not yet Worker-live**. Paper only — no Zone live apply from this fold. See [brochure-worker-ci.md](brochure-worker-ci.md) · [brochure-worker-deploy.md](brochure-worker-deploy.md). Post-dual-CLEAR **brochure lane** after **#82** Worker-live: [post-dual-clear-go.md](post-dual-clear-go.md) (**#83** ∥ **Bulk Phase1**, not Bulk→#83; Member after Bulk; unfreeze+#79.1 is Shop OS parallel). **#82 Worker-live ≠ retire Dynamic (`49ccf90`):** PASS (upload + purge + freshness: canonical / og / sitemap + nav `/index.html`, no `href="/"`) does **not** delete the **10/10** pack. Keep full Option A Dynamic (`root`/`shop` + pretty-URL + `chat`) until Bulk Phase1 is **LIVE** and smoke-equal; then drop pretty-URL Dynamic per parked Redirect A; keep `root`/`shop` unless Bulk covers them. Do **not** migrate Dynamic on upload day.
- **Living dual-OPEN (2026-09-14 stamp — #70 SSOT; supersedes Fri Soft-530→LIVE):** Soft-530 **OPEN**: CF **1033** since 2026-09-13 ~11:57 America/Edmonton (`api.` / `app.` / `ops.` / `cloud.` + waitlist OPTIONS). Vault independently **OPEN**: **502** since ~19:45 MT Sep 13 (**not** 1033) on `/alive` + `/api/config`. ListMachines: **Mac.lan only** — `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning` (lid-restore + vault wake **blocked** until the right hosts appear). Brochure Option A **LIVE**: Zone Redirect **FULL 10/10**; `waitlist.js?v=3`; `styles.css?v=36`; Soft-530 Discord honesty; Home still bare `href="/"` + canonical/og apex until **#82**. Freeze intact **`4cf8924`** / BUILD_ID **`5swmVz`**. **#82** still Ben GO. Apex deferred. Fri 2026-09-11 ~06:50 Soft-530 CLEAR is **historical**. Index pointers stay: [vault-stay-up.md](vault-stay-up.md), [dual-host-outage.md](dual-host-outage.md), [soft-530-extended-open.md](soft-530-extended-open.md) (quiet-ops — duration ≠ GO), [doc-reappear-first-hop.md](doc-reappear-first-hop.md) (first hop when `Docs-MacBook-Pro` reappears — machineId `95a229f5-9296-4a18-aa98-70fd300dabdf`; never `Mac.lan` / Porsche), [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (first hop when `lightning` / McKing reappears — machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` label **lightning**; never `Mac.lan`), [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (first Soft-530 recovery smoke after extended-OPEN CLEAR — **before** desk / **#79.1** / unfreeze), [vault-clear-smoke.md](vault-clear-smoke.md) (first vault recovery smoke after extended-OPEN CLEAR — **before** Bitwarden / desk; **502→200** not **1033→200**), Soft-530 post-CLEAR ([doc-lid-restore.md](doc-lid-restore.md) `@55e10d0`), vault post-CLEAR (`@d88cacb`), [post-dual-clear-go.md](post-dual-clear-go.md). This paper does **not** invent CLEAR, unfreeze, **#82**, Zone execute, companion re-ask, or Bitwarden.
- **#82 Worker Direct Upload smoke gate (paper — not executed):** **#82** Ben GO unchanged. Soft-530 companions **HOLD**. After GO, Zone smoke is **ordered**: (1) **Direct Upload** `apps/website/html` onto Worker **`projectcar-brochure`**; (2) **mandatory purge** `projectcar-brochure` Worker/Cache (**apex + www**); (3) **then** assert body freshness. Do **not** treat `Cache-Control: no-cache, must-revalidate` as sufficient — live HTML steady-state is `cf-cache-status: HIT` + no-cache. Freshness: Home canonical/og/sitemap = `https://projectcar.ca/index.html`; nav/logo not `href="/"`; `/shop.html`→`/the-shop.html` **301 via Zone**; Worker `_redirects` **thin** (chat→contact only); Soft-530 assets `waitlist.js?v=3` + `styles.css?v=36`. Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) (assert list) · [brochure-worker-deploy.md](brochure-worker-deploy.md) (upload click-path). This fold does **not** execute the upload or purge. **Never** Direct Upload from **#81** (draft + superseded). **PASS ≠ delete Option A Dynamic.** Keep the **10/10** pack until Bulk Phase1 is **LIVE** and smoke-equal — do **not** migrate Dynamic on upload day.
- **Hard lock / prefer merge (paper):** **Never #81.** Wrong lock was `#82 → Zone Direct Upload → Bulk Phase1 → #83`. Correct after **#82** Ben GO + Zone Direct Upload + mandatory purge/freshness: **#83** and **Bulk Phase1** are **parallel** (not Bulk→#83). (A) **#83** merge anytime on the **#82** base — CI-only thin `_redirects` assert; **may run in parallel with the upload**; does **not** need Bulk Phase1. (B) Bulk Phase1 **only** to free Dynamic slots when Member needs capacity — **after** Bulk is **LIVE** and smoke-equal to today’s **10/10** (keep `root`/`shop`/`chat` Dynamic until then). **#82 PASS ≠ wipe Dynamic.** Do **not** migrate Dynamic on upload day. **Member edge only after Bulk.** Prefer merge **#83** on the **#82** base — do **not** wait for Bulk. Post-dual-CLEAR menu ([post-dual-clear-go.md](post-dual-clear-go.md)): after **#82** Worker-live this is the **brochure lane** — unfreeze+#79.1 is **Shop OS parallel**, not “next after **#82**.” Soft-530 companions **HOLD**. Reality quarantine until **#82** unchanged. Paper only — no Zone/Garage execute.
- **Post-#82 reconcile (one tip-fold — paper now):** After Zone Direct Upload of **#82** **and** the **mandatory purge** (smoke gate above), fold `main` Reality to live: (1) Home canonical / og / sitemap = `https://projectcar.ca/index.html`; (2) nav / logo not `href="/"`; (3) Zone Redirect pack SSOT (**FULL 10/10** / Bulk Phase1 path); (4) Worker `_redirects` **thin** (chat→contact only); (5) assets `waitlist.js?v=3` + `styles.css?v=36`; (6) `/shop.html` → `/the-shop.html` **301 via Zone** — never reintroduce Worker `/shop` **302** fight; (7) keep `/member*` Worker **404** until Member edge Ben GO after Bulk Phase1 — never **301** into `membership.html`. Do **not** treat HTML `Cache-Control: no-cache, must-revalidate` as proof freshness — assert **after** purge. Until that upload, **#70 Reality tip / Option A** is brochure live SSOT — do **not** execute from `main` Reality tip. See [brochure-worker-ci.md](brochure-worker-ci.md), [brochure-worker-deploy.md](brochure-worker-deploy.md), [member-zone-edge.md](member-zone-edge.md). This fold does **not** execute the upload.
- **Pages:** Home, About, The Shop, Membership, Roadmap, Contact. Chat nav/page stripped (PR #5). Apex deferred.
- **Brochure hygiene:** P0-3…P0-7 is in git (`apps/website/html`: robots, sitemap, `404.html`, favicon set, Home progress bar removed) **and already live** on the Worker (re-uploaded 2026-09-06). Classic Pages git still skipped (plan: `brochure-pages-cutover.md`).
- **Brochure P1-1 / P1-3:** **LIVE** on Worker `projectcar-brochure` (2026-09-07). Garage PR #52 → `5361212`. Home `cta-hero` waitlist CTA weight; Contact Phone/IG/TikTok/YouTube/Skool collapsed to one `contact-soon` line. Zone Direct Upload; smoke PASS (Home CTA + Contact; apex/www 200). P1-6 stays deferred.
- **Brochure P2-1:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #55 → `453e44d`. Absolute `og:url` / `og:image` + `twitter:card`. Zone smoke PASS.
- **Brochure P2-2:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #56 → `ed41d23`. Zone Direct Upload ~15:48 America/Regina. Apex `<link rel="canonical">` matches P2-1 `og:url` (host `https://projectcar.ca`, not www). Zone smoke PASS: Home + Membership `rel=canonical` matches `og:url` apex; www serves apex canonicals.
- **Brochure P2-3:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #57 → `be60a01`. Zone Direct Upload ~16:01 America/Regina. Cache `?v=` SSOT: `styles.css?v=34` + `banner-logo.png?v=30` on public HTML. Smoke PASS: Home/About/Contact at minimum; apex+www.
- **Brochure P2-4:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Zone Transform Rules on apex+www: **`brochure-security-headers`** (`X-Content-Type-Options: nosniff`; `Referrer-Policy: strict-origin-when-cross-origin`; `X-Frame-Options: DENY`; `Permissions-Policy` static defaults); **`brochure-html-no-cache`** (`/` + `*.html`: `Cache-Control: no-cache, must-revalidate`); **`brochure-asset-immutable`** (`?v=` `.css`/`.js`/`.png`: `Cache-Control: public, max-age=31536000, immutable`). Smoke PASS (Worker **200**s, not challenge 403): `/` + www 200 + required security + HTML no-cache; `styles.css?v=34` 200 + security + immutable long-cache; `robots.txt` 200 `text/plain` + security + `max-age=0`. DNS/tunnels untouched. Record: [brochure-security-headers.md](brochure-security-headers.md). Do **not** re-apply from a docs PR.
- **Brochure P3-1:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #61 → `88c9820`. Zone Direct Upload ~16:50 America/Regina. Home is pitch + `cta-hero` Join the waitlist + Project Underway + Shop/Membership pointers; no feature-bullet dump. Smoke PASS. P3-2 / P3-3 About stay **held / deferred**.
- **Brochure P3-4:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #65 → `f191ad5`. Zone Direct Upload ~18:46 America/Regina. Shop 200: facility bullets (bays/hoists, shared fab, safety/cleanliness, structured shop) + honesty “intended shop capability — not live yet”; no 24/7 packaging on Shop. Membership still “24/7 shop access (when operational)”. Home still P3-1 pitch + `cta-hero` waitlist + Project Underway; no capability-bullet dump. `styles.css?v=34`; P2-4 headers still present. Smoke PASS apex+www. P3-2 / P3-3 About stay **held / deferred**.
- **Brochure P3-5:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #62 → `9d83bfc`. Zone Direct Upload. Shop/Membership/Roadmap use “24/7 shop access (when operational)”. Home (and About/Contact) have no absolute 24/7. Smoke PASS. P3-2 / P3-3 About stay **held / deferred**.
- **Brochure P4-2:** **LIVE** on Worker `projectcar-brochure` (2026-09-07 live verify). Garage PR #67 → `2b772ff`. Zone Direct Upload ~19:15 America/Regina. Home: equal `cta-hero` Join the waitlist + Join Discord (`discord.gg/projectcar`); contact email · Discord. Membership: waitlist form unchanged; Discord contact-primary + alongside-waitlist copy. sitemap lastmod bumps (2026-09-08 present). `styles.css?v=35` at that upload; later Soft-530 **#80** is `styles.css?v=36` + `waitlist.js?v=3` (Worker-live). Pretty URLs are the Zone Redirect pack (Live URL SSOT — **FULL 10/10** capacity lock) — not Worker `_redirects` `/shop` 302. P2-4 headers still; P3-4 Shop facility bullets still. Smoke PASS apex+www. P3-2 / P3-3 About stay **held / deferred**. P1-6 stays deferred.
- **Waitlist:** Membership / Contact `POST` JSON to `https://api.projectcar.ca/waitlist`. Soft-530 is **Worker-live** (PR **#80**): `waitlist.js?v=3` + `styles.css?v=36`; Membership/Contact fail-soft to Discord (`discord.gg/projectcar`) + mailto on 502 / 530 / 1033 — not “try again.” Fail-soft is the **living** waitlist path: Soft-530 **OPEN** CF **1033** since 2026-09-13 ~11:57 America/Edmonton (waitlist OPTIONS **530** / **1033**). Fri 2026-09-11 **OPTIONS 200** / Chief **POST 201** + CORS is **historical**. CORS allowlist includes brochure origins (`https://projectcar.ca`, `https://www.projectcar.ca`), localhost, **`https://ops.projectcar.ca`**, and the temporary alias **`https://app.projectcar.ca`**. **e2e PASS** (brochure → api) when public **GET /health** is **200**. See `cors-origins.md`. **Permanent #80 product behavior** (overnight sleep / tunnel flaps — **not** temporary outage banners): Membership/Contact Soft-530 Discord honesty intro + `waitlist.js?v=3` fail-soft stay after CLEAR. Soft-530 **CLEAR** recovery smoke ([soft-530-clear-smoke.md](soft-530-clear-smoke.md)) ≠ Garage GO to strip that copy. **CLEAR ≠ honesty-off.** Tone tweak only with Ben GO. Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) Soft-530 Discord · [website-improvements.md](website-improvements.md) P1-5.
- **Waitlist Owner desk (LIVE when origin up):** Already shipped — **retired** from the post–`brochure-worker-ci` parked queue. Do **not** open `Docs/waitlist-owner-desk.md`. `apps/project-car/web/app/waitlist/page.tsx` — `OwnerShell` `current=waitlist`; GET `/waitlist` list + mark contacted. OwnerShell nav Waitlist → `/waitlist`. Live when Doc origin is up: `ops.` / `app.` `/waitlist` **307** → `/login?next=/waitlist` (route on frozen BUILD_ID **`5swmVz`**); `api` GET `/waitlist` **401** unauthorized without Owner (endpoint live **when origin up**; Soft-530 **OPEN** CF **1033**). Optional CSV export stays **Later**. No email blast / Stripe / Member host. Anti-goal: Soft-530 **CLEAR** or desk existence ≠ Doc unfreeze ≠ **#79.1** rebuild GO. **#82** Ben GO / Soft-530 companions HOLD / weekend coverage (`84107e7`) / dual-Nextcloud / `brochure-worker-ci` (`6e6efe8`) / **#79.1** git-only until Doc unfreeze — **unchanged**. This fold does **not** execute Zone or Garage.
- **Lookout `projectcar-api-health-watch`:** **resumed** (`enabled:true`) 2026-09-11 ~06:52 America/Edmonton (Lookout confirmed; resume-time `/health` **200** — **living** Soft-530 is CF **1033** since 2026-09-13 ~11:57 America/Edmonton). Lead interim probe ended. **#78 lookout-resume** is **LIVE-SUPERSEDED** — api-health is already **`enabled:true`** while Doc checkout stays frozen at `4cf8924` / `5swmVz`. Soft-530 companion watches stay **HOLD / not armed** (Ben skipped ~14:35 America/Edmonton — do **not** re-ask). Future lid-close **530 / 1033** on `api.` / `ops.` / `app.` is still expected. This Soft-530 watch is **api-only** — `ops.` / `app.` (and `cloud.`) can **530 while `/health` stays 200** (same class as pre-vault-watch; companion watches **HOLD / not armed**, Live below). It also does **not** cover vault — `api.` `/health` can stay green while vault dies (vault flip watch is a **separate LIVE/armed** watch, Live below). Soft-530 companion watches ≠ vault `/alive` watch ≠ Doc lid-restore. Weekend flip coverage (plan-improve off Sat/Sun) = this api `/health` + vault `/alive` **only**. See P4-5 in [website-improvements.md](website-improvements.md).
- **Lookout brochure redirect watch (paper — GONE / HOLD recreate):** While Soft-530 **OPEN**, Option A is the only public **LIVE** surface. Living watch `brochure-option-a-redirect-watch` was **deleted** to fix stalled cron; Auto-review **blocked** recreate — watch **GONE**. **Never delete** an armed Lookout watch to fix cron — prefer in-place update / pause-resume / re-save. **HOLD** after one Ben ask. Interim coverage = Chief plan-improve **`*/20` redirect smoke + Soft-530 UX acceptance** until recreate is enabled. **Redirect-only PASS ≠ Soft-530 UX PASS** — `membership.html` + `contact.html` must still show Soft-530 Discord honesty; `waitlist.js?v=3` must still fail-soft **502 / 530 / 1033** (`0705b37`). After Ben approves: assert routine **enabled** + first fire within **10m** + baseline still **LIVE 10/10**. Flip **Chief + Lead only**; **never Ben**. **Not** the Soft-530 ops/app companion watches (Ben skipped ~14:35 America/Edmonton — do **not** re-ask). **#78 lookout-resume** is **LIVE-SUPERSEDED** (api-health already `enabled:true`) — this is **not** a #78 re-run. Redirect asserts (apex+www): `/` **301** → `/index.html`; `/shop` + `/shop.html` **301** → `/the-shop.html`; extensionless pretty pack **301** → matching `*.html`; `styles.css?v=36` + `waitlist.js?v=3` **200**. Sep 9 Lookout `brochure-routing-baseline.json` still says extensionless **IN FLIGHT** — stamp living **LIVE 10/10**. This fold does **not** recreate the watch and does **not** execute Zone/Garage. Essay: [brochure-redirect-watch.md](brochure-redirect-watch.md). SOP: [lookout-rearm-sop.md](lookout-rearm-sop.md). Quiet-ops: brochure-redirect **paper** continues; continuous watch **GONE** ([soft-530-extended-open.md](soft-530-extended-open.md)). Living dual-OPEN (`5f2fd1c`) **unchanged**.
- **Lookout rearm SOP (paper — never delete to fix cron):** Living incident: Option A watch **GONE** (delete-to-fix-cron → Auto-review blocked recreate). (1) **Never delete** an armed Project Car Lookout watch to fix stalled cron. (2) Prefer in-place update / pause-resume / re-save schedule. (3) If delete already happened and recreate needs Auto-review: **HOLD** after one Ben ask. (4) Interim Option A coverage = Chief plan-improve `*/20` redirect smoke **+ Soft-530 UX** until recreate is enabled — **Redirect-only PASS ≠ Soft-530 UX PASS**. (5) After Ben approves: assert routine enabled + first fire within **10m** + baseline still **LIVE 10/10**. (6) Soft-530 companions stay **HOLD**. Anti-goals: not **#82** merge nag, not Dynamic wipe, not honesty-off, not companions rearm, not unfreeze, not Zone execute. **#78 lookout-resume** is **LIVE-SUPERSEDED** — do **not** collapse this SOP into #78. Essay: [lookout-rearm-sop.md](lookout-rearm-sop.md). Spec: [brochure-redirect-watch.md](brochure-redirect-watch.md). Quiet-ops: [soft-530-extended-open.md](soft-530-extended-open.md). Honesty permanence: [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (`0705b37`). Paper only — no Lookout/Zone/Garage execute.
- **Lookout Soft-530 companion watches (HOLD / not armed):** Soft-530 five-row is Doc shop hosts `cloud.` / `api.` / `app.` / `ops.`, but Lookout Soft-530 coverage is **api-only** (`/health`). `ops.` / `app.` (and `cloud.`) can **530 while `api.` stays 200**. Companion flip watches are **HOLD / not armed** (Ben skipped companion-watch approval ~14:35 America/Edmonton — do **not** re-ask). Do **not** claim `enabled:true`: (1) `https://ops.projectcar.ca/login` — ok = **200** (or **307** → `/login` then **200**); down = **502 / 530 / 1033 / timeout**. (2) `https://app.projectcar.ca/login` — same. Optional `https://cloud.projectcar.ca/login` for five-row parity (Lead probed **200**). Cadence ≈ api watch. Baselines (Lookout-owned): `/workspace/lookout/projectcar-ops-health-baseline.json` + `projectcar-app-health-baseline.json` (+ cloud if later armed). Flip-only alerts: **Chief + Lead only**; **never Ben**; never restart / mutate. Soft-530 companion watches ≠ vault `/alive` watch ≠ Doc lid-restore. Soft-530 living is **OPEN** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton) — Fri CLEAR is historical. Api + vault watches continue. Vault flip watch stays **LIVE/armed**. Weekend does **not** arm these companions — do **not** re-ask. See [shop-web-stay-up.md](shop-web-stay-up.md), [api-stay-up.md](api-stay-up.md).
- **Lid-close / morning 530:** **Current: OPEN** — CF **1033** since 2026-09-13 ~11:57 America/Edmonton (Fri 2026-09-11 restore is **historical**). Soft **530 / 1033** on `api.` / `ops.` / `app.` after lid-close is still expected. Amphetamine / plugged-in no-sleep is mitigation, not a guarantee (`doc-software-baseline.md`). Stay quiet and restore when Doc is reachable; ping Ben only if Doc will not wake / Grok Bot desktop is offline / the outage is prolonged. Ordered Lead sequence: [doc-lid-restore.md](doc-lid-restore.md) (process wake only — **not** a pull). Doc pull after **Ben GO**: [doc-unfreeze.md](doc-unfreeze.md). Green **shop-os-ci** is **not** that GO. Zone only if local origin is healthy but public is still 1033. No edge flip, no CF↔GitHub re-ask, no Member GO.
- **Soft-530 extended OPEN — quiet-ops lock (paper):** Soft-530 **OPEN** has crossed **~24h** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton) **and** vault is **OPEN** (**502** since ~19:45 MT Sep 13) **and** ListMachines is **Mac.lan only**. **Quiet-ops:** (1) **no Ben re-nag** — Doc-wake + McKing wake already asked; (2) **Lead** stays armed for `Docs-MacBook-Pro` reappear → [doc-reappear-first-hop.md](doc-reappear-first-hop.md) (identity `95a229f5-9296-4a18-aa98-70fd300dabdf` **then** [doc-lid-restore.md](doc-lid-restore.md) process wake); **Chief** stays armed for `lightning` reappear → [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (identity `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` **then** [vault-stay-up.md](vault-stay-up.md) vault wake); (3) Lookout `api.` + vault stay armed (**never delete** to fix cron); brochure-redirect **paper** **continues** ([brochure-redirect-watch.md](brochure-redirect-watch.md)); living Option A watch **GONE** — **HOLD** recreate after one Ben ask; interim Chief `*/20` ([lookout-rearm-sop.md](lookout-rearm-sop.md)); Soft-530 companions stay **HOLD until Ben reopens** — **not** weekend-only; (4) Brochure Option A is public SSOT during extended OPEN; (5) **duration alone ≠** **#82** GO ≠ unfreeze ≠ Zone Direct Upload ≠ Bitwarden ≠ companion re-ask. CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md). Next Soft-530 CLEAR after this lock: [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (**before** desk / **#79.1** / unfreeze). Essay: [soft-530-extended-open.md](soft-530-extended-open.md). Wake order stays [dual-host-outage.md](dual-host-outage.md). First hop when Doc reappears: [doc-reappear-first-hop.md](doc-reappear-first-hop.md). First hop when McKing / `lightning` reappears: [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md). This fold does **not** invent CLEAR or execute Zone/Garage.
- **Doc first reappear — first hop (paper — after quiet-ops):** When `Docs-MacBook-Pro` **first reappears** on ListMachines after extended Soft-530 OPEN / quiet-ops: (1) assert identity machineId **`95a229f5-9296-4a18-aa98-70fd300dabdf`** — **never** treat `Mac.lan` or Porsche as Doc; (2) run [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (`api.` `/health` **200**, waitlist OPTIONS CORS apex+www, POST **422**/**201**) after Lead process wake on the **asserted** host ([doc-lid-restore.md](doc-lid-restore.md)); (3) **only if CLEAR** → Soft-530 post-CLEAR forensic baseline on lid-restore (`55e10d0`); (4) then follow [post-dual-clear-go.md](post-dual-clear-go.md) — **never auto-fire** **#82** / unfreeze / **#79.1** / companions / Zone upload / Bitwarden; (5) Chief may write a **chief-to-doc** hop **only after** CLEAR smoke. Vault CLEAR stays independent (Chief `lightning` / McKing lane). Anti-goals: `Mac.lan` Shell ≠ Doc evidence; duration alone ≠ Ben wake; **CLEAR ≠ honesty-off**; not Lookout companion re-ask. Living this fold: Soft-530 still **OPEN** CF **1033**; vault still **OPEN** **502**; only `Mac.lan`. Essay: [doc-reappear-first-hop.md](doc-reappear-first-hop.md). Monday map: [deployment-guide.md](deployment-guide.md). Paper only — no Zone/Garage execute. Does **not** invent a reappear or CLEAR.
- **McKing first reappear — first hop (paper — after quiet-ops / extended vault OPEN):** When **`lightning` / McKing first reappears** on ListMachines after extended vault OPEN / quiet-ops: (1) assert identity machineId **`9067d14b-46e5-4ef0-82d5-fce0febdc8f7`** (label **`lightning`**) — **never** treat `Mac.lan` as McKing; (2) run [vault-clear-smoke.md](vault-clear-smoke.md) (`/alive` **200** + `/api/config` VW **2026.6.0** + classify **502→200**) after Chief vault wake on the **asserted** host ([vault-stay-up.md](vault-stay-up.md)); (3) **only if CLEAR** → vault post-CLEAR forensic baseline on vault-stay-up (`d88cacb`); (4) Soft-530 independence — Bitwarden stays **blocked** until Soft-530 **CLEAR** is **separate**; **never auto Bitwarden**; **never** treat vault CLEAR as Soft-530 CLEAR, unfreeze, or **#82** GO. Anti-goals: `Mac.lan` Shell ≠ McKing evidence; duration alone ≠ Ben wake; name-only ≠ identity; vault CLEAR ≠ sequencer. Living this fold: Soft-530 **OPEN** `api.`/`app.`/`ops.`/`cloud.` **530** CF **1033**; waitlist OPTIONS **530**; vault `/alive` **502**; ListMachines **Mac.lan only**; freeze intact **`4cf8924`** / **`5swmVz`**; **#82** still Ben GO. Essay: [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md). Monday map: [deployment-guide.md](deployment-guide.md). Paper only — no Zone/Garage execute. Does **not** invent a reappear or CLEAR.
- **Soft-530 CLEAR recovery smoke (paper — after extended OPEN):** After Soft-530 **CLEAR** following **extended OPEN** (>~24h), run this **first** recovery smoke **before** any Owner desk / **#79.1** / unfreeze talk: (1) `api.` `/health` **200**; (2) waitlist OPTIONS **200** + CORS Origin `https://projectcar.ca` **and** `https://www.projectcar.ca`; (3) waitlist POST validation **422** or success **201** (prove route alive, not just tunnel-up); (4) `app.`+`ops.` may still **307**→`/login` with print `` `changeme` `` — that is freeze (`4cf8924` / **`5swmVz`**), **not** Soft-530; (5) when Doc public is up, `cloud.` `status.php` **200**. Anti-goals: this smoke ≠ **#82** GO ≠ companion re-ask ≠ Bitwarden; Soft-530 CLEAR alone ≠ unfreeze; vault CLEAR still separate McKing lane ([vault-clear-smoke.md](vault-clear-smoke.md)); this smoke ≠ Garage GO to strip **#80** Discord honesty / `waitlist.js?v=3` fail-soft (**CLEAR ≠ honesty-off**; overnight sleep / tunnel flaps, not temporary banners; tone tweak only with Ben GO). CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire**). Essay: [soft-530-clear-smoke.md](soft-530-clear-smoke.md). Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) Soft-530 Discord · STATUS Waitlist Live · [website-improvements.md](website-improvements.md) P1-5. Living this fold: Soft-530 still **OPEN** CF **1033** since 2026-09-13 ~11:57 America/Edmonton; vault still **OPEN** **502** since ~19:45 MT Sep 13. Paper only — no Zone/Garage execute. Does **not** invent CLEAR.
- **Vault CLEAR recovery smoke (paper — after extended OPEN):** After vault **CLEAR** following **extended OPEN** (**502** since ~19:45 MT Sep 13), run this **first** recovery smoke **before** any Bitwarden import/rotate or desk talk: (1) `https://vault.projectcar.ca/alive` **200**; (2) `https://vault.projectcar.ca/api/config` **200** with VW version present (expect McKing **2026.6.0** class, **not** Doc **2025.12.0**); (3) classify recovery path — this OPEN is origin-down **502→200**, **not** CF **1033→200** (Soft-530 tunnel class stays separate); (4) Soft-530 may still be **OPEN** — vault CLEAR alone ≠ Soft-530 CLEAR ≠ **#82** ≠ unfreeze ≠ companions; (5) Bitwarden import/rotate stays **Ben GO** after CLEAR, **never auto-fire**. Anti-goals: not Zone Direct Upload, not **#82**, not Doc unfreeze / **#79.1** execute, not companion re-ask, not Soft-530 CLEAR smoke (already `502ab2e`). CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire**). Capture forensics still [vault-stay-up.md](vault-stay-up.md) (`d88cacb`). Essay: [vault-clear-smoke.md](vault-clear-smoke.md). Living this fold: Soft-530 still **OPEN** CF **1033** since 2026-09-13 ~11:57 America/Edmonton; vault still **OPEN** **502** since ~19:45 MT Sep 13. Paper only — no Zone/Garage execute. Does **not** invent CLEAR.
- **Weekend Soft-530 coverage (plan-improve off Sat/Sun):** plan-improve is **off Sat/Sun**. Soft-530 companion ops/app watches stay **HOLD / not armed** (Ben skipped ~14:35 America/Edmonton — do **not** re-ask). Weekend flip coverage = Lookout `api.` `/health` + vault `/alive` **only**. Weekend Soft-530 control plane = Doc KeepAlive / lid-close (`com.projectcar.cloudflared` + uvicorn `:8000` + `next start` `:3000` LaunchAgents) — **not** Doc unfreeze, **not** **#82**. If Lookout `api.` `/health` flips non-200 → Chief/Lead run [doc-lid-restore.md](doc-lid-restore.md) **process wake only** (no pull while freeze `4cf8924` / `5swmVz` intact). Vault flips stay on the **separate** McKing watch (Live above) — [vault-stay-up.md](vault-stay-up.md). Dual-OPEN (Soft-530 OPEN + vault OPEN) recovery glue: [dual-host-outage.md](dual-host-outage.md) — wake **Doc first, then McKing**; parallel only if both hosts appear on ListMachines; `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning` (no wrong-host Shell; `Mac.lan` is not Doc shop host). Independent CLEAR: Soft-530 = `api.` `/health` **200** + `ops.`/`app.` → `/login` + waitlist OPTIONS **200** + `cloud.` `status.php`; vault = `/alive` **200** + VW `/api/config`. After Soft-530 CLEAR, stamp **post-CLEAR stay-up evidence** (cloudflared lastExit / KeepAlive loaded / caffeinate·CDM / Tailscale) on [doc-lid-restore.md](doc-lid-restore.md). After vault CLEAR, stamp **vault post-CLEAR stay-up evidence** (cloudflared.service ActiveState/Result/ExecMainStatus · VW healthy + DOMAIN · Tailscale · 1033→200 vs 502→200) on [vault-stay-up.md](vault-stay-up.md). Wake order stays [dual-host-outage.md](dual-host-outage.md). Living OPEN class: Soft-530 = CF **1033** since 2026-09-13 ~11:57 America/Edmonton; vault = **502** since ~19:45 MT Sep 13 on `/alive` + `/api/config` (**not** 1033). Soft-530 still **OPEN** this fold; vault independently **OPEN**; ListMachines **Mac.lan only**; this paper does **not** invent CLEAR. Bitwarden import/rotate **blocked** until vault CLEAR. Do **not** schedule weekend Zone Direct Upload / Worker work — first Monday plan-improve resumes Soft-530 smoke. Anti-goal: Fri Soft-530 **CLEAR** is historical — living dual-OPEN ≠ unfreeze GO ≠ companion re-ask ≠ **#82**. **#82** Ben GO / Soft-530 companions HOLD / dual-Nextcloud (`4cde204`) / `brochure-worker-ci` (`6e6efe8`) / waitlist-owner-desk **retired** (Owner desk LIVE; optional CSV Later) / **#79.1** git-only until Doc unfreeze — **unchanged**. Paper: [api-stay-up.md](api-stay-up.md), [shop-web-stay-up.md](shop-web-stay-up.md), [doc-lid-restore.md](doc-lid-restore.md), [vault-stay-up.md](vault-stay-up.md), [dual-host-outage.md](dual-host-outage.md).
- **Shop OS CI (git-only, PR #73 / `babdc58`):** GitHub Actions workflow `.github/workflows/shop-os-ci.yml` — shop-api `pytest` + shop-web lint / typecheck / `next build` on PRs (and push to `main`) that touch `apps/project-car/**`. **Not** a Doc deploy, **not** a Worker upload, **not** tunnel/live secrets, **not** a Doc unfreeze. Green CI is **not** Ben GO. Doc checkout stays frozen at `4cf8924` / `5swmVz` until **Ben GO** ([doc-unfreeze.md](doc-unfreeze.md)). Runbook: [shop-os-ci.md](shop-os-ci.md).
- **Shop API:** `api.projectcar.ca` = Cloudflare tunnel → Doc `:8000` KeepAlive. Primary stay-up: LaunchAgent `com.projectcar.shop-api` → `~/hermes-tools/mission-control/shop-api/run-shop-api.sh` → uvicorn `:8000`. **Lead owns Doc `:8000`.** Do **not** hand uvicorn restarts to Chief. Public `GET /health` is **200 when Doc origin is up**. **Living:** CF **1033** since 2026-09-13 ~11:57 America/Edmonton. Fri 2026-09-11 ~06:50 **200** is **historical** — not a permanent guarantee. Lid-close / sleep (mornings included) still kills the Mac; the public edge then returns Cloudflare **502** or **530 / error 1033**. Lookout **`projectcar-api-health-watch`** **resumed** (`enabled:true`) 2026-09-11 ~06:52 America/Edmonton (Lookout confirmed; resume-time `/health` **200**; living Soft-530 is CF **1033**). Lead interim probe ended. See [api-stay-up.md](api-stay-up.md). Ordered restore: [doc-lid-restore.md](doc-lid-restore.md).
- **Ops management UI (LIVE at edge):** **`https://ops.projectcar.ca` is LIVE** (2026-09-06 ~12:55 America/Edmonton) **when Doc origin is up**. Zone Cloudflare tunnel v8 hostname `ops` → `http://127.0.0.1:3000` (same Doc shop UI as `app.`). KeepAlive `com.projectcar.shop-web` runs **`next start`** (not `next dev`) on Doc. Shop-web **BUILD** last recorded on Doc after Dashboard **#28** — Doc checkout **frozen** at **`4cf8924`** / BUILD_ID **`5swmVz-T2CqKEQzTk1ifU`**. **`main` tip `2dd61a2` (#74 doc-unfreeze checklist on `main`).** OwnerShell **#69** (`b9f9019`) and host allowlist **#36** (`f952cd3`) are **git-only** — do not call #36 or #69 live on Doc; do **not** re-propose pull/rebuild. Aligns with [doc-lid-restore.md](doc-lid-restore.md) (**#72**): wake is kickstart-if-down only. Green **shop-os-ci** / lid-restore process-wake are **not** Ben GO. After **Ben GO**, pull: [doc-unfreeze.md](doc-unfreeze.md). `shop-web-stay-up` rebuild/pull is **Ben GO** while frozen. Tunnel origin preferred `http://127.0.0.1:3000` (not bare localhost) after IPv6 `[::1]` 502. Public probes **when Doc origin is up**: `/` → `Location: https://ops.projectcar.ca/login` (no localhost hop); `/login` **200**. **Fri LIVE (historical)** 2026-09-11 ~06:50: `ops.` + `app.` **307** → `/login`. **Living:** CF **1033** since 2026-09-13 ~11:57 America/Edmonton. Soft-530 **CLEAR** (when origin is up) re-exposes public demo credentials: Lead/Chief verified live `https://ops.projectcar.ca/login` (and `app.` alias) still shows banner Password `changeme` and prefills the password input **on frozen Doc BUILD_ID `5swmVz`** — Soft-530-CLEAR **Next Ben GO** **#79** (**parallel to #82**), plan [ops-demo-hardening.md](ops-demo-hardening.md). Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull** — merge alone will **not** clear public login while freeze holds (`4cf8924` / `5swmVz`). Do **not** execute Garage/Zone from this tip-fold. Lid-close / sleep (mornings included) still kills the Mac; the public edge then returns Cloudflare **502** or **530 / error 1033** — tunnel / lid-close, not a new product break. Lookout **`projectcar-api-health-watch`** **resumed** (`enabled:true`) 2026-09-11 ~06:52 America/Edmonton (Lookout confirmed; resume-time `/health` **200**; living Soft-530 is CF **1033**). Lead interim probe ended. That api watch is **api-only** — `ops.` / `app.` can **530 while `/health` stays 200**. Soft-530 companion watches on `ops.` / `app.` `/login` are **HOLD / not armed** (Live above; Ben skipped ~14:35 America/Edmonton — do **not** re-ask); ≠ vault `/alive` watch ≠ Doc lid-restore. CORS includes `https://ops.projectcar.ca`. Some clients still have **flaky local DNS cache** for `ops.` — **use the `app.` alias**. Still the Doc demo — not a shop opening. Stay-up / rebuild / 1033-vs-flap: [shop-web-stay-up.md](shop-web-stay-up.md). Ordered restore: [doc-lid-restore.md](doc-lid-restore.md).
- **Temporary `app.` alias (still live, not removed):** **`https://app.projectcar.ca`** remains a **temporary alias** on the same Doc `:3000` origin until Ben cuts that DNS. **Fri LIVE (historical)** 2026-09-11 (307 → `/login`). **Living:** CF **1033** since 2026-09-13 ~11:57 America/Edmonton. Reachable **when Doc origin is up** (same lid-close **530 / 1033** class as ops — not a new product break). Do not treat the alias as gone.
- **Vault (`vault.projectcar.ca`) — McKing-owned; independently **OPEN** (separate from paper shop CF cutover):** McKing `cloudflared` → `localhost:8222`. **Living:** vault **OPEN** as **502** since ~19:45 MT 2026-09-13 — **not** 1033 — on `/alive` **and** `/api/config`. Fri 2026-09-11 `/alive` **200** + `/api/config` **2026.6.0** is **historical** (when CLEAR, prefer `/alive`). Soft-530 five-row dual-run gate is **Doc shop hosts only** (`cloud.` / `api.` / `app.` / `ops.`) — **vault is EXCLUDED**. Doc lid-restore / LaunchAgent KeepAlive must **not** recreate `vault.` ingress on Doc. Hop OPEN ≠ shop cutover GO ≠ Doc unfreeze. Anti-goals: never move the Doc mission-control token for `vault.`; vault LIVE ≠ shop hostname leave ≠ Doc unfreeze. Weekend dual-outage proved Soft-530 (Doc) and vault (McKing) fail independently. Dual-OPEN recovery glue: [dual-host-outage.md](dual-host-outage.md) (Doc first, then McKing; ListMachines identity lock). After quiet-ops, first hop when `lightning` reappears is [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` — **never** `Mac.lan`). After **extended OPEN**, first card is [vault-clear-smoke.md](vault-clear-smoke.md) — **before** Bitwarden / desk. After next McKing vault CLEAR, stamp **vault post-CLEAR stay-up evidence** on [vault-stay-up.md](vault-stay-up.md) (`d88cacb`). Bitwarden import/rotate **blocked** until vault CLEAR, then still **Ben GO** (**never auto-fire**). Essay: [vault-stay-up.md](vault-stay-up.md). Shop cutover stays paper: [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md). Lock: Locks below.
- **Dual-host outage (weekend dual-OPEN glue):** Soft-530 OPEN + vault OPEN is two restores, not one. Wake **Doc first, then McKing**. Parallel only if both hosts appear on ListMachines. Identity lock: `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning` — no wrong-host Shell (`Mac.lan` is **not** Doc shop host). Independent CLEAR: Soft-530 = `api.` `/health` **200** + `ops.`/`app.` → `/login` + waitlist OPTIONS **200** + `cloud.` `status.php` (after **extended OPEN**, first card is [soft-530-clear-smoke.md](soft-530-clear-smoke.md) — OPTIONS CORS + POST **422**/**201** **before** desk / **#79.1**; `changeme` on `/login` is freeze); vault = `/alive` **200** + VW `/api/config` (after quiet-ops, first hop when `lightning` reappears is [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) — machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7`; after **extended OPEN**, first card is [vault-clear-smoke.md](vault-clear-smoke.md) — `/alive` **200** + `/api/config` McKing **2026.6.0** class + **502→200** **before** Bitwarden / desk). This file is **wake order**. Soft-530 post-CLEAR = **Doc forensics** ([doc-lid-restore.md](doc-lid-restore.md)). Vault post-CLEAR = **McKing forensics** ([vault-stay-up.md](vault-stay-up.md)). After **both** CLEARs + both forensic papers (`55e10d0` / `d88cacb`), Ben GO menu is [post-dual-clear-go.md](post-dual-clear-go.md) — **never auto-fire**. Soft-530 OPEN **~24h+** + vault OPEN + **Mac.lan only** = quiet-ops ([soft-530-extended-open.md](soft-530-extended-open.md)) — **no Ben re-nag**; duration ≠ GO. Anti-goals: not Doc unfreeze, not **#82**, not Zone Direct Upload; Soft-530 companions **HOLD**; Bitwarden import/rotate **blocked** until vault CLEAR. Essay: [dual-host-outage.md](dual-host-outage.md). Plane essays: [api-stay-up.md](api-stay-up.md), [vault-stay-up.md](vault-stay-up.md), [doc-lid-restore.md](doc-lid-restore.md), [shop-web-stay-up.md](shop-web-stay-up.md).
- **Soft-530 post-CLEAR stay-up evidence (paper — next CF 1033):** Soft-530 is still **OPEN** this fold as CF **1033** since 2026-09-13 ~11:57 America/Edmonton (`api.` / `ops.` / `app.` / `cloud.` **530** / error **1033**; waitlist OPTIONS **530**). Vault independently **OPEN** as **502** since ~19:45 MT Sep 13 — **not** 1033 — on `/alive` **and** `/api/config`. ListMachines only `Mac.lan` (Ben Laptop, LocalHostName Laptop; `:8000`/`:8080` down) — `Docs-MacBook-Pro` + `lightning` absent. Brochure Option A + Soft-530 Discord assets still **LIVE**. **#82** unchanged (**never #81**). This fold does **not** invent CLEAR. After the **next** lid-restore Soft-530 CLEAR, stamp forensic baseline before walking away: cloudflared LaunchAgent lastExit, KeepAlive loaded, caffeinate / CDM, Tailscale. [dual-host-outage.md](dual-host-outage.md) is wake order; this is **Doc forensics** (why it died after Fri→Sun flip-flops). Vault twin: [vault-stay-up.md](vault-stay-up.md). Essay: [doc-lid-restore.md](doc-lid-restore.md) · [api-stay-up.md](api-stay-up.md). Anti-goals: not Doc unfreeze, not **#82**, not Zone/Garage execute, not companion re-ask.
- **Vault post-CLEAR stay-up evidence (paper — next vault flip):** Twin of Soft-530 post-CLEAR. Soft-530 still **OPEN** as CF **1033** since 2026-09-13 ~11:57 America/Edmonton; vault independently **OPEN** as **502** since ~19:45 MT Sep 13 (**not** 1033) on `/alive` + `/api/config`. ListMachines only `Mac.lan` — `Docs-MacBook-Pro` + `lightning` absent. Different first checks when `lightning` returns: Soft-530 **1033** → Doc tunnel; vault **502** → VW origin. This fold does **not** invent CLEAR. After **extended OPEN**, first card is [vault-clear-smoke.md](vault-clear-smoke.md) — **before** Bitwarden / desk. After the **next** McKing vault CLEAR, stamp forensic baseline before walking away: `cloudflared.service` ActiveState/Result/ExecMainStatus (or lastExit); VW container healthy + `DOMAIN=https://vault.projectcar.ca`; Tailscale `lightning` up; whether recovery was **1033→200** vs **502→200**. [dual-host-outage.md](dual-host-outage.md) is wake order; Soft-530 post-CLEAR is Doc forensics; this is **McKing forensics**. Essay: [vault-stay-up.md](vault-stay-up.md). Anti-goals: not Doc unfreeze, not **#82**, not Zone/Garage execute, not companion re-ask.
- **Post-dual-CLEAR Ben GO sequencer (paper — never auto-fire):** After **extended OPEN**, Soft-530 CLEAR first runs [soft-530-clear-smoke.md](soft-530-clear-smoke.md) — **before** this menu / desk / **#79.1**. Vault CLEAR first runs [vault-clear-smoke.md](vault-clear-smoke.md) — **before** Bitwarden / desk. After Soft-530 CLEAR (Doc lid-restore) **and** vault CLEAR (McKing) **and** both forensic papers already ack’d (`55e10d0` Soft-530 post-CLEAR + `d88cacb` vault post-CLEAR), Ben’s GO menu is **ordered**: (1) keep Soft-530 healthy (KeepAlive / CDM / caffeinate) as ops baseline; (2) **#82** Worker-live (Direct Upload + **mandatory** purge/freshness — **not only** Direct Upload). After **#82** Worker-live the **brochure lane** is **parallel**: (A) **#83** CI thin `_redirects` anytime on the **#82** base (not Bulk-gated; parallel with upload OK); (B) **Bulk Phase1** frees Option A **FULL 10/10** Dynamic slots when Member needs capacity (keep `root`/`shop`/`chat` Dynamic until then). **Member edge only after Bulk.** **Never #81.** Never Bulk→#83. (3) Doc unfreeze + pull + Garage **#79.1** strip-changeme is **Shop OS parallel** — not “next after **#82**” in place of Bulk / **#83**. Lane-done = [post-dual-clear-go.md](post-dual-clear-go.md) **unfreeze + #79.1 acceptance smoke**: public `app.`/`ops.` `/login` without Password `` `changeme` ``; tip / `BUILD_ID` ≠ `4cf8924` / `5swmVz`; `api` `/health` **200** + waitlist OPTIONS CORS Origin `https://projectcar.ca` (+ www); **#79.1** merge-alone without Doc unfreeze+pull does **NOT** clear public `changeme`. Soft-530 CLEAR alone ≠ that card. Not **#82**, not Zone Direct Upload, not companions re-ask, not Bitwarden. **Never** auto-fire on CLEAR. Anti-goals: CLEAR ≠ companion re-ask; CLEAR ≠ Bitwarden import/rotate; Soft-530 CLEAR alone ≠ Doc unfreeze; vault CLEAR ≠ Soft-530 work; dual CLEAR ≠ **#82** auto-go; **#82** ≠ end of brochure. Living this fold: Soft-530 still **OPEN** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton); vault still **OPEN** (**502** since ~19:45 MT Sep 13); ListMachines only `Mac.lan`. Essay: [post-dual-clear-go.md](post-dual-clear-go.md). Wake order: [dual-host-outage.md](dual-host-outage.md). **#82** gates + Option A receipt: [brochure-worker-ci.md](brochure-worker-ci.md). Member-edge capacity: [member-zone-edge.md](member-zone-edge.md). Unfreeze: [doc-unfreeze.md](doc-unfreeze.md). **#79.1**: [ops-demo-hardening.md](ops-demo-hardening.md). This fold does **not** invent CLEAR or execute Zone/Garage.
- **Dual-Nextcloud ownership (paper — not a `cloud.*` CF cutover):** Public `cloud.projectcar.ca` stays on **Doc** until explicit **Ben GO**. McKing `/opt/mission-control` NC remains **lab/hub loopback-only** (unpublished — not on the McKing CF tunnel). Dual-tunnel matrix **unchanged**: Doc=`cloud`/`api`/`app`/`ops`; McKing=`vault` (+ unpublished NC). Paper path = Tailscale Serve HTTPS mirroring VW Serve at `lightning.tailbe8f55.ts.net` + `NEXTCLOUD_TRUSTED_DOMAINS` MagicDNS — **not** a Cloudflare cutover of `cloud.*`. Anti-goal: do **not** point `cloud.*` tunnel/CNAME at McKing until a dual-run acceptance gate (parity vs Doc `status.php`) **and** Ben GO. Hub dual-run NC+VW healthy / hop OPEN / vault LIVE / Serve stand-up do **not** pass that gate. #82 unchanged. Soft-530 companions **HOLD**. Vault LIVE unchanged. Paper: [home-lab-specification.md](home-lab-specification.md), [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md).
- **Lookout vault flip watch (LIVE/armed — Lookout confirmed `enabled:true`; McKing-only):** Soft-530 / `https://api.projectcar.ca/health` can stay **green while vault dies**. Watch URL: `https://vault.projectcar.ca/alive` (fallback `/api/config` if `/alive` 404s). **Living:** vault **OPEN** **502** since ~19:45 MT Sep 13 — **not** 1033. Fri Lead `/alive` **200** + `/api/config` **2026.6.0** is **historical**. Cadence: every **5m**. Baseline (Lookout-owned): `/workspace/lookout/projectcar-vault-health-baseline.json`. Flip-only alerts: **Chief + Lead only** on **200↔non-200**; **never Ben**; never mutate. Vault watch ≠ Soft-530 / Soft-530 companion watches / Doc lid-restore. Vault is McKing-only (`cloudflared` → `:8222`). Soft-530 five-row gate remains **Doc shop hosts only** (`cloud.` / `api.` / `app.` / `ops.`). Soft-530 companion watches (`ops.` / `app.` `/login`) are **HOLD / not armed** (Live above; Ben skipped ~14:35 America/Edmonton — do **not** re-ask) — **not** this vault watch. Api + vault watches continue. Weekend flip coverage (plan-improve off Sat/Sun) includes this vault `/alive` watch as the **McKing** half — vault flips stay here, not on Doc lid-restore. Vault flip ≠ Doc lid-restore; Doc Soft-530 ≠ McKing vault wake. After **extended OPEN** vault CLEAR, first card is [vault-clear-smoke.md](vault-clear-smoke.md) — **before** Bitwarden / desk. After vault CLEAR, stamp **vault post-CLEAR stay-up evidence** on [vault-stay-up.md](vault-stay-up.md) (`d88cacb`). Essay: [vault-stay-up.md](vault-stay-up.md). Shop CF cutover stays paper.
- **Owner + Member booking + tokens (live):** duration × 100 × band × advance overlay in `America/Regina` is on `main` (PR #12 / #14) and **live on Doc** — shop UI `:3000` — **and reachable via `https://ops.projectcar.ca` and `https://app.projectcar.ca`**. Shop OS seed: **6 hoists** (one shop-priority, Owner-only bay) and **Basic 1000 / Premium 1500**. Demo session cookies — **not OIDC**. HTTPS ops/app demo requires **Secure** cookies (`COOKIE_SECURE` / `SHOP_COOKIE_SECURE`, PR #22). No Stripe. The shop is not open.
- **Member self-serve (demo, still `/member` on the shop UI):** PR #14 merged → `main` `9baf3c4` and walked on Doc; reachable via `https://ops.projectcar.ca/member` and `https://app.projectcar.ca/member`. Member login (seed `ada.reyes@example.com`); `/member/me` balance + ledger; `/member/hoists` + schedule on **Bays 1–5 only**; shop hoist Owner-only (`400 shop_hoist_owner_only`). Demo session cookie (`pc_member_session`) — **not OIDC**. **Not** migrated to projectcar.ca.
- **Calendar redesign (on `main`, PR #18):** Monthly = hoist-density heat-map vs 08:00–21:00. Weekly = **separate per-hoist hour grids**. Owner includes the shop hoist (Owner-only); Member is Bays 1–5. Create / quote / cancel unchanged.
- **Next-day fill (on `main`, PR #20):** leftover customer-bay hours tomorrow get a **10–25%** fill factor; urgency drives the cut. Notification outbox (email stub / later push / SMS). Management `/fill` on Doc demo (`ops.` + temporary `app.` alias). Explicit extra multiplier — not a change to the locked v1 band / overlay tables. See `token-pricing.md`. Not a public price. No Stripe.
- **Breadth-first placeholders (on `main`, PR #21 — demo UI only; Tools / Parts deepened in #26):** Ops on **`ops.`** and the temporary `app.` alias: `/parts` (PT stock + qty / reorder + POs — **not** the member catalog), `/tools` (B1–B6 bay kits + TC crib checkout stub + orders / requests / planned), `/jobs` (sample tasks **with token bounties**), `/cameras` (every cam + door logs + AI stubs), `/payments` (membership + parts billing stubs; **AI tracks by default**, humans on exceptions). Member (customer-facing `/member`): Parts **request desk** (PT / TC SKUs, not commerce), job board with token amounts, **primary camera only**. **Not** live purchasing, QR checkout, Frigate, Stripe, or claim/complete. Demo SKUs are labeled placeholder. McKing Tailnet Frigate **0.17.2** + Jellyfin lab ≠ this `/cameras` stub and does **not** flip `pc.cameras`.
- **Schedule harden (on `main`, PR #24):** Owner `/schedule` and Member `/member/schedule` **fail-soft** if bookings list 500s — month heat-map + weekly grids still render. Windows are sent as **America/Regina instants (UTC ISO)** so FastAPI cannot 422 on naive wall-clock strings.
- **Inventory prefixes (on `main`, PR #26):** **B1–B6** / **TC** / **PT** locked (CM later). **B6 = shop hoist bay**, not `SH`. Demo lists only.
- **Shop OS Chat v1 (LIVE on Doc, PR #27):** Owner-start rooms; Member↔Ops; human polling. Owner/Ops `/chat` — see-all threads, **Owner-only create room**, mute. Member `/member/chat` — own rooms, reply, poll every few seconds while a thread is open. Logged-out `/chat` and `/member/chat` **auth-gate 307**. Seeded demo threads. **Not** live Matrix, **not** Apex, **not** Grok. Demo cookies. The shop is not open.
- **Dashboard 24h + todos (LIVE on Doc, PR #28):** Owner `/` shows **Bays 1–6** (Bay 6 = shop hoist) with every booked hour in the next **24 hours** (`America/Regina`) plus member + vehicle/notes. **No “Today’s bookings” list.** Personal to-dos (CRUD) + ICS / copy-event. Parts orders strip (PT SKU / ordered–shipped–in transit–received stubs). Member `/member` gets the same todos + only bays that member booked. Google / Apple **Connect** stubs only. Not a shop opening. Doc still serves this **#28** layout (checkout **frozen** until **Ben GO**).
- **OwnerShell + bay-hero dashboard (on git, PR #69 — not LIVE on Doc):** `main` **`b9f9019`** (product SHA; later docs tip is **`2dd61a2` / #74**). Primary nav Dashboard / Schedule / Chat / Members; secondary Inventory (Parts/Tools), Floor (Jobs/Cameras/Hoists), Admin (Payments/Tiers/Fill/Waitlist); compact dismissible demo banner. Dashboard: 3 primary metrics (booked hours, open to-dos, open POs) + 3×2 bay grid + Parts/To-dos split. Same #28 data — layout polish on the keep path, not a new breadth slice. OwnerDemoBanner honesty: **ops.projectcar.ca is LIVE**; **app.** is the temporary alias — not “naming only, no DNS yet.” Banner copy is on git with this follow-up (same **#36** / **#69** **git-only** bucket). Doc checkout **frozen** at **`4cf8924`** / BUILD_ID **`5swmVz-T2CqKEQzTk1ifU`** until **Ben GO**. Do **not** invent #69 LIVE on Doc. Do **not** re-propose pull/rebuild.

---

## Next (not shipped)

Two gates — do **not** collapse them into “v1 includes everything forever.” Calendar (#18), fill (#20), placeholders (#21), cookies (#22), Docs (#23 / #25 / **#30–#74**, website **#52–#67**), schedule harden (#24), inventory (#26), Chat (#27), Dashboard (#28), shop-web host allowlist (**#36**, `f952cd3`), OwnerShell + bay-hero dashboard (**#69**, `b9f9019`), google-calendar-oauth plan (**#71**), lid-restore no-auto-pull (**#72**, `a2ec088`), Shop OS CI (**#73**, `babdc58` — git-only, not a Doc unfreeze), doc-unfreeze checklist (**#74**, `2dd61a2` — on `main`; green shop-os-ci / lid-restore process-wake are **not** Ben GO), brochure P1-1 / P1-3 (**#52**, `5361212`), brochure P2-1 (**#55**, `453e44d`), brochure P2-2 (**#56**, `ed41d23`), brochure P2-3 (**#57**, `be60a01`), brochure P3-1 (**#61**, `88c9820`), brochure P3-5 (**#62**, `9d83bfc`), brochure P3-4 (**#65**, `f191ad5`), brochure P4-2 (**#67**, `2b772ff`), and Soft-530 waitlist honesty (**#80**, Worker-live) are **on `main`** — they are not Next. P2-1 / P2-2 / P2-3 / P2-4 / P3-1 / P3-4 / P3-5 / P4-2 / Soft-530 **#80** are **LIVE** on Worker. Live URL SSOT is the Zone Redirect pack (Option A) — **FULL 10/10** capacity lock; no new Dynamic rules for brochure pretty-URLs until **#82** Worker-live + Bulk Phase1. Not Worker `_redirects` `/shop` 302. **#82** Home canonical/og/sitemap `/index.html` is **not yet Worker-live**. When GO'd, smoke is Direct Upload → **mandatory purge** → body freshness ([brochure-worker-ci.md](brochure-worker-ci.md)). Prefer merge **#83** after **#82** base (CI-only thin `_redirects` assert) — **parallel with upload OK**; **#83** does **not** need Bulk Phase1. **Never #81.** Bulk Phase1 stays after upload only for Member-edge Dynamic slots. P3-2 / P3-3 About stay **held / deferred**. P1-6 stays deferred. **#36 and #69 are git-only** — Doc checkout **frozen** until **Ben GO**; do **not** re-propose pull/rebuild. After **Ben GO**, pull: [doc-unfreeze.md](doc-unfreeze.md). **`ops.projectcar.ca` is LIVE** at the edge. `app.` remains the temporary alias — do not cut it here.

| Gate | Meaning |
|------|---------|
| **Build breadth (now)** | Put planned features into the app as placeholders / rough IA (Parts, Tools, job board, cams, calendar / fill, member surfaces, Chat v1 demo, …) so layout can be ironed out. Placeholders are OK. Do **not** leave a planned surface out of the **build** for polish. |
| **Ship-MVP cut (later gate)** | Before the **public MVP** ship, cut unfinished and unnecessary features. Do **not** ship every placeholder as the public product. Draft table: [ship-mvp-cut.md](ship-mvp-cut.md). |

**Soft-530-CLEAR Next Ben GO — #79 ops demo hardening (parallel to #82).** Soft-530 CLEAR + live `ops.` / `app.` `/login` still print `changeme` on frozen Doc BUILD_ID **`5swmVz`** (`4cf8924`). Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull** — merge alone will **not** clear public login while the freeze holds. Plan: [ops-demo-hardening.md](ops-demo-hardening.md). (1) Garage **#79.1**: strip plaintext password from public Owner/Member login banners + stop prefilling password (email ok) — **git-only** until [doc-unfreeze.md](doc-unfreeze.md). (2) optional Zone Cloudflare Access on `ops` (+ optional `app`) until OIDC — brochure stays public; exclude `api` waitlist paths. After Ben GO unfreeze + pull + **#79.1** rebuild, lane-done = [post-dual-clear-go.md](post-dual-clear-go.md) **unfreeze + #79.1 acceptance smoke** (public `app.`/`ops.` `/login` without `changeme`; tip / `BUILD_ID` ≠ `4cf8924` / `5swmVz`; `/health` **200** + waitlist OPTIONS CORS Origin `https://projectcar.ca` (+ www); **#79.1** merge-alone does **not** clear public `changeme`). Soft-530 CLEAR alone ≠ that card. Not **#82**, not Zone Direct Upload, not companions re-ask, not Bitwarden. **#79 is parallel to #82** — not blocked on Worker SEO / Option A upload / Bulk Phase1. **#82** sequence stays **#82 → Zone upload+purge; #83 after #82; Bulk for Member**. **Never #81.** Do **not** execute Garage/Zone from this tip-fold. **Ben GO** required. Soft-530 CLEAR makes this more urgent than when Doc was 530. Hardening GO ≠ unfreeze GO.

1. **Host migration — Member UI on projectcar.ca.** Customer surface (self-serve booking + balance) moves to **projectcar.ca**. **Canonical Member cookie host = apex `projectcar.ca`.** **Capacity-blocked** by Option A — CF Dynamic Redirect pack is **FULL 10/10**. Member cutover needs Dynamic (or equivalent) for `/member*` path/tunnel ranking **BEFORE** brochure Redirect Rules, plus www→apex `/member*` **301** without fighting brochure dual-host. **Hard gate:** Member edge **Ben GO** only after **#82** Worker-live + Bulk Phase1 frees pretty-URL Dynamic slots (Redirect SSOT pick A already parked — receipt: [brochure-worker-ci.md](brochure-worker-ci.md) Option A matrix; upload click-path [brochure-worker-deploy.md](brochure-worker-deploy.md)). **#83** is **not** this gate — prefer merge after **#82** base (CI-only thin `_redirects` assert); **parallel with upload OK**; does **not** need Bulk Phase1. **Never #81.** Bulk Phase1 stays after the **#82** upload **only** to free those Member slots. Keep `root` / `shop` Dynamic until Member needs those slots. Smoke anti-collision: apex+www `/member/login` must **NOT** 301 into `membership.html` (today `/member*` is Worker **404** — preserve that). Zone plan after that GO: **301** `www.projectcar.ca/member*` → `https://projectcar.ca/member*` so host-only `pc_member_session` cannot drop across www↔apex (brochure static stays dual-host). Management stays on **`ops.`** (temporary `app.` alias still live). Today Member demo still lives on shop-UI `/member`. Cutover checklist (plan only — not shipped, no DNS, no `app.` cut, no live edge flip): **`member-host-cutover.md`**. Zone path-split checklist (plan only — `/member*` → Doc `:3000`, everything else stays Worker): **`member-zone-edge.md`**. Garage (site) / Zone (path rules / tunnel hostname / CORS edge) wait for these docs, **#82** + Bulk Phase1 slot-free, **and Ben GO**. Do **not** execute the edge plan from a docs PR. Do **not** execute **#82** / Bulk Phase1 / Option A rewrite from this tip-fold. Prefer merge **#83** after **#82** base — not after Bulk. **Never #81.** Cutover planning outranks new breadth placeholders **and** Classic Pages git (`brochure-pages-cutover.md`). Do **not** claim this is shipped. After that GO, brochure Membership secondary CTA is `website-improvements.md` **P1-6** (apex `https://projectcar.ca/member/login` only — never www, never ops/app).
2. **Ben cuts the `app.` alias.** `ops.` is already LIVE at the edge and **stays the management host**. `app.` stays until Ben cuts that DNS. Member cutover (Next #1) does **not** require cutting `app.` first. Plan only (not executed — no DNS / tunnel / CORS / allowlist from a docs PR): **`app-alias-cut.md`**. Do not remove the alias from docs or edge config in a docs PR.
3. **Chat follow-ons (not Chat v1).** Chat v1 human/polling/Owner-starts-rooms is **LIVE on Doc** (Live above). Later: Grok on **projectcar.ca**, assign / staff notes / escalate, websockets. **Apex sidecar stays deferred.** Do not revive Matrix.
4. **Mission Control cockpit** still needs **Ben GO** before start. Owner + Member booking are already live on Doc **and** the public ops / app hosts — that earlier hold is satisfied. Do not start the cockpit from a docs PR.
5. Staff **OIDC** later (on **`ops.`**). **Full Stripe later.** Public Apex chat **deferred (Ben)** — not P0; do not revive. Do not dump the rest of v2 here.
6. **Google Calendar two-way sync.** PR #28 shipped ICS download + “copy event” + a Connect Google / Apple stub (`GET /calendar/google/start` is `501` until `GOOGLE_OAUTH_*` is set). Token exchange and two-way event sync stay **Next**. Apple stays ICS import (no public OAuth in this slice). Standing plan (not a GO): [google-calendar-oauth.md](google-calendar-oauth.md).

McKing Docker shop-host (api / ops / app origin move; brochure stays Worker) — path **OPEN** 2026-09-11 ~10:32 America/Edmonton (sshd / docker / `/opt/mission-control`; hub dual-run NC+VW healthy on Doc+McKing). Doc KeepAlive still owns public `api.` / `ops.` / `app.`. Hop OPEN ≠ shop cutover GO ≠ Doc unfreeze. Soft-530 dual-run five-row gate still blocks any public **shop** CF hostname leave (`cloud.` / `api.` / `app.` / `ops.` only — **vault EXCLUDED**; vault is **LIVE verified** on McKing). Shop cutover paper: [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md). **Not** this Next list. **Not** GO’d. Paper only for shop.

Placeholder pages (Parts, Tools, Job board, Cameras, Payments) are **demo UI** (Live above). Item 5 still means **live Stripe / OIDC** — not the Payments placeholder. They do not rewrite calendar (#18) or drop fill (#20). Do not cut the `app.` alias here.

---

## Later (parked)

- Member-to-member hoist time trades/offers — bookings should not be glued to one member forever (transferable booking or trade-offer entity). Design note only; do not design the trade system now.
- Member booking assistant (Ben later-want) — a bot that helps members book hoist dates. Same Later bucket as trades. After Member UI is solid; likely Grok / Apex-replacement public chat lane, **not** Owner admin. Do **not** build in current Shop OS slices. Related to the planned chat dual surface (Next) — still not an implementation.
- **Member parts purchasing (full).** Member `/member/parts` is a **request desk** (build-breadth now) — PT stock or TC crib tool, not a cart. Ops Parts is PT qty / reorder + POs, not the member catalog. Full member purchasing is **Later**. Do not claim a shop is selling parts. Flag `pc.marketplace` stays **off**.
- **Job board claim / complete / assign** — workflow is Later. Placeholder board with sample token-paid chores is what shipped.
- **Live Frigate (or equivalent NVR)** — flag `pc.cameras` stays **off** until wired. Occupancy is a **hint, not source of truth**. Door logs ops-only. **Anti-goal:** today’s McKing Tailnet Frigate **0.17.2** + Jellyfin lab ≠ Shop OS `/cameras` live and does **not** flip `pc.cameras`. No camera wiring from this Frigate/Jellyfin stand-up. This Later row stays **Later**.
- **Live tool inventory / QR / checkout / hardware** — prefix model is locked (below); ops `/tools` lists are demo SKUs. Return mail, overdue workflow, and readers stay Later.
- **Consumables (`CM`)** — prefix reserved. Full CM desk Later. Trivial stub note only.
- **Stripe / live billing** — ops Payments placeholder (membership + parts; AI default, human on exceptions) is IA only. `pc.payments` stays **off**. Full Stripe is Later. AI tracks payments by default; humans get alerts on exceptions.
- **Waitlist optional CSV export** — Owner desk (list + mark contacted) is **LIVE** when origin is up. Optional CSV export stays **Later**. No email blast. No Stripe. No Member host. Do **not** draft `waitlist-owner-desk.md`. Soft-530 **CLEAR** or desk existence ≠ Doc unfreeze ≠ **#79.1** rebuild GO.

---

## Locks

- **Host split** is locked (table above). Customer = `projectcar.ca` / www. Management = **`ops.projectcar.ca`** (LIVE at edge; staff-on-shift, not Owner-only). `app.` is a **temporary alias** until Ben cuts that DNS — **not removed**. Local DNS cache on some clients is not a product rollback — use `app.`.
- **Soft-530-CLEAR Next Ben GO (#79):** Public Owner/Member `/login` still prints and prefills demo password `changeme` on frozen BUILD_ID **`5swmVz`** after Soft-530 CLEAR (Lead/Chief live). Garage **#79.1** strip-changeme is **git-only until Doc unfreeze+pull** — merge alone will **not** clear public login while freeze holds (`4cf8924` / `5swmVz`). Lane-done after Ben GO unfreeze + pull + rebuild = [post-dual-clear-go.md](post-dual-clear-go.md) **unfreeze + #79.1 acceptance smoke**. Soft-530 CLEAR alone ≠ that card. Not **#82**, not Zone Direct Upload, not companions re-ask, not Bitwarden. **#79 is parallel to #82** — not blocked on Worker SEO / Option A upload. Plan [ops-demo-hardening.md](ops-demo-hardening.md). Do **not** execute Garage/Zone from this tip-fold. Hardening GO ≠ Doc unfreeze GO ≠ shop CF cutover. **#82** → Zone upload+purge; **#83** after **#82**; Bulk for Member — **unchanged**. **Never #81.**
- **Doc checkout frozen (LOCKED — until Ben GO):** Doc working tree / shop-web BUILD stay at **`4cf8924`** / BUILD_ID **`5swmVz-T2CqKEQzTk1ifU`** (Dashboard **#28**). **#36** (`f952cd3`) and **#69** (`b9f9019`) are **git-only** — OwnerShell + bay-hero are **on git**, not LIVE on Doc. plan-improve / Chief / Lead must **not** re-propose Doc pull/rebuild. Only **Ben GO** lifts this. Aligns with [doc-lid-restore.md](doc-lid-restore.md) (**#72**, `a2ec088`): lid-restore is process wake / kickstart-if-down only — **not** auto-`git pull`. After **Ben GO**, ordered pull: [doc-unfreeze.md](doc-unfreeze.md). Green **shop-os-ci** / lid-restore process-wake are **not** that GO. **#78 lookout-resume** is **LIVE-SUPERSEDED** — `projectcar-api-health-watch` is already **`enabled:true`**. Soft-530 companion watches are **HOLD / not armed** (Ben skipped ~14:35 America/Edmonton — do **not** re-ask). Api + vault watches continue. Freeze does **not** invent a companion GO. Garage **#79.1** strip-changeme is **git-only** in the same freeze bucket as **#36** / **#69** until unfreeze+pull. Fri Soft-530 **CLEAR** is historical — living dual-OPEN ≠ unfreeze GO ≠ companion re-ask.
- **Living dual-OPEN anti-goals (LOCKED — this fold):** Soft-530 **OPEN** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton) + vault **OPEN** (**502** since ~19:45 MT Sep 13) + ListMachines **Mac.lan only** is **not** Doc unfreeze, **not** **#82** merge/upload, **not** Zone execute, **not** Soft-530 companions re-ask, **not** Bitwarden import/rotate. Freeze intact **`4cf8924`** / BUILD_ID **`5swmVz`**. **#82** still Ben GO. Apex deferred. Index pointers stay: [vault-stay-up.md](vault-stay-up.md), [dual-host-outage.md](dual-host-outage.md), [soft-530-extended-open.md](soft-530-extended-open.md) (quiet-ops), [doc-reappear-first-hop.md](doc-reappear-first-hop.md) (Doc first hop), [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (McKing first hop — machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` label **lightning**; never `Mac.lan`), [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (first Soft-530 recovery smoke after extended-OPEN CLEAR — **before** desk / **#79.1** / unfreeze), [vault-clear-smoke.md](vault-clear-smoke.md) (first vault recovery smoke after extended-OPEN CLEAR — **before** Bitwarden / desk; **502→200** not **1033→200**), Soft-530 post-CLEAR ([doc-lid-restore.md](doc-lid-restore.md) `@55e10d0`), vault post-CLEAR (`@d88cacb`), [post-dual-clear-go.md](post-dual-clear-go.md). Paper only — no Zone/Garage execute.
- **Soft-530 extended OPEN — quiet-ops (LOCKED — this fold):** Soft-530 OPEN **~24h+** + vault OPEN + **Mac.lan only** = quiet-ops. **No Ben re-nag** (Doc-wake + McKing wake already asked). Lead stays armed for `Docs-MacBook-Pro` reappear → [doc-reappear-first-hop.md](doc-reappear-first-hop.md) (machineId `95a229f5-9296-4a18-aa98-70fd300dabdf` **then** lid-restore); Chief stays armed for `lightning` reappear → [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md) (machineId `9067d14b-46e5-4ef0-82d5-fce0febdc8f7` **then** vault wake). Lookout api+vault stay armed (**never delete** to fix cron); brochure-redirect **paper** **continues**; living Option A watch **GONE** — **HOLD** recreate; interim Chief `*/20` ([lookout-rearm-sop.md](lookout-rearm-sop.md)); companions **HOLD until Ben reopens** (not weekend-only). Option A is public SSOT. **Duration alone ≠** **#82** GO ≠ unfreeze ≠ Zone Direct Upload ≠ Bitwarden ≠ companion re-ask. CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md). Next Soft-530 CLEAR after this lock: [soft-530-clear-smoke.md](soft-530-clear-smoke.md). Next vault CLEAR after this lock: [vault-clear-smoke.md](vault-clear-smoke.md). Essay: [soft-530-extended-open.md](soft-530-extended-open.md). First hop Doc: [doc-reappear-first-hop.md](doc-reappear-first-hop.md). First hop McKing: [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md). Paper only — no Zone/Garage execute.
- **Doc first reappear — first hop (LOCKED — this fold):** When `Docs-MacBook-Pro` **first reappears** after quiet-ops: assert machineId **`95a229f5-9296-4a18-aa98-70fd300dabdf`** (**never** `Mac.lan` / Porsche as Doc) → [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (`/health` **200** + OPTIONS CORS apex+www + POST **422**/**201**) → **only if CLEAR** stamp lid-restore forensics → [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire** **#82** / unfreeze / **#79.1** / companions / Zone upload / Bitwarden). Chief-to-doc hop **only after** CLEAR smoke. Vault CLEAR stays Chief `lightning` / McKing. Anti-goals: `Mac.lan` Shell ≠ Doc evidence; duration ≠ Ben wake; **CLEAR ≠ honesty-off**; not companion re-ask. Living this fold: Soft-530 still **OPEN** CF **1033**; vault still **OPEN** **502**; only `Mac.lan`. Essay: [doc-reappear-first-hop.md](doc-reappear-first-hop.md). Monday map: [deployment-guide.md](deployment-guide.md). Paper only — no Zone/Garage execute. Does **not** invent a reappear or CLEAR.
- **McKing first reappear — first hop (LOCKED — this fold):** When `lightning` / McKing **first reappears** after quiet-ops / extended vault OPEN: assert machineId **`9067d14b-46e5-4ef0-82d5-fce0febdc8f7`** (label **`lightning`**) — **never** `Mac.lan` as McKing → [vault-clear-smoke.md](vault-clear-smoke.md) (`/alive` **200** + `/api/config` VW **2026.6.0** + **502→200**) → **only if CLEAR** stamp vault-stay-up forensics → Soft-530 independence (Bitwarden **blocked** until Soft-530 CLEAR **separate**; **never auto Bitwarden**; vault CLEAR ≠ Soft-530 CLEAR ≠ unfreeze / **#82** GO). Anti-goals: `Mac.lan` Shell ≠ McKing evidence; duration ≠ Ben wake; name-only ≠ identity. Living this fold: Soft-530 **OPEN** `api.`/`app.`/`ops.`/`cloud.` **530** CF **1033**; waitlist OPTIONS **530**; vault `/alive` **502**; ListMachines **Mac.lan only**; freeze intact; **#82** still Ben GO. Essay: [mcking-reappear-first-hop.md](mcking-reappear-first-hop.md). Monday map: [deployment-guide.md](deployment-guide.md). Paper only — no Zone/Garage execute. Does **not** invent a reappear or CLEAR.
- **Soft-530 CLEAR recovery smoke (LOCKED — this fold):** After Soft-530 **CLEAR** following **extended OPEN** (>~24h), **first** recovery smoke **before** Owner desk / **#79.1** / unfreeze talk: `api.` `/health` **200**; waitlist OPTIONS **200** + CORS Origin `https://projectcar.ca` **and** www; waitlist POST **422** or **201** (route alive, not just tunnel-up); `app.`/`ops.` may still **307**→`/login` with print `` `changeme` `` (freeze, **not** Soft-530); `cloud.` `status.php` **200** when Doc public is up. ≠ **#82** GO ≠ companion re-ask ≠ Bitwarden; Soft-530 CLEAR alone ≠ unfreeze; vault CLEAR still McKing ([vault-clear-smoke.md](vault-clear-smoke.md)); **CLEAR ≠ honesty-off** — **#80** Membership/Contact Discord honesty intro + `waitlist.js?v=3` fail-soft (502/530/1033→Discord+mailto) stay (overnight sleep / tunnel flaps, **not** temporary banners; tone tweak only with Ben GO; this smoke ≠ Garage GO to strip). CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire**). Essay: [soft-530-clear-smoke.md](soft-530-clear-smoke.md). Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) Soft-530 Discord · STATUS Waitlist Live · [website-improvements.md](website-improvements.md) P1-5. Living this fold: Soft-530 still **OPEN** CF **1033**; vault still **OPEN** **502**; only `Mac.lan`. Paper only — no Zone/Garage execute. Does **not** invent CLEAR.
- **Vault CLEAR recovery smoke (LOCKED — this fold):** After vault **CLEAR** following **extended OPEN** (**502** since ~19:45 MT Sep 13), **first** recovery smoke **before** Bitwarden import/rotate or desk talk: `vault.` `/alive` **200**; `/api/config` **200** + McKing **2026.6.0** class (**not** Doc **2025.12.0**); classify **502→200** (origin-down), **not** CF **1033→200**; Soft-530 may still be **OPEN** — vault CLEAR alone ≠ Soft-530 CLEAR ≠ **#82** ≠ unfreeze ≠ companions; Bitwarden stays **Ben GO**, **never auto-fire**. ≠ Zone Direct Upload ≠ **#82** ≠ unfreeze / **#79.1** execute ≠ companion re-ask ≠ Soft-530 CLEAR smoke (`502ab2e`). CLEAR path still [post-dual-clear-go.md](post-dual-clear-go.md) (**never auto-fire**). Forensics still [vault-stay-up.md](vault-stay-up.md) (`d88cacb`). Essay: [vault-clear-smoke.md](vault-clear-smoke.md). Living this fold: Soft-530 still **OPEN** CF **1033**; vault still **OPEN** **502**; only `Mac.lan`. Paper only — no Zone/Garage execute. Does **not** invent CLEAR.
- **Weekend Soft-530 coverage (LOCKED — plan-improve off Sat/Sun):** Weekend flip coverage = Lookout `api.` `/health` + vault `/alive` **only** (companions stay **HOLD / not armed** — do **not** re-ask). Weekend control plane = Doc KeepAlive / lid-close (`com.projectcar.cloudflared` + uvicorn `:8000` + `next start` `:3000` LaunchAgents) — **not** Doc unfreeze, **not** **#82**. If `api.` `/health` flips non-200 → Chief/Lead [doc-lid-restore.md](doc-lid-restore.md) **process wake only** (no pull while freeze intact). Vault flips stay on the separate McKing watch — [vault-stay-up.md](vault-stay-up.md). Dual-OPEN glue: [dual-host-outage.md](dual-host-outage.md) — **Doc first, then McKing** (parallel only if both hosts on ListMachines); `Mac.lan` ≠ `Docs-MacBook-Pro` ≠ `lightning`; independent CLEAR gates; Bitwarden import/rotate **blocked** until vault CLEAR. After Soft-530 CLEAR, stamp **post-CLEAR stay-up evidence** (lastExit / KeepAlive / caffeinate·CDM / Tailscale) on [doc-lid-restore.md](doc-lid-restore.md) — wake order stays dual-host-outage. Soft-530 still **OPEN** this fold (CF **1033** since 2026-09-13 ~11:57 America/Edmonton); vault independently **OPEN** (**502** since ~19:45 MT Sep 13); ListMachines **Mac.lan only**; this paper does **not** invent CLEAR. Do **not** schedule weekend Zone Direct Upload / Worker work — first Monday plan-improve resumes Soft-530 smoke. Anti-goal: Fri Soft-530 **CLEAR** is historical — living dual-OPEN ≠ unfreeze GO ≠ companion re-ask ≠ **#82**. **#82** Ben GO / dual-Nextcloud (`4cde204`) / `brochure-worker-ci` (`6e6efe8`) / waitlist-owner-desk **retired** (Owner desk LIVE; optional CSV Later) / **#79.1** git-only until Doc unfreeze — **unchanged**. Paper: [api-stay-up.md](api-stay-up.md), [shop-web-stay-up.md](shop-web-stay-up.md), [doc-lid-restore.md](doc-lid-restore.md), [vault-stay-up.md](vault-stay-up.md), [dual-host-outage.md](dual-host-outage.md).
- **Post-dual-CLEAR Ben GO sequencer (LOCKED — never auto-fire):** After **extended OPEN**, Soft-530 CLEAR first runs [soft-530-clear-smoke.md](soft-530-clear-smoke.md) — **before** this menu / desk / **#79.1**. Vault CLEAR first runs [vault-clear-smoke.md](vault-clear-smoke.md) — **before** Bitwarden / desk. After Soft-530 CLEAR (Doc lid-restore) **and** vault CLEAR (McKing) **and** both forensic papers already ack’d (`55e10d0` + `d88cacb`), Ben’s GO menu is **ordered**: (1) keep Soft-530 healthy (KeepAlive / CDM / caffeinate); (2) **#82** Worker-live (upload + **mandatory** purge/freshness). After **#82** the **brochure lane** is **parallel**: (A) **#83** CI thin `_redirects` anytime on the **#82** base (not Bulk-gated; parallel with upload OK); (B) **Bulk Phase1** frees Option A **FULL 10/10** slots when Member needs capacity — **#82 PASS ≠ wipe Dynamic**; keep full Dynamic until Bulk is **LIVE** and smoke-equal (keep `root`/`shop`/`chat` Dynamic until then; do **not** migrate Dynamic on upload day). Member edge **only after Bulk.** **Never #81.** Never Bulk→#83. (3) Doc unfreeze + pull + Garage **#79.1** is **Shop OS parallel** — not “next after **#82**” in place of Bulk / **#83**. Lane-done = [post-dual-clear-go.md](post-dual-clear-go.md) **unfreeze + #79.1 acceptance smoke** (public `app.`/`ops.` `/login` without `changeme`; tip / `BUILD_ID` ≠ `4cf8924` / `5swmVz`; `/health` **200** + waitlist OPTIONS CORS Origin `https://projectcar.ca` (+ www); **#79.1** merge-alone does **not** clear public `changeme`). Soft-530 CLEAR alone ≠ that card. Not **#82**, not Zone Direct Upload, not companions re-ask, not Bitwarden. **Never** auto-fire on CLEAR. Anti-goals: CLEAR ≠ companion re-ask; CLEAR ≠ Bitwarden import/rotate; Soft-530 CLEAR alone ≠ Doc unfreeze; vault CLEAR ≠ Soft-530 work; dual CLEAR ≠ **#82** auto-go; **#82** ≠ end of brochure. Living this fold: Soft-530 still **OPEN** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton); vault still **OPEN** (**502** since ~19:45 MT Sep 13); only `Mac.lan`. Paper: [post-dual-clear-go.md](post-dual-clear-go.md). Option A / **#82** gates: [brochure-worker-ci.md](brochure-worker-ci.md). Member-edge capacity: [member-zone-edge.md](member-zone-edge.md). **#82** / companions HOLD / **#79.1** git-only until unfreeze — **unchanged**.
- **Waitlist-owner-desk retired:** Owner desk is **LIVE** when origin is up (login + `/waitlist` on frozen **`5swmVz`**). Post–`brochure-worker-ci` parked queue item is done — do **not** open `Docs/waitlist-owner-desk.md`. Optional CSV stays Later. No email blast / Stripe / Member host. Soft-530 **CLEAR** or desk existence ≠ Doc unfreeze ≠ **#79.1** rebuild GO. **#82** Ben GO / Soft-530 companions HOLD / weekend coverage (`84107e7`) / dual-Nextcloud / `brochure-worker-ci` (`6e6efe8`) / **#79.1** git-only until Doc unfreeze — **unchanged**.
- **McKing shop-host cutover (plan only — path OPEN, cutover not GO):** McKing path **OPEN** 2026-09-11 ~10:32 America/Edmonton — sshd / docker / `/opt/mission-control`; hub dual-run NC+VW healthy on Doc+McKing. Doc KeepAlive still owns public `api.` / `ops.` / `app.` (Doc stays public CF origin / KeepAlive backup). Soft-530 dual-run five-row gate still blocks any public **shop** CF hostname leave (`cloud.` / `api.` / `app.` / `ops.` only — **vault EXCLUDED**). Hop OPEN ≠ shop cutover GO ≠ Doc unfreeze (freeze `4cf8924` / `5swmVz` until **Ben GO**). Vault is **McKing-owned** (living **OPEN 502**) — that is the **separate** living-ops row below, not this shop-cutover pointer. Gate row 2 acceptance artifact when it lands: in-flight Doc dual-run evidence card (Tailscale / private only) — still no public McKing attach for shop. Paper: [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md). **Not** Next #1. This STATUS fold is a pointer, not a live shop cut. Brochure Worker stays. Soft-530 is **OPEN** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton); Fri CLEAR / Doc-up is **historical**. Future lid-close **530 / 1033** still expected while Doc is origin.
- **Dual-tunnel ownership (separate living-ops row — not the shop CF cutover):** Doc tunnel = **`cloud.` + `api.` + `app.` + `ops.` only** (shop Soft-530 / KeepAlive). McKing-only tunnel **`vault.projectcar.ca` is McKing-owned** (+ **unpublished** NC, not a tunnel hostname): McKing `cloudflared` → `localhost:8222`. **Living:** vault **OPEN** as **502** since ~19:45 MT 2026-09-13 — **not** 1033. Fri `/alive` **200** + `/api/config` **2026.6.0** is **historical** (when CLEAR, prefer `/alive`). Soft-530 five-row dual-run gate applies to **Doc shop hosts only** (`cloud.` / `api.` / `app.` / `ops.`) — **vault is EXCLUDED**. Soft-530 / `api.` `/health` can stay green while vault dies. Lookout vault flip watch is **LIVE/armed** (`enabled:true`, Lookout confirmed) on `https://vault.projectcar.ca/alive` every **5m** (fallback `/api/config` if `/alive` 404s; baseline Lookout-owned: `/workspace/lookout/projectcar-vault-health-baseline.json`). Flip-only alerts: **Chief + Lead only** on **200↔non-200**; **never Ben**; never mutate. Vault watch ≠ Soft-530 / Soft-530 companion watches / Doc lid-restore. Soft-530 five-row is Doc shop hosts `cloud.` / `api.` / `app.` / `ops.`, but Lookout Soft-530 coverage was **api-only** (`/health`) — `ops.` / `app.` (and `cloud.`) can **530 while `api.` stays 200** (same class as pre-vault-watch). Soft-530 companion watches on `ops.` / `app.` `/login` (optional `cloud.` `/login`; Lead probed **200**) are **HOLD / not armed** (Ben skipped companion-watch approval ~14:35 America/Edmonton — do **not** re-ask). Companion watches ≠ vault `/alive` watch ≠ Doc lid-restore. Soft-530 living is **OPEN** (CF **1033** since 2026-09-13 ~11:57 America/Edmonton) — Fri CLEAR is historical. Doc lid-restore / LaunchAgent KeepAlive must **not** recreate `vault.` ingress on Doc. Anti-goals: never move the Doc mission-control token for `vault.`; vault LIVE ≠ shop hostname leave ≠ Doc unfreeze. Hop OPEN ≠ shop cutover GO ≠ Doc unfreeze. Shop CF cutover stays paper — no Zone live for shop. Machine map: [home-lab-specification.md](home-lab-specification.md). Cross-links: [vault-stay-up.md](vault-stay-up.md), [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md), [api-stay-up.md](api-stay-up.md), [deployment-guide.md](deployment-guide.md), [home-lab-specification.md](home-lab-specification.md).
- **Dual-Nextcloud ownership (paper — not a `cloud.*` CF cutover):** Public `cloud.projectcar.ca` stays on **Doc** until explicit **Ben GO**. McKing `/opt/mission-control` NC remains **lab/hub loopback-only** (unpublished). Dual-tunnel matrix **unchanged**: Doc=`cloud`/`api`/`app`/`ops`; McKing=`vault` (+ unpublished NC). Paper path = Tailscale Serve HTTPS mirroring VW Serve at `lightning.tailbe8f55.ts.net` + `NEXTCLOUD_TRUSTED_DOMAINS` MagicDNS — **not** a Cloudflare cutover of `cloud.*`. Do **not** point `cloud.*` tunnel/CNAME at McKing until a dual-run acceptance gate (parity vs Doc `status.php`) **and** Ben GO. Hub dual-run NC+VW healthy / hop OPEN / vault LIVE / Serve stand-up do **not** pass that gate. #82 gate unchanged. Soft-530 companions **HOLD**. Vault LIVE unchanged. Paper: [home-lab-specification.md](home-lab-specification.md), [mcking-shop-host-cutover.md](mcking-shop-host-cutover.md).
- **IA freeze.** Ben is happy with the current app direction. Do **not** start major IA reshuffles.
- **Build breadth (now)** and **Ship-MVP cut (later gate)** are **two locks** (tables above). Build = placeholders in the app for IA. Public MVP = later cut of unfinished / unnecessary. Do **not** read this as “v1 includes everything forever.” Proposed keep vs cut (draft): [ship-mvp-cut.md](ship-mvp-cut.md).
- **Inventory prefixes (LOCKED — Ben + Chief):** **B1–B6** = resident bay hand-tool kits (one kit per hoist/bay). **B6 = shop hoist bay** — same B-scheme as other bays, **not `SH`**. **TC** = tool-crib specialty tools (checkout / return / overdue). **PT** = parts inventory (stock, PO, member requests). **CM** = consumables **later** (do not build a full CM UI now). SKU shape `PREFIX-CATEGORY-NNN` (category may include extra hyphen segments): `B2-WR-014`, `TC-TQ-003`, `PT-OIL-5W30-012`. Bay-resident tools stay on the cart (no crib checkout). Crib tools check out. Parts are qty / reorder + requests — not tool checkout and not member commerce. Demo UI / seed SKUs only until a thin API lands. Existing `tools` table (`name` / `part_number`) stays schema-only in this slice.
- **Ops sections (locked IA, placeholders now):** **Parts** (PT list + POs) and **Tools** (B1–B6 lists + TC checkout stub, plus orders / requests / planned). Not a claim that inventory or purchasing is shipped.
- **Job board (locked IA, placeholder now):** shop chores (cleaning, tool maintenance, random upkeep). Ops posts a **token bounty**; member claim/complete credits the **append-only token ledger** (same tokens as hoist booking, **not Stripe**). Placeholder must show sample amounts. Full workflow Later.
- **Cameras (locked IA, placeholder now):** members = **one primary shop cam**. Ops = **all cams** + door entry logs + Frigate / AI collection. Not a claim that Frigate is wired on `main`. **Anti-goal:** McKing Tailnet Frigate **0.17.2** + Jellyfin lab ≠ Shop OS `/cameras` live and does **not** flip `pc.cameras`. No camera wiring from that stand-up. Later Frigate / cameras placeholder stays **Later**.
- **McKing Frigate / Jellyfin lab ≠ Shop OS cameras (anti-goal):** Today’s McKing Tailnet Frigate **0.17.2** + Jellyfin lab is **not** Shop OS `/cameras` live. It does **not** flip `pc.cameras`. Do **not** wire cameras / Frigate / NVR into Shop OS from this stand-up. Later stays **Later**. Machine map: [home-lab-specification.md](home-lab-specification.md).
- **Member parts / tool requests** = request desk now; **full purchasing Later**. No member Tools nav.
- **Chat v1** = human messaging + polling. **Owners start rooms.** Members reply in rooms they are in. Ops/Owner see-all + mute. Dual surfaces `/chat` and `/member/chat`. No AI/Grok/Matrix bridge. Not the brochure Chat page (still stripped). Apex sidecar **deferred**.
- No Stripe. No “shop is open” claims. No live public pricing until Ben says so.
- Token pricing v1 is locked: bands + overlay in `America/Regina` — `token-pricing.md`. `hours × 100 × band × overlay × fill`. Defaults are Owner-editable placeholders, not public prices. Allotments: **Basic 1000 / Premium 1500** per period. Two tiers only (no Pro, no Weekly). Fill is an explicit extra factor (10–25%), not a rewrite of the overlay table.
- **6 hoists** in calendar/seed. Exactly one is the shop hoist (`is_shop`) — **Bay 6**. **v1 = (A) Owner-only** — customers cannot book that **bay** (`400 shop_hoist_owner_only`). **(B) bumpable** is a later tweak only. See `token-pricing.md`. Owner Dashboard is Bays 1–6 next-24h strips; no “Today’s bookings” list.
- HTTPS ops/app Doc demo requires **Secure** session cookies (`COOKIE_SECURE` / `SHOP_COOKIE_SECURE`). Demo cookies — **not OIDC**.
- Shop-web KeepAlive on Doc uses **`next start`**, not `next dev`. See [shop-web-stay-up.md](shop-web-stay-up.md).
- Mission Control cockpit needs **Ben GO** before start. Do not start the cockpit from a docs PR.
- Do not revive Apex. Brochure stays Worker / Pages — no Apex sidecar.
- **Live URL SSOT (Option A):** Zone Redirect pack is **FULL 10/10** Active Dynamic **301**s (apex+www) — capacity lock; no new Dynamic rules for brochure pretty-URLs. Owns pretty URLs, including brochure-shop-html-to-the-shop (`/shop.html`→`/the-shop.html`). Worker Assets **`html_handling: none`** — `*.html` is canonical. Worker `_redirects` is **thin**: chat → contact.html only (no `/` or `/shop`). Do **not** claim Worker `/shop` → 302 `/the-shop`. **#83** is the CI-only thin `_redirects` assert on the **#82** base — **not** Bulk-gated. **Never #81.**
- **Hard lock / prefer merge (brochure sequence):** **Never #81.** Wrong lock was `#82 → Zone Direct Upload → Bulk Phase1 → #83`. After **#82** Ben GO + Zone Direct Upload + mandatory purge/freshness, **#83** and **Bulk Phase1** are **parallel** (not Bulk→#83). (A) **#83** merge anytime on the **#82** base (CI-only thin `_redirects` assert; **parallel with upload OK**; **not** Bulk-gated). (B) Bulk Phase1 **only** to free Dynamic slots when Member needs capacity — **after** Bulk is **LIVE** and smoke-equal to today’s **10/10** (keep `root`/`shop`/`chat` Dynamic until then). **#82 PASS ≠ wipe Dynamic.** Do **not** migrate Dynamic on upload day. **Member edge only after Bulk.** Do **not** write `#82 → upload → Bulk Phase1 → #83`. Option A **FULL 10/10** still capacity-blocks **new Dynamic pretty-URLs** and **Member edge** until Bulk Phase1 — that lock is **not** a **#83** gate. Soft-530 companions **HOLD**. Reality quarantine until **#82** unchanged. Paper only.
- **Brochure Reality quarantine (held #70):** Until **#82** Worker-live + Zone Direct Upload, **#70 Reality tip / Option A** is brochure + living-ops SSOT. **Public STATUS tip honesty:** `main` still stamps P4-2 **`2b772ff` / #67** — **stale** vs live Option A Redirect pack + Soft-530 Discord waitlist + McKing vault **LIVE (when CLEAR)** — living vault is **OPEN 502**. **Reinforce (no Ben ask / no tip-only PR):** `main` Reality tip is still **`2b772ff` / `styles.css?v=35` / Worker `/shop` 302** — quarantine stands; **#70** is SSOT until **#82**. Same class: `main` `doc-lid-restore.md` Soft-530 table still says Lookout api-health **paused** vs living **`enabled:true`** (**#78 LIVE-SUPERSEDED**). **Living dual-OPEN:** Soft-530 **OPEN** CF **1033** since 2026-09-13 ~11:57 America/Edmonton (**~24h+** — quiet-ops [soft-530-extended-open.md](soft-530-extended-open.md)); Home still bare `href="/"` + canonical/og apex until **#82**. Option A is public SSOT during extended OPEN. Do **not** open a tip-only PR; keep this quarantine until **#82**. Do **not** execute Zone/Garage from **`main` Reality tip** (`main` still pre-Option-A Worker `/shop` **302** + `styles.css?v=35`). After **#82** upload + **mandatory purge** (`projectcar-brochure` Worker/Cache, apex+www), one tip-fold reconciles `main` Reality to live (Live post-#82 checklist: Home canonical/og/sitemap `https://projectcar.ca/index.html`; nav/logo not `href="/"`; Zone Redirect pack SSOT FULL 10/10 / Bulk Phase1 path; Worker `_redirects` thin chat→contact only; `waitlist.js?v=3` + `styles.css?v=36`; `/shop.html`→`/the-shop.html` 301 via Zone — never reintroduce Worker `/shop` 302). Do **not** treat `Cache-Control: no-cache, must-revalidate` as sufficient — live HTML steady-state is `cf-cache-status: HIT` + no-cache. `/member*` stays Worker **404** until Member edge Ben GO after Bulk Phase1 — never 301 into `membership.html`. Soft-530 companion watches **HOLD / not armed** (Ben skipped ~14:35 America/Edmonton — do **not** re-ask). Paper only. Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) · [brochure-worker-deploy.md](brochure-worker-deploy.md) · [brochure-redirect-watch.md](brochure-redirect-watch.md) · [lookout-rearm-sop.md](lookout-rearm-sop.md). Edge paper: [member-zone-edge.md](member-zone-edge.md).
- **Lookout brochure redirect watch (paper — GONE / HOLD recreate):** While Soft-530 **OPEN**, Option A is the only public **LIVE** surface. Living watch **GONE** (deleted to fix stalled cron; Auto-review blocked recreate). **Never delete** to fix cron. **HOLD** after one Ben ask. Interim Chief `*/20` **+ Soft-530 UX**. **Redirect-only PASS ≠ Soft-530 UX PASS.** After Ben approves: enabled + first fire ≤ **10m** + baseline **LIVE 10/10**. Flip **Chief + Lead only**; **never Ben**. **Not** companion re-ask. **#78 lookout-resume** is **LIVE-SUPERSEDED**. Essay: [brochure-redirect-watch.md](brochure-redirect-watch.md). SOP: [lookout-rearm-sop.md](lookout-rearm-sop.md). Quiet-ops: paper continues; continuous watch **GONE** ([soft-530-extended-open.md](soft-530-extended-open.md)). Living dual-OPEN (`5f2fd1c`) **unchanged**. Paper only — no Zone/Garage execute.
- **Lookout rearm SOP (LOCKED — this fold):** (1) **Never delete** an armed Project Car Lookout watch to fix stalled cron. (2) Prefer in-place update / pause-resume / re-save. (3) Delete already happened + Auto-review blocked recreate → **HOLD** after one Ben ask. (4) Interim Option A = Chief `*/20` redirect smoke **+ Soft-530 UX** — **Redirect-only PASS ≠ Soft-530 UX PASS**. (5) After Ben approves: routine enabled + first fire within **10m** + baseline **LIVE 10/10**. (6) Soft-530 companions stay **HOLD**. Anti-goals: not **#82** merge nag, not Dynamic wipe, not honesty-off, not companions rearm, not unfreeze, not Zone execute. **#78** is **LIVE-SUPERSEDED** (api-health already `enabled:true`). Essay: [lookout-rearm-sop.md](lookout-rearm-sop.md). Honesty permanence: [soft-530-clear-smoke.md](soft-530-clear-smoke.md) (`0705b37`). Paper only — no Lookout/Zone/Garage execute.
- **Interim Chief `*/20` Soft-530 UX (LOCKED — this fold):** While Lookout redirect watch is **GONE**, Chief `*/20` must assert Soft-530 UX, not only pretty-URL **301**s. `membership.html` + `contact.html` still show Soft-530 Discord honesty; `waitlist.js?v=3` still fail-softs **502 / 530 / 1033** → Discord+mailto. **Redirect-only PASS ≠ Soft-530 UX PASS.** **CLEAR ≠ honesty-off** (`0705b37`). Not **#82** merge nag, not Dynamic wipe, not companions rearm. Essay: [brochure-redirect-watch.md](brochure-redirect-watch.md) · [lookout-rearm-sop.md](lookout-rearm-sop.md). Paper only.
- **#82 Worker-live ≠ retire Option A Dynamic (LOCKED — this fold) (`49ccf90`):** After Ben GO **#82** → Zone Direct Upload + **mandatory** purge + body freshness (canonical / og / sitemap + nav home = `/index.html`, **no** bare `href="/"`), **PASS** does **not** authorize deleting the CF Dynamic Redirect **10/10** pack. Keep **full** Option A Dynamic (`root` / `shop` + pretty-URL + `chat`) until **Bulk Phase1 is LIVE and smoke-equal** to today’s **10/10**. Then Zone may drop pretty-URL Dynamic per parked Redirect A / Bulk Phase1; keep `root` / `shop` Dynamic unless Bulk covers them too. Frozen Sep 10 2026 inventory is Docs SSOT — do **not** invent rules. Anti-goals: not “Worker-live so wipe Dynamic”; not migrating Dynamic on upload day; not **#81**; not unfreeze / **#79.1** / companions / Bitwarden. Aligns **#83** ∥ Bulk Phase1 after **#82**. Paper: [post-dual-clear-go.md](post-dual-clear-go.md) brochure lane · [brochure-worker-ci.md](brochure-worker-ci.md) Option A matrix · [brochure-pages-cutover.md](brochure-pages-cutover.md) hard cutover gate. Paper only — no Zone/Garage execute.
- **Option A Dynamic pack (FULL 10/10 — LOCKED):** Frozen live inventory (Zone refresh **2026-09-10**, `rule_count: 10`) is Docs SSOT — [brochure-worker-ci.md](brochure-worker-ci.md) Option A matrix · [brochure-pages-cutover.md](brochure-pages-cutover.md) **hard cutover gate**. Do **not** invent rules. Garage must **not** invent new brochure extensionless pretty-URLs that need another Dynamic Redirect Rule until **#82** Worker-live + Bulk Phase1 frees Dynamic slots. Redirect plan A is parked — Bulk Phase1 pretty-URL pack already drafted: `membership` / `about` / `the-shop` / `contact` / `roadmap` ± slash → `*.html`; `root` / `index` / `shop` / `chat` stay Dynamic until migrate (keep `root` / `shop` Dynamic until Member needs those slots, or Bulk covers them). **#82 Worker-live ≠ retire this pack (`49ccf90`):** after Ben GO **#82** → Zone Direct Upload + purge + body freshness (canonical / og / sitemap + nav home = `/index.html`, no bare `href="/"`), **PASS** does **not** authorize deleting the CF Dynamic Redirect **10/10** pack. Keep **full** Option A Dynamic (`root` / `shop` + pretty-URL + `chat`) until **Bulk Phase1 is LIVE and smoke-equal** to today’s **10/10**. Then Zone may drop pretty-URL Dynamic per parked Redirect A; keep `root` / `shop` unless Bulk covers them too. Do **not** migrate Dynamic on upload day. Not “Worker-live so wipe Dynamic.” **Next #1 Member edge is capacity-blocked** until that slot-free — `/member*` must rank **BEFORE** the brochure Redirect pack; apex+www `/member/login` must **not** 301 into `membership.html`. **#83** is CI-only on the **#82** base (thin `_redirects` assert) and is **not** gated by Bulk Phase1 — prefer merge after **#82**; **parallel with upload OK**. **Never #81.** Do **not** write `#82 → upload → Bulk Phase1 → #83`. This lock does **not** touch unfreeze / **#79.1** / companions / Bitwarden. Paper only — no Zone live apply. Receipt: [brochure-worker-ci.md](brochure-worker-ci.md) (Option A matrix + **#82** mandatory purge before body-freshness; **never** Direct Upload from **#81**). Upload click-path: [brochure-worker-deploy.md](brochure-worker-deploy.md). Lane: [post-dual-clear-go.md](post-dual-clear-go.md). Edge paper: [member-zone-edge.md](member-zone-edge.md).
- Shop members never get Nextcloud accounts.
- No n8n.
- **Lead owns Doc `:8000`.** Do not hand uvicorn restarts to Chief.
- **Ops Parts** is PT stock + PO tracker — distinct from `/member/parts` (requests only).
- **Payments (ops):** membership + parts. AI tracks by default; humans on exceptions. Placeholder only. Stripe Later.

Specs: `project-car-application-specification.md` §2 / §13 / §15 / §16 / §17, `token-pricing.md`, `website-webapp-specification.md`, `platform-architecture.md` §5.
